name: Pull Request
on:
  pull_request:
    types: [opened, reopened, synchronize, ready_for_review]

  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  # 1. Lightweight change-detection (runs in ~10 s)

  detect-changes:
    name: Detect Changes
    runs-on: ubuntu-latest
    permissions:
      pull-requests: read
    outputs:
      core: ${{ steps.filter.outputs.core }}
      web: ${{ steps.filter.outputs.web }}
      react-native: ${{ steps.filter.outputs.react-native }}
      docs: ${{ steps.filter.outputs.docs }}
      cli: ${{ steps.filter.outputs.cli }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            core:
              - 'js/shared/core/**'
              - 'js/web/react/**'
              - 'js/shared/types/**'
              - 'js/package.json'
              - 'js/pnpm-lock.yaml'
              - 'js/tsconfig.base.json'
              - '.github/workflows/**'
              - '.github/actions/**'
            web:
<<<<<<< HEAD
              - 'js/shared/core/**'
              - 'js/shared/types/**'
              - 'js/web/core-web/**'
              - 'js/web/transport-web/**'
              - 'js/web/wasm-bundler/**'
              - 'js/web/wasm-web/**'
              - 'js/web/integration-tests/**'
=======
              - 'package.json'
              - 'pnpm-lock.yaml'
              - 'tsconfig.base.json'
              - 'packages/core/**'
              - 'packages/types/**'
              - 'packages/core-web/**'
              - 'packages/transport-web/**'
              - 'packages/wasm-bundler/**'
              - 'packages/wasm-web/**'
              - 'packages/integration-tests/**'
>>>>>>> e9af9b3 (ci: fix manual dispatch, root workspace triggers, and CLI typecheck coverage)
              - 'flake.nix'
              - 'flake.lock'
              - 'js/vitest.config.ts'
              - 'js/vitest.workspace.ts'
              - 'scripts/**'
              - '.github/workflows/**'
              - '.github/actions/**'
            react-native:
<<<<<<< HEAD
              - 'js/react-native/react-native/**'
              - 'js/react-native/react-native-bindings/**'
              - 'js/shared/core/**'
              - 'js/shared/types/**'
              - 'rust/fedimint-client-uniffi/**'
=======
              - 'package.json'
              - 'pnpm-lock.yaml'
              - 'tsconfig.base.json'
              - 'packages/react-native/**'
              - 'packages/react-native-bindings/**'
              - 'packages/core/**'
              - 'packages/types/**'
              - 'fedimint-sdk-ffi'
              - 'fedimint-sdk-ffi/**'
>>>>>>> e9af9b3 (ci: fix manual dispatch, root workspace triggers, and CLI typecheck coverage)
              - 'flake.nix'
              - 'flake.lock'
              - 'Justfile'
              - '.github/workflows/**'
              - '.github/actions/**'
            docs:
<<<<<<< HEAD
              - 'js/docs/**'
              - 'js/shared/core/**'
              - 'js/shared/types/**'
              - '.github/workflows/**'
              - '.github/actions/**'
            cli:
              - 'js/tools/create-fedimint-app/**'
=======
              - 'package.json'
              - 'pnpm-lock.yaml'
              - 'tsconfig.base.json'
              - 'docs/**'
              - 'packages/core/**'
              - 'packages/types/**'
              - '.github/workflows/**'
              - '.github/actions/**'
            cli:
              - 'package.json'
              - 'pnpm-lock.yaml'
              - 'tsconfig.base.json'
              - 'packages/create-fedimint-app/**'
>>>>>>> e9af9b3 (ci: fix manual dispatch, root workspace triggers, and CLI typecheck coverage)
              - '.github/workflows/**'
              - '.github/actions/**'

  # Reusable verify jobs (lint always runs; others are conditional)

  verify:
    name: Verify
    needs: detect-changes
    uses: ./.github/workflows/verify.yml
    secrets: inherit
    with:
      run_core: ${{ github.event_name == 'workflow_dispatch' || needs.detect-changes.outputs.core == 'true' || needs.detect-changes.outputs.web == 'true' || needs.detect-changes.outputs.cli == 'true' }}
      run_web: ${{ github.event_name == 'workflow_dispatch' || needs.detect-changes.outputs.web == 'true' }}
      run_docs: ${{ github.event_name == 'workflow_dispatch' || needs.detect-changes.outputs.docs == 'true' }}
      run_cli: ${{ github.event_name == 'workflow_dispatch' || needs.detect-changes.outputs.cli == 'true' }}

  # 3. React Native builds (only when relevant paths change)

  build-android:
    name: Build Android
    needs: detect-changes
    if: ${{ github.event_name == 'workflow_dispatch' || needs.detect-changes.outputs.react-native == 'true' }}
    runs-on: [self-hosted, linux, x64]
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: ./.github/actions/setup-nix
        with:
          cachix_auth_token: ${{ secrets.CACHIX_AUTH_TOKEN }}
      - uses: ./.github/actions/build-rn-android

  build-ios:
    name: Build iOS
    needs: detect-changes
    if: ${{ github.event_name == 'workflow_dispatch' || needs.detect-changes.outputs.react-native == 'true' }}
    runs-on: macos-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: ./.github/actions/setup-nix
        with:
          cachix_auth_token: ${{ secrets.CACHIX_AUTH_TOKEN }}
      - uses: ./.github/actions/build-rn-ios
        with:
          device-only: 'true'

