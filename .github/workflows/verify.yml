name: Verify

on:
  workflow_call:
    inputs:
      react_native:
        required: true
        type: string
      web:
        required: true
        type: string
      core:
        required: true
        type: string
  workflow_dispatch:

jobs:
  lint:
    name: Lint
    if: ${{ inputs.core == 'true' || inputs.web == 'true' || inputs.react_native == 'true' }}
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - name: Clone repository
        uses: actions/checkout@v4
      - name: Install dependencies
        uses: ./.github/actions/install-deps
      - name: Lint repo
        run: pnpm lint:repo
      - name: Lint code
        run: pnpm lint

  build:
    name: Build JS
    if: ${{ inputs.core == 'true' }}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Clone repository
        uses: actions/checkout@v4
      - name: Install dependencies
        uses: ./.github/actions/install-deps
      - name: Build
        run: pnpm build

  types:
    name: Check Types
    if: ${{ inputs.core == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - name: Clone repository
        uses: actions/checkout@v4
      - name: Install dependencies
        uses: ./.github/actions/install-deps
      - name: Build types package
        run: pnpm --filter @fedimint/types build
      - name: Check types
        run: pnpm typecheck

  test:
    name: Test
    if: ${{ inputs.core == 'true' || inputs.web == 'true' }}
    runs-on: [self-hosted, linux, x64]
    timeout-minutes: 30
    steps:
      - name: Clone repository
        uses: actions/checkout@v4
      - name: Setup Nix
        uses: ./.github/actions/setup-nix
        with:
          cachix_auth_token: ${{ secrets.CACHIX_AUTH_TOKEN }}
      - name: Build WASM
        uses: ./.github/actions/build-wasm
      - name: Run pnpm tests
        shell: bash
        run: |
          nix develop --accept-flake-config .#wasm-tests --command bash << 'EOF'
            set -euo pipefail
            pnpm install
            pnpm build
            pnpm test
          EOF
      - name: Report Size
        uses: alexlwn123/compressed-size-action@alex/fix-build
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          pattern: '{packages/**/dist/**/*.{js,mjs,cjs},packages/**/*.wasm}'
          install-script: nix develop --accept-flake-config -c pnpm install
          build-script: nix develop --accept-flake-config -c pnpm build

  build-docs:
    name: Build Docs
    if: ${{ inputs.core == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - name: Clone repository
        uses: actions/checkout@v4
      - name: Install dependencies
        uses: ./.github/actions/install-deps
      - name: Build Packages (dependency of docs)
        run: pnpm build
      - name: Build Docs
        run: pnpm docs:build

  # --- NEW: Android Build (Runs on Linux) ---
  react-native-android:
    name: React Native Android
    if: ${{ inputs.react_native == 'true' && inputs.core != 'true' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dependencies
        uses: ./.github/actions/install-deps

      - name: Build Android
        run: pnpm ubrn:android:release

  # --- NEW: iOS Build (Runs on GitHub's Mac) ---
  react-native-ios:
    name: React Native iOS
    if: ${{ inputs.react_native == 'true' && inputs.core != 'true' }}
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Nix
        uses: ./.github/actions/setup-nix
        with:
          cachix_auth_token: ${{ secrets.CACHIX_AUTH_TOKEN }}

      - name: Build React Native Bindings (iOS)
        uses: ./.github/actions/build-react-native
        timeout-minutes: 90
