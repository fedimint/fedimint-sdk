---
'@fedimint/core': minor
---

Initialize Swift / iOS SDK (`ios/Package.swift`) generated via UniFFI from `fedimint-sdk`, backed by a multi-platform `FedimintSdkFFI.xcframework` (iOS devices, iOS simulator, and macOS), automated build scripts, offline XCTest suite, and interactive SwiftUI demo application.
