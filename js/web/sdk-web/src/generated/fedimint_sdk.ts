// This file was autogenerated by some hot garbage in the `uniffi-bindgen-react-native` crate.
// Trust me, you don't want to mess with it!
/* tslint:disable */
/* eslint-disable */
// @ts-nocheck
import nativeModule from './fedimint_sdk-ffi'
import { type UniffiRustFutureContinuationCallback } from './fedimint_sdk-ffi'
import {
  type FfiConverter,
  type RustBufferAllocator,
  type UniffiByteArray,
  type UniffiGcObject,
  type UniffiHandle,
  type UniffiObjectFactory,
  AbstractFfiConverterByteArray,
  Cursor,
  FfiConverterArray,
  FfiConverterArrayBuffer,
  FfiConverterBool,
  FfiConverterMap,
  FfiConverterObject,
  FfiConverterObjectAsError,
  FfiConverterOptional,
  FfiConverterUInt16,
  FfiConverterUInt32,
  FfiConverterUInt64,
  RustBuffer,
  UniffiAbstractObject,
  UniffiEnum,
  UniffiInternalError,
  UniffiRustCaller,
  UniffiThrownObject,
  destructorGuardSymbol,
  pointerLiteralSymbol,
  uniffiCreateFfiConverterString,
  uniffiCreateRecord,
  uniffiRustCallAsync,
  uniffiTypeNameSymbol,
} from '@ubjs/core'
const uniffiCaller = new UniffiRustCaller(() => ({ code: 0 }))

const uniffiIsDebug =
  // @ts-ignore -- The process global might not be defined
  typeof process !== 'object' ||
  // @ts-ignore -- The process global might not be defined
  process?.env?.NODE_ENV !== 'production' ||
  false

// Public interface members begin here.

/**
 * Opens an instance over `data_dir`: on native targets a directory path, in a browser the name
 * of an origin-scoped store (1 to 64 characters of letters, digits, `-`, `_` or `.`).
 *
 * Pass a `mnemonic` ([`Mnemonic::from_words`] / [`Mnemonic::generate`]) to establish that seed.
 * Pass `None` to use the seed the storage already holds, or, over storage proven empty, to
 * generate and persist a fresh one. The failure modes are exactly those of
 * [`SdkBuilder::build`](crate::SdkBuilder::build).
 *
 * The flattened form of [`SdkBuilder`](crate::SdkBuilder), which cannot itself cross the FFI: a
 * builder hands out `Self` by value, and UniFFI objects cross as `Arc`.
 */
export async function createFedimintSdk(
  dataDir: string,
  mnemonic: MnemonicLike | undefined,
  asyncOpts_?: { signal: AbortSignal },
): Promise<SdkLike> /*throws*/ {
  return await uniffiRustCallAsync(
    /*rustCaller:*/ uniffiCaller,
    /*rustFutureFunc:*/ () => {
      return nativeModule().uniffi_fedimint_sdk_fn_func_create_fedimint_sdk(
        FfiConverterString.lower(dataDir, nativeModule().rustbuffer_alloc),
        FfiConverterOptionalTypeMnemonic.lower(
          mnemonic,
          nativeModule().rustbuffer_alloc,
        ),
      )
    },
    /*pollFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_poll_u64,
    /*cancelFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_cancel_u64,
    /*completeFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_complete_u64,
    /*freeFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_free_u64,
    // Async returns always go through the JS-side converter: the
    // FFI symbol returns the future handle (u64), and the user-level
    // RustBuffer comes back via the shared `rust_future_complete_*`
    // export. The bytes the runtime hands back must be deserialized
    // here using the per-callable return-type converter.
    /*liftFunc:*/ FfiConverterTypeSdk.lift.bind(FfiConverterTypeSdk),
    /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    /*asyncOpts:*/ asyncOpts_,
    /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
      FfiConverterTypeError__as_error,
    ),
  )
}

const stringConverter = (() => {
  const encoder = new TextEncoder()
  const decoder = new TextDecoder()
  return {
    stringToBytes: (s: string) => encoder.encode(s),
    bytesToString: (ab: UniffiByteArray) => decoder.decode(ab),
    stringByteLength: (s: string) => encoder.encode(s).byteLength,
    writeStringIntoBuffer: (s: string, buf: any, offset: number): number => {
      const view = new Uint8Array(
        buf.arrayBuffer,
        offset,
        buf.arrayBuffer.byteLength - offset,
      )
      return encoder.encodeInto(s, view).written
    },
    readStringFromBuffer: (buf: any, offset: number, length: number): string =>
      decoder.decode(new Uint8Array(buf.arrayBuffer, offset, length)),
  }
})()
const FfiConverterString = uniffiCreateFfiConverterString(stringConverter)

/**
 * Typealias from the type name used in the UDL file to the builtin type.  This
 * is needed because the UDL type name is used in function/method signatures.
 */
export type OperationId = string
// FfiConverter for OperationId, a type alias for string.
const FfiConverterTypeOperationId = FfiConverterString

/**
 * What kind of work an operation is doing.
 *
 * Reported by [`AnyOperation::kind`] and carried on
 * [`ActivityItem`](crate::ActivityItem), so that a history screen can
 * label and group rows without having to resolve each one to a typed
 * handle first.
 *
 * `#[non_exhaustive]`: new kinds arrive with new modules, and Rust callers
 * must include a wildcard arm. [`Unknown`](OperationKind::Unknown) is a real
 * variant every binding already has, so a kind added later is reported
 * through it rather than left undecodable.
 */
export enum OperationKind {
  /**
   * Ecash spent out of band, tracked by
   * [`EcashSendState`](crate::EcashSendState).
   */
  EcashSend,
  /**
   * Ecash notes redeemed into the balance, tracked by
   * [`EcashReceiveState`](crate::EcashReceiveState).
   */
  EcashReceive,
  /**
   * An outgoing lightning payment, tracked by
   * [`LnSendState`](crate::LnSendState).
   */
  LnSend,
  /**
   * An incoming lightning payment, tracked by
   * [`LnReceiveState`](crate::LnReceiveState).
   */
  LnReceive,
  /**
   * An on-chain withdrawal, tracked by
   * [`OnchainSendState`](crate::OnchainSendState).
   */
  OnchainSend,
  /**
   * An on-chain deposit, tracked by
   * [`OnchainReceiveState`](crate::OnchainReceiveState).
   */
  OnchainReceive,
  /**
   * Restoring a wallet from its seed, tracked by
   * [`RecoveryState`](crate::RecoveryState).
   */
  Recovery,
  /**
   * An operation this SDK version cannot interpret.
   *
   * Persisted operations outlive the version that created them: an
   * application may be downgraded, or a federation may have been used
   * with a build that supported a module this one does not. Such an
   * operation is still real, still recorded, and still identifiable by
   * id. What was actually persisted stays readable through
   * [`AnyOperation::raw_kind`]: the tag, the module it belonged to, and
   * the schema version it was written at.
   *
   * None of the typed accessors on [`AnyOperation`] match it; they return
   * `None`, as they do for a mismatched kind.
   *
   * In [activity history](crate::ActivityItem) such a row reports
   * [`ActivityStatus::Unknown`](crate::ActivityStatus::Unknown) rather
   * than a guessed outcome, with
   * [`ActivityItem::is_final`](crate::ActivityItem::is_final) still
   * answering whether it has finished.
   */
  Unknown,
}

const FfiConverterTypeOperationKind = (() => {
  type TypeName = OperationKind
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      switch (c.readI32()) {
        case 1:
          return OperationKind.EcashSend
        case 2:
          return OperationKind.EcashReceive
        case 3:
          return OperationKind.LnSend
        case 4:
          return OperationKind.LnReceive
        case 5:
          return OperationKind.OnchainSend
        case 6:
          return OperationKind.OnchainReceive
        case 7:
          return OperationKind.Recovery
        case 8:
          return OperationKind.Unknown
        default:
          throw new UniffiInternalError.UnexpectedEnumCase()
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      switch (value) {
        case OperationKind.EcashSend:
          return c.writeI32(1)
        case OperationKind.EcashReceive:
          return c.writeI32(2)
        case OperationKind.LnSend:
          return c.writeI32(3)
        case OperationKind.LnReceive:
          return c.writeI32(4)
        case OperationKind.OnchainSend:
          return c.writeI32(5)
        case OperationKind.OnchainReceive:
          return c.writeI32(6)
        case OperationKind.Recovery:
          return c.writeI32(7)
        case OperationKind.Unknown:
          return c.writeI32(8)
      }
    }
    allocationSize(value: TypeName): number {
      return 4
    }
  }
  return new FFIConverter()
})()

/**
 * Typealias from the type name used in the UDL file to the builtin type.  This
 * is needed because the UDL type name is used in function/method signatures.
 */
export type Timestamp = bigint
// FfiConverter for Timestamp, a type alias for bigint.
const FfiConverterTypeTimestamp = FfiConverterUInt64

/**
 * Typealias from the type name used in the UDL file to the builtin type.  This
 * is needed because the UDL type name is used in function/method signatures.
 */
export type Amount = bigint
// FfiConverter for Amount, a type alias for bigint.
const FfiConverterTypeAmount = FfiConverterUInt64

/**
 * Which way a transfer moves value.
 *
 * The intended direction of the operation's terms: it says which way the
 * balance moves when the operation succeeds, and is the same for a row that
 * is still pending, was refunded, or failed.
 */
export enum Direction {
  /**
   * Value comes into this federation's balance.
   */
  Incoming,
  /**
   * Value leaves this federation's balance.
   */
  Outgoing,
}

const FfiConverterTypeDirection = (() => {
  type TypeName = Direction
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      switch (c.readI32()) {
        case 1:
          return Direction.Incoming
        case 2:
          return Direction.Outgoing
        default:
          throw new UniffiInternalError.UnexpectedEnumCase()
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      switch (value) {
        case Direction.Incoming:
          return c.writeI32(1)
        case Direction.Outgoing:
          return c.writeI32(2)
      }
    }
    allocationSize(value: TypeName): number {
      return 4
    }
  }
  return new FFIConverter()
})()

/**
 * How an activity row turned out.
 *
 * Coarse on purpose: this is the summary a list row shows, and the full
 * detail lives on the operation itself, reachable through
 * [`ActivityItem::operation_id`]. Mapping a rich set of operation states
 * down to a handful of buckets is the whole job of this type.
 *
 * Every variant here is an *outcome*. Whether the operation has finished is
 * a separate axis, carried by [`ActivityItem::is_final`], and the two are
 * only redundant for the five buckets this SDK can interpret; see
 * [`Unknown`](Self::Unknown).
 *
 * [`Refunded`](Self::Refunded) and [`Canceled`](Self::Canceled) are
 * first-class rather than being folded into
 * [`Failed`](Self::Failed) because transaction lists need them: "your
 * payment failed and the money is back" and "you took your ecash back" are
 * outcomes users understand and expect to see distinguished, and both are
 * ordinary rather than alarming.
 *
 * # Which operation state lands in which bucket
 *
 * For a kind this SDK version understands, every non-final state maps to
 * [`Pending`](Self::Pending), so only the final ones are listed. There is no
 * unmapped state: if a state is not here, it is not final. A row whose state
 * cannot be read at all is the one case outside this table, and it is
 * [`Unknown`](Self::Unknown).
 *
 * | operation state | bucket |
 * | --- | --- |
 * | [`EcashSendState::Redeemed`](crate::EcashSendState::Redeemed) | [`Success`](Self::Success) |
 * | [`EcashSendState::Canceled`](crate::EcashSendState::Canceled) | [`Canceled`](Self::Canceled) |
 * | [`EcashReceiveState::Done`](crate::EcashReceiveState::Done) | [`Success`](Self::Success) |
 * | [`EcashReceiveState::Failed`](crate::EcashReceiveState::Failed) | [`Failed`](Self::Failed) |
 * | [`LnSendState::Success`](crate::LnSendState::Success) | [`Success`](Self::Success) |
 * | [`LnSendState::Refunded`](crate::LnSendState::Refunded) | [`Refunded`](Self::Refunded) |
 * | [`LnSendState::Failed`](crate::LnSendState::Failed) | [`Failed`](Self::Failed) |
 * | [`LnReceiveState::Claimed`](crate::LnReceiveState::Claimed) | [`Success`](Self::Success) |
 * | [`LnReceiveState::Canceled`](crate::LnReceiveState::Canceled) | [`Canceled`](Self::Canceled) |
 * | [`LnReceiveState::Expired`](crate::LnReceiveState::Expired) | [`Canceled`](Self::Canceled) |
 * | [`LnReceiveState::Failed`](crate::LnReceiveState::Failed) | [`Failed`](Self::Failed) |
 * | [`OnchainSendState::Succeeded`](crate::OnchainSendState::Succeeded) | [`Success`](Self::Success) |
 * | [`OnchainSendState::Refunded`](crate::OnchainSendState::Refunded) | [`Refunded`](Self::Refunded) |
 * | [`OnchainSendState::Failed`](crate::OnchainSendState::Failed) | [`Failed`](Self::Failed) |
 * | [`OnchainReceiveState::Claimed`](crate::OnchainReceiveState::Claimed) | [`Success`](Self::Success) |
 * | [`OnchainReceiveState::Failed`](crate::OnchainReceiveState::Failed) | [`Failed`](Self::Failed) |
 * | [`RecoveryState::Done`](crate::RecoveryState::Done) | [`Success`](Self::Success) |
 * | [`RecoveryState::Failed`](crate::RecoveryState::Failed) | [`Failed`](Self::Failed) |
 *
 * The one placement that is a judgement rather than a reading is
 * [`LnReceiveState::Expired`](crate::LnReceiveState::Expired). An invoice
 * that simply lapsed unpaid is not [`Failed`](Self::Failed), nothing
 * broke, so it joins the withdrawn-invoice case under
 * [`Canceled`](Self::Canceled).
 *
 * # An uninterpretable row
 *
 * A row whose [`kind`](crate::ActivityItem::kind) is
 * [`OperationKind::Unknown`](crate::OperationKind::Unknown) was written by a
 * version of the SDK that understood something this one does not, so its
 * outcome cannot be interpreted and must not be guessed at. Such a row
 * reports [`Unknown`](Self::Unknown), never [`Pending`](Self::Pending):
 * finality is recorded independently of the outcome, so a finished row this
 * build cannot read reports `status == Unknown` with `is_final == true`,
 * and one still running reports the same status with `is_final == false`.
 * See [`ActivityItem::is_final`].
 *
 * [`amount`](crate::ActivityItem::amount),
 * [`fee`](crate::ActivityItem::fee) and
 * [`direction`](crate::ActivityItem::direction) stay `None`, so there is
 * nothing to render wrongly. What the row actually said about itself is
 * still readable: resolve [`operation_id`](crate::ActivityItem::operation_id)
 * with [`Federation::operation`](crate::Federation::operation) and read
 * [`AnyOperation::raw_kind`](crate::AnyOperation::raw_kind), so an
 * application can log or display which thing it did not understand instead
 * of only that something was unrecognised. Render it as an opaque entry,
 * not as a stalled payment and not as a failure.
 */
export enum ActivityStatus {
  /**
   * Still in flight: the operation has not reached a final state.
   *
   * Reported only for rows this SDK version can interpret. A row it
   * cannot interpret reports [`Unknown`](Self::Unknown) instead, whether
   * or not it has finished.
   */
  Pending,
  /**
   * Completed as intended.
   */
  Success,
  /**
   * Ended without completing, and without the value being known to be
   * safe in the balance.
   */
  Failed,
  /**
   * Ended without completing, leaving no lasting debit: the value the
   * operation was authorised for is spendable again.
   *
   * Both the ordinary endings arrive here, a lightning payment that could
   * not be routed and had its contract refunded as much as one whose
   * funding transaction the federation rejected. What they share is the
   * promise, which is about the balance now rather than about what
   * happened on the way: no part of the authorised total is still
   * standing against it.
   *
   * An operation is not put in this bucket until that is true of it.
   * Value committed to a transaction the federation then rejects is
   * neither spent nor immediately spendable, and returning it is a later
   * transaction of its own; the row stays [`Pending`](Self::Pending)
   * while that runs. A recovery that settles without establishing the
   * return lands in [`Failed`](Self::Failed) instead, which is exactly
   * the distinction between the two buckets.
   */
  Refunded,
  /**
   * Ended without completing, because the operation was called off or
   * simply lapsed: reclaimed out-of-band ecash, whose notes went out and
   * came back; a lightning receive withdrawn before it was paid; or an
   * invoice whose expiry passed unpaid.
   *
   * None of these is alarming, and none of them is
   * [`Failed`](Self::Failed): nothing went wrong, the transfer just did
   * not happen.
   */
  Canceled,
  /**
   * This SDK version cannot interpret how the row turned out.
   *
   * Not a sixth outcome, but the absence of a reading, and it is reported
   * in three situations:
   *
   * - the row's [`kind`](crate::ActivityItem::kind) is
   * [`OperationKind::Unknown`](crate::OperationKind::Unknown), a record
   * written by a build that understood a module or an operation this one
   * does not;
   * - the kind is one this build knows but the persisted final state is a
   * variant its state enum does not have, which a newer build can write
   * because every state enum here is `#[non_exhaustive]`; and
   * - the kind is one this build knows, no ending has been recorded, and
   * the operation's current state could not be read on this call. Such
   * a row reports [`is_final`](crate::ActivityItem::is_final) as
   * `false`, which says only that no ending is on record, and a later
   * page may read it.
   *
   * In all three, the outcome is unreadable while the row itself is
   * perfectly real. Guessing is not an option, and neither is
   * [`Pending`](Self::Pending): see [the section
   * above](ActivityStatus#an-uninterpretable-row).
   *
   * Whether such an operation has finished is a separate question, and one
   * the SDK does answer: read [`ActivityItem::is_final`]. A UI should show
   * the row, its time, and that its outcome is unknown, and should show
   * neither an amount nor a failure.
   */
  Unknown,
}

const FfiConverterTypeActivityStatus = (() => {
  type TypeName = ActivityStatus
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      switch (c.readI32()) {
        case 1:
          return ActivityStatus.Pending
        case 2:
          return ActivityStatus.Success
        case 3:
          return ActivityStatus.Failed
        case 4:
          return ActivityStatus.Refunded
        case 5:
          return ActivityStatus.Canceled
        case 6:
          return ActivityStatus.Unknown
        default:
          throw new UniffiInternalError.UnexpectedEnumCase()
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      switch (value) {
        case ActivityStatus.Pending:
          return c.writeI32(1)
        case ActivityStatus.Success:
          return c.writeI32(2)
        case ActivityStatus.Failed:
          return c.writeI32(3)
        case ActivityStatus.Refunded:
          return c.writeI32(4)
        case ActivityStatus.Canceled:
          return c.writeI32(5)
        case ActivityStatus.Unknown:
          return c.writeI32(6)
      }
    }
    allocationSize(value: TypeName): number {
      return 4
    }
  }
  return new FFIConverter()
})()

/**
 * One row of a federation's activity history.
 *
 * Read through [`Federation::activity`](crate::Federation::activity),
 * which returns them newest first. The point of this type is to let an
 * application render a single transaction list across ecash, lightning,
 * and on-chain activity without querying each facade separately and
 * merging the results itself.
 *
 * # This is local history, not complete history
 *
 * An activity row exists because this SDK instance recorded it while it
 * was happening, not because the federation kept a record of the account:
 *
 * - Restoring a seed does not restore this history. Recovery reconstructs
 * what the federation can prove (notes, spendable balance,
 * recoverable operations), not a narrative of past activity.
 * A wallet restored on a new device has a correct balance and an empty
 * or partial activity list.
 * - Activity from another device or another client is not here. The same
 * seed used in another application produces rows in that application's
 * own storage.
 * - Forgetting a federation erases its rows along with the rest of its
 * local state.
 *
 * An application that needs durable, portable history must keep its own,
 * keyed by [`ActivityItem::operation_id`].
 *
 * # What the numbers mean
 *
 * [`amount`](ActivityItem::amount) and [`fee`](ActivityItem::fee) are
 * defined here, once, for every kind, so that two bindings rendering the
 * same row produce the same two numbers. One rule, in three clauses, all
 * describing the terms of the transfer the operation set out to make, and
 * holding for every row whatever its outcome:
 *
 * 1. `amount` is the counterparty figure of those terms: what the other
 * side was to receive (outgoing) or to send (incoming). Gross of this
 * wallet's fees, and as executed rather than as requested: an ecash
 * send reports the notes actually issued, not the amount typed.
 * 2. `fee` is what this wallet pays on those terms if the transfer
 * succeeds: on top of the counterparty figure when outgoing, out of
 * it when incoming.
 * 3. `direction` is which way the transfer was to move value.
 *
 * For a row whose [`status`](ActivityItem::status) is
 * [`Success`](ActivityStatus::Success) the terms are also what happened:
 * the counterparty received or paid `amount`, this wallet paid `fee`, and
 * the balance moved by `amount + fee` outgoing or `amount - fee` incoming.
 * No row folds a fee into `amount`, and no row reports a net figure there.
 * What the numbers mean for the other outcomes is stated under [The
 * identity describes what was
 * attempted](ActivityItem#the-identity-describes-what-was-attempted).
 *
 * | kind | `amount` (the counterparty figure) | `fee` |
 * | --- | --- | --- |
 * | [`EcashSend`](OperationKind::EcashSend) | value of the notes handed over, may be rounded up from the request | cost of issuing those notes |
 * | [`EcashReceive`](OperationKind::EcashReceive) | face value of the notes presented | reissuance fee taken out of it |
 * | [`LnSend`](OperationKind::LnSend) | invoice amount, what the payee receives on success | fee bound by the executed quote |
 * | [`LnReceive`](OperationKind::LnReceive) | invoice's face value, what the payer is asked for | receive-side fee taken out of it |
 * | [`OnchainSend`](OperationKind::OnchainSend) | amount bound for the destination address | all federation-side funding costs, aggregated (peg-out, network, mint funding, change, dust) |
 * | [`OnchainReceive`](OperationKind::OnchainReceive) | gross amount that arrived on chain; `None` until a transaction is seen | all federation-side claim costs, aggregated, per [`OnchainReceiveDetails::fee`](crate::OnchainReceiveDetails::fee); `None` until the claim settles |
 * | [`Recovery`](OperationKind::Recovery) | `None`, nothing was transferred | `None` |
 * | [`Unknown`](OperationKind::Unknown) | `None`, nothing may be guessed | `None` |
 *
 * ## The identity describes what was attempted
 *
 * A row's numbers describe the transfer the operation set out to make. They
 * are *also* the realised balance movement exactly when
 * [`status`](ActivityItem::status) is [`Success`](ActivityStatus::Success).
 * For the other buckets:
 *
 * - [`Refunded`](ActivityStatus::Refunded): the attempt left no lasting
 * debit and the value is safe in the balance. That is a statement about
 * the balance once the row reads `Refunded`, not about the route taken to
 * it: an attempt may have removed value and had it restored, and the
 * bucket is not reported until that has settled. The fields go on
 * describing the attempt, "1000 sat, refunded" is what a list needs to
 * show, and it is the bucket, not the numbers, that says how it ended;
 * read the operation and the balance for what the attempt actually
 * cost.
 * - [`Canceled`](ActivityStatus::Canceled): the transfer did not happen.
 * Outgoing value that was committed has been reclaimed, and an incoming
 * transfer called off or lapsed unpaid never arrived at all, so there is
 * nothing for `amount` to have moved. The fields still describe what was
 * attempted, and again it is the bucket that says how it ended.
 * - [`Failed`](ActivityStatus::Failed): the transfer neither completed nor
 * resolved into a clean return, so the balance effect is not something
 * this row can assert. Render the attempt; read the operation and the
 * balance for the rest.
 * - [`Unknown`](ActivityStatus::Unknown): `amount`, `fee` and
 * [`direction`](ActivityItem::direction) are all `None`, so there is no
 * identity to reconcile and nothing to render wrongly.
 *
 * ## Requested is not actual
 *
 * For an ecash send the two genuinely differ, and this row reports the
 * actual. A mint issues notes in fixed denominations, so a request for
 * 1234 msat may be satisfied by notes worth more than that, and selecting
 * them may itself cost a fee. `amount` is the value of the notes the
 * receiver can redeem, which is the only figure that reconciles with what
 * left the balance. What the caller asked for is not thrown away, it is on
 * the send's own details record, as `EcashSendDetails::requested_amount`,
 * so a receipt can show both without this row carrying a third number.
 *
 * ## On-chain rows mix two denominations, exactly
 *
 * An on-chain operation's chain-side figures are whole [`Sats`](crate::Sats)
 * (a transaction output cannot be a fraction of a satoshi), while this row
 * is in millisatoshi [`Amount`]s so that one list can mix kinds. The
 * conversion is [`Sats::to_amount`](crate::Sats::to_amount), which rounds
 * nothing: a satoshi is exactly 1000 msat. So an on-chain row's `amount` is
 * always a whole multiple of 1000 msat, and a caller that wants satoshis
 * back should use [`Amount::to_sats_exact`](crate::Amount::to_sats_exact)
 * rather than dividing and hoping.
 *
 * Its `fee` is not: the federation-side costs of an on-chain operation are
 * quoted in millisatoshis and can carry sub-satoshi precision, so `fee`
 * here, and a deposit's net credit, may not divide by 1000. Read a fee
 * rather than deriving one from a chain-side figure.
 *
 * ## When two numbers are not enough
 *
 * They often are not, and that is deliberate: this is a list row, and the
 * full account of an operation lives on the operation. Resolve
 * [`operation_id`](ActivityItem::operation_id) with
 * [`Federation::operation`](crate::Federation::operation), then read
 * [`Operation::details`](crate::Operation::details) for the requested
 * amount, the gross deposit, the invoice, the destination, the route and the
 * executed quote, and [`Operation::state`](crate::Operation::state) for
 * where it stands. Two numbers and a bucket are what a list needs; a receipt
 * screen should not be built out of them.
 */
export type ActivityItem = {
  /**
   * The operation this row describes.
   *
   * Pass it to [`Federation::operation`](crate::Federation::operation)
   * to reattach to the operation itself and read its full, typed state.
   */
  operationId: OperationId
  /**
   * What kind of operation it is, for labelling and grouping.
   *
   * May be [`OperationKind::Unknown`](crate::OperationKind::Unknown) for
   * a row recorded by a version of the SDK that understood something
   * this one does not.
   */
  kind: OperationKind
  /**
   * When this SDK instance recorded the activity.
   *
   * This is a **local** clock reading, not a consensus timestamp: the
   * federation does not attest to it, it comes from the device that
   * happened to record the row, and a device with a wrong clock produces
   * wrong times here. It is suitable for ordering and displaying a
   * user's own history and unsuitable as evidence of when anything
   * actually happened.
   */
  time: Timestamp
  /**
   * The counterparty figure of the terms the operation executed on: what
   * the other side was to send or receive, gross of this wallet's fees,
   * and as executed rather than as requested. For a
   * [`Success`](ActivityStatus::Success) row it is also what the other
   * side sent or received.
   *
   * Which figure that is for each kind, and what it deliberately is not,
   * is fixed by the table in [What the numbers
   * mean](ActivityItem#what-the-numbers-mean), which is the contract that
   * stops two bindings from rendering the same row differently.
   *
   * `None` for a kind with no single counterparty figure: a recovery,
   * which transfers nothing; a row this SDK cannot interpret, where any
   * number would be invented; or an on-chain deposit before a transaction
   * has been seen, where there is nothing yet to report. A figure that
   * starts `None` and becomes known is written once and never changes
   * afterwards.
   */
  amount?: Amount
  /**
   * The fee the operation's terms carry, what this wallet pays for the
   * transfer if it succeeds, when it is known.
   *
   * Always a separate field from [`ActivityItem::amount`] and never folded
   * into it: a successful outgoing row debited `amount + fee`, a
   * successful incoming row credited `amount - fee`, and a list showing
   * "1000 sat" wants the number the user typed or the payee received
   * rather than a fee-inclusive total that matches neither.
   *
   * `None` when the kind has no fee at all, or when the fee is not knowable
   * yet, an operation still in flight, or an on-chain deposit whose
   * claim fee only exists once something has arrived. `Some(zero)` and
   * `None` are different answers, and a UI should treat them so: the first
   * says the terms carry no fee, the second that this row cannot say yet.
   */
  fee?: Amount
  /**
   * Which way the transfer was to move value: in or out.
   *
   * `None` for kinds that have no direction, a recovery, for example,
   * is neither incoming nor outgoing. This is `Option` rather than a
   * third "neither" variant on [`Direction`] so that a UI branching on
   * direction handles the no-direction case by not drawing an arrow at
   * all, rather than by drawing a third kind of arrow.
   *
   * It also selects which half of the accounting identity applies to a
   * [`Success`](ActivityStatus::Success) row:
   * [`Outgoing`](Direction::Outgoing) debited `amount + fee`,
   * [`Incoming`](Direction::Incoming) credited `amount - fee`. A row with
   * no direction has no identity to reconcile, and both figures are
   * `None`.
   */
  direction?: Direction
  /**
   * How the operation turned out, or that it has not yet.
   */
  status: ActivityStatus
  /**
   * Whether the operation has finished.
   *
   * `true` once it has reached a state it will never leave, the same
   * predicate [`OperationState::is_final`](crate::OperationState::is_final)
   * applies to a typed state.
   *
   * This is recorded independently of [`status`](ActivityItem::status)
   * because for one bucket the two genuinely come apart. An
   * [`Unknown`](ActivityStatus::Unknown) row's outcome cannot be
   * interpreted, and inferring from that that it must still be running
   * would be a fabrication: a row this SDK version cannot read may well
   * have settled years ago. Whether an operation *finished* is a different
   * question from *how it turned out*, and it is one this SDK can still
   * answer for a record it cannot otherwise interpret.
   *
   * For every other bucket the two agree, and must:
   * `is_final` is `false` exactly when `status` is
   * [`Pending`](ActivityStatus::Pending). That is what makes this the one
   * predicate a list can use to decide whether to draw a spinner, on every
   * row, with no special case for the rows it does not understand.
   */
  isFinal: boolean
}

/**
 * Generated factory for {@link ActivityItem} record objects.
 */
export const ActivityItem = (() => {
  const defaults = () => ({})
  const create = (() => {
    return uniffiCreateRecord<ActivityItem, ReturnType<typeof defaults>>(
      defaults,
    )
  })()
  return Object.freeze({
    create,
    new: create,
    defaults: () => Object.freeze(defaults()) as Partial<ActivityItem>,
  })
})()

const FfiConverterTypeActivityItem = (() => {
  type TypeName = ActivityItem
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      return {
        operationId: FfiConverterTypeOperationId.readFromCursor(c),
        kind: FfiConverterTypeOperationKind.readFromCursor(c),
        time: FfiConverterTypeTimestamp.readFromCursor(c),
        amount: FfiConverterOptionalTypeAmount.readFromCursor(c),
        fee: FfiConverterOptionalTypeAmount.readFromCursor(c),
        direction: FfiConverterOptionalTypeDirection.readFromCursor(c),
        status: FfiConverterTypeActivityStatus.readFromCursor(c),
        isFinal: FfiConverterBool.readFromCursor(c),
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      FfiConverterTypeOperationId.writeIntoCursor(value.operationId, c)
      FfiConverterTypeOperationKind.writeIntoCursor(value.kind, c)
      FfiConverterTypeTimestamp.writeIntoCursor(value.time, c)
      FfiConverterOptionalTypeAmount.writeIntoCursor(value.amount, c)
      FfiConverterOptionalTypeAmount.writeIntoCursor(value.fee, c)
      FfiConverterOptionalTypeDirection.writeIntoCursor(value.direction, c)
      FfiConverterTypeActivityStatus.writeIntoCursor(value.status, c)
      FfiConverterBool.writeIntoCursor(value.isFinal, c)
    }
    allocationSize(value: TypeName): number {
      return (
        FfiConverterTypeOperationId.allocationSize(value.operationId) +
        FfiConverterTypeOperationKind.allocationSize(value.kind) +
        FfiConverterTypeTimestamp.allocationSize(value.time) +
        FfiConverterOptionalTypeAmount.allocationSize(value.amount) +
        FfiConverterOptionalTypeAmount.allocationSize(value.fee) +
        FfiConverterOptionalTypeDirection.allocationSize(value.direction) +
        FfiConverterTypeActivityStatus.allocationSize(value.status) +
        FfiConverterBool.allocationSize(value.isFinal)
      )
    }
  }
  return new FFIConverter()
})()

/**
 * Typealias from the type name used in the UDL file to the builtin type.  This
 * is needed because the UDL type name is used in function/method signatures.
 */
export type Cursor = string
// FfiConverter for Cursor, a type alias for string.
const FfiConverterTypeCursor = FfiConverterString

/**
 * One page of activity history.
 *
 * Returned by [`Federation::activity`](crate::Federation::activity). Pages
 * run newest first; carry [`ActivityPage::next`] into the following call
 * to continue.
 */
export type ActivityPage = {
  /**
   * The rows in this page, newest first. May contain fewer items than
   * the requested limit, including none at all.
   */
  items: Array<ActivityItem>
  /**
   * The cursor for the following page, or `None` when this page is the
   * last one.
   *
   * Treat it as an opaque value: pass it back unchanged, or persist and
   * reload it, but never construct or interpret one. See
   * [`Cursor`](crate::Cursor).
   */
  next?: Cursor
}

/**
 * Generated factory for {@link ActivityPage} record objects.
 */
export const ActivityPage = (() => {
  const defaults = () => ({})
  const create = (() => {
    return uniffiCreateRecord<ActivityPage, ReturnType<typeof defaults>>(
      defaults,
    )
  })()
  return Object.freeze({
    create,
    new: create,
    defaults: () => Object.freeze(defaults()) as Partial<ActivityPage>,
  })
})()

const FfiConverterTypeActivityPage = (() => {
  type TypeName = ActivityPage
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      return {
        items: FfiConverterSequenceTypeActivityItem.readFromCursor(c),
        next: FfiConverterOptionalTypeCursor.readFromCursor(c),
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      FfiConverterSequenceTypeActivityItem.writeIntoCursor(value.items, c)
      FfiConverterOptionalTypeCursor.writeIntoCursor(value.next, c)
    }
    allocationSize(value: TypeName): number {
      return (
        FfiConverterSequenceTypeActivityItem.allocationSize(value.items) +
        FfiConverterOptionalTypeCursor.allocationSize(value.next)
      )
    }
  }
  return new FFIConverter()
})()

/**
 * Which capabilities a federation offers.
 *
 * Each flag says whether the corresponding accessor on [`Federation`]
 * would return `Some`. Reading them is how an application decides what to
 * put on screen before the user acts, rather than discovering an absent
 * capability by attempting an operation and handling the failure.
 *
 * `#[non_exhaustive]` like every data type here: a federation gaining a
 * new kind of capability must be an additive change, not a breaking one.
 */
export type Capabilities = {
  /**
   * Whether [`Federation::ecash`] is available.
   */
  ecash: boolean
  /**
   * Whether [`Federation::lightning`] is available.
   */
  lightning: boolean
  /**
   * Whether [`Federation::onchain`] is available.
   */
  onchain: boolean
}

/**
 * Generated factory for {@link Capabilities} record objects.
 */
export const Capabilities = (() => {
  const defaults = () => ({})
  const create = (() => {
    return uniffiCreateRecord<Capabilities, ReturnType<typeof defaults>>(
      defaults,
    )
  })()
  return Object.freeze({
    create,
    new: create,
    defaults: () => Object.freeze(defaults()) as Partial<Capabilities>,
  })
})()

const FfiConverterTypeCapabilities = (() => {
  type TypeName = Capabilities
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      return {
        ecash: FfiConverterBool.readFromCursor(c),
        lightning: FfiConverterBool.readFromCursor(c),
        onchain: FfiConverterBool.readFromCursor(c),
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      FfiConverterBool.writeIntoCursor(value.ecash, c)
      FfiConverterBool.writeIntoCursor(value.lightning, c)
      FfiConverterBool.writeIntoCursor(value.onchain, c)
    }
    allocationSize(value: TypeName): number {
      return (
        FfiConverterBool.allocationSize(value.ecash) +
        FfiConverterBool.allocationSize(value.lightning) +
        FfiConverterBool.allocationSize(value.onchain)
      )
    }
  }
  return new FFIConverter()
})()

/**
 * A revision of a federation's consensus metadata.
 *
 * The guardians can change consensus metadata while the federation runs;
 * each agreed change increments [`ConsensusMetadata::revision`]. Comparing
 * revisions is how an application detects a change without diffing the
 * document.
 */
export type ConsensusMetadata = {
  /**
   * The revision number of this metadata. Monotonically increasing;
   * a larger number is a later version of the same document.
   */
  revision: bigint
  /**
   * The metadata document as raw bytes, exactly as consensus stores it.
   *
   * Commonly, but not necessarily, UTF-8 JSON. The meta module's value is
   * an arbitrary byte string with no encoding guarantee, so this field is
   * `Vec<u8>` rather than `String`: a `String` could not hold what the
   * guardians actually agreed on, and anything that did not decode would
   * have to be mangled or dropped to fit. The SDK does not require,
   * validate, reformat, or re-encode any of it: this is the unprojected
   * value, byte for byte, for the application to interpret.
   *
   * The flat, string-valued projection used by [`Meta::get`] and
   * [`Meta::all`] is derived from these bytes and is lossy; see the
   * type-level documentation on [`Meta`] for exactly what it drops. This
   * field is what remains authoritative when it does.
   */
  value: ArrayBuffer
}

/**
 * Generated factory for {@link ConsensusMetadata} record objects.
 */
export const ConsensusMetadata = (() => {
  const defaults = () => ({})
  const create = (() => {
    return uniffiCreateRecord<ConsensusMetadata, ReturnType<typeof defaults>>(
      defaults,
    )
  })()
  return Object.freeze({
    create,
    new: create,
    defaults: () => Object.freeze(defaults()) as Partial<ConsensusMetadata>,
  })
})()

const FfiConverterTypeConsensusMetadata = (() => {
  type TypeName = ConsensusMetadata
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      return {
        revision: FfiConverterUInt64.readFromCursor(c),
        value: FfiConverterArrayBuffer.readFromCursor(c),
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      FfiConverterUInt64.writeIntoCursor(value.revision, c)
      FfiConverterArrayBuffer.writeIntoCursor(value.value, c)
    }
    allocationSize(value: TypeName): number {
      return (
        FfiConverterUInt64.allocationSize(value.revision) +
        FfiConverterArrayBuffer.allocationSize(value.value)
      )
    }
  }
  return new FFIConverter()
})()

/**
 * Stable, machine-readable failure category for [`Error`].
 *
 * Additive-only after 1.0: new variants may be added in minor releases, and
 * existing variants are never removed or renamed. `#[non_exhaustive]`, so a
 * Rust caller must write a wildcard arm; a variant added later is not a
 * breaking change for Rust.
 *
 * That guarantee is Rust-only. A generated Swift, Kotlin or TypeScript
 * decoder does not tolerate a tag it has never seen: it fails to decode the
 * error rather than falling back to an "unknown" case. Regenerate the
 * binding against the SDK version it talks to (the default expectation for
 * this crate) to avoid the gap, or, where that is not possible, hand-write
 * and test an adapter that carries the code across as its stable variant
 * name with an explicit unknown fallback.
 *
 * [`ErrorDetails`] is the one place forward decodability is built into the
 * wire format itself, because there the payload is length-delimited opaque
 * bytes; see [`RawErrorDetails`].
 */
export enum ErrorCode {
  /**
   * The input could not be parsed or was structurally invalid, a
   * malformed invite code, invoice, ecash notes, address, mnemonic, or
   * activity cursor.
   */
  InvalidInput,
  /**
   * The federation identified by an invite code has already been joined.
   */
  AlreadyJoined,
  /**
   * The federation cannot be used as configured. This includes mixed
   * module generations within one federation (all modules must share the
   * same v1/v2 generation) and configurations the SDK otherwise refuses
   * to operate on. For the mixed-generation case,
   * [`ErrorDetails::MixedModuleGenerations`] names the conflicting
   * modules and the generation each declares.
   */
  UnsupportedFederation,
  /**
   * No guardian could be reached to service the request.
   */
  FederationUnreachable,
  /**
   * The spendable balance is too low to cover the requested amount.
   * [`ErrorDetails::InsufficientBalance`] carries the required and
   * available amounts.
   */
  InsufficientBalance,
  /**
   * A request to permanently forget a federation was made while spendable
   * balance still remains in it. [`ErrorDetails::BalanceNotEmpty`]
   * carries how much.
   */
  BalanceNotEmpty,
  /**
   * A request to permanently forget a federation was made while
   * non-final operations or reclaimable outgoing value still exist for
   * it.
   *
   * An incomplete recovery is deliberately *not* one of those reasons:
   * erasing the federation is the only way out of a recovery that cannot
   * be finished, so this code is never returned on that account.
   */
  PendingOperations,
  /**
   * No usable lightning gateway is currently available.
   */
  GatewayUnavailable,
  /**
   * The requested action is unavailable because this federation's
   * recovery is incomplete.
   *
   * Incomplete is not the same as still running: a recovery that stopped
   * short leaves the lock in place, because a wallet restored only partly
   * must not be spendable. Only a recovery that runs to completion
   * releases it.
   */
  Recovering,
  /**
   * The federation does not have the module backing this facade. This
   * occurs when a facade obtained earlier is used after the federation's
   * configuration changed to drop that module.
   */
  NotSupported,
  /**
   * A persisted operation exists but this build cannot interpret it: the
   * record names a kind this build does not know, or a state schema newer
   * than it reads. The operation is still observable (its id, and its kind
   * where known, are readable) but not actionable.
   * [`OperationSupport`](crate::OperationSupport) names which of the two
   * it was.
   */
  UnsupportedOperation,
  /**
   * Storage already holds a seed and it does not match the mnemonic
   * supplied to open it. [`ErrorDetails::SeedMismatch`] names the
   * storage location.
   */
  SeedMismatch,
  /**
   * The storage location is already open, in this process or another.
   * [`ErrorDetails::StorageInUse`] names the location.
   */
  StorageInUse,
  /**
   * Storage holds federation or client state but no usable seed, so it
   * cannot be opened. The seed entry is either **absent** altogether, or
   * **present and unusable**, truncated, corrupt, or written in a format
   * this build does not understand. [`ErrorDetails::StorageOrphaned`] names
   * the location and says which of the two it was.
   *
   * **Permanent, not transient**, unlike [`Storage`](ErrorCode::Storage): nothing about a
   * store whose state has no matching seed changes by asking again. Nothing is mutated
   * by the refusal either, so the location is exactly as it was found.
   *
   * **What a caller can do.** Not retry, and not repair it automatically. Report it and
   * offer the ways out, in this order:
   *
   * 1. Where the entry was present but unusable, **update the SDK or the
   * app**: a newer build may read a format this one cannot.
   * 2. **Point at a different location.** The everyday cause is a path
   * that moved, or storage belonging to another app, profile, or user.
   * If the seed phrase is known, it also recovers the funds, at a
   * *fresh* location, never by writing into this one.
   * 3. **Abandon the location deliberately**, by deleting its contents,
   * which makes it provably empty and therefore openable. This destroys
   * any funds whose only backup was that seed, so it is a last resort a
   * person chooses explicitly, never a step an application takes on
   * their behalf.
   */
  StorageOrphaned,
  /**
   * The quote passed to `send` is no longer valid: either its validity
   * window has passed, or it has already been executed, and a quote funds
   * exactly one payment. Both have the same remedy: obtain a fresh quote
   * and retry.
   *
   * The already-executed case is what a binding reports when a quote
   * object crosses the boundary and is used a second time.
   *
   * [`ErrorDetails::QuoteExpired`] carries the validity window that
   * lapsed and says which of the two sub-cases occurred, for a UI that
   * wants to phrase them differently.
   */
  QuoteExpired,
  /**
   * Conditions material to the quote (fees, routing, federation state)
   * changed since it was issued. Obtain a fresh quote and retry.
   * [`ErrorDetails::QuoteTermsChanged`] carries the total debit that was
   * quoted and the total it moved to.
   */
  QuoteChanged,
  /**
   * The bolt11 invoice specifies no amount, and such an invoice cannot be
   * paid.
   *
   * This is **not** a request for the caller to supply an amount, and no
   * amount the caller supplies can make the invoice payable. Fedimint
   * does not support paying amountless bolt11 invoices at all: that is a
   * permanent limitation, not a gap in this SDK that a later release will
   * fill.
   *
   * The only remedy is a different invoice, one that names its own
   * amount. An application taking an invoice from a QR code or a paste
   * buffer should say so plainly ("this invoice does not specify an
   * amount and cannot be paid here") instead of prompting for a number it
   * cannot use.
   */
  AmountlessInvoice,
  /**
   * A supplied address or invoice is for a different network than the
   * federation's.
   *
   * [`ErrorDetails::NetworkMismatch`] carries the federation's network
   * exactly, and, for the rejected value, what its encoding actually
   * proves: the set of networks it could have been for, plus the prefix it
   * was spelled with. That is deliberately not one exact network: a
   * `tb1…` address is testnet3, testnet4 or signet, and a BOLT11 `tb`
   * invoice is either public testnet, so naming one would be a guess.
   */
  NetworkMismatch,
  /**
   * The federation handle is closed, either because it was individually
   * closed while retaining its data, or because the whole SDK instance
   * was shut down.
   */
  FederationClosed,
  /**
   * The operation did not complete within an internal time budget.
   */
  Timeout,
  /**
   * The platform's secure random source was unavailable or failed, so no
   * entropy could be drawn, as when
   * [`Mnemonic::generate`](crate::Mnemonic::generate) creates a fresh seed,
   * directly or through [`SdkBuilder::build`](crate::SdkBuilder::build)
   * establishing one for empty storage.
   *
   * This is the one failure a caller can do nothing about: there is no
   * input to correct, no permission to grant, and no retry that reliably
   * helps. It is still surfaced rather than papered over, because the
   * alternatives are worse: panicking would take down a binding layer
   * that must not panic, and falling back to a weaker source would mint a
   * guessable seed and lose funds silently. Report it and stop; do not
   * substitute entropy of your own.
   */
  Entropy,
  /**
   * The local storage backend failed to read or write.
   *
   * A fault in the backend itself: the I/O failed, the browser store
   * rejected the operation, the device is out of space, and therefore
   * potentially **transient**: retrying, or retrying once the underlying
   * condition is gone, is a reasonable thing for a caller to do.
   *
   * It does **not** cover a store whose contents are wrong. Storage that
   * holds state but no usable seed is [`StorageOrphaned`], which is
   * permanent and needs a human;
   * [`SeedMismatch`](ErrorCode::SeedMismatch) and
   * [`StorageInUse`](ErrorCode::StorageInUse) are the other two conditions
   * that are about the store rather than the backend.
   *
   * [`StorageOrphaned`]: ErrorCode::StorageOrphaned
   */
  Storage,
  /**
   * An internal error that does not fit any other category. Its presence
   * generally indicates a bug; `message` carries what diagnostic detail
   * is available.
   */
  Internal,
}

const FfiConverterTypeErrorCode = (() => {
  type TypeName = ErrorCode
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      switch (c.readI32()) {
        case 1:
          return ErrorCode.InvalidInput
        case 2:
          return ErrorCode.AlreadyJoined
        case 3:
          return ErrorCode.UnsupportedFederation
        case 4:
          return ErrorCode.FederationUnreachable
        case 5:
          return ErrorCode.InsufficientBalance
        case 6:
          return ErrorCode.BalanceNotEmpty
        case 7:
          return ErrorCode.PendingOperations
        case 8:
          return ErrorCode.GatewayUnavailable
        case 9:
          return ErrorCode.Recovering
        case 10:
          return ErrorCode.NotSupported
        case 11:
          return ErrorCode.UnsupportedOperation
        case 12:
          return ErrorCode.SeedMismatch
        case 13:
          return ErrorCode.StorageInUse
        case 14:
          return ErrorCode.StorageOrphaned
        case 15:
          return ErrorCode.QuoteExpired
        case 16:
          return ErrorCode.QuoteChanged
        case 17:
          return ErrorCode.AmountlessInvoice
        case 18:
          return ErrorCode.NetworkMismatch
        case 19:
          return ErrorCode.FederationClosed
        case 20:
          return ErrorCode.Timeout
        case 21:
          return ErrorCode.Entropy
        case 22:
          return ErrorCode.Storage
        case 23:
          return ErrorCode.Internal
        default:
          throw new UniffiInternalError.UnexpectedEnumCase()
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      switch (value) {
        case ErrorCode.InvalidInput:
          return c.writeI32(1)
        case ErrorCode.AlreadyJoined:
          return c.writeI32(2)
        case ErrorCode.UnsupportedFederation:
          return c.writeI32(3)
        case ErrorCode.FederationUnreachable:
          return c.writeI32(4)
        case ErrorCode.InsufficientBalance:
          return c.writeI32(5)
        case ErrorCode.BalanceNotEmpty:
          return c.writeI32(6)
        case ErrorCode.PendingOperations:
          return c.writeI32(7)
        case ErrorCode.GatewayUnavailable:
          return c.writeI32(8)
        case ErrorCode.Recovering:
          return c.writeI32(9)
        case ErrorCode.NotSupported:
          return c.writeI32(10)
        case ErrorCode.UnsupportedOperation:
          return c.writeI32(11)
        case ErrorCode.SeedMismatch:
          return c.writeI32(12)
        case ErrorCode.StorageInUse:
          return c.writeI32(13)
        case ErrorCode.StorageOrphaned:
          return c.writeI32(14)
        case ErrorCode.QuoteExpired:
          return c.writeI32(15)
        case ErrorCode.QuoteChanged:
          return c.writeI32(16)
        case ErrorCode.AmountlessInvoice:
          return c.writeI32(17)
        case ErrorCode.NetworkMismatch:
          return c.writeI32(18)
        case ErrorCode.FederationClosed:
          return c.writeI32(19)
        case ErrorCode.Timeout:
          return c.writeI32(20)
        case ErrorCode.Entropy:
          return c.writeI32(21)
        case ErrorCode.Storage:
          return c.writeI32(22)
        case ErrorCode.Internal:
          return c.writeI32(23)
      }
    }
    allocationSize(value: TypeName): number {
      return 4
    }
  }
  return new FFIConverter()
})()

/**
 * The raw, length-delimited form of an error's structured detail: the only
 * form that ever crosses a language boundary.
 *
 * A version, a kind, and an opaque payload. That is the whole record, and it never grows
 * another field: a frozen record whose only variable part is a byte string with its length
 * in front is the one shape a reader of *any* vintage can consume completely, even one that
 * has never heard of a given kind. Everything that varies is inside
 * [`payload`](RawErrorDetails::payload), and everything in it is skippable by its length.
 *
 * It is deliberately **not** `#[non_exhaustive]`, unlike every other public
 * struct in this crate: its fields are frozen for good, so a binding layer can destructure
 * the record exhaustively, secure that a later release cannot add a field it would then
 * silently ignore. [`RawErrorDetails::new`] exists for convenience, not
 * because a struct literal is unavailable.
 *
 * # The encoding contract
 *
 * This crate has no serialization dependency and will not grow one, so the
 * payload is opaque bytes here and the encoding is a *documented contract*
 * that each boundary implements. Framing the record's three fields is the
 * boundary's own business, a native record, a JSON object, anything, subject
 * to one requirement:
 *
 * > `version` and the *length* of `kind` and `payload` are readable without
 * > interpreting either, so `payload` can be consumed or skipped by its
 * > length by a reader that has never heard of its kind.
 *
 * ## Kinds
 *
 * `kind` is a stable ASCII identifier, spelled exactly as the
 * [`ErrorDetails`] variant it projects to: `"InsufficientBalance"`,
 * `"NetworkMismatch"`, and so on; [`ErrorDetails::kind`] is the mapping.
 * Unlike [`Error::message`], these strings **are** part of the stability
 * contract: they are the discriminator, so one is never renamed, never
 * reused for a different meaning, and never case-shifted. A kind a reader
 * does not know is not an error: it skips the payload and keeps the
 * envelope as [`DetailEnvelope::Opaque`].
 *
 * ## Primitives inside the payload
 *
 * | Form | Encoding |
 * |------|----------|
 * | `u32`, `u64` | big-endian, 4 or 8 bytes, unframed |
 * | `bool` | one byte, `0` or `1`; any other value makes the payload uninterpretable |
 * | `str`, `bytes` | a `u32` big-endian byte length, then that many bytes; `str` is UTF-8 |
 * | `list<T>` | a `u32` big-endian element count, then that many `T` encodings |
 * | record | its fields in the documented order, with no framing of its own |
 * | fieldless enum | as `str`, holding the Rust variant name verbatim (`"Bitcoin"`, `"Testnet4"`) |
 *
 * A fieldless enum travels as its *name*, never as an integer tag. A name is
 * legible in a log without a table to look it up in, and a name a reader does
 * not know is skipped by its own length like any other string, the same
 * trick as the payload itself, one level down.
 *
 * ## Payload layout per kind
 *
 * | `kind` | Since | Payload fields, in order |
 * |--------|-------|--------------------------|
 * | `InsufficientBalance` | 1 | `required: u64`, `available: u64` (millisatoshis) |
 * | `NetworkMismatch` | 1 | `expected: str`, `compatible: list<str>`, `observed_prefix: str` |
 * | `MixedModuleGenerations` | 1 | `modules: list<record { kind: str, generation: u32 }>` |
 * | `QuoteExpired` | 1 | `expires_at: u64` (epoch milliseconds), `already_executed: bool` |
 * | `QuoteTermsChanged` | 1 | `quoted_total: u64`, `current_total: u64` (millisatoshis) |
 * | `BalanceNotEmpty` | 1 | `remaining: u64` (millisatoshis) |
 * | `StorageInUse` | 1 | `location: str` |
 * | `SeedMismatch` | 1 | `location: str` |
 * | `StorageOrphaned` | 1 | `location: str`, `seed_present: bool` |
 *
 * ## What a reader must do
 *
 * - **Unknown kind:** consume `payload` by its length, do not look inside it,
 * and keep the envelope as [`DetailEnvelope::Opaque`].
 * - **Known kind:** read that kind's fields in order, and require the
 * payload to end exactly where the last field does. A kind's layout never
 * drifts, so a well-formed newer producer never has more to
 * say under an old kind: it says it under a new kind instead. Trailing
 * bytes therefore mean the payload does not match the layout this kind
 * froze, and the envelope is kept as [`DetailEnvelope::Opaque`], exactly
 * like a short one.
 * - **Short or invalid payload:** a payload that ends before the last field,
 * holds invalid UTF-8, or holds a `bool` that is neither `0` nor `1` is
 * *uninterpretable*. Keep it as [`DetailEnvelope::Opaque`] and never guess
 * at a missing value: a fabricated amount in an error about amounts is
 * worse than no amount at all.
 * - **Never gate on `version`.** The kind alone decides whether a projection
 * is possible. A producer at envelope version 7 still emits version-1
 * kinds, so refusing to read a payload because `version` is unfamiliar
 * would throw away details this build understands perfectly well.
 * `version` is for saying how far ahead the producer is.
 */
export type RawErrorDetails = {
  /**
   * The envelope version the producing side declared it speaks, normally
   * that side's own [`CURRENT_VERSION`](RawErrorDetails::CURRENT_VERSION).
   *
   * `0` is reserved for "unstated" and is never a real envelope version, so
   * a reader that received no version has something honest to record.
   * Compare it against this build's `CURRENT_VERSION` to say precisely how
   * far ahead the producer is, rather than guessing.
   *
   * It is a diagnostic, not a gate: see *What a reader must do* on the
   * type.
   */
  version: number
  /**
   * The stable kind discriminator: the [`ErrorDetails`] variant name this
   * payload projects to, or a name from a newer SDK that this build has no
   * projection for.
   *
   * Part of the stability contract, unlike [`Error::message`]: this string
   * is what a reader dispatches on. An empty string is permitted for a
   * reader that genuinely received no kind, and projects to nothing.
   */
  kind: string
  /**
   * The kind's fields, encoded per the contract on this type and opaque at
   * this layer.
   *
   * A raw envelope only exists where a detail crossed a boundary or is
   * about to, so the payload is normally the encoded truth of the detail.
   * It may still be empty: a producer that stated a kind and nothing else
   * is legal, and a reader treats the missing fields as an uninterpretable
   * payload rather than guessing at them.
   */
  payload: ArrayBuffer
}

/**
 * Generated factory for {@link RawErrorDetails} record objects.
 */
export const RawErrorDetails = (() => {
  const defaults = () => ({})
  const create = (() => {
    return uniffiCreateRecord<RawErrorDetails, ReturnType<typeof defaults>>(
      defaults,
    )
  })()
  return Object.freeze({
    create,
    new: create,
    defaults: () => Object.freeze(defaults()) as Partial<RawErrorDetails>,
  })
})()

const FfiConverterTypeRawErrorDetails = (() => {
  type TypeName = RawErrorDetails
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      return {
        version: FfiConverterUInt32.readFromCursor(c),
        kind: FfiConverterString.readFromCursor(c),
        payload: FfiConverterArrayBuffer.readFromCursor(c),
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      FfiConverterUInt32.writeIntoCursor(value.version, c)
      FfiConverterString.writeIntoCursor(value.kind, c)
      FfiConverterArrayBuffer.writeIntoCursor(value.payload, c)
    }
    allocationSize(value: TypeName): number {
      return (
        FfiConverterUInt32.allocationSize(value.version) +
        FfiConverterString.allocationSize(value.kind) +
        FfiConverterArrayBuffer.allocationSize(value.payload)
      )
    }
  }
  return new FFIConverter()
})()

/**
 * Typealias from the type name used in the UDL file to the builtin type.  This
 * is needed because the UDL type name is used in function/method signatures.
 */
export type DetailEnvelope = RawErrorDetails
// FfiConverter for DetailEnvelope, a type alias for RawErrorDetails.
const FfiConverterTypeDetailEnvelope = FfiConverterTypeRawErrorDetails

/**
 * A failure described as a **value to read** rather than an error to raise: a
 * stable [`ErrorCode`], a human-readable message, and the same optional
 * [`DetailEnvelope`] an [`Error`] carries.
 *
 * This is the type a state that means "something went wrong here" holds. The
 * case it exists for today is
 * [`FederationStatus::Quarantined`](crate::FederationStatus::Quarantined):
 * the SDK could not or would not open a stored federation, and rather than
 * failing the whole instance it records why, for an application to read later
 * with [`Sdk::federation_status`](crate::Sdk::federation_status) or
 * [`Sdk::stored_federations`](crate::Sdk::stored_federations). The same shape
 * fits any later status with a reason attached.
 *
 * Unlike [`Error`], `Diagnostic` is `PartialEq` and `Eq`: a status is diffed to decide
 * whether anything changed, which is what
 * [`Sdk::federation_status_updates`](crate::Sdk::federation_status_updates) emits on.
 * It also does not implement [`core::error::Error`], since a value sitting in a status
 * field is not an in-flight failure. Convert between the two with `Error::from(diagnostic)`
 * (or `.into()`), and `Diagnostic::from(error)` the other way, which is how the SDK records
 * the error that stopped a federation opening. A diagnosis can outlive the call that
 * produced it: it may have been decided by an earlier build, in an earlier process, and
 * read back long after.
 *
 * A plain record of a fieldless [`Copy`] enum, a string, and an optional envelope: it
 * generates mechanically into a Swift or Kotlin record and a TypeScript interface, and a
 * binding decodes its `details` field with the same projection it uses for
 * [`Error::details`]. Like [`Error`], this record does not grow fields: more structured
 * detail about a diagnosed situation arrives as a new kind inside the envelope. It is
 * `#[non_exhaustive]`; build one with [`Diagnostic::new`], [`Diagnostic::with_details`] or
 * [`Diagnostic::with_raw_details`].
 */
export type Diagnostic = {
  /**
   * Stable, machine-readable failure category: the same taxonomy, with
   * the same meanings, as [`Error::code`]. Safe to match on, and the only
   * field a caller *has* to look at.
   */
  code: ErrorCode
  /**
   * Human-readable context for logs and diagnostics. Not part of the
   * stability contract: never match on this field's contents.
   */
  message: string
  /**
   * Structured, machine-readable detail for this failure, where it has
   * any, identical in meaning to [`Error::details`].
   *
   * Without it, an application that wanted to name the modules whose generations
   * conflict in a quarantined federation would have to parse `message`, which the
   * contract forbids; with it, the quarantine carries
   * [`ErrorDetails::MixedModuleGenerations`] and the modules are readable.
   *
   * `None` means no detail was attached, and never that the diagnosis is
   * less real: `code` is authoritative on its own. An envelope written by a newer SDK
   * than the one reading it stays a value, not a failure. Most callers want
   * [`Diagnostic::detail`]; match the typed case with a wildcard arm.
   */
  details?: DetailEnvelope
}

/**
 * Generated factory for {@link Diagnostic} record objects.
 */
export const Diagnostic = (() => {
  const defaults = () => ({})
  const create = (() => {
    return uniffiCreateRecord<Diagnostic, ReturnType<typeof defaults>>(defaults)
  })()
  return Object.freeze({
    create,
    new: create,
    defaults: () => Object.freeze(defaults()) as Partial<Diagnostic>,
  })
})()

const FfiConverterTypeDiagnostic = (() => {
  type TypeName = Diagnostic
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      return {
        code: FfiConverterTypeErrorCode.readFromCursor(c),
        message: FfiConverterString.readFromCursor(c),
        details: FfiConverterOptionalTypeDetailEnvelope.readFromCursor(c),
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      FfiConverterTypeErrorCode.writeIntoCursor(value.code, c)
      FfiConverterString.writeIntoCursor(value.message, c)
      FfiConverterOptionalTypeDetailEnvelope.writeIntoCursor(value.details, c)
    }
    allocationSize(value: TypeName): number {
      return (
        FfiConverterTypeErrorCode.allocationSize(value.code) +
        FfiConverterString.allocationSize(value.message) +
        FfiConverterOptionalTypeDetailEnvelope.allocationSize(value.details)
      )
    }
  }
  return new FFIConverter()
})()

/**
 * An out-of-band ecash token string, handed from a sender to a receiver
 * outside the federation (over a message, a QR code, a file).
 *
 * `Notes` bundles one or more signed ecash tokens together with enough
 * federation context for a receiver to redeem them. It is opaque: callers
 * treat it as a value to display, copy, transmit, and hand to a receive
 * call, not as something to parse apart. It round-trips through
 * [`Display`](core::fmt::Display) and [`FromStr`](core::str::FromStr) with a
 * validating parse.
 *
 * Notes obtained from a sender should be redeemed promptly: unredeemed
 * notes that a sender created are subject to that sender's automatic
 * reclaim policy, after which they stop being redeemable.
 *
 * # `Display` prints the notes, `Debug` never does
 *
 * This value **is** the money: anyone holding the string can redeem it, so
 * it is a bearer instrument in exactly the way a banknote is.
 * [`Display`](core::fmt::Display) prints the notes and is the deliberate,
 * visible way to get the value out. [`Debug`] is redacted instead, because
 * it is what logging, crash reporters and `assert!` failures reach for, and
 * a struct holding a `Notes` (such as [`EcashSend`](crate::EcashSend))
 * would otherwise print the token merely by being logged.
 */
export interface NotesLike {
  /**
   * The notes in their canonical string form, for handing to the receiver.
   *
   * This is the deliberate way to get the token out; see the type-level
   * documentation for why [`Debug`] is not.
   */
  display(): string
  /**
   * Returns the total value carried by these notes.
   *
   * This reads the value encoded in the notes themselves and does not
   * contact the federation, so it does not confirm the notes are still
   * redeemable (they could already have been spent or reclaimed), only
   * a receive call does that.
   */
  value(): Amount
}
/**
 * @deprecated Use `NotesLike` instead.
 */
export type NotesInterface = NotesLike

/**
 * An out-of-band ecash token string, handed from a sender to a receiver
 * outside the federation (over a message, a QR code, a file).
 *
 * `Notes` bundles one or more signed ecash tokens together with enough
 * federation context for a receiver to redeem them. It is opaque: callers
 * treat it as a value to display, copy, transmit, and hand to a receive
 * call, not as something to parse apart. It round-trips through
 * [`Display`](core::fmt::Display) and [`FromStr`](core::str::FromStr) with a
 * validating parse.
 *
 * Notes obtained from a sender should be redeemed promptly: unredeemed
 * notes that a sender created are subject to that sender's automatic
 * reclaim policy, after which they stop being redeemable.
 *
 * # `Display` prints the notes, `Debug` never does
 *
 * This value **is** the money: anyone holding the string can redeem it, so
 * it is a bearer instrument in exactly the way a banknote is.
 * [`Display`](core::fmt::Display) prints the notes and is the deliberate,
 * visible way to get the value out. [`Debug`] is redacted instead, because
 * it is what logging, crash reporters and `assert!` failures reach for, and
 * a struct holding a `Notes` (such as [`EcashSend`](crate::EcashSend))
 * would otherwise print the token merely by being logged.
 */
export class Notes extends UniffiAbstractObject implements NotesLike {
  readonly [uniffiTypeNameSymbol] = 'Notes'
  readonly [destructorGuardSymbol]: UniffiGcObject
  readonly [pointerLiteralSymbol]: UniffiHandle
  // No primary constructor declared for this class.
  private constructor(pointer: UniffiHandle) {
    super()
    this[pointerLiteralSymbol] = pointer
    this[destructorGuardSymbol] = uniffiTypeNotesObjectFactory.bless(pointer)
  }

  /**
   * Parses and validates ecash notes from their canonical string form.
   *
   * # Errors
   *
   * [`InvalidInput`](crate::ErrorCode::InvalidInput) for a malformed value.
   */
  static parse(notes: string): NotesLike /*throws*/ {
    return FfiConverterTypeNotes.lift(
      uniffiCaller.rustCallWithError(
        /*liftError:*/ FfiConverterTypeError__as_error.lift.bind(
          FfiConverterTypeError__as_error,
        ),
        /*caller:*/ (callStatus) => {
          return nativeModule().uniffi_fedimint_sdk_fn_constructor_notes_parse(
            FfiConverterString.lower(notes, nativeModule().rustbuffer_alloc),
            callStatus,
          )
        },
        /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      ),
    )
  }

  /**
   * The notes in their canonical string form, for handing to the receiver.
   *
   * This is the deliberate way to get the token out; see the type-level
   * documentation for why [`Debug`] is not.
   */
  display(): string {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_notes_display(
          uniffiTypeNotesObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterString.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * Returns the total value carried by these notes.
   *
   * This reads the value encoded in the notes themselves and does not
   * contact the federation, so it does not confirm the notes are still
   * redeemable (they could already have been spent or reclaimed), only
   * a receive call does that.
   */
  value(): Amount {
    return FfiConverterTypeAmount.lift(
      uniffiCaller.rustCall(
        /*caller:*/ (callStatus) => {
          return nativeModule().uniffi_fedimint_sdk_fn_method_notes_value(
            uniffiTypeNotesObjectFactory.clonePointer(this),
            callStatus,
          )
        },
        /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      ),
    )
  }

  uniffiDestroy(): void {
    const ptr = (this as any)[destructorGuardSymbol]
    if (ptr !== undefined) {
      const pointer = uniffiTypeNotesObjectFactory.pointer(this)
      uniffiTypeNotesObjectFactory.freePointer(pointer)
      uniffiTypeNotesObjectFactory.unbless(ptr)
      delete (this as any)[destructorGuardSymbol]
    }
  }

  static instanceOf(obj_: any): obj_ is Notes {
    return uniffiTypeNotesObjectFactory.isConcreteType(obj_)
  }
}

const uniffiTypeNotesObjectFactory: UniffiObjectFactory<NotesLike> = (() => {
  /// <reference lib="es2021" />
  const registry =
    typeof FinalizationRegistry !== 'undefined'
      ? new FinalizationRegistry<UniffiHandle>((heldValue: UniffiHandle) => {
          uniffiTypeNotesObjectFactory.freePointer(heldValue)
        })
      : null

  return {
    create(pointer: UniffiHandle): NotesLike {
      const instance = Object.create(Notes.prototype)
      instance[pointerLiteralSymbol] = pointer
      instance[destructorGuardSymbol] = this.bless(pointer)
      instance[uniffiTypeNameSymbol] = 'Notes'
      return instance
    },

    bless(p: UniffiHandle): UniffiGcObject {
      const ptr = {
        p, // make sure this object doesn't get optimized away.
        markDestroyed: () => undefined,
      }
      if (registry) {
        registry.register(ptr, p, ptr)
      }
      return ptr
    },

    unbless(ptr_: UniffiGcObject) {
      if (registry) {
        registry.unregister(ptr_)
      }
    },

    pointer(obj_: NotesLike): UniffiHandle {
      if ((obj_ as any)[destructorGuardSymbol] === undefined) {
        throw new UniffiInternalError.UnexpectedNullPointer()
      }
      return (obj_ as any)[pointerLiteralSymbol]
    },

    clonePointer(obj_: NotesLike): UniffiHandle {
      const pointer = this.pointer(obj_)
      return uniffiCaller.rustCall(
        /*caller:*/ (callStatus) =>
          nativeModule().uniffi_fedimint_sdk_fn_clone_notes(
            pointer,
            callStatus,
          ),
        /*liftString:*/ FfiConverterString.lift,
      )
    },

    freePointer(pointer: UniffiHandle): void {
      uniffiCaller.rustCall(
        /*caller:*/ (callStatus) =>
          nativeModule().uniffi_fedimint_sdk_fn_free_notes(pointer, callStatus),
        /*liftString:*/ FfiConverterString.lift,
      )
    },

    isConcreteType(obj_: any): obj_ is NotesLike {
      return (
        obj_[destructorGuardSymbol] && obj_[uniffiTypeNameSymbol] === 'Notes'
      )
    },
  }
})()
const FfiConverterTypeNotes = new FfiConverterObject(
  uniffiTypeNotesObjectFactory,
)

/**
 * The UniFFI view of [`EcashReceiveDetails`], with `notes` crossing as `Option<Arc<Notes>>`, for
 * the same reason as the send projection above. Exported as `EcashReceiveDetails`.
 */
export type EcashReceiveDetails = {
  /**
   * See [`EcashReceiveDetails::notes`].
   */
  notes?: NotesLike
  /**
   * See [`EcashReceiveDetails::notes_value`].
   */
  notesValue: Amount
  /**
   * See [`EcashReceiveDetails::fee`].
   */
  fee: Amount
  /**
   * See [`EcashReceiveDetails::net_credit`].
   */
  netCredit: Amount
  /**
   * See [`EcashReceiveDetails::created_at`].
   */
  createdAt: Timestamp
}

/**
 * Generated factory for {@link EcashReceiveDetails} record objects.
 */
export const EcashReceiveDetails = (() => {
  const defaults = () => ({})
  const create = (() => {
    return uniffiCreateRecord<EcashReceiveDetails, ReturnType<typeof defaults>>(
      defaults,
    )
  })()
  return Object.freeze({
    create,
    new: create,
    defaults: () => Object.freeze(defaults()) as Partial<EcashReceiveDetails>,
  })
})()

const FfiConverterTypeEcashReceiveDetails = (() => {
  type TypeName = EcashReceiveDetails
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      return {
        notes: FfiConverterOptionalTypeNotes.readFromCursor(c),
        notesValue: FfiConverterTypeAmount.readFromCursor(c),
        fee: FfiConverterTypeAmount.readFromCursor(c),
        netCredit: FfiConverterTypeAmount.readFromCursor(c),
        createdAt: FfiConverterTypeTimestamp.readFromCursor(c),
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      FfiConverterOptionalTypeNotes.writeIntoCursor(value.notes, c)
      FfiConverterTypeAmount.writeIntoCursor(value.notesValue, c)
      FfiConverterTypeAmount.writeIntoCursor(value.fee, c)
      FfiConverterTypeAmount.writeIntoCursor(value.netCredit, c)
      FfiConverterTypeTimestamp.writeIntoCursor(value.createdAt, c)
    }
    allocationSize(value: TypeName): number {
      return (
        FfiConverterOptionalTypeNotes.allocationSize(value.notes) +
        FfiConverterTypeAmount.allocationSize(value.notesValue) +
        FfiConverterTypeAmount.allocationSize(value.fee) +
        FfiConverterTypeAmount.allocationSize(value.netCredit) +
        FfiConverterTypeTimestamp.allocationSize(value.createdAt)
      )
    }
  }
  return new FFIConverter()
})()

/**
 * The UniFFI view of [`EcashSendDetails`], with `notes` crossing as `Arc<Notes>`: [`Notes`] is a
 * UniFFI object, and an object can sit in a record only behind an `Arc`. Exported as
 * `EcashSendDetails`; the real type never crosses.
 */
export type EcashSendDetails = {
  /**
   * See [`EcashSendDetails::notes`].
   */
  notes: NotesLike
  /**
   * See [`EcashSendDetails::requested_amount`].
   */
  requestedAmount: Amount
  /**
   * See [`EcashSendDetails::notes_value`].
   */
  notesValue: Amount
  /**
   * See [`EcashSendDetails::fee`].
   */
  fee: Amount
  /**
   * See [`EcashSendDetails::total_debited`].
   */
  totalDebited: Amount
  /**
   * See [`EcashSendDetails::reclaim_at`].
   */
  reclaimAt: Timestamp
  /**
   * See [`EcashSendDetails::created_at`].
   */
  createdAt: Timestamp
}

/**
 * Generated factory for {@link EcashSendDetails} record objects.
 */
export const EcashSendDetails = (() => {
  const defaults = () => ({})
  const create = (() => {
    return uniffiCreateRecord<EcashSendDetails, ReturnType<typeof defaults>>(
      defaults,
    )
  })()
  return Object.freeze({
    create,
    new: create,
    defaults: () => Object.freeze(defaults()) as Partial<EcashSendDetails>,
  })
})()

const FfiConverterTypeEcashSendDetails = (() => {
  type TypeName = EcashSendDetails
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      return {
        notes: FfiConverterTypeNotes.readFromCursor(c),
        requestedAmount: FfiConverterTypeAmount.readFromCursor(c),
        notesValue: FfiConverterTypeAmount.readFromCursor(c),
        fee: FfiConverterTypeAmount.readFromCursor(c),
        totalDebited: FfiConverterTypeAmount.readFromCursor(c),
        reclaimAt: FfiConverterTypeTimestamp.readFromCursor(c),
        createdAt: FfiConverterTypeTimestamp.readFromCursor(c),
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      FfiConverterTypeNotes.writeIntoCursor(value.notes, c)
      FfiConverterTypeAmount.writeIntoCursor(value.requestedAmount, c)
      FfiConverterTypeAmount.writeIntoCursor(value.notesValue, c)
      FfiConverterTypeAmount.writeIntoCursor(value.fee, c)
      FfiConverterTypeAmount.writeIntoCursor(value.totalDebited, c)
      FfiConverterTypeTimestamp.writeIntoCursor(value.reclaimAt, c)
      FfiConverterTypeTimestamp.writeIntoCursor(value.createdAt, c)
    }
    allocationSize(value: TypeName): number {
      return (
        FfiConverterTypeNotes.allocationSize(value.notes) +
        FfiConverterTypeAmount.allocationSize(value.requestedAmount) +
        FfiConverterTypeAmount.allocationSize(value.notesValue) +
        FfiConverterTypeAmount.allocationSize(value.fee) +
        FfiConverterTypeAmount.allocationSize(value.totalDebited) +
        FfiConverterTypeTimestamp.allocationSize(value.reclaimAt) +
        FfiConverterTypeTimestamp.allocationSize(value.createdAt)
      )
    }
  }
  return new FFIConverter()
})()

/**
 * The lifecycle of an out-of-band ecash send.
 *
 * An ecash send has exactly two terminal outcomes: the notes came back
 * ([`Canceled`](Self::Canceled)) or the receiver got them
 * ([`Redeemed`](Self::Redeemed)), because those are the only two things
 * that can happen to the money. There is no failure state: if storage
 * cannot be read, no guardian answers, or the federation handle is closed,
 * that is a failure to *observe* the send, reported as `Err` from
 * [`Operation::state`](crate::Operation::state),
 * [`Operation::await_final`](crate::Operation::await_final) or
 * [`OperationUpdates::next`](crate::OperationUpdates::next), not a state
 * of the send itself. The send keeps running, unaffected by the fact that
 * nobody could see it: bearer notes out in the world can still be redeemed
 * or reclaimed long after some call failed to observe them. See
 * [`Sdk::forget_federation`](crate::Sdk::forget_federation), which refuses
 * while reclaimable outgoing value remains.
 */
export enum EcashSendState {
  /**
   * The notes have been issued and handed to the caller. The value has
   * left the spendable balance; nobody has redeemed or reclaimed it
   * yet.
   */
  Created,
  /**
   * A reclaim has been requested, either by
   * [`request_cancel`](Operation::request_cancel) or by the automatic
   * reclaim timer, and is being processed. Not final: the request may
   * still lose to a redemption.
   */
  CancelRequested,
  /**
   * Final: the notes were reclaimed and their value is back in the
   * spendable balance.
   */
  Canceled,
  /**
   * Final: the receiver redeemed the notes. The value is theirs; a
   * cancellation request, if one was made, lost the race.
   */
  Redeemed,
}

const FfiConverterTypeEcashSendState = (() => {
  type TypeName = EcashSendState
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      switch (c.readI32()) {
        case 1:
          return EcashSendState.Created
        case 2:
          return EcashSendState.CancelRequested
        case 3:
          return EcashSendState.Canceled
        case 4:
          return EcashSendState.Redeemed
        default:
          throw new UniffiInternalError.UnexpectedEnumCase()
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      switch (value) {
        case EcashSendState.Created:
          return c.writeI32(1)
        case EcashSendState.CancelRequested:
          return c.writeI32(2)
        case EcashSendState.Canceled:
          return c.writeI32(3)
        case EcashSendState.Redeemed:
          return c.writeI32(4)
      }
    }
    allocationSize(value: TypeName): number {
      return 4
    }
  }
  return new FFIConverter()
})()

/**
 * The one error type returned from every fallible call in this crate.
 *
 * It has three fields, and each has a different job:
 *
 * - [`code`](Error::code) is the stable, machine-readable failure category. It is always
 * enough on its own to decide what to do about a failure.
 * - [`details`](Error::details) carries structured detail where a failure has any: the
 * balance that was short, the networks that disagreed, the modules whose generations
 * conflicted, and so on. [`Error::detail`] is the short path to the typed
 * [`ErrorDetails`] case; no caller has to parse `message` to get at these numbers.
 * - [`message`](Error::message) is for humans only (logs, error banners, bug reports). It
 * is not part of the stability contract and must never be parsed or matched on.
 *
 * The same three fields, with the same meanings, also travel as [`Diagnostic`]: a value for
 * when a failure is reported as state rather than raised, such as why a federation sits in
 * [`FederationStatus::Quarantined`](crate::FederationStatus::Quarantined).
 * [`Error`] and [`Diagnostic`] convert both ways.
 *
 * # The details envelope
 *
 * New structured detail always arrives as a new kind inside the `details` envelope, never
 * as a new field on `Error` and never as a new field on an existing [`ErrorDetails`] case.
 * See [`RawErrorDetails`] for the wire form the envelope takes crossing a language boundary.
 * Three rules govern it:
 *
 * 1. **`code` is authoritative; `details` only sharpens it.** `details` is always
 * `Option`, and `None` never means the error is less real: it means this failure had no
 * numbers worth reporting. A caller that ignores `details` entirely still branches
 * correctly on `code`. What `details` buys is what a caller can *show*, such as "you
 * need 1,500 msat and have 1,200" instead of "insufficient balance".
 * 2. **An uninterpretable detail is a value, not a failure.** A caller that meets a `kind`
 * it has no vocabulary for keeps the raw envelope as [`DetailEnvelope::Opaque`]. Nothing
 * is dropped, nothing is fatal, and `code` and `message` still describe the failure
 * completely and correctly.
 * 3. **A kind's meaning and payload layout never drift.** [`ErrorDetails::version`] reports
 * the envelope version that introduced a case, and
 * [`RawErrorDetails::CURRENT_VERSION`] is the newest version this build speaks. A
 * reinterpreted situation is a new kind at a new version; the old kind keeps meaning
 * exactly what it always meant.
 *
 * [`ErrorCode`] never carries detail either: it is a fieldless `Copy` enum, so it maps onto
 * a plain Swift, Kotlin, or TypeScript enum. No addition ever removes or repurposes `code`,
 * `details`, or `message`.
 */
export interface ExceptionLike {
  /**
   * The stable [`ErrorCode`] for this failure.
   */
  code(): ErrorCode
  /**
   * The structured detail for this failure, where it has any.
   */
  details(): RawErrorDetails | undefined
  /**
   * The human-readable message. Never parse it; branch on [`code`](Self::code).
   */
  reason(): string
}
/**
 * @deprecated Use `ExceptionLike` instead.
 */
export type ExceptionInterface = ExceptionLike

/**
 * The one error type returned from every fallible call in this crate.
 *
 * It has three fields, and each has a different job:
 *
 * - [`code`](Error::code) is the stable, machine-readable failure category. It is always
 * enough on its own to decide what to do about a failure.
 * - [`details`](Error::details) carries structured detail where a failure has any: the
 * balance that was short, the networks that disagreed, the modules whose generations
 * conflicted, and so on. [`Error::detail`] is the short path to the typed
 * [`ErrorDetails`] case; no caller has to parse `message` to get at these numbers.
 * - [`message`](Error::message) is for humans only (logs, error banners, bug reports). It
 * is not part of the stability contract and must never be parsed or matched on.
 *
 * The same three fields, with the same meanings, also travel as [`Diagnostic`]: a value for
 * when a failure is reported as state rather than raised, such as why a federation sits in
 * [`FederationStatus::Quarantined`](crate::FederationStatus::Quarantined).
 * [`Error`] and [`Diagnostic`] convert both ways.
 *
 * # The details envelope
 *
 * New structured detail always arrives as a new kind inside the `details` envelope, never
 * as a new field on `Error` and never as a new field on an existing [`ErrorDetails`] case.
 * See [`RawErrorDetails`] for the wire form the envelope takes crossing a language boundary.
 * Three rules govern it:
 *
 * 1. **`code` is authoritative; `details` only sharpens it.** `details` is always
 * `Option`, and `None` never means the error is less real: it means this failure had no
 * numbers worth reporting. A caller that ignores `details` entirely still branches
 * correctly on `code`. What `details` buys is what a caller can *show*, such as "you
 * need 1,500 msat and have 1,200" instead of "insufficient balance".
 * 2. **An uninterpretable detail is a value, not a failure.** A caller that meets a `kind`
 * it has no vocabulary for keeps the raw envelope as [`DetailEnvelope::Opaque`]. Nothing
 * is dropped, nothing is fatal, and `code` and `message` still describe the failure
 * completely and correctly.
 * 3. **A kind's meaning and payload layout never drift.** [`ErrorDetails::version`] reports
 * the envelope version that introduced a case, and
 * [`RawErrorDetails::CURRENT_VERSION`] is the newest version this build speaks. A
 * reinterpreted situation is a new kind at a new version; the old kind keeps meaning
 * exactly what it always meant.
 *
 * [`ErrorCode`] never carries detail either: it is a fieldless `Copy` enum, so it maps onto
 * a plain Swift, Kotlin, or TypeScript enum. No addition ever removes or repurposes `code`,
 * `details`, or `message`.
 */
export class Exception extends UniffiAbstractObject implements ExceptionLike {
  readonly [uniffiTypeNameSymbol] = 'Exception'
  readonly [destructorGuardSymbol]: UniffiGcObject
  readonly [pointerLiteralSymbol]: UniffiHandle
  // No primary constructor declared for this class.
  private constructor(pointer: UniffiHandle) {
    super()
    this[pointerLiteralSymbol] = pointer
    this[destructorGuardSymbol] =
      uniffiTypeExceptionObjectFactory.bless(pointer)
  }

  /**
   * The stable [`ErrorCode`] for this failure.
   */
  code(): ErrorCode {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_error_code(
          uniffiTypeExceptionObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterTypeErrorCode.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * The structured detail for this failure, where it has any.
   */
  details(): RawErrorDetails | undefined {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_error_details(
          uniffiTypeExceptionObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterOptionalTypeRawErrorDetails.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * The human-readable message. Never parse it; branch on [`code`](Self::code).
   */
  reason(): string {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_error_reason(
          uniffiTypeExceptionObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterString.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  uniffiDestroy(): void {
    const ptr = (this as any)[destructorGuardSymbol]
    if (ptr !== undefined) {
      const pointer = uniffiTypeExceptionObjectFactory.pointer(this)
      uniffiTypeExceptionObjectFactory.freePointer(pointer)
      uniffiTypeExceptionObjectFactory.unbless(ptr)
      delete (this as any)[destructorGuardSymbol]
    }
  }

  static instanceOf(obj_: any): obj_ is Exception {
    return uniffiTypeExceptionObjectFactory.isConcreteType(obj_)
  }

  static hasInner(obj_: any): obj_ is UniffiThrownObject<Exception> {
    return (
      UniffiThrownObject.instanceOf(obj_) && Exception.instanceOf(obj_.inner)
    )
  }

  static getInner(err: UniffiThrownObject<Exception>): Exception {
    return err.inner
  }
}

const uniffiTypeExceptionObjectFactory: UniffiObjectFactory<ExceptionLike> =
  (() => {
    /// <reference lib="es2021" />
    const registry =
      typeof FinalizationRegistry !== 'undefined'
        ? new FinalizationRegistry<UniffiHandle>((heldValue: UniffiHandle) => {
            uniffiTypeExceptionObjectFactory.freePointer(heldValue)
          })
        : null

    return {
      create(pointer: UniffiHandle): ExceptionLike {
        const instance = Object.create(Exception.prototype)
        instance[pointerLiteralSymbol] = pointer
        instance[destructorGuardSymbol] = this.bless(pointer)
        instance[uniffiTypeNameSymbol] = 'Exception'
        return instance
      },

      bless(p: UniffiHandle): UniffiGcObject {
        const ptr = {
          p, // make sure this object doesn't get optimized away.
          markDestroyed: () => undefined,
        }
        if (registry) {
          registry.register(ptr, p, ptr)
        }
        return ptr
      },

      unbless(ptr_: UniffiGcObject) {
        if (registry) {
          registry.unregister(ptr_)
        }
      },

      pointer(obj_: ExceptionLike): UniffiHandle {
        if ((obj_ as any)[destructorGuardSymbol] === undefined) {
          throw new UniffiInternalError.UnexpectedNullPointer()
        }
        return (obj_ as any)[pointerLiteralSymbol]
      },

      clonePointer(obj_: ExceptionLike): UniffiHandle {
        const pointer = this.pointer(obj_)
        return uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_clone_error(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      freePointer(pointer: UniffiHandle): void {
        uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_free_error(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      isConcreteType(obj_: any): obj_ is ExceptionLike {
        return (
          obj_[destructorGuardSymbol] &&
          obj_[uniffiTypeNameSymbol] === 'Exception'
        )
      },
    }
  })()
const FfiConverterTypeError = new FfiConverterObject(
  uniffiTypeExceptionObjectFactory,
)
const FfiConverterTypeError__as_error = new FfiConverterObjectAsError(
  'Exception',
  FfiConverterTypeError,
)

/**
 * The UniFFI view of one `OperationUpdates<S>` instantiation, behind a lock so `next`'s
 * `&mut self` can cross as `&self`.
 */
export interface EcashSendOperationUpdatesLike {
  /**
   * See [`OperationUpdates::next`](crate::OperationUpdates::next).
   */
  next(asyncOpts_?: {
    signal: AbortSignal
  }) /*throws*/ : Promise<EcashSendState | undefined>
}
/**
 * @deprecated Use `EcashSendOperationUpdatesLike` instead.
 */
export type EcashSendOperationUpdatesInterface = EcashSendOperationUpdatesLike

/**
 * The UniFFI view of one `OperationUpdates<S>` instantiation, behind a lock so `next`'s
 * `&mut self` can cross as `&self`.
 */
export class EcashSendOperationUpdates
  extends UniffiAbstractObject
  implements EcashSendOperationUpdatesLike
{
  readonly [uniffiTypeNameSymbol] = 'EcashSendOperationUpdates'
  readonly [destructorGuardSymbol]: UniffiGcObject
  readonly [pointerLiteralSymbol]: UniffiHandle
  // No primary constructor declared for this class.
  private constructor(pointer: UniffiHandle) {
    super()
    this[pointerLiteralSymbol] = pointer
    this[destructorGuardSymbol] =
      uniffiTypeEcashSendOperationUpdatesObjectFactory.bless(pointer)
  }

  /**
   * See [`OperationUpdates::next`](crate::OperationUpdates::next).
   */
  async next(asyncOpts_?: {
    signal: AbortSignal
  }): Promise<EcashSendState | undefined> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_ecashsendoperationupdates_next(
          uniffiTypeEcashSendOperationUpdatesObjectFactory.clonePointer(this),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterOptionalTypeEcashSendState.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  uniffiDestroy(): void {
    const ptr = (this as any)[destructorGuardSymbol]
    if (ptr !== undefined) {
      const pointer =
        uniffiTypeEcashSendOperationUpdatesObjectFactory.pointer(this)
      uniffiTypeEcashSendOperationUpdatesObjectFactory.freePointer(pointer)
      uniffiTypeEcashSendOperationUpdatesObjectFactory.unbless(ptr)
      delete (this as any)[destructorGuardSymbol]
    }
  }

  static instanceOf(obj_: any): obj_ is EcashSendOperationUpdates {
    return uniffiTypeEcashSendOperationUpdatesObjectFactory.isConcreteType(obj_)
  }
}

const uniffiTypeEcashSendOperationUpdatesObjectFactory: UniffiObjectFactory<EcashSendOperationUpdatesLike> =
  (() => {
    /// <reference lib="es2021" />
    const registry =
      typeof FinalizationRegistry !== 'undefined'
        ? new FinalizationRegistry<UniffiHandle>((heldValue: UniffiHandle) => {
            uniffiTypeEcashSendOperationUpdatesObjectFactory.freePointer(
              heldValue,
            )
          })
        : null

    return {
      create(pointer: UniffiHandle): EcashSendOperationUpdatesLike {
        const instance = Object.create(EcashSendOperationUpdates.prototype)
        instance[pointerLiteralSymbol] = pointer
        instance[destructorGuardSymbol] = this.bless(pointer)
        instance[uniffiTypeNameSymbol] = 'EcashSendOperationUpdates'
        return instance
      },

      bless(p: UniffiHandle): UniffiGcObject {
        const ptr = {
          p, // make sure this object doesn't get optimized away.
          markDestroyed: () => undefined,
        }
        if (registry) {
          registry.register(ptr, p, ptr)
        }
        return ptr
      },

      unbless(ptr_: UniffiGcObject) {
        if (registry) {
          registry.unregister(ptr_)
        }
      },

      pointer(obj_: EcashSendOperationUpdatesLike): UniffiHandle {
        if ((obj_ as any)[destructorGuardSymbol] === undefined) {
          throw new UniffiInternalError.UnexpectedNullPointer()
        }
        return (obj_ as any)[pointerLiteralSymbol]
      },

      clonePointer(obj_: EcashSendOperationUpdatesLike): UniffiHandle {
        const pointer = this.pointer(obj_)
        return uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_clone_ecashsendoperationupdates(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      freePointer(pointer: UniffiHandle): void {
        uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_free_ecashsendoperationupdates(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      isConcreteType(obj_: any): obj_ is EcashSendOperationUpdatesLike {
        return (
          obj_[destructorGuardSymbol] &&
          obj_[uniffiTypeNameSymbol] === 'EcashSendOperationUpdates'
        )
      },
    }
  })()
const FfiConverterTypeEcashSendOperationUpdates = new FfiConverterObject(
  uniffiTypeEcashSendOperationUpdatesObjectFactory,
)

/**
 * The UniFFI view of one `Operation<S>` instantiation: an opaque object wrapping the real
 * handle, forwarding every method.
 */
export interface EcashSendOperationLike {
  /**
   * See [`Operation::await_final`](crate::Operation::await_final).
   */
  awaitFinal(asyncOpts_?: {
    signal: AbortSignal
  }) /*throws*/ : Promise<EcashSendState>
  /**
   * See [`Operation::details`](crate::Operation::details).
   */
  details(asyncOpts_?: {
    signal: AbortSignal
  }) /*throws*/ : Promise<EcashSendDetails>
  /**
   * See [`Operation::id`](crate::Operation::id).
   */
  id(): OperationId
  /**
   * See `Operation::<EcashSendState>::request_cancel`.
   */
  requestCancel(asyncOpts_?: { signal: AbortSignal }) /*throws*/ : Promise<void>
  /**
   * See [`Operation::state`](crate::Operation::state).
   */
  state(asyncOpts_?: {
    signal: AbortSignal
  }) /*throws*/ : Promise<EcashSendState>
  /**
   * See [`Operation::updates`](crate::Operation::updates).
   */
  updates(): EcashSendOperationUpdatesLike
}
/**
 * @deprecated Use `EcashSendOperationLike` instead.
 */
export type EcashSendOperationInterface = EcashSendOperationLike

/**
 * The UniFFI view of one `Operation<S>` instantiation: an opaque object wrapping the real
 * handle, forwarding every method.
 */
export class EcashSendOperation
  extends UniffiAbstractObject
  implements EcashSendOperationLike
{
  readonly [uniffiTypeNameSymbol] = 'EcashSendOperation'
  readonly [destructorGuardSymbol]: UniffiGcObject
  readonly [pointerLiteralSymbol]: UniffiHandle
  // No primary constructor declared for this class.
  private constructor(pointer: UniffiHandle) {
    super()
    this[pointerLiteralSymbol] = pointer
    this[destructorGuardSymbol] =
      uniffiTypeEcashSendOperationObjectFactory.bless(pointer)
  }

  /**
   * See [`Operation::await_final`](crate::Operation::await_final).
   */
  async awaitFinal(asyncOpts_?: {
    signal: AbortSignal
  }): Promise<EcashSendState> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_ecashsendoperation_await_final(
          uniffiTypeEcashSendOperationObjectFactory.clonePointer(this),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterTypeEcashSendState.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * See [`Operation::details`](crate::Operation::details).
   */
  async details(asyncOpts_?: {
    signal: AbortSignal
  }): Promise<EcashSendDetails> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_ecashsendoperation_details(
          uniffiTypeEcashSendOperationObjectFactory.clonePointer(this),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterTypeEcashSendDetails.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * See [`Operation::id`](crate::Operation::id).
   */
  id(): OperationId {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_ecashsendoperation_id(
          uniffiTypeEcashSendOperationObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterTypeOperationId.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * See `Operation::<EcashSendState>::request_cancel`.
   */
  async requestCancel(asyncOpts_?: {
    signal: AbortSignal
  }): Promise<void> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_ecashsendoperation_request_cancel(
          uniffiTypeEcashSendOperationObjectFactory.clonePointer(this),
        )
      },
      /*pollFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_poll_void,
      /*cancelFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_cancel_void,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_void,
      /*freeFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_free_void,
      /*liftFunc:*/ (_v) => {},
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * See [`Operation::state`](crate::Operation::state).
   */
  async state(asyncOpts_?: {
    signal: AbortSignal
  }): Promise<EcashSendState> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_ecashsendoperation_state(
          uniffiTypeEcashSendOperationObjectFactory.clonePointer(this),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterTypeEcashSendState.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * See [`Operation::updates`](crate::Operation::updates).
   */
  updates(): EcashSendOperationUpdatesLike {
    return FfiConverterTypeEcashSendOperationUpdates.lift(
      uniffiCaller.rustCall(
        /*caller:*/ (callStatus) => {
          return nativeModule().uniffi_fedimint_sdk_fn_method_ecashsendoperation_updates(
            uniffiTypeEcashSendOperationObjectFactory.clonePointer(this),
            callStatus,
          )
        },
        /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      ),
    )
  }

  uniffiDestroy(): void {
    const ptr = (this as any)[destructorGuardSymbol]
    if (ptr !== undefined) {
      const pointer = uniffiTypeEcashSendOperationObjectFactory.pointer(this)
      uniffiTypeEcashSendOperationObjectFactory.freePointer(pointer)
      uniffiTypeEcashSendOperationObjectFactory.unbless(ptr)
      delete (this as any)[destructorGuardSymbol]
    }
  }

  static instanceOf(obj_: any): obj_ is EcashSendOperation {
    return uniffiTypeEcashSendOperationObjectFactory.isConcreteType(obj_)
  }
}

const uniffiTypeEcashSendOperationObjectFactory: UniffiObjectFactory<EcashSendOperationLike> =
  (() => {
    /// <reference lib="es2021" />
    const registry =
      typeof FinalizationRegistry !== 'undefined'
        ? new FinalizationRegistry<UniffiHandle>((heldValue: UniffiHandle) => {
            uniffiTypeEcashSendOperationObjectFactory.freePointer(heldValue)
          })
        : null

    return {
      create(pointer: UniffiHandle): EcashSendOperationLike {
        const instance = Object.create(EcashSendOperation.prototype)
        instance[pointerLiteralSymbol] = pointer
        instance[destructorGuardSymbol] = this.bless(pointer)
        instance[uniffiTypeNameSymbol] = 'EcashSendOperation'
        return instance
      },

      bless(p: UniffiHandle): UniffiGcObject {
        const ptr = {
          p, // make sure this object doesn't get optimized away.
          markDestroyed: () => undefined,
        }
        if (registry) {
          registry.register(ptr, p, ptr)
        }
        return ptr
      },

      unbless(ptr_: UniffiGcObject) {
        if (registry) {
          registry.unregister(ptr_)
        }
      },

      pointer(obj_: EcashSendOperationLike): UniffiHandle {
        if ((obj_ as any)[destructorGuardSymbol] === undefined) {
          throw new UniffiInternalError.UnexpectedNullPointer()
        }
        return (obj_ as any)[pointerLiteralSymbol]
      },

      clonePointer(obj_: EcashSendOperationLike): UniffiHandle {
        const pointer = this.pointer(obj_)
        return uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_clone_ecashsendoperation(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      freePointer(pointer: UniffiHandle): void {
        uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_free_ecashsendoperation(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      isConcreteType(obj_: any): obj_ is EcashSendOperationLike {
        return (
          obj_[destructorGuardSymbol] &&
          obj_[uniffiTypeNameSymbol] === 'EcashSendOperation'
        )
      },
    }
  })()
const FfiConverterTypeEcashSendOperation = new FfiConverterObject(
  uniffiTypeEcashSendOperationObjectFactory,
)

/**
 * The result of [`Ecash::send`], with `operation` crossing as [`EcashSendOperation`] rather than
 * the generic `Operation<EcashSendState>` the real [`EcashSend`] carries.
 */
export type EcashSendHandle = {
  /**
   * See [`EcashSend::notes`].
   */
  notes: NotesLike
  /**
   * See [`EcashSend::operation`].
   */
  operation: EcashSendOperationLike
}

/**
 * Generated factory for {@link EcashSendHandle} record objects.
 */
export const EcashSendHandle = (() => {
  const defaults = () => ({})
  const create = (() => {
    return uniffiCreateRecord<EcashSendHandle, ReturnType<typeof defaults>>(
      defaults,
    )
  })()
  return Object.freeze({
    create,
    new: create,
    defaults: () => Object.freeze(defaults()) as Partial<EcashSendHandle>,
  })
})()

const FfiConverterTypeEcashSendHandle = (() => {
  type TypeName = EcashSendHandle
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      return {
        notes: FfiConverterTypeNotes.readFromCursor(c),
        operation: FfiConverterTypeEcashSendOperation.readFromCursor(c),
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      FfiConverterTypeNotes.writeIntoCursor(value.notes, c)
      FfiConverterTypeEcashSendOperation.writeIntoCursor(value.operation, c)
    }
    allocationSize(value: TypeName): number {
      return (
        FfiConverterTypeNotes.allocationSize(value.notes) +
        FfiConverterTypeEcashSendOperation.allocationSize(value.operation)
      )
    }
  }
  return new FFIConverter()
})()

/**
 * Typealias from the type name used in the UDL file to the builtin type.  This
 * is needed because the UDL type name is used in function/method signatures.
 */
export type FederationId = string
// FfiConverter for FederationId, a type alias for string.
const FfiConverterTypeFederationId = FfiConverterString

/**
 * The Bitcoin network a federation's on-chain module is configured for.
 *
 * Every federation operates on exactly one network; this value is read
 * from federation configuration and reported on
 * [`FederationPreview`](crate::FederationPreview) and the federation handle.
 * It is also what an [`Address`](crate::Address) is checked against when an
 * on-chain quote is requested, failing with
 * [`ErrorCode::NetworkMismatch`](crate::ErrorCode::NetworkMismatch) on
 * disagreement.
 *
 * This enum is `#[non_exhaustive]`: Bitcoin has occasionally grown new test
 * networks, and a new variant here is an additive change for Rust callers,
 * who must write a non-exhaustive match with a wildcard arm.
 *
 * It is not additive for a generated binding, though, and the crate-level
 * documentation's forward-compatibility section explains why: a decoder
 * pinned to an older SDK fails on a tag it has never seen rather than
 * mapping it to an unknown case. A binding is expected to be regenerated
 * alongside the SDK it talks to.
 */
export enum Network {
  /**
   * Bitcoin mainnet.
   */
  Bitcoin,
  /**
   * The long-running public Bitcoin testnet, testnet3.
   */
  Testnet,
  /**
   * Testnet4: the successor public testnet, introduced after testnet3's
   * difficulty and supply problems made it unreliable to test against.
   * A separate network with its own genesis block, not a continuation of
   * [`Testnet`](Self::Testnet).
   */
  Testnet4,
  /**
   * Signet: a public test network secured by a signer rather than
   * proof-of-work, generally more stable than testnet.
   */
  Signet,
  /**
   * A privately operated regression-test network, typically used for
   * local development.
   */
  Regtest,
}

const FfiConverterTypeNetwork = (() => {
  type TypeName = Network
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      switch (c.readI32()) {
        case 1:
          return Network.Bitcoin
        case 2:
          return Network.Testnet
        case 3:
          return Network.Testnet4
        case 4:
          return Network.Signet
        case 5:
          return Network.Regtest
        default:
          throw new UniffiInternalError.UnexpectedEnumCase()
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      switch (value) {
        case Network.Bitcoin:
          return c.writeI32(1)
        case Network.Testnet:
          return c.writeI32(2)
        case Network.Testnet4:
          return c.writeI32(3)
        case Network.Signet:
          return c.writeI32(4)
        case Network.Regtest:
          return c.writeI32(5)
      }
    }
    allocationSize(value: TypeName): number {
      return 4
    }
  }
  return new FFIConverter()
})()

// Enum: FederationStatus
export enum FederationStatus_Tags {
  Running = 'Running',
  Recovering = 'Recovering',
  Quarantined = 'Quarantined',
  Closed = 'Closed',
  Forgetting = 'Forgetting',
  Forgotten = 'Forgotten',
}
/**
 * What an SDK instance's storage currently knows about one federation.
 *
 * This is the crate's answer to "why can I not use this wallet right now",
 * and it is a **value to read** rather than an error to provoke.
 *
 * Read one with [`Sdk::federation_status`], read them all with
 * [`Sdk::stored_federations`], and follow changes with
 * [`Sdk::federation_status_updates`]. The lifecycle these variants form
 * is documented in one place, on [`Sdk`].
 *
 * The enum is `#[non_exhaustive]`; its variants are not. Rust callers
 * write a wildcard arm, and more detail about a situation arrives as a
 * new, more specific variant rather than a field grown on an existing one.
 */

type FederationStatus_Running_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'FederationStatus'
  tag: FederationStatus_Tags.Running
}

type FederationStatus_Recovering_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'FederationStatus'
  tag: FederationStatus_Tags.Recovering
}

type FederationStatus_Quarantined_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'FederationStatus'
  tag: FederationStatus_Tags.Quarantined
  inner: Readonly<{ diagnostic: Diagnostic }>
}

type FederationStatus_Closed_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'FederationStatus'
  tag: FederationStatus_Tags.Closed
}

type FederationStatus_Forgetting_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'FederationStatus'
  tag: FederationStatus_Tags.Forgetting
}

type FederationStatus_Forgotten_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'FederationStatus'
  tag: FederationStatus_Tags.Forgotten
}
export type FederationStatus =
  | FederationStatus_Running_interface
  | FederationStatus_Recovering_interface
  | FederationStatus_Quarantined_interface
  | FederationStatus_Closed_interface
  | FederationStatus_Forgetting_interface
  | FederationStatus_Forgotten_interface

export const FederationStatus = (() => {
  /**
   * Open and fully working: workers are running, operations are
   * progressing, and nothing the federation offers is withheld.
   *
   * One of the two open states, so the federation is listed by
   * [`Sdk::federations`] and [`Sdk::federation`] hands out a live handle;
   * [`Recovering`](FederationStatus::Recovering) is the other, and differs
   * only in refusing fund-touching calls.
   */
  class Running_
    extends UniffiEnum
    implements FederationStatus_Running_interface
  {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'FederationStatus'
    readonly tag = FederationStatus_Tags.Running
    constructor() {
      super('FederationStatus', 'Running')
    }

    static new(): Running_ {
      return new Running_()
    }

    static instanceOf(obj: any): obj is Running_ {
      return obj.tag === FederationStatus_Tags.Running
    }
  }
  /**
   * Open, but the wallet has not finished being reconstructed from the
   * seed, so the federation is **recovery-locked**.
   *
   * A live handle exists and identity, metadata and capabilities are
   * readable, while balance and activity are incomplete and still
   * moving and every spend and receive is refused with
   * [`Recovering`](crate::ErrorCode::Recovering).
   *
   * This is the second **open** state, so the federation is listed by
   * [`Sdk::federations`] and returned by [`Sdk::federation`] exactly as
   * a [`Running`](FederationStatus::Running) one is.
   *
   * This state covers a rescan that is running and one that has stopped
   * without completing: only a completed recovery releases the lock,
   * and a stopped attempt holds it just as firmly.
   *
   * Not to be confused with [`Quarantined`](FederationStatus::Quarantined):
   * a recovering federation is one the SDK is happily operating, a
   * quarantined one is one it refuses to operate. Closing or
   * quarantining a recovering federation moves it out of this state
   * without finishing the reconstruction, which is persisted, so
   * reopening lands it back here; only [`Sdk::forget_federation`] ends
   * an unfinished one, by erasing it.
   *
   * This state only arises for a federation joined with
   * [`Sdk::recover`]; one joined with [`Sdk::join`] never enters it.
   */
  class Recovering_
    extends UniffiEnum
    implements FederationStatus_Recovering_interface
  {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'FederationStatus'
    readonly tag = FederationStatus_Tags.Recovering
    constructor() {
      super('FederationStatus', 'Recovering')
    }

    static new(): Recovering_ {
      return new Recovering_()
    }

    static instanceOf(obj: any): obj is Recovering_ {
      return obj.tag === FederationStatus_Tags.Recovering
    }
  }
  /**
   * Stored and intact, but not running, because the SDK could not or
   * would not open it.
   *
   * Nothing has been deleted, and [`Sdk::reopen_federation`] retries.
   * Quarantine means "meant to be running, currently cannot", so later
   * builds retry it too; [`Sdk::close_federation`] is how an
   * application gives up on it.
   *
   * This state also carries the answer to the question a caller would
   * otherwise have to ask by failing a call.
   */
  class Quarantined_
    extends UniffiEnum
    implements FederationStatus_Quarantined_interface
  {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'FederationStatus'
    readonly tag = FederationStatus_Tags.Quarantined
    readonly inner: Readonly<{ diagnostic: Diagnostic }>
    constructor(inner: { diagnostic: Diagnostic }) {
      super('FederationStatus', 'Quarantined')

      this.inner = Object.freeze(inner)
    }
    static new(inner: { diagnostic: Diagnostic }): Quarantined_ {
      return new Quarantined_(inner)
    }

    static instanceOf(obj: any): obj is Quarantined_ {
      return obj.tag === FederationStatus_Tags.Quarantined
    }
  }
  /**
   * Stored and intact, and stopped because the application asked for
   * that with [`Sdk::close_federation`].
   *
   * Distinct from [`Quarantined`](FederationStatus::Quarantined): later
   * builds do *not* reopen it, because someone chose this.
   * [`Sdk::reopen_federation`] undoes the choice.
   */
  class Closed_
    extends UniffiEnum
    implements FederationStatus_Closed_interface
  {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'FederationStatus'
    readonly tag = FederationStatus_Tags.Closed
    constructor() {
      super('FederationStatus', 'Closed')
    }

    static new(): Closed_ {
      return new Closed_()
    }

    static instanceOf(obj: any): obj is Closed_ {
      return obj.tag === FederationStatus_Tags.Closed
    }
  }
  /**
   * An erase has been committed and is being carried out, or is waiting
   * to be finished by a retry or by a later
   * [`SdkBuilder::build`](crate::SdkBuilder::build).
   *
   * The federation will not be opened again and cannot be resurrected;
   * see [`Sdk::forget_federation`]. An application seeing this should
   * render "removing…" rather than a wallet.
   *
   * The id is listed by [`Sdk::stored_federations`] and answered here
   * by [`Sdk::federation_status`], absent from [`Sdk::federations`] and
   * from [`Sdk::federation`], and refused by [`Sdk::reopen_federation`]
   * with [`InvalidInput`](crate::ErrorCode::InvalidInput). Its balance,
   * operation log and activity history are gone as far as this API is
   * concerned from the moment the tombstone lands. Joining the same
   * federation again is allowed and produces a new federation with no
   * local history, after the committed erase is finished; see
   * [`Sdk::join`].
   */
  class Forgetting_
    extends UniffiEnum
    implements FederationStatus_Forgetting_interface
  {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'FederationStatus'
    readonly tag = FederationStatus_Tags.Forgetting
    constructor() {
      super('FederationStatus', 'Forgetting')
    }

    static new(): Forgetting_ {
      return new Forgetting_()
    }

    static instanceOf(obj: any): obj is Forgetting_ {
      return obj.tag === FederationStatus_Tags.Forgetting
    }
  }
  /**
   * The erase completed: this federation is gone.
   *
   * This is a *notification*, not a stored state. It is delivered once
   * by [`FederationStatusUpdates::next`] so a list screen can drop the
   * row. [`Sdk::federation_status`] returns `None` for a forgotten
   * federation.
   */
  class Forgotten_
    extends UniffiEnum
    implements FederationStatus_Forgotten_interface
  {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'FederationStatus'
    readonly tag = FederationStatus_Tags.Forgotten
    constructor() {
      super('FederationStatus', 'Forgotten')
    }

    static new(): Forgotten_ {
      return new Forgotten_()
    }

    static instanceOf(obj: any): obj is Forgotten_ {
      return obj.tag === FederationStatus_Tags.Forgotten
    }
  }

  function instanceOf(obj: any): obj is FederationStatus {
    return obj[uniffiTypeNameSymbol] === 'FederationStatus'
  }

  return Object.freeze({
    instanceOf,
    Running: Running_,
    Recovering: Recovering_,
    Quarantined: Quarantined_,
    Closed: Closed_,
    Forgetting: Forgetting_,
    Forgotten: Forgotten_,
  })
})()

// FfiConverter for enum FederationStatus
const FfiConverterTypeFederationStatus = (() => {
  type TypeName = FederationStatus
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      switch (c.readI32()) {
        case 1:
          return new FederationStatus.Running()
        case 2:
          return new FederationStatus.Recovering()
        case 3:
          return new FederationStatus.Quarantined({
            diagnostic: FfiConverterTypeDiagnostic.readFromCursor(c),
          })
        case 4:
          return new FederationStatus.Closed()
        case 5:
          return new FederationStatus.Forgetting()
        case 6:
          return new FederationStatus.Forgotten()
        default:
          throw new UniffiInternalError.UnexpectedEnumCase()
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      switch (value.tag) {
        case FederationStatus_Tags.Running: {
          c.writeI32(1)
          return
        }
        case FederationStatus_Tags.Recovering: {
          c.writeI32(2)
          return
        }
        case FederationStatus_Tags.Quarantined: {
          c.writeI32(3)
          const inner = value.inner
          FfiConverterTypeDiagnostic.writeIntoCursor(inner.diagnostic, c)
          return
        }
        case FederationStatus_Tags.Closed: {
          c.writeI32(4)
          return
        }
        case FederationStatus_Tags.Forgetting: {
          c.writeI32(5)
          return
        }
        case FederationStatus_Tags.Forgotten: {
          c.writeI32(6)
          return
        }
        default:
          // Throwing from here means that FederationStatus_Tags hasn't matched an ordinal.
          throw new UniffiInternalError.UnexpectedEnumCase()
      }
    }
    allocationSize(value: TypeName): number {
      switch (value.tag) {
        case FederationStatus_Tags.Running: {
          return 4
        }
        case FederationStatus_Tags.Recovering: {
          return 4
        }
        case FederationStatus_Tags.Quarantined: {
          const inner = value.inner
          let size = 4
          size += FfiConverterTypeDiagnostic.allocationSize(inner.diagnostic)
          return size
        }
        case FederationStatus_Tags.Closed: {
          return 4
        }
        case FederationStatus_Tags.Forgetting: {
          return 4
        }
        case FederationStatus_Tags.Forgotten: {
          return 4
        }
        default:
          throw new UniffiInternalError.UnexpectedEnumCase()
      }
    }
  }
  return new FFIConverter()
})()

/**
 * A stored federation, described without a live handle.
 *
 * [`Sdk::stored_federations`] returns these and
 * [`FederationStatusUpdates::next`] yields them. A closed federation, a
 * quarantined one, or one whose erase is finishing has no [`Federation`]
 * handle to describe it, so listing them uses this small record instead.
 *
 * A [`Recovering`](FederationStatus::Recovering) federation is in this
 * list and can still be acted on through a live [`Federation`] handle; a
 * record here has no client behind it at all.
 *
 * `#[non_exhaustive]`: fields may be added, so match it with `..` or by
 * field access.
 */
export type FederationInfo = {
  /**
   * The federation's id: the key for [`Sdk::federation`],
   * [`Sdk::federation_status`] and [`Sdk::reopen_federation`], and what
   * identifies the row to replace when this record arrives from
   * [`FederationStatusUpdates::next`].
   */
  id: FederationId
  /**
   * The federation's human-readable name, when its configuration
   * declares one.
   *
   * From the last configuration that validated, exactly as
   * [`Federation::name`](crate::Federation::name) reports it, so a
   * closed or quarantined federation still has a label to show. Not a
   * verified or unique identifier: identity is [`id`](FederationInfo::id).
   */
  name?: string
  /**
   * The Bitcoin network this federation operates on, from the same
   * last-good configuration as [`name`](FederationInfo::name).
   */
  network: Network
  /**
   * What the SDK can currently do with it.
   */
  status: FederationStatus
}

/**
 * Generated factory for {@link FederationInfo} record objects.
 */
export const FederationInfo = (() => {
  const defaults = () => ({})
  const create = (() => {
    return uniffiCreateRecord<FederationInfo, ReturnType<typeof defaults>>(
      defaults,
    )
  })()
  return Object.freeze({
    create,
    new: create,
    defaults: () => Object.freeze(defaults()) as Partial<FederationInfo>,
  })
})()

const FfiConverterTypeFederationInfo = (() => {
  type TypeName = FederationInfo
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      return {
        id: FfiConverterTypeFederationId.readFromCursor(c),
        name: FfiConverterOptionalString.readFromCursor(c),
        network: FfiConverterTypeNetwork.readFromCursor(c),
        status: FfiConverterTypeFederationStatus.readFromCursor(c),
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      FfiConverterTypeFederationId.writeIntoCursor(value.id, c)
      FfiConverterOptionalString.writeIntoCursor(value.name, c)
      FfiConverterTypeNetwork.writeIntoCursor(value.network, c)
      FfiConverterTypeFederationStatus.writeIntoCursor(value.status, c)
    }
    allocationSize(value: TypeName): number {
      return (
        FfiConverterTypeFederationId.allocationSize(value.id) +
        FfiConverterOptionalString.allocationSize(value.name) +
        FfiConverterTypeNetwork.allocationSize(value.network) +
        FfiConverterTypeFederationStatus.allocationSize(value.status)
      )
    }
  }
  return new FFIConverter()
})()

/**
 * Everything needed to render a "join this federation?" screen before
 * committing to anything.
 *
 * A `FederationPreview` is fetched (from the federation's guardians, over
 * the network) without joining or persisting any state locally: it lets an
 * application show the user what they're about to join. Producing one also
 * validates the federation-wide rule that every module must share the same
 * generation (all v1 or all v2, never mixed); a federation that fails that
 * check fails with [`ErrorCode::UnsupportedFederation`](crate::ErrorCode::UnsupportedFederation)
 * before a preview is ever returned, rather than returning a preview for
 * something the SDK cannot actually operate on.
 *
 * This type is `#[non_exhaustive]`: new fields may be added in future
 * releases, so construct it only through the SDK and match it only with a
 * `..` pattern or by field access, never by exhaustive destructuring.
 */
export type FederationPreview = {
  /**
   * The federation's identifier.
   */
  id: FederationId
  /**
   * The federation's human-readable name, when its configuration
   * provides one.
   */
  name?: string
  /**
   * The Bitcoin network this federation operates on.
   */
  network: Network
  /**
   * The number of guardians in the federation.
   */
  guardians: number
  /**
   * The kind names of *every* module this federation runs, e.g.
   * `"mint"`, `"ln"`, `"wallet"`.
   *
   * Presence here does not imply a corresponding facade after joining.
   * The SDK exposes facades for the mint, lightning and wallet modules
   * only, while this list is the federation's full module set: a
   * federation may run modules this SDK has no facade for, and they
   * appear here all the same.
   *
   * The single-generation rule is not a per-module gate and plays no
   * part in this: it is federation-wide, and any preview that was
   * returned at all has already satisfied it (see the type
   * documentation).
   */
  modules: Array<string>
  /**
   * Config-level metadata (for example, a welcome message), keyed by
   * arbitrary string keys as defined by the federation's configuration.
   */
  meta: MetaMap
}

/**
 * Generated factory for {@link FederationPreview} record objects.
 */
export const FederationPreview = (() => {
  const defaults = () => ({})
  const create = (() => {
    return uniffiCreateRecord<FederationPreview, ReturnType<typeof defaults>>(
      defaults,
    )
  })()
  return Object.freeze({
    create,
    new: create,
    defaults: () => Object.freeze(defaults()) as Partial<FederationPreview>,
  })
})()

const FfiConverterTypeFederationPreview = (() => {
  type TypeName = FederationPreview
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      return {
        id: FfiConverterTypeFederationId.readFromCursor(c),
        name: FfiConverterOptionalString.readFromCursor(c),
        network: FfiConverterTypeNetwork.readFromCursor(c),
        guardians: FfiConverterUInt16.readFromCursor(c),
        modules: FfiConverterSequenceString.readFromCursor(c),
        meta: FfiConverterTypeMetaMap.readFromCursor(c),
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      FfiConverterTypeFederationId.writeIntoCursor(value.id, c)
      FfiConverterOptionalString.writeIntoCursor(value.name, c)
      FfiConverterTypeNetwork.writeIntoCursor(value.network, c)
      FfiConverterUInt16.writeIntoCursor(value.guardians, c)
      FfiConverterSequenceString.writeIntoCursor(value.modules, c)
      FfiConverterTypeMetaMap.writeIntoCursor(value.meta, c)
    }
    allocationSize(value: TypeName): number {
      return (
        FfiConverterTypeFederationId.allocationSize(value.id) +
        FfiConverterOptionalString.allocationSize(value.name) +
        FfiConverterTypeNetwork.allocationSize(value.network) +
        FfiConverterUInt16.allocationSize(value.guardians) +
        FfiConverterSequenceString.allocationSize(value.modules) +
        FfiConverterTypeMetaMap.allocationSize(value.meta)
      )
    }
  }
  return new FFIConverter()
})()

/**
 * The parts [`LnQuote::fee`] is made of.
 *
 * Obtained from [`LnQuote::fee_breakdown`], for an approval screen that
 * would rather say "1,050 msat of fees, of which 1,000 is the gateway's and
 * 50 the federation's" than show one unexplained lump.
 *
 * The components sum to [`LnQuote::fee`] exactly. Take the total from that
 * accessor rather than adding these up, so the number on screen stays the
 * number the quote committed to even if a later release itemises the fee
 * more finely.
 *
 * Any component may be zero. On [`LightningRoute::Internal`] the gateway
 * component always is. Zero components are reported as zero rather than
 * omitted.
 */
export type LnFeeBreakdown = {
  /**
   * The gateway's own charge for carrying the payment out to the lightning
   * network. Zero on [`LightningRoute::Internal`].
   */
  gateway: Amount
  /**
   * The lightning module's fee on the output that funds the payment.
   */
  lightningModule: Amount
  /**
   * The primary module's fees for assembling the funding transaction: what
   * it charges on the ecash inputs spent and on the change reissued.
   */
  primaryModule: Amount
  /**
   * Value lost to denominations: the part of the change too small for any
   * note denomination to represent, which is therefore never reissued.
   *
   * Nobody charges it, but it leaves the balance and does not come back,
   * so it belongs in the number a user approves.
   */
  dust: Amount
}

/**
 * Generated factory for {@link LnFeeBreakdown} record objects.
 */
export const LnFeeBreakdown = (() => {
  const defaults = () => ({})
  const create = (() => {
    return uniffiCreateRecord<LnFeeBreakdown, ReturnType<typeof defaults>>(
      defaults,
    )
  })()
  return Object.freeze({
    create,
    new: create,
    defaults: () => Object.freeze(defaults()) as Partial<LnFeeBreakdown>,
  })
})()

const FfiConverterTypeLnFeeBreakdown = (() => {
  type TypeName = LnFeeBreakdown
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      return {
        gateway: FfiConverterTypeAmount.readFromCursor(c),
        lightningModule: FfiConverterTypeAmount.readFromCursor(c),
        primaryModule: FfiConverterTypeAmount.readFromCursor(c),
        dust: FfiConverterTypeAmount.readFromCursor(c),
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      FfiConverterTypeAmount.writeIntoCursor(value.gateway, c)
      FfiConverterTypeAmount.writeIntoCursor(value.lightningModule, c)
      FfiConverterTypeAmount.writeIntoCursor(value.primaryModule, c)
      FfiConverterTypeAmount.writeIntoCursor(value.dust, c)
    }
    allocationSize(value: TypeName): number {
      return (
        FfiConverterTypeAmount.allocationSize(value.gateway) +
        FfiConverterTypeAmount.allocationSize(value.lightningModule) +
        FfiConverterTypeAmount.allocationSize(value.primaryModule) +
        FfiConverterTypeAmount.allocationSize(value.dust)
      )
    }
  }
  return new FFIConverter()
})()

/**
 * Typealias from the type name used in the UDL file to the builtin type.  This
 * is needed because the UDL type name is used in function/method signatures.
 */
export type Bolt11Invoice = string
// FfiConverter for Bolt11Invoice, a type alias for string.
const FfiConverterTypeBolt11Invoice = FfiConverterString

/**
 * Typealias from the type name used in the UDL file to the builtin type.  This
 * is needed because the UDL type name is used in function/method signatures.
 */
export type GatewayId = string
// FfiConverter for GatewayId, a type alias for string.
const FfiConverterTypeGatewayId = FfiConverterString

/**
 * The invoice an incoming lightning payment was issued for, and its terms.
 *
 * Read with [`Operation::details`](crate::Operation::details). Persisted
 * when [`Lightning::receive`] creates the operation and never changed, so a
 * receive screen can re-display the same QR code and resume the same
 * countdown after a restart from the operation's id alone.
 *
 * # Which amount is which
 *
 * The fee is deducted from the invoice, not added on top of it. The
 * invoice's face value is exactly what [`Lightning::receive`] was asked for,
 * and the receive-side fee comes out of it:
 *
 * ```text
 * invoice_amount == requested_amount == net_credit + fee
 * ```
 *
 * All three amounts are recorded so a caller can render "you asked for X,
 * the payer pays Y, you receive Z" without doing the arithmetic.
 */
export type LnReceiveDetails = {
  /**
   * The invoice that was issued, the same value [`LnReceive::invoice`]
   * returned.
   */
  invoice: Bolt11Invoice
  /**
   * The description embedded in the invoice, as it was passed to
   * [`Lightning::receive`].
   *
   * Kept separately because an invoice may carry only a hash of its
   * description, in which case
   * [`Bolt11Invoice::description`](crate::Bolt11Invoice::description) has
   * nothing to return.
   */
  description: string
  /**
   * The amount asked of [`Lightning::receive`].
   */
  requestedAmount: Amount
  /**
   * The invoice's face value: what the payer is asked to pay.
   *
   * Equal to [`requested_amount`](LnReceiveDetails::requested_amount), and
   * to [`net_credit`](LnReceiveDetails::net_credit) plus
   * [`fee`](LnReceiveDetails::fee).
   */
  invoiceAmount: Amount
  /**
   * The receive-side fee: the gateway's charge for taking the payment in,
   * plus what the federation charges to issue the ecash for it.
   *
   * This is the whole difference between what the payer pays and what
   * lands; no other deduction appears later. It can be zero but usually is
   * not, since issuing the notes is itself a federation transaction.
   *
   * Exact for an operation this SDK created, even one recovered after a
   * restart; a record rebuilt from a log entry this SDK did not create
   * has no fee to recover and reports zero.
   */
  fee: Amount
  /**
   * What lands in the spendable balance:
   * [`invoice_amount`](LnReceiveDetails::invoice_amount) minus
   * [`fee`](LnReceiveDetails::fee).
   *
   * The number to show as "you will receive".
   */
  netCredit: Amount
  /**
   * The gateway that agreed to take the payment in, if there was one.
   *
   * `None` means no gateway took part, not that the gateway is unknown.
   */
  gatewayId?: GatewayId
  /**
   * When the invoice stops being payable.
   *
   * The countdown to render beside the QR code, and the moment after which
   * [`LnReceiveState::Expired`] is the ending to expect.
   */
  expiresAt: Timestamp
  /**
   * When the receive was started.
   *
   * The timestamp to sort and label a history row by.
   */
  createdAt: Timestamp
}

/**
 * Generated factory for {@link LnReceiveDetails} record objects.
 */
export const LnReceiveDetails = (() => {
  const defaults = () => ({})
  const create = (() => {
    return uniffiCreateRecord<LnReceiveDetails, ReturnType<typeof defaults>>(
      defaults,
    )
  })()
  return Object.freeze({
    create,
    new: create,
    defaults: () => Object.freeze(defaults()) as Partial<LnReceiveDetails>,
  })
})()

const FfiConverterTypeLnReceiveDetails = (() => {
  type TypeName = LnReceiveDetails
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      return {
        invoice: FfiConverterTypeBolt11Invoice.readFromCursor(c),
        description: FfiConverterString.readFromCursor(c),
        requestedAmount: FfiConverterTypeAmount.readFromCursor(c),
        invoiceAmount: FfiConverterTypeAmount.readFromCursor(c),
        fee: FfiConverterTypeAmount.readFromCursor(c),
        netCredit: FfiConverterTypeAmount.readFromCursor(c),
        gatewayId: FfiConverterOptionalTypeGatewayId.readFromCursor(c),
        expiresAt: FfiConverterTypeTimestamp.readFromCursor(c),
        createdAt: FfiConverterTypeTimestamp.readFromCursor(c),
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      FfiConverterTypeBolt11Invoice.writeIntoCursor(value.invoice, c)
      FfiConverterString.writeIntoCursor(value.description, c)
      FfiConverterTypeAmount.writeIntoCursor(value.requestedAmount, c)
      FfiConverterTypeAmount.writeIntoCursor(value.invoiceAmount, c)
      FfiConverterTypeAmount.writeIntoCursor(value.fee, c)
      FfiConverterTypeAmount.writeIntoCursor(value.netCredit, c)
      FfiConverterOptionalTypeGatewayId.writeIntoCursor(value.gatewayId, c)
      FfiConverterTypeTimestamp.writeIntoCursor(value.expiresAt, c)
      FfiConverterTypeTimestamp.writeIntoCursor(value.createdAt, c)
    }
    allocationSize(value: TypeName): number {
      return (
        FfiConverterTypeBolt11Invoice.allocationSize(value.invoice) +
        FfiConverterString.allocationSize(value.description) +
        FfiConverterTypeAmount.allocationSize(value.requestedAmount) +
        FfiConverterTypeAmount.allocationSize(value.invoiceAmount) +
        FfiConverterTypeAmount.allocationSize(value.fee) +
        FfiConverterTypeAmount.allocationSize(value.netCredit) +
        FfiConverterOptionalTypeGatewayId.allocationSize(value.gatewayId) +
        FfiConverterTypeTimestamp.allocationSize(value.expiresAt) +
        FfiConverterTypeTimestamp.allocationSize(value.createdAt)
      )
    }
  }
  return new FFIConverter()
})()

// Enum: LnReceiveState
export enum LnReceiveState_Tags {
  Created = 'Created',
  WaitingForPayment = 'WaitingForPayment',
  Funded = 'Funded',
  Claimed = 'Claimed',
  Canceled = 'Canceled',
  Expired = 'Expired',
  Failed = 'Failed',
}
/**
 * The lifecycle of an incoming lightning payment.
 *
 * The invoice to show and the expiry to count down to are in
 * [`LnReceiveDetails`], not in any state, so a receive screen can be rebuilt
 * from the operation's id alone.
 *
 * Three endings other than [`Claimed`](Self::Claimed) are told apart:
 * [`Expired`](Self::Expired), the invoice lapsed unpaid;
 * [`Canceled`](Self::Canceled), the receive was called off before anything
 * was funded; and [`Failed`](Self::Failed), a payment got past "nobody paid"
 * and still produced no credit. Only the last warrants alarming a user.
 */

type LnReceiveState_Created_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'LnReceiveState'
  tag: LnReceiveState_Tags.Created
}

type LnReceiveState_WaitingForPayment_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'LnReceiveState'
  tag: LnReceiveState_Tags.WaitingForPayment
}

type LnReceiveState_Funded_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'LnReceiveState'
  tag: LnReceiveState_Tags.Funded
}

type LnReceiveState_Claimed_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'LnReceiveState'
  tag: LnReceiveState_Tags.Claimed
}

type LnReceiveState_Canceled_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'LnReceiveState'
  tag: LnReceiveState_Tags.Canceled
  inner: Readonly<{ reason: string }>
}

type LnReceiveState_Expired_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'LnReceiveState'
  tag: LnReceiveState_Tags.Expired
}

type LnReceiveState_Failed_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'LnReceiveState'
  tag: LnReceiveState_Tags.Failed
}
export type LnReceiveState =
  | LnReceiveState_Created_interface
  | LnReceiveState_WaitingForPayment_interface
  | LnReceiveState_Funded_interface
  | LnReceiveState_Claimed_interface
  | LnReceiveState_Canceled_interface
  | LnReceiveState_Expired_interface
  | LnReceiveState_Failed_interface

export const LnReceiveState = (() => {
  /**
   * The invoice is being created and registered with the gateway.
   */
  class Created_
    extends UniffiEnum
    implements LnReceiveState_Created_interface
  {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'LnReceiveState'
    readonly tag = LnReceiveState_Tags.Created
    constructor() {
      super('LnReceiveState', 'Created')
    }

    static new(): Created_ {
      return new Created_()
    }

    static instanceOf(obj: any): obj is Created_ {
      return obj.tag === LnReceiveState_Tags.Created
    }
  }
  /**
   * The invoice exists and nobody has paid it yet.
   *
   * The invoice and its expiry are in [`LnReceiveDetails`].
   */
  class WaitingForPayment_
    extends UniffiEnum
    implements LnReceiveState_WaitingForPayment_interface
  {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'LnReceiveState'
    readonly tag = LnReceiveState_Tags.WaitingForPayment
    constructor() {
      super('LnReceiveState', 'WaitingForPayment')
    }

    static new(): WaitingForPayment_ {
      return new WaitingForPayment_()
    }

    static instanceOf(obj: any): obj is WaitingForPayment_ {
      return obj.tag === LnReceiveState_Tags.WaitingForPayment
    }
  }
  /**
   * Someone paid; the funds are being settled into the federation.
   *
   * A receive stays here while the SDK retries a claim the federation
   * rejected. It only becomes [`Failed`](Self::Failed) once no further
   * claim is possible.
   */
  class Funded_ extends UniffiEnum implements LnReceiveState_Funded_interface {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'LnReceiveState'
    readonly tag = LnReceiveState_Tags.Funded
    constructor() {
      super('LnReceiveState', 'Funded')
    }

    static new(): Funded_ {
      return new Funded_()
    }

    static instanceOf(obj: any): obj is Funded_ {
      return obj.tag === LnReceiveState_Tags.Funded
    }
  }
  /**
   * Final: the amount is in the spendable balance.
   *
   * The amount that landed is [`LnReceiveDetails::net_credit`], the
   * invoice's face value less the receive-side fee.
   */
  class Claimed_
    extends UniffiEnum
    implements LnReceiveState_Claimed_interface
  {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'LnReceiveState'
    readonly tag = LnReceiveState_Tags.Claimed
    constructor() {
      super('LnReceiveState', 'Claimed')
    }

    static new(): Claimed_ {
      return new Claimed_()
    }

    static instanceOf(obj: any): obj is Claimed_ {
      return obj.tag === LnReceiveState_Tags.Claimed
    }
  }
  /**
   * Final: the receive was called off before anything was funded, for
   * example because the gateway withdrew the offer.
   */
  class Canceled_
    extends UniffiEnum
    implements LnReceiveState_Canceled_interface
  {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'LnReceiveState'
    readonly tag = LnReceiveState_Tags.Canceled
    readonly inner: Readonly<{ reason: string }>
    constructor(inner: { reason: string }) {
      super('LnReceiveState', 'Canceled')

      this.inner = Object.freeze(inner)
    }
    static new(inner: { reason: string }): Canceled_ {
      return new Canceled_(inner)
    }

    static instanceOf(obj: any): obj is Canceled_ {
      return obj.tag === LnReceiveState_Tags.Canceled
    }
  }
  /**
   * Final: the invoice's expiry passed without it being paid.
   */
  class Expired_
    extends UniffiEnum
    implements LnReceiveState_Expired_interface
  {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'LnReceiveState'
    readonly tag = LnReceiveState_Tags.Expired
    constructor() {
      super('LnReceiveState', 'Expired')
    }

    static new(): Expired_ {
      return new Expired_()
    }

    static instanceOf(obj: any): obj is Expired_ {
      return obj.tag === LnReceiveState_Tags.Expired
    }
  }
  /**
   * Final: a payment got past "nobody paid" and no ecash was issued for
   * it.
   *
   * The amount is not in the balance and will not arrive by waiting.
   * Either a confirmed payment did not become spendable notes and the
   * payer is out of pocket, or the protocol went wrong on a funded
   * contract and the payment was unwound before anyone was paid. The
   * application cannot tell the two apart from this state. Render it as
   * an error the user should report, not as an expired invoice.
   */
  class Failed_ extends UniffiEnum implements LnReceiveState_Failed_interface {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'LnReceiveState'
    readonly tag = LnReceiveState_Tags.Failed
    constructor() {
      super('LnReceiveState', 'Failed')
    }

    static new(): Failed_ {
      return new Failed_()
    }

    static instanceOf(obj: any): obj is Failed_ {
      return obj.tag === LnReceiveState_Tags.Failed
    }
  }

  function instanceOf(obj: any): obj is LnReceiveState {
    return obj[uniffiTypeNameSymbol] === 'LnReceiveState'
  }

  return Object.freeze({
    instanceOf,
    Created: Created_,
    WaitingForPayment: WaitingForPayment_,
    Funded: Funded_,
    Claimed: Claimed_,
    Canceled: Canceled_,
    Expired: Expired_,
    Failed: Failed_,
  })
})()

// FfiConverter for enum LnReceiveState
const FfiConverterTypeLnReceiveState = (() => {
  type TypeName = LnReceiveState
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      switch (c.readI32()) {
        case 1:
          return new LnReceiveState.Created()
        case 2:
          return new LnReceiveState.WaitingForPayment()
        case 3:
          return new LnReceiveState.Funded()
        case 4:
          return new LnReceiveState.Claimed()
        case 5:
          return new LnReceiveState.Canceled({
            reason: FfiConverterString.readFromCursor(c),
          })
        case 6:
          return new LnReceiveState.Expired()
        case 7:
          return new LnReceiveState.Failed()
        default:
          throw new UniffiInternalError.UnexpectedEnumCase()
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      switch (value.tag) {
        case LnReceiveState_Tags.Created: {
          c.writeI32(1)
          return
        }
        case LnReceiveState_Tags.WaitingForPayment: {
          c.writeI32(2)
          return
        }
        case LnReceiveState_Tags.Funded: {
          c.writeI32(3)
          return
        }
        case LnReceiveState_Tags.Claimed: {
          c.writeI32(4)
          return
        }
        case LnReceiveState_Tags.Canceled: {
          c.writeI32(5)
          const inner = value.inner
          FfiConverterString.writeIntoCursor(inner.reason, c)
          return
        }
        case LnReceiveState_Tags.Expired: {
          c.writeI32(6)
          return
        }
        case LnReceiveState_Tags.Failed: {
          c.writeI32(7)
          return
        }
        default:
          // Throwing from here means that LnReceiveState_Tags hasn't matched an ordinal.
          throw new UniffiInternalError.UnexpectedEnumCase()
      }
    }
    allocationSize(value: TypeName): number {
      switch (value.tag) {
        case LnReceiveState_Tags.Created: {
          return 4
        }
        case LnReceiveState_Tags.WaitingForPayment: {
          return 4
        }
        case LnReceiveState_Tags.Funded: {
          return 4
        }
        case LnReceiveState_Tags.Claimed: {
          return 4
        }
        case LnReceiveState_Tags.Canceled: {
          const inner = value.inner
          let size = 4
          size += FfiConverterString.allocationSize(inner.reason)
          return size
        }
        case LnReceiveState_Tags.Expired: {
          return 4
        }
        case LnReceiveState_Tags.Failed: {
          return 4
        }
        default:
          throw new UniffiInternalError.UnexpectedEnumCase()
      }
    }
  }
  return new FFIConverter()
})()

/**
 * The UniFFI view of one `OperationUpdates<S>` instantiation, behind a lock so `next`'s
 * `&mut self` can cross as `&self`.
 */
export interface LnReceiveOperationUpdatesLike {
  /**
   * See [`OperationUpdates::next`](crate::OperationUpdates::next).
   */
  next(asyncOpts_?: {
    signal: AbortSignal
  }) /*throws*/ : Promise<LnReceiveState | undefined>
}
/**
 * @deprecated Use `LnReceiveOperationUpdatesLike` instead.
 */
export type LnReceiveOperationUpdatesInterface = LnReceiveOperationUpdatesLike

/**
 * The UniFFI view of one `OperationUpdates<S>` instantiation, behind a lock so `next`'s
 * `&mut self` can cross as `&self`.
 */
export class LnReceiveOperationUpdates
  extends UniffiAbstractObject
  implements LnReceiveOperationUpdatesLike
{
  readonly [uniffiTypeNameSymbol] = 'LnReceiveOperationUpdates'
  readonly [destructorGuardSymbol]: UniffiGcObject
  readonly [pointerLiteralSymbol]: UniffiHandle
  // No primary constructor declared for this class.
  private constructor(pointer: UniffiHandle) {
    super()
    this[pointerLiteralSymbol] = pointer
    this[destructorGuardSymbol] =
      uniffiTypeLnReceiveOperationUpdatesObjectFactory.bless(pointer)
  }

  /**
   * See [`OperationUpdates::next`](crate::OperationUpdates::next).
   */
  async next(asyncOpts_?: {
    signal: AbortSignal
  }): Promise<LnReceiveState | undefined> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_lnreceiveoperationupdates_next(
          uniffiTypeLnReceiveOperationUpdatesObjectFactory.clonePointer(this),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterOptionalTypeLnReceiveState.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  uniffiDestroy(): void {
    const ptr = (this as any)[destructorGuardSymbol]
    if (ptr !== undefined) {
      const pointer =
        uniffiTypeLnReceiveOperationUpdatesObjectFactory.pointer(this)
      uniffiTypeLnReceiveOperationUpdatesObjectFactory.freePointer(pointer)
      uniffiTypeLnReceiveOperationUpdatesObjectFactory.unbless(ptr)
      delete (this as any)[destructorGuardSymbol]
    }
  }

  static instanceOf(obj_: any): obj_ is LnReceiveOperationUpdates {
    return uniffiTypeLnReceiveOperationUpdatesObjectFactory.isConcreteType(obj_)
  }
}

const uniffiTypeLnReceiveOperationUpdatesObjectFactory: UniffiObjectFactory<LnReceiveOperationUpdatesLike> =
  (() => {
    /// <reference lib="es2021" />
    const registry =
      typeof FinalizationRegistry !== 'undefined'
        ? new FinalizationRegistry<UniffiHandle>((heldValue: UniffiHandle) => {
            uniffiTypeLnReceiveOperationUpdatesObjectFactory.freePointer(
              heldValue,
            )
          })
        : null

    return {
      create(pointer: UniffiHandle): LnReceiveOperationUpdatesLike {
        const instance = Object.create(LnReceiveOperationUpdates.prototype)
        instance[pointerLiteralSymbol] = pointer
        instance[destructorGuardSymbol] = this.bless(pointer)
        instance[uniffiTypeNameSymbol] = 'LnReceiveOperationUpdates'
        return instance
      },

      bless(p: UniffiHandle): UniffiGcObject {
        const ptr = {
          p, // make sure this object doesn't get optimized away.
          markDestroyed: () => undefined,
        }
        if (registry) {
          registry.register(ptr, p, ptr)
        }
        return ptr
      },

      unbless(ptr_: UniffiGcObject) {
        if (registry) {
          registry.unregister(ptr_)
        }
      },

      pointer(obj_: LnReceiveOperationUpdatesLike): UniffiHandle {
        if ((obj_ as any)[destructorGuardSymbol] === undefined) {
          throw new UniffiInternalError.UnexpectedNullPointer()
        }
        return (obj_ as any)[pointerLiteralSymbol]
      },

      clonePointer(obj_: LnReceiveOperationUpdatesLike): UniffiHandle {
        const pointer = this.pointer(obj_)
        return uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_clone_lnreceiveoperationupdates(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      freePointer(pointer: UniffiHandle): void {
        uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_free_lnreceiveoperationupdates(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      isConcreteType(obj_: any): obj_ is LnReceiveOperationUpdatesLike {
        return (
          obj_[destructorGuardSymbol] &&
          obj_[uniffiTypeNameSymbol] === 'LnReceiveOperationUpdates'
        )
      },
    }
  })()
const FfiConverterTypeLnReceiveOperationUpdates = new FfiConverterObject(
  uniffiTypeLnReceiveOperationUpdatesObjectFactory,
)

/**
 * The UniFFI view of one `Operation<S>` instantiation: an opaque object wrapping the real
 * handle, forwarding every method.
 */
export interface LnReceiveOperationLike {
  /**
   * See [`Operation::await_final`](crate::Operation::await_final).
   */
  awaitFinal(asyncOpts_?: {
    signal: AbortSignal
  }) /*throws*/ : Promise<LnReceiveState>
  /**
   * See [`Operation::details`](crate::Operation::details).
   */
  details(asyncOpts_?: {
    signal: AbortSignal
  }) /*throws*/ : Promise<LnReceiveDetails>
  /**
   * See [`Operation::id`](crate::Operation::id).
   */
  id(): OperationId
  /**
   * See [`Operation::state`](crate::Operation::state).
   */
  state(asyncOpts_?: {
    signal: AbortSignal
  }) /*throws*/ : Promise<LnReceiveState>
  /**
   * See [`Operation::updates`](crate::Operation::updates).
   */
  updates(): LnReceiveOperationUpdatesLike
}
/**
 * @deprecated Use `LnReceiveOperationLike` instead.
 */
export type LnReceiveOperationInterface = LnReceiveOperationLike

/**
 * The UniFFI view of one `Operation<S>` instantiation: an opaque object wrapping the real
 * handle, forwarding every method.
 */
export class LnReceiveOperation
  extends UniffiAbstractObject
  implements LnReceiveOperationLike
{
  readonly [uniffiTypeNameSymbol] = 'LnReceiveOperation'
  readonly [destructorGuardSymbol]: UniffiGcObject
  readonly [pointerLiteralSymbol]: UniffiHandle
  // No primary constructor declared for this class.
  private constructor(pointer: UniffiHandle) {
    super()
    this[pointerLiteralSymbol] = pointer
    this[destructorGuardSymbol] =
      uniffiTypeLnReceiveOperationObjectFactory.bless(pointer)
  }

  /**
   * See [`Operation::await_final`](crate::Operation::await_final).
   */
  async awaitFinal(asyncOpts_?: {
    signal: AbortSignal
  }): Promise<LnReceiveState> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_lnreceiveoperation_await_final(
          uniffiTypeLnReceiveOperationObjectFactory.clonePointer(this),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterTypeLnReceiveState.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * See [`Operation::details`](crate::Operation::details).
   */
  async details(asyncOpts_?: {
    signal: AbortSignal
  }): Promise<LnReceiveDetails> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_lnreceiveoperation_details(
          uniffiTypeLnReceiveOperationObjectFactory.clonePointer(this),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterTypeLnReceiveDetails.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * See [`Operation::id`](crate::Operation::id).
   */
  id(): OperationId {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_lnreceiveoperation_id(
          uniffiTypeLnReceiveOperationObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterTypeOperationId.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * See [`Operation::state`](crate::Operation::state).
   */
  async state(asyncOpts_?: {
    signal: AbortSignal
  }): Promise<LnReceiveState> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_lnreceiveoperation_state(
          uniffiTypeLnReceiveOperationObjectFactory.clonePointer(this),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterTypeLnReceiveState.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * See [`Operation::updates`](crate::Operation::updates).
   */
  updates(): LnReceiveOperationUpdatesLike {
    return FfiConverterTypeLnReceiveOperationUpdates.lift(
      uniffiCaller.rustCall(
        /*caller:*/ (callStatus) => {
          return nativeModule().uniffi_fedimint_sdk_fn_method_lnreceiveoperation_updates(
            uniffiTypeLnReceiveOperationObjectFactory.clonePointer(this),
            callStatus,
          )
        },
        /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      ),
    )
  }

  uniffiDestroy(): void {
    const ptr = (this as any)[destructorGuardSymbol]
    if (ptr !== undefined) {
      const pointer = uniffiTypeLnReceiveOperationObjectFactory.pointer(this)
      uniffiTypeLnReceiveOperationObjectFactory.freePointer(pointer)
      uniffiTypeLnReceiveOperationObjectFactory.unbless(ptr)
      delete (this as any)[destructorGuardSymbol]
    }
  }

  static instanceOf(obj_: any): obj_ is LnReceiveOperation {
    return uniffiTypeLnReceiveOperationObjectFactory.isConcreteType(obj_)
  }
}

const uniffiTypeLnReceiveOperationObjectFactory: UniffiObjectFactory<LnReceiveOperationLike> =
  (() => {
    /// <reference lib="es2021" />
    const registry =
      typeof FinalizationRegistry !== 'undefined'
        ? new FinalizationRegistry<UniffiHandle>((heldValue: UniffiHandle) => {
            uniffiTypeLnReceiveOperationObjectFactory.freePointer(heldValue)
          })
        : null

    return {
      create(pointer: UniffiHandle): LnReceiveOperationLike {
        const instance = Object.create(LnReceiveOperation.prototype)
        instance[pointerLiteralSymbol] = pointer
        instance[destructorGuardSymbol] = this.bless(pointer)
        instance[uniffiTypeNameSymbol] = 'LnReceiveOperation'
        return instance
      },

      bless(p: UniffiHandle): UniffiGcObject {
        const ptr = {
          p, // make sure this object doesn't get optimized away.
          markDestroyed: () => undefined,
        }
        if (registry) {
          registry.register(ptr, p, ptr)
        }
        return ptr
      },

      unbless(ptr_: UniffiGcObject) {
        if (registry) {
          registry.unregister(ptr_)
        }
      },

      pointer(obj_: LnReceiveOperationLike): UniffiHandle {
        if ((obj_ as any)[destructorGuardSymbol] === undefined) {
          throw new UniffiInternalError.UnexpectedNullPointer()
        }
        return (obj_ as any)[pointerLiteralSymbol]
      },

      clonePointer(obj_: LnReceiveOperationLike): UniffiHandle {
        const pointer = this.pointer(obj_)
        return uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_clone_lnreceiveoperation(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      freePointer(pointer: UniffiHandle): void {
        uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_free_lnreceiveoperation(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      isConcreteType(obj_: any): obj_ is LnReceiveOperationLike {
        return (
          obj_[destructorGuardSymbol] &&
          obj_[uniffiTypeNameSymbol] === 'LnReceiveOperation'
        )
      },
    }
  })()
const FfiConverterTypeLnReceiveOperation = new FfiConverterObject(
  uniffiTypeLnReceiveOperationObjectFactory,
)

/**
 * The result of [`Lightning::receive`], with `operation` crossing as [`LnReceiveOperation`] rather
 * than the generic `Operation<LnReceiveState>` the real [`LnReceive`] carries.
 */
export type LnReceiveHandle = {
  /**
   * See [`LnReceive::invoice`].
   */
  invoice: Bolt11Invoice
  /**
   * See [`LnReceive::operation`].
   */
  operation: LnReceiveOperationLike
}

/**
 * Generated factory for {@link LnReceiveHandle} record objects.
 */
export const LnReceiveHandle = (() => {
  const defaults = () => ({})
  const create = (() => {
    return uniffiCreateRecord<LnReceiveHandle, ReturnType<typeof defaults>>(
      defaults,
    )
  })()
  return Object.freeze({
    create,
    new: create,
    defaults: () => Object.freeze(defaults()) as Partial<LnReceiveHandle>,
  })
})()

const FfiConverterTypeLnReceiveHandle = (() => {
  type TypeName = LnReceiveHandle
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      return {
        invoice: FfiConverterTypeBolt11Invoice.readFromCursor(c),
        operation: FfiConverterTypeLnReceiveOperation.readFromCursor(c),
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      FfiConverterTypeBolt11Invoice.writeIntoCursor(value.invoice, c)
      FfiConverterTypeLnReceiveOperation.writeIntoCursor(value.operation, c)
    }
    allocationSize(value: TypeName): number {
      return (
        FfiConverterTypeBolt11Invoice.allocationSize(value.invoice) +
        FfiConverterTypeLnReceiveOperation.allocationSize(value.operation)
      )
    }
  }
  return new FFIConverter()
})()

// Enum: LightningRoute
export enum LightningRoute_Tags {
  Internal = 'Internal',
  Gateway = 'Gateway',
}
/**
 * How a lightning payment is, or was, routed.
 *
 * Available from the quote before paying and from the final state
 * afterwards. The distinction matters to a user: an internal payment pays
 * no gateway, and "this stayed inside the federation" is meaningful privacy
 * information.
 */

type LightningRoute_Internal_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'LightningRoute'
  tag: LightningRoute_Tags.Internal
}

type LightningRoute_Gateway_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'LightningRoute'
  tag: LightningRoute_Tags.Gateway
  inner: Readonly<{ gatewayId: GatewayId }>
}
export type LightningRoute =
  | LightningRoute_Internal_interface
  | LightningRoute_Gateway_interface

export const LightningRoute = (() => {
  /**
   * The payee holds their invoice in this same federation, so the
   * payment settles internally without touching the lightning network
   * and without a gateway.
   */
  class Internal_
    extends UniffiEnum
    implements LightningRoute_Internal_interface
  {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'LightningRoute'
    readonly tag = LightningRoute_Tags.Internal
    constructor() {
      super('LightningRoute', 'Internal')
    }

    static new(): Internal_ {
      return new Internal_()
    }

    static instanceOf(obj: any): obj is Internal_ {
      return obj.tag === LightningRoute_Tags.Internal
    }
  }
  /**
   * The payment leaves the federation through a lightning gateway.
   */
  class Gateway_
    extends UniffiEnum
    implements LightningRoute_Gateway_interface
  {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'LightningRoute'
    readonly tag = LightningRoute_Tags.Gateway
    readonly inner: Readonly<{ gatewayId: GatewayId }>
    constructor(inner: { gatewayId: GatewayId }) {
      super('LightningRoute', 'Gateway')

      this.inner = Object.freeze(inner)
    }
    static new(inner: { gatewayId: GatewayId }): Gateway_ {
      return new Gateway_(inner)
    }

    static instanceOf(obj: any): obj is Gateway_ {
      return obj.tag === LightningRoute_Tags.Gateway
    }
  }

  function instanceOf(obj: any): obj is LightningRoute {
    return obj[uniffiTypeNameSymbol] === 'LightningRoute'
  }

  return Object.freeze({
    instanceOf,
    Internal: Internal_,
    Gateway: Gateway_,
  })
})()

// FfiConverter for enum LightningRoute
const FfiConverterTypeLightningRoute = (() => {
  type TypeName = LightningRoute
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      switch (c.readI32()) {
        case 1:
          return new LightningRoute.Internal()
        case 2:
          return new LightningRoute.Gateway({
            gatewayId: FfiConverterTypeGatewayId.readFromCursor(c),
          })
        default:
          throw new UniffiInternalError.UnexpectedEnumCase()
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      switch (value.tag) {
        case LightningRoute_Tags.Internal: {
          c.writeI32(1)
          return
        }
        case LightningRoute_Tags.Gateway: {
          c.writeI32(2)
          const inner = value.inner
          FfiConverterTypeGatewayId.writeIntoCursor(inner.gatewayId, c)
          return
        }
        default:
          // Throwing from here means that LightningRoute_Tags hasn't matched an ordinal.
          throw new UniffiInternalError.UnexpectedEnumCase()
      }
    }
    allocationSize(value: TypeName): number {
      switch (value.tag) {
        case LightningRoute_Tags.Internal: {
          return 4
        }
        case LightningRoute_Tags.Gateway: {
          const inner = value.inner
          let size = 4
          size += FfiConverterTypeGatewayId.allocationSize(inner.gatewayId)
          return size
        }
        default:
          throw new UniffiInternalError.UnexpectedEnumCase()
      }
    }
  }
  return new FFIConverter()
})()

/**
 * The terms an outgoing lightning payment was executed on.
 *
 * Read with [`Operation::details`](crate::Operation::details). Persisted
 * when [`Lightning::send`] creates the operation and never changed, so a
 * payment picked up after a restart, or one that was refunded or failed,
 * still has an invoice, amounts, a fee and a route to show.
 */
export type LnSendDetails = {
  /**
   * The invoice this payment was authorised to pay.
   *
   * The payee, the payment hash, the description and the expiry all read
   * back off it.
   */
  invoice: Bolt11Invoice
  /**
   * The invoice's own amount: what the payee receives when the payment
   * succeeds.
   */
  invoiceAmount: Amount
  /**
   * The aggregate fee the executed quote committed to, [`LnQuote::fee`].
   */
  fee: Amount
  /**
   * The total the payment was authorised for, [`LnQuote::total`]: equal to
   * [`invoice_amount`](LnSendDetails::invoice_amount) plus
   * [`fee`](LnSendDetails::fee).
   *
   * This is a term, not an outcome. On [`LnSendState::Success`] it is what
   * was debited; on [`LnSendState::Refunded`] it is what was at stake.
   * The one exception is a payment quoted through a gateway that the
   * module settled inside the federation after all, where the gateway's
   * charge is known not to have applied and the rest of the fee is the
   * quote's estimate, so this is an upper bound.
   *
   * Exact for an operation this SDK created, including one recovered
   * after a restart, except for the residual case above.
   *
   * That residual case is never corrected: even once recovered after a
   * restart, it still reports the quote's upper bound, not the settled
   * figure.
   *
   * An estimate only for a log entry this SDK did not create.
   */
  total: Amount
  /**
   * How the payment is routed, [`LnQuote::route`].
   */
  route: LightningRoute
  /**
   * When the payment was started.
   *
   * The timestamp to sort and label a history row by. It is the moment
   * the payment was committed, not the moment it settled.
   */
  createdAt: Timestamp
}

/**
 * Generated factory for {@link LnSendDetails} record objects.
 */
export const LnSendDetails = (() => {
  const defaults = () => ({})
  const create = (() => {
    return uniffiCreateRecord<LnSendDetails, ReturnType<typeof defaults>>(
      defaults,
    )
  })()
  return Object.freeze({
    create,
    new: create,
    defaults: () => Object.freeze(defaults()) as Partial<LnSendDetails>,
  })
})()

const FfiConverterTypeLnSendDetails = (() => {
  type TypeName = LnSendDetails
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      return {
        invoice: FfiConverterTypeBolt11Invoice.readFromCursor(c),
        invoiceAmount: FfiConverterTypeAmount.readFromCursor(c),
        fee: FfiConverterTypeAmount.readFromCursor(c),
        total: FfiConverterTypeAmount.readFromCursor(c),
        route: FfiConverterTypeLightningRoute.readFromCursor(c),
        createdAt: FfiConverterTypeTimestamp.readFromCursor(c),
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      FfiConverterTypeBolt11Invoice.writeIntoCursor(value.invoice, c)
      FfiConverterTypeAmount.writeIntoCursor(value.invoiceAmount, c)
      FfiConverterTypeAmount.writeIntoCursor(value.fee, c)
      FfiConverterTypeAmount.writeIntoCursor(value.total, c)
      FfiConverterTypeLightningRoute.writeIntoCursor(value.route, c)
      FfiConverterTypeTimestamp.writeIntoCursor(value.createdAt, c)
    }
    allocationSize(value: TypeName): number {
      return (
        FfiConverterTypeBolt11Invoice.allocationSize(value.invoice) +
        FfiConverterTypeAmount.allocationSize(value.invoiceAmount) +
        FfiConverterTypeAmount.allocationSize(value.fee) +
        FfiConverterTypeAmount.allocationSize(value.total) +
        FfiConverterTypeLightningRoute.allocationSize(value.route) +
        FfiConverterTypeTimestamp.allocationSize(value.createdAt)
      )
    }
  }
  return new FFIConverter()
})()

/**
 * Typealias from the type name used in the UDL file to the builtin type.  This
 * is needed because the UDL type name is used in function/method signatures.
 */
export type Address = string
// FfiConverter for Address, a type alias for string.
const FfiConverterTypeAddress = FfiConverterString

/**
 * Typealias from the type name used in the UDL file to the builtin type.  This
 * is needed because the UDL type name is used in function/method signatures.
 */
export type Txid = string
// FfiConverter for Txid, a type alias for string.
const FfiConverterTypeTxid = FfiConverterString

/**
 * Typealias from the type name used in the UDL file to the builtin type.  This
 * is needed because the UDL type name is used in function/method signatures.
 */
export type Sats = bigint
// FfiConverter for Sats, a type alias for bigint.
const FfiConverterTypeSats = FfiConverterUInt64

/**
 * What claiming a deposit cost, component by component.
 *
 * Obtained from [`OnchainReceiveDetails::fee_breakdown`]. Every field is an
 * exact millisatoshi [`Amount`](crate::Amount), for the reason
 * [`OnchainQuote::fee`] gives on the withdrawal side. The components sum to
 * [`OnchainReceiveDetails::fee`] exactly, with no rounding and no residue.
 *
 * Unlike [`OnchainSendFeeBreakdown`], which explains a quote, this explains
 * an outcome: the parts are what the claim was charged, not a prediction.
 *
 * # Read the aggregate; use these to explain it
 *
 * The type is `#[non_exhaustive]`, so a later version may split a component
 * in two or add one, and only the aggregate stays correct across that
 * change. It is also the figure
 * [`OnchainReceiveDetails::net_credit`] was actually computed from, so it is
 * the only one guaranteed to reconcile with the balance movement.
 */
export type OnchainReceiveFeeBreakdown = {
  /**
   * The federation's own charge for accepting the deposit.
   *
   * The component a user means by "the federation's deposit fee". On its
   * own it is not what reduced the credit.
   */
  pegIn: Amount
  /**
   * What sweeping the deposit costs on the Bitcoin network, if anything.
   */
  networkClaim: Amount
  /**
   * What it costs to turn the deposit into spendable notes: a
   * federation-internal, millisatoshi-denominated cost with no on-chain
   * counterpart, and the component most likely to make the aggregate a
   * non-whole number of satoshis.
   */
  primaryModule: Amount
  /**
   * The residue that note issuance leaves behind: value too small to be
   * represented in the federation's denominations, and therefore given up.
   * Small, frequently sub-satoshi, and genuinely part of why the credit is
   * less than what arrived.
   */
  dust: Amount
}

/**
 * Generated factory for {@link OnchainReceiveFeeBreakdown} record objects.
 */
export const OnchainReceiveFeeBreakdown = (() => {
  const defaults = () => ({})
  const create = (() => {
    return uniffiCreateRecord<
      OnchainReceiveFeeBreakdown,
      ReturnType<typeof defaults>
    >(defaults)
  })()
  return Object.freeze({
    create,
    new: create,
    defaults: () =>
      Object.freeze(defaults()) as Partial<OnchainReceiveFeeBreakdown>,
  })
})()

const FfiConverterTypeOnchainReceiveFeeBreakdown = (() => {
  type TypeName = OnchainReceiveFeeBreakdown
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      return {
        pegIn: FfiConverterTypeAmount.readFromCursor(c),
        networkClaim: FfiConverterTypeAmount.readFromCursor(c),
        primaryModule: FfiConverterTypeAmount.readFromCursor(c),
        dust: FfiConverterTypeAmount.readFromCursor(c),
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      FfiConverterTypeAmount.writeIntoCursor(value.pegIn, c)
      FfiConverterTypeAmount.writeIntoCursor(value.networkClaim, c)
      FfiConverterTypeAmount.writeIntoCursor(value.primaryModule, c)
      FfiConverterTypeAmount.writeIntoCursor(value.dust, c)
    }
    allocationSize(value: TypeName): number {
      return (
        FfiConverterTypeAmount.allocationSize(value.pegIn) +
        FfiConverterTypeAmount.allocationSize(value.networkClaim) +
        FfiConverterTypeAmount.allocationSize(value.primaryModule) +
        FfiConverterTypeAmount.allocationSize(value.dust)
      )
    }
  }
  return new FFIConverter()
})()

/**
 * What an on-chain deposit is: the address to display, and the facts about
 * the funding transaction as they become known.
 *
 * Read with [`Operation::details`](crate::Operation::details) on an
 * `Operation<OnchainReceiveState>`. The record is committed in the same
 * storage transaction that creates the operation, so it is readable from
 * the moment [`Onchain::receive`] returns. No state carries the address, so
 * this record is what makes an operation id enough to rebuild a deposit
 * screen after a restart.
 *
 * # Five fields fill in over time, each once and for good
 *
 * [`txid`](OnchainReceiveDetails::txid) and
 * [`gross_deposited`](OnchainReceiveDetails::gross_deposited) fill in when a
 * transaction is seen, at
 * [`WaitingForConfirmation`](OnchainReceiveState::WaitingForConfirmation).
 * [`fee`](OnchainReceiveDetails::fee),
 * [`fee_breakdown`](OnchainReceiveDetails::fee_breakdown) and
 * [`net_credit`](OnchainReceiveDetails::net_credit) fill in when the claim
 * settles, at [`Claimed`](OnchainReceiveState::Claimed).
 *
 * Each field goes from `None` to `Some` at most once, in the same write
 * that records the transition establishing it, and never changes to a
 * different value and never reverts. So a caller need not order this call
 * against [`Operation::state`](crate::Operation::state), and reading the
 * record twice cannot produce two contradictory receipts.
 *
 * `None` means "not established", never "lost", and a field may stay `None`
 * for good: a deposit still in
 * [`WaitingForTransaction`](OnchainReceiveState::WaitingForTransaction) has
 * all five absent, and one that [`Failed`](OnchainReceiveState::Failed)
 * after its transaction was seen has the first two set and the rest `None`,
 * since no claim settled.
 *
 * # The aggregate, and the arithmetic these fields satisfy
 *
 * [`fee`](OnchainReceiveDetails::fee) is the aggregate of everything
 * claiming the deposit cost, not the deposit fee alone: this record's
 * identity is
 * [`gross_deposited`](OnchainReceiveDetails::gross_deposited) in
 * millisatoshis, less [`fee`](OnchainReceiveDetails::fee), equals
 * [`net_credit`](OnchainReceiveDetails::net_credit), which is the same
 * value [`Claimed`](OnchainReceiveState::Claimed) reports.
 *
 * The aggregate is the figure to read;
 * [`fee_breakdown`](OnchainReceiveDetails::fee_breakdown) names its parts
 * for a screen that wants to explain the difference between what was sent
 * and what was credited rather than merely state it.
 */
export type OnchainReceiveDetails = {
  /**
   * The deposit address this operation watches.
   *
   * Fixed when the operation was created and never changes. Display it,
   * encode it as a QR code, or hand it to a sender.
   *
   * Fresh for this operation: never handed out before it, and never
   * handed out again; see [`Onchain::receive`].
   */
  address: Address
  /**
   * The funding transaction, once one paying the address has been seen.
   *
   * `None` until then. Filled in when the deposit reaches
   * [`WaitingForConfirmation`](OnchainReceiveState::WaitingForConfirmation)
   * and never changed afterwards, including if the deposit then
   * [`Failed`](OnchainReceiveState::Failed), which carries no transaction
   * of its own.
   *
   * This tracks the first output detected at the address; see
   * [`Onchain::receive`].
   */
  txid?: Txid
  /**
   * The gross amount that arrived on chain, before anything the
   * federation charges to claim it.
   *
   * Whole [`Sats`](crate::Sats): it is the value of an output in the
   * funding transaction. `None` until a transaction is seen, then fixed.
   *
   * This is the counterparty figure, what the sender sent, and it is the
   * number to show beside the credit when a user asks why the two differ.
   */
  grossDeposited?: Sats
  /**
   * The aggregate of everything the federation charged to bring this
   * deposit into the balance, once the claim has settled.
   *
   * This is the figure [`net_credit`](OnchainReceiveDetails::net_credit)
   * is computed from; [`fee_breakdown`](OnchainReceiveDetails::fee_breakdown)
   * names the parts.
   *
   * `None` until then. Millisatoshi-denominated, like every other fee in
   * this facade.
   */
  fee?: Amount
  /**
   * [`fee`](OnchainReceiveDetails::fee), split into the named parts it is
   * made of.
   *
   * `Some` exactly when the aggregate is, set in the same write, and
   * re-reporting the same money rather than an additional charge. The
   * aggregate stays authoritative; see [`OnchainReceiveFeeBreakdown`] for
   * why a caller should not re-derive it by summing these.
   */
  feeBreakdown?: OnchainReceiveFeeBreakdown
  /**
   * The amount credited to the balance: `gross_deposited` in
   * millisatoshis less [`fee`](OnchainReceiveDetails::fee), the
   * aggregate, not a deposit fee alone.
   *
   * `None` until the claim completes. Equal to the
   * [`Claimed`](OnchainReceiveState::Claimed) state's own net figure, so
   * a receipt built from the record and one built from the state cannot
   * disagree.
   */
  netCredit?: Amount
  /**
   * When the deposit address was allocated, by this device's clock.
   *
   * A local reading, like [`ActivityItem::time`](crate::ActivityItem::time).
   * Note that this is when the *address* was handed out, not when the
   * funding transaction arrived; a deposit may be paid days later.
   */
  createdAt: Timestamp
}

/**
 * Generated factory for {@link OnchainReceiveDetails} record objects.
 */
export const OnchainReceiveDetails = (() => {
  const defaults = () => ({})
  const create = (() => {
    return uniffiCreateRecord<
      OnchainReceiveDetails,
      ReturnType<typeof defaults>
    >(defaults)
  })()
  return Object.freeze({
    create,
    new: create,
    defaults: () => Object.freeze(defaults()) as Partial<OnchainReceiveDetails>,
  })
})()

const FfiConverterTypeOnchainReceiveDetails = (() => {
  type TypeName = OnchainReceiveDetails
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      return {
        address: FfiConverterTypeAddress.readFromCursor(c),
        txid: FfiConverterOptionalTypeTxid.readFromCursor(c),
        grossDeposited: FfiConverterOptionalTypeSats.readFromCursor(c),
        fee: FfiConverterOptionalTypeAmount.readFromCursor(c),
        feeBreakdown:
          FfiConverterOptionalTypeOnchainReceiveFeeBreakdown.readFromCursor(c),
        netCredit: FfiConverterOptionalTypeAmount.readFromCursor(c),
        createdAt: FfiConverterTypeTimestamp.readFromCursor(c),
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      FfiConverterTypeAddress.writeIntoCursor(value.address, c)
      FfiConverterOptionalTypeTxid.writeIntoCursor(value.txid, c)
      FfiConverterOptionalTypeSats.writeIntoCursor(value.grossDeposited, c)
      FfiConverterOptionalTypeAmount.writeIntoCursor(value.fee, c)
      FfiConverterOptionalTypeOnchainReceiveFeeBreakdown.writeIntoCursor(
        value.feeBreakdown,
        c,
      )
      FfiConverterOptionalTypeAmount.writeIntoCursor(value.netCredit, c)
      FfiConverterTypeTimestamp.writeIntoCursor(value.createdAt, c)
    }
    allocationSize(value: TypeName): number {
      return (
        FfiConverterTypeAddress.allocationSize(value.address) +
        FfiConverterOptionalTypeTxid.allocationSize(value.txid) +
        FfiConverterOptionalTypeSats.allocationSize(value.grossDeposited) +
        FfiConverterOptionalTypeAmount.allocationSize(value.fee) +
        FfiConverterOptionalTypeOnchainReceiveFeeBreakdown.allocationSize(
          value.feeBreakdown,
        ) +
        FfiConverterOptionalTypeAmount.allocationSize(value.netCredit) +
        FfiConverterTypeTimestamp.allocationSize(value.createdAt)
      )
    }
  }
  return new FFIConverter()
})()

// Enum: OnchainReceiveState
export enum OnchainReceiveState_Tags {
  WaitingForTransaction = 'WaitingForTransaction',
  WaitingForConfirmation = 'WaitingForConfirmation',
  Confirmed = 'Confirmed',
  Claimed = 'Claimed',
  Failed = 'Failed',
}
/**
 * The lifecycle of an on-chain deposit.
 *
 * The five variants are the application-level lifecycle of a deposit:
 * nothing seen, seen, confirmed, credited, or could not be credited.
 *
 * A deposit can stay in [`Confirmed`](Self::Confirmed) across an internal
 * retry of the claim, under the same operation id, until the claim
 * succeeds; [`Failed`](Self::Failed) is emitted only once no further claim
 * is possible, so an application never sees a still-claimable deposit
 * finalized.
 *
 * # The final state is self-contained
 *
 * [`Claimed`](Self::Claimed) carries the funding transaction, the gross
 * amount that arrived, and the net amount credited, and that is not
 * redundancy. A subscription yields the state an operation is in now and
 * never replays the ones before it, so an application that reattaches to a
 * deposit by id, after a restart, from an activity row, or from a
 * notification, may see [`Claimed`](Self::Claimed) as the very first state
 * it is ever shown, and it can render a full receipt from that state alone.
 *
 * The one state that is deliberately not self-contained is
 * [`Failed`](Self::Failed), which carries only a diagnostic reason even
 * though a deposit can fail after its transaction was seen. That is what
 * [`OnchainReceiveDetails`] is for: the address, and the transaction and
 * gross amount once one was seen, are on the details record too, so an
 * application never needs to have observed an earlier state to describe a
 * failed deposit.
 */

type OnchainReceiveState_WaitingForTransaction_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'OnchainReceiveState'
  tag: OnchainReceiveState_Tags.WaitingForTransaction
}

type OnchainReceiveState_WaitingForConfirmation_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'OnchainReceiveState'
  tag: OnchainReceiveState_Tags.WaitingForConfirmation
  inner: Readonly<{ txid: Txid; grossDeposited: Sats }>
}

type OnchainReceiveState_Confirmed_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'OnchainReceiveState'
  tag: OnchainReceiveState_Tags.Confirmed
  inner: Readonly<{ txid: Txid; grossDeposited: Sats }>
}

type OnchainReceiveState_Claimed_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'OnchainReceiveState'
  tag: OnchainReceiveState_Tags.Claimed
  inner: Readonly<{ txid: Txid; grossDeposited: Sats; netCredit: Amount }>
}

type OnchainReceiveState_Failed_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'OnchainReceiveState'
  tag: OnchainReceiveState_Tags.Failed
  inner: Readonly<{ reason: string }>
}
export type OnchainReceiveState =
  | OnchainReceiveState_WaitingForTransaction_interface
  | OnchainReceiveState_WaitingForConfirmation_interface
  | OnchainReceiveState_Confirmed_interface
  | OnchainReceiveState_Claimed_interface
  | OnchainReceiveState_Failed_interface

export const OnchainReceiveState = (() => {
  /**
   * The address is being watched and no transaction paying it has been
   * seen yet.
   *
   * A deposit can sit here indefinitely, and there is no call that ends
   * it; see [`Onchain::receive`] for what that means for
   * [`Operation::await_final`](crate::Operation::await_final) and for
   * [`Sdk::forget_federation`](crate::Sdk::forget_federation).
   */
  class WaitingForTransaction_
    extends UniffiEnum
    implements OnchainReceiveState_WaitingForTransaction_interface
  {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'OnchainReceiveState'
    readonly tag = OnchainReceiveState_Tags.WaitingForTransaction
    constructor() {
      super('OnchainReceiveState', 'WaitingForTransaction')
    }

    static new(): WaitingForTransaction_ {
      return new WaitingForTransaction_()
    }

    static instanceOf(obj: any): obj is WaitingForTransaction_ {
      return obj.tag === OnchainReceiveState_Tags.WaitingForTransaction
    }
  }
  /**
   * A transaction paying the address has been seen and is waiting for
   * enough confirmations for the federation to accept it.
   */
  class WaitingForConfirmation_
    extends UniffiEnum
    implements OnchainReceiveState_WaitingForConfirmation_interface
  {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'OnchainReceiveState'
    readonly tag = OnchainReceiveState_Tags.WaitingForConfirmation
    readonly inner: Readonly<{ txid: Txid; grossDeposited: Sats }>
    constructor(inner: { txid: Txid; grossDeposited: Sats }) {
      super('OnchainReceiveState', 'WaitingForConfirmation')

      this.inner = Object.freeze(inner)
    }
    static new(inner: {
      txid: Txid
      grossDeposited: Sats
    }): WaitingForConfirmation_ {
      return new WaitingForConfirmation_(inner)
    }

    static instanceOf(obj: any): obj is WaitingForConfirmation_ {
      return obj.tag === OnchainReceiveState_Tags.WaitingForConfirmation
    }
  }
  /**
   * The transaction has the confirmations the federation requires; the
   * deposit is being claimed into the balance.
   */
  class Confirmed_
    extends UniffiEnum
    implements OnchainReceiveState_Confirmed_interface
  {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'OnchainReceiveState'
    readonly tag = OnchainReceiveState_Tags.Confirmed
    readonly inner: Readonly<{ txid: Txid; grossDeposited: Sats }>
    constructor(inner: { txid: Txid; grossDeposited: Sats }) {
      super('OnchainReceiveState', 'Confirmed')

      this.inner = Object.freeze(inner)
    }
    static new(inner: { txid: Txid; grossDeposited: Sats }): Confirmed_ {
      return new Confirmed_(inner)
    }

    static instanceOf(obj: any): obj is Confirmed_ {
      return obj.tag === OnchainReceiveState_Tags.Confirmed
    }
  }
  /**
   * Final: the deposit is in the spendable balance.
   *
   * Self-contained on purpose, see the enum's own documentation. A
   * caller holding only this state can name the transaction, what
   * arrived, and what was credited, without having observed anything
   * earlier.
   */
  class Claimed_
    extends UniffiEnum
    implements OnchainReceiveState_Claimed_interface
  {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'OnchainReceiveState'
    readonly tag = OnchainReceiveState_Tags.Claimed
    readonly inner: Readonly<{
      txid: Txid
      grossDeposited: Sats
      netCredit: Amount
    }>
    constructor(inner: {
      txid: Txid
      grossDeposited: Sats
      netCredit: Amount
    }) {
      super('OnchainReceiveState', 'Claimed')

      this.inner = Object.freeze(inner)
    }
    static new(inner: {
      txid: Txid
      grossDeposited: Sats
      netCredit: Amount
    }): Claimed_ {
      return new Claimed_(inner)
    }

    static instanceOf(obj: any): obj is Claimed_ {
      return obj.tag === OnchainReceiveState_Tags.Claimed
    }
  }
  /**
   * Final: the deposit could not be claimed.
   *
   * Carries no transaction and no amount even when one was seen. What
   * arrived is on [`OnchainReceiveDetails`], which is where a caller that
   * only ever saw this state reads it; no claim settled, so that record
   * has no fee and no credit for it either.
   */
  class Failed_
    extends UniffiEnum
    implements OnchainReceiveState_Failed_interface
  {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'OnchainReceiveState'
    readonly tag = OnchainReceiveState_Tags.Failed
    readonly inner: Readonly<{ reason: string }>
    constructor(inner: { reason: string }) {
      super('OnchainReceiveState', 'Failed')

      this.inner = Object.freeze(inner)
    }
    static new(inner: { reason: string }): Failed_ {
      return new Failed_(inner)
    }

    static instanceOf(obj: any): obj is Failed_ {
      return obj.tag === OnchainReceiveState_Tags.Failed
    }
  }

  function instanceOf(obj: any): obj is OnchainReceiveState {
    return obj[uniffiTypeNameSymbol] === 'OnchainReceiveState'
  }

  return Object.freeze({
    instanceOf,
    WaitingForTransaction: WaitingForTransaction_,
    WaitingForConfirmation: WaitingForConfirmation_,
    Confirmed: Confirmed_,
    Claimed: Claimed_,
    Failed: Failed_,
  })
})()

// FfiConverter for enum OnchainReceiveState
const FfiConverterTypeOnchainReceiveState = (() => {
  type TypeName = OnchainReceiveState
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      switch (c.readI32()) {
        case 1:
          return new OnchainReceiveState.WaitingForTransaction()
        case 2:
          return new OnchainReceiveState.WaitingForConfirmation({
            txid: FfiConverterTypeTxid.readFromCursor(c),
            grossDeposited: FfiConverterTypeSats.readFromCursor(c),
          })
        case 3:
          return new OnchainReceiveState.Confirmed({
            txid: FfiConverterTypeTxid.readFromCursor(c),
            grossDeposited: FfiConverterTypeSats.readFromCursor(c),
          })
        case 4:
          return new OnchainReceiveState.Claimed({
            txid: FfiConverterTypeTxid.readFromCursor(c),
            grossDeposited: FfiConverterTypeSats.readFromCursor(c),
            netCredit: FfiConverterTypeAmount.readFromCursor(c),
          })
        case 5:
          return new OnchainReceiveState.Failed({
            reason: FfiConverterString.readFromCursor(c),
          })
        default:
          throw new UniffiInternalError.UnexpectedEnumCase()
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      switch (value.tag) {
        case OnchainReceiveState_Tags.WaitingForTransaction: {
          c.writeI32(1)
          return
        }
        case OnchainReceiveState_Tags.WaitingForConfirmation: {
          c.writeI32(2)
          const inner = value.inner
          FfiConverterTypeTxid.writeIntoCursor(inner.txid, c)
          FfiConverterTypeSats.writeIntoCursor(inner.grossDeposited, c)
          return
        }
        case OnchainReceiveState_Tags.Confirmed: {
          c.writeI32(3)
          const inner = value.inner
          FfiConverterTypeTxid.writeIntoCursor(inner.txid, c)
          FfiConverterTypeSats.writeIntoCursor(inner.grossDeposited, c)
          return
        }
        case OnchainReceiveState_Tags.Claimed: {
          c.writeI32(4)
          const inner = value.inner
          FfiConverterTypeTxid.writeIntoCursor(inner.txid, c)
          FfiConverterTypeSats.writeIntoCursor(inner.grossDeposited, c)
          FfiConverterTypeAmount.writeIntoCursor(inner.netCredit, c)
          return
        }
        case OnchainReceiveState_Tags.Failed: {
          c.writeI32(5)
          const inner = value.inner
          FfiConverterString.writeIntoCursor(inner.reason, c)
          return
        }
        default:
          // Throwing from here means that OnchainReceiveState_Tags hasn't matched an ordinal.
          throw new UniffiInternalError.UnexpectedEnumCase()
      }
    }
    allocationSize(value: TypeName): number {
      switch (value.tag) {
        case OnchainReceiveState_Tags.WaitingForTransaction: {
          return 4
        }
        case OnchainReceiveState_Tags.WaitingForConfirmation: {
          const inner = value.inner
          let size = 4
          size += FfiConverterTypeTxid.allocationSize(inner.txid)
          size += FfiConverterTypeSats.allocationSize(inner.grossDeposited)
          return size
        }
        case OnchainReceiveState_Tags.Confirmed: {
          const inner = value.inner
          let size = 4
          size += FfiConverterTypeTxid.allocationSize(inner.txid)
          size += FfiConverterTypeSats.allocationSize(inner.grossDeposited)
          return size
        }
        case OnchainReceiveState_Tags.Claimed: {
          const inner = value.inner
          let size = 4
          size += FfiConverterTypeTxid.allocationSize(inner.txid)
          size += FfiConverterTypeSats.allocationSize(inner.grossDeposited)
          size += FfiConverterTypeAmount.allocationSize(inner.netCredit)
          return size
        }
        case OnchainReceiveState_Tags.Failed: {
          const inner = value.inner
          let size = 4
          size += FfiConverterString.allocationSize(inner.reason)
          return size
        }
        default:
          throw new UniffiInternalError.UnexpectedEnumCase()
      }
    }
  }
  return new FFIConverter()
})()

/**
 * The UniFFI view of one `OperationUpdates<S>` instantiation, behind a lock so `next`'s
 * `&mut self` can cross as `&self`.
 */
export interface OnchainReceiveOperationUpdatesLike {
  /**
   * See [`OperationUpdates::next`](crate::OperationUpdates::next).
   */
  next(asyncOpts_?: {
    signal: AbortSignal
  }) /*throws*/ : Promise<OnchainReceiveState | undefined>
}
/**
 * @deprecated Use `OnchainReceiveOperationUpdatesLike` instead.
 */
export type OnchainReceiveOperationUpdatesInterface =
  OnchainReceiveOperationUpdatesLike

/**
 * The UniFFI view of one `OperationUpdates<S>` instantiation, behind a lock so `next`'s
 * `&mut self` can cross as `&self`.
 */
export class OnchainReceiveOperationUpdates
  extends UniffiAbstractObject
  implements OnchainReceiveOperationUpdatesLike
{
  readonly [uniffiTypeNameSymbol] = 'OnchainReceiveOperationUpdates'
  readonly [destructorGuardSymbol]: UniffiGcObject
  readonly [pointerLiteralSymbol]: UniffiHandle
  // No primary constructor declared for this class.
  private constructor(pointer: UniffiHandle) {
    super()
    this[pointerLiteralSymbol] = pointer
    this[destructorGuardSymbol] =
      uniffiTypeOnchainReceiveOperationUpdatesObjectFactory.bless(pointer)
  }

  /**
   * See [`OperationUpdates::next`](crate::OperationUpdates::next).
   */
  async next(asyncOpts_?: {
    signal: AbortSignal
  }): Promise<OnchainReceiveState | undefined> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_onchainreceiveoperationupdates_next(
          uniffiTypeOnchainReceiveOperationUpdatesObjectFactory.clonePointer(
            this,
          ),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterOptionalTypeOnchainReceiveState.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  uniffiDestroy(): void {
    const ptr = (this as any)[destructorGuardSymbol]
    if (ptr !== undefined) {
      const pointer =
        uniffiTypeOnchainReceiveOperationUpdatesObjectFactory.pointer(this)
      uniffiTypeOnchainReceiveOperationUpdatesObjectFactory.freePointer(pointer)
      uniffiTypeOnchainReceiveOperationUpdatesObjectFactory.unbless(ptr)
      delete (this as any)[destructorGuardSymbol]
    }
  }

  static instanceOf(obj_: any): obj_ is OnchainReceiveOperationUpdates {
    return uniffiTypeOnchainReceiveOperationUpdatesObjectFactory.isConcreteType(
      obj_,
    )
  }
}

const uniffiTypeOnchainReceiveOperationUpdatesObjectFactory: UniffiObjectFactory<OnchainReceiveOperationUpdatesLike> =
  (() => {
    /// <reference lib="es2021" />
    const registry =
      typeof FinalizationRegistry !== 'undefined'
        ? new FinalizationRegistry<UniffiHandle>((heldValue: UniffiHandle) => {
            uniffiTypeOnchainReceiveOperationUpdatesObjectFactory.freePointer(
              heldValue,
            )
          })
        : null

    return {
      create(pointer: UniffiHandle): OnchainReceiveOperationUpdatesLike {
        const instance = Object.create(OnchainReceiveOperationUpdates.prototype)
        instance[pointerLiteralSymbol] = pointer
        instance[destructorGuardSymbol] = this.bless(pointer)
        instance[uniffiTypeNameSymbol] = 'OnchainReceiveOperationUpdates'
        return instance
      },

      bless(p: UniffiHandle): UniffiGcObject {
        const ptr = {
          p, // make sure this object doesn't get optimized away.
          markDestroyed: () => undefined,
        }
        if (registry) {
          registry.register(ptr, p, ptr)
        }
        return ptr
      },

      unbless(ptr_: UniffiGcObject) {
        if (registry) {
          registry.unregister(ptr_)
        }
      },

      pointer(obj_: OnchainReceiveOperationUpdatesLike): UniffiHandle {
        if ((obj_ as any)[destructorGuardSymbol] === undefined) {
          throw new UniffiInternalError.UnexpectedNullPointer()
        }
        return (obj_ as any)[pointerLiteralSymbol]
      },

      clonePointer(obj_: OnchainReceiveOperationUpdatesLike): UniffiHandle {
        const pointer = this.pointer(obj_)
        return uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_clone_onchainreceiveoperationupdates(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      freePointer(pointer: UniffiHandle): void {
        uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_free_onchainreceiveoperationupdates(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      isConcreteType(obj_: any): obj_ is OnchainReceiveOperationUpdatesLike {
        return (
          obj_[destructorGuardSymbol] &&
          obj_[uniffiTypeNameSymbol] === 'OnchainReceiveOperationUpdates'
        )
      },
    }
  })()
const FfiConverterTypeOnchainReceiveOperationUpdates = new FfiConverterObject(
  uniffiTypeOnchainReceiveOperationUpdatesObjectFactory,
)

/**
 * The UniFFI view of one `Operation<S>` instantiation: an opaque object wrapping the real
 * handle, forwarding every method.
 */
export interface OnchainReceiveOperationLike {
  /**
   * See [`Operation::await_final`](crate::Operation::await_final).
   */
  awaitFinal(asyncOpts_?: {
    signal: AbortSignal
  }) /*throws*/ : Promise<OnchainReceiveState>
  /**
   * See [`Operation::details`](crate::Operation::details).
   */
  details(asyncOpts_?: {
    signal: AbortSignal
  }) /*throws*/ : Promise<OnchainReceiveDetails>
  /**
   * See [`Operation::id`](crate::Operation::id).
   */
  id(): OperationId
  /**
   * See [`Operation::state`](crate::Operation::state).
   */
  state(asyncOpts_?: {
    signal: AbortSignal
  }) /*throws*/ : Promise<OnchainReceiveState>
  /**
   * See [`Operation::updates`](crate::Operation::updates).
   */
  updates(): OnchainReceiveOperationUpdatesLike
}
/**
 * @deprecated Use `OnchainReceiveOperationLike` instead.
 */
export type OnchainReceiveOperationInterface = OnchainReceiveOperationLike

/**
 * The UniFFI view of one `Operation<S>` instantiation: an opaque object wrapping the real
 * handle, forwarding every method.
 */
export class OnchainReceiveOperation
  extends UniffiAbstractObject
  implements OnchainReceiveOperationLike
{
  readonly [uniffiTypeNameSymbol] = 'OnchainReceiveOperation'
  readonly [destructorGuardSymbol]: UniffiGcObject
  readonly [pointerLiteralSymbol]: UniffiHandle
  // No primary constructor declared for this class.
  private constructor(pointer: UniffiHandle) {
    super()
    this[pointerLiteralSymbol] = pointer
    this[destructorGuardSymbol] =
      uniffiTypeOnchainReceiveOperationObjectFactory.bless(pointer)
  }

  /**
   * See [`Operation::await_final`](crate::Operation::await_final).
   */
  async awaitFinal(asyncOpts_?: {
    signal: AbortSignal
  }): Promise<OnchainReceiveState> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_onchainreceiveoperation_await_final(
          uniffiTypeOnchainReceiveOperationObjectFactory.clonePointer(this),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterTypeOnchainReceiveState.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * See [`Operation::details`](crate::Operation::details).
   */
  async details(asyncOpts_?: {
    signal: AbortSignal
  }): Promise<OnchainReceiveDetails> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_onchainreceiveoperation_details(
          uniffiTypeOnchainReceiveOperationObjectFactory.clonePointer(this),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterTypeOnchainReceiveDetails.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * See [`Operation::id`](crate::Operation::id).
   */
  id(): OperationId {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_onchainreceiveoperation_id(
          uniffiTypeOnchainReceiveOperationObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterTypeOperationId.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * See [`Operation::state`](crate::Operation::state).
   */
  async state(asyncOpts_?: {
    signal: AbortSignal
  }): Promise<OnchainReceiveState> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_onchainreceiveoperation_state(
          uniffiTypeOnchainReceiveOperationObjectFactory.clonePointer(this),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterTypeOnchainReceiveState.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * See [`Operation::updates`](crate::Operation::updates).
   */
  updates(): OnchainReceiveOperationUpdatesLike {
    return FfiConverterTypeOnchainReceiveOperationUpdates.lift(
      uniffiCaller.rustCall(
        /*caller:*/ (callStatus) => {
          return nativeModule().uniffi_fedimint_sdk_fn_method_onchainreceiveoperation_updates(
            uniffiTypeOnchainReceiveOperationObjectFactory.clonePointer(this),
            callStatus,
          )
        },
        /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      ),
    )
  }

  uniffiDestroy(): void {
    const ptr = (this as any)[destructorGuardSymbol]
    if (ptr !== undefined) {
      const pointer =
        uniffiTypeOnchainReceiveOperationObjectFactory.pointer(this)
      uniffiTypeOnchainReceiveOperationObjectFactory.freePointer(pointer)
      uniffiTypeOnchainReceiveOperationObjectFactory.unbless(ptr)
      delete (this as any)[destructorGuardSymbol]
    }
  }

  static instanceOf(obj_: any): obj_ is OnchainReceiveOperation {
    return uniffiTypeOnchainReceiveOperationObjectFactory.isConcreteType(obj_)
  }
}

const uniffiTypeOnchainReceiveOperationObjectFactory: UniffiObjectFactory<OnchainReceiveOperationLike> =
  (() => {
    /// <reference lib="es2021" />
    const registry =
      typeof FinalizationRegistry !== 'undefined'
        ? new FinalizationRegistry<UniffiHandle>((heldValue: UniffiHandle) => {
            uniffiTypeOnchainReceiveOperationObjectFactory.freePointer(
              heldValue,
            )
          })
        : null

    return {
      create(pointer: UniffiHandle): OnchainReceiveOperationLike {
        const instance = Object.create(OnchainReceiveOperation.prototype)
        instance[pointerLiteralSymbol] = pointer
        instance[destructorGuardSymbol] = this.bless(pointer)
        instance[uniffiTypeNameSymbol] = 'OnchainReceiveOperation'
        return instance
      },

      bless(p: UniffiHandle): UniffiGcObject {
        const ptr = {
          p, // make sure this object doesn't get optimized away.
          markDestroyed: () => undefined,
        }
        if (registry) {
          registry.register(ptr, p, ptr)
        }
        return ptr
      },

      unbless(ptr_: UniffiGcObject) {
        if (registry) {
          registry.unregister(ptr_)
        }
      },

      pointer(obj_: OnchainReceiveOperationLike): UniffiHandle {
        if ((obj_ as any)[destructorGuardSymbol] === undefined) {
          throw new UniffiInternalError.UnexpectedNullPointer()
        }
        return (obj_ as any)[pointerLiteralSymbol]
      },

      clonePointer(obj_: OnchainReceiveOperationLike): UniffiHandle {
        const pointer = this.pointer(obj_)
        return uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_clone_onchainreceiveoperation(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      freePointer(pointer: UniffiHandle): void {
        uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_free_onchainreceiveoperation(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      isConcreteType(obj_: any): obj_ is OnchainReceiveOperationLike {
        return (
          obj_[destructorGuardSymbol] &&
          obj_[uniffiTypeNameSymbol] === 'OnchainReceiveOperation'
        )
      },
    }
  })()
const FfiConverterTypeOnchainReceiveOperation = new FfiConverterObject(
  uniffiTypeOnchainReceiveOperationObjectFactory,
)

/**
 * The result of [`Onchain::receive`], with `operation` crossing as [`OnchainReceiveOperation`]
 * rather than the generic `Operation<OnchainReceiveState>` the real [`OnchainReceive`] carries.
 */
export type OnchainReceiveHandle = {
  /**
   * See [`OnchainReceive::address`].
   */
  address: Address
  /**
   * See [`OnchainReceive::operation`].
   */
  operation: OnchainReceiveOperationLike
}

/**
 * Generated factory for {@link OnchainReceiveHandle} record objects.
 */
export const OnchainReceiveHandle = (() => {
  const defaults = () => ({})
  const create = (() => {
    return uniffiCreateRecord<
      OnchainReceiveHandle,
      ReturnType<typeof defaults>
    >(defaults)
  })()
  return Object.freeze({
    create,
    new: create,
    defaults: () => Object.freeze(defaults()) as Partial<OnchainReceiveHandle>,
  })
})()

const FfiConverterTypeOnchainReceiveHandle = (() => {
  type TypeName = OnchainReceiveHandle
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      return {
        address: FfiConverterTypeAddress.readFromCursor(c),
        operation: FfiConverterTypeOnchainReceiveOperation.readFromCursor(c),
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      FfiConverterTypeAddress.writeIntoCursor(value.address, c)
      FfiConverterTypeOnchainReceiveOperation.writeIntoCursor(
        value.operation,
        c,
      )
    }
    allocationSize(value: TypeName): number {
      return (
        FfiConverterTypeAddress.allocationSize(value.address) +
        FfiConverterTypeOnchainReceiveOperation.allocationSize(value.operation)
      )
    }
  }
  return new FFIConverter()
})()

/**
 * What an on-chain withdrawal *is*: the destination and the terms it was
 * executed on.
 *
 * Read with [`Operation::details`](crate::Operation::details) on an
 * `Operation<OnchainSendState>`. Every field here is fixed by the executed
 * [`OnchainQuote`] and committed in the same storage transaction that
 * creates the operation, so it is readable from the first moment
 * [`Onchain::send`] returns, survives a restart, and reads the same however
 * the withdrawal ends. That last part matters: a withdrawal that failed has
 * a destination and a quoted fee just as a successful one does, and a
 * receipt that can only be produced for successes is not a receipt.
 *
 * [`amount`](OnchainSendDetails::amount) is whole [`Sats`](crate::Sats),
 * since it is an output in a Bitcoin transaction, while
 * [`fee`](OnchainSendDetails::fee) and
 * [`total`](OnchainSendDetails::total) are millisatoshi
 * [`Amount`](crate::Amount)s; see the [unit note](Onchain) and
 * [`OnchainQuote::fee`].
 *
 * There is no `txid` field here: the broadcast transaction id appears on
 * [`Succeeded`](OnchainSendState::Succeeded), which is final and therefore
 * stays readable from [`Operation::state`](crate::Operation::state) for the
 * rest of the operation's life.
 */
export type OnchainSendDetails = {
  /**
   * The destination the withdrawal pays.
   *
   * The address the quote was built against and bound to, network-checked
   * at quote time. This is what a receipt shows and what a "sent to"
   * line reads from after a restart.
   */
  address: Address
  /**
   * The amount bound for [`address`](OnchainSendDetails::address), in
   * whole satoshis.
   *
   * The counterparty figure of the executed quote: what the recipient
   * receives when the withdrawal is broadcast, gross of this wallet's
   * fees. Not the debit, that is [`total`](OnchainSendDetails::total).
   */
  amount: Sats
  /**
   * The aggregate fee as quoted, exactly.
   *
   * The same figure [`OnchainQuote::fee`] reported and the same one
   * [`Onchain::send`] was authorised against. Recorded because it cannot
   * be re-derived afterwards: the mempool it was estimated against has
   * moved on.
   */
  fee: Amount
  /**
   * The total the withdrawal was authorised for: `amount` converted to
   * millisatoshis plus [`fee`](OnchainSendDetails::fee), which is
   * [`OnchainQuote::total`].
   *
   * A term, not an outcome: it is what a
   * [`Succeeded`](OnchainSendState::Succeeded) withdrawal debited, and
   * what a [`Refunded`](OnchainSendState::Refunded) one leaves no lasting
   * debit of. The state says which; this record says how much was at
   * stake.
   */
  total: Amount
  /**
   * When the withdrawal was started, by this device's clock.
   *
   * A local reading, like [`ActivityItem::time`](crate::ActivityItem::time):
   * the federation does not attest to it. Fine for ordering and display,
   * not evidence of when anything happened.
   */
  createdAt: Timestamp
}

/**
 * Generated factory for {@link OnchainSendDetails} record objects.
 */
export const OnchainSendDetails = (() => {
  const defaults = () => ({})
  const create = (() => {
    return uniffiCreateRecord<OnchainSendDetails, ReturnType<typeof defaults>>(
      defaults,
    )
  })()
  return Object.freeze({
    create,
    new: create,
    defaults: () => Object.freeze(defaults()) as Partial<OnchainSendDetails>,
  })
})()

const FfiConverterTypeOnchainSendDetails = (() => {
  type TypeName = OnchainSendDetails
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      return {
        address: FfiConverterTypeAddress.readFromCursor(c),
        amount: FfiConverterTypeSats.readFromCursor(c),
        fee: FfiConverterTypeAmount.readFromCursor(c),
        total: FfiConverterTypeAmount.readFromCursor(c),
        createdAt: FfiConverterTypeTimestamp.readFromCursor(c),
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      FfiConverterTypeAddress.writeIntoCursor(value.address, c)
      FfiConverterTypeSats.writeIntoCursor(value.amount, c)
      FfiConverterTypeAmount.writeIntoCursor(value.fee, c)
      FfiConverterTypeAmount.writeIntoCursor(value.total, c)
      FfiConverterTypeTimestamp.writeIntoCursor(value.createdAt, c)
    }
    allocationSize(value: TypeName): number {
      return (
        FfiConverterTypeAddress.allocationSize(value.address) +
        FfiConverterTypeSats.allocationSize(value.amount) +
        FfiConverterTypeAmount.allocationSize(value.fee) +
        FfiConverterTypeAmount.allocationSize(value.total) +
        FfiConverterTypeTimestamp.allocationSize(value.createdAt)
      )
    }
  }
  return new FFIConverter()
})()

/**
 * What [`OnchainQuote::fee`] is made of, component by component.
 *
 * Obtained from [`OnchainQuote::fee_breakdown`]. Every field is an exact
 * millisatoshi [`Amount`](crate::Amount), for the reason
 * [`OnchainQuote::fee`] gives. The components sum to [`OnchainQuote::fee`]
 * exactly, with no rounding and no residue.
 *
 * # Read the aggregate; use these to explain it
 *
 * A caller that needs the number to charge, to compare against a balance,
 * or to put in a receipt should read [`OnchainQuote::fee`] (or
 * [`OnchainQuote::total`]) and not sum these fields: the type is
 * `#[non_exhaustive]`, so a later version may split a component in two or
 * add one, and only the aggregate stays correct across that change. It is
 * also the figure the quote commits to and [`Onchain::send`] is authorised
 * against.
 */
export type OnchainSendFeeBreakdown = {
  /**
   * What it costs to put the destination output on chain: the
   * federation's charge for building it, including its share of the
   * Bitcoin network fee at the feerate the quote was computed against.
   *
   * This is the component a user intuitively expects a withdrawal to
   * cost, and on its own it is not the whole cost.
   */
  walletOutput: Amount
  /**
   * What it costs to fund that output from the balance: selecting and
   * spending the ecash that pays for the withdrawal.
   *
   * This is a federation-internal, millisatoshi-denominated cost with no
   * on-chain counterpart, and it is the component most likely to make
   * [`OnchainQuote::fee`] a non-whole number of satoshis.
   */
  funding: Amount
  /**
   * What the change from that funding costs: reissuing the remainder as
   * notes, plus any residue too small to be worth returning and
   * therefore given up.
   *
   * Small, frequently sub-satoshi, and part of the debit.
   */
  change: Amount
}

/**
 * Generated factory for {@link OnchainSendFeeBreakdown} record objects.
 */
export const OnchainSendFeeBreakdown = (() => {
  const defaults = () => ({})
  const create = (() => {
    return uniffiCreateRecord<
      OnchainSendFeeBreakdown,
      ReturnType<typeof defaults>
    >(defaults)
  })()
  return Object.freeze({
    create,
    new: create,
    defaults: () =>
      Object.freeze(defaults()) as Partial<OnchainSendFeeBreakdown>,
  })
})()

const FfiConverterTypeOnchainSendFeeBreakdown = (() => {
  type TypeName = OnchainSendFeeBreakdown
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      return {
        walletOutput: FfiConverterTypeAmount.readFromCursor(c),
        funding: FfiConverterTypeAmount.readFromCursor(c),
        change: FfiConverterTypeAmount.readFromCursor(c),
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      FfiConverterTypeAmount.writeIntoCursor(value.walletOutput, c)
      FfiConverterTypeAmount.writeIntoCursor(value.funding, c)
      FfiConverterTypeAmount.writeIntoCursor(value.change, c)
    }
    allocationSize(value: TypeName): number {
      return (
        FfiConverterTypeAmount.allocationSize(value.walletOutput) +
        FfiConverterTypeAmount.allocationSize(value.funding) +
        FfiConverterTypeAmount.allocationSize(value.change)
      )
    }
  }
  return new FFIConverter()
})()

/**
 * The discriminator an operation was persisted under, as it was written.
 *
 * Returned by [`AnyOperation::raw_kind`]. This is the record's own account
 * of itself, kept readable so that a build which cannot interpret an
 * operation can still say what it could not interpret, and so that a build
 * which can is not left unable to report the schema it read.
 *
 * These fields are diagnostics: log them, show them in a bug report, put
 * them behind a "details" disclosure. Do not branch on them; use
 * [`OperationKind`], [`OperationSupport`] and
 * [`AnyOperation::supported_kind`] for control flow instead.
 */
export type RawOperationKind = {
  /**
   * The operation-kind tag as persisted, verbatim and unnormalised.
   *
   * For an operation this SDK wrote, the tag it wrote for it; for a
   * record written by another build or another module set, whatever that
   * build recorded. Always present: a record with no readable
   * discriminator at all is not a record this crate can produce a handle
   * for.
   */
  kind: string
  /**
   * The federation module the record belongs to, when the record names
   * one separately from [`kind`](RawOperationKind::kind), for example
   * `"mint"`, `"ln"`, `"wallet"`, or a module this build has never heard
   * of.
   *
   * `None` when the persisted form carries no separate module marker,
   * which is not a failure to read one: some records simply do not have
   * it.
   */
  module?: string
  /**
   * The schema version the record was written with, when one was
   * recorded.
   *
   * `None` when the record predates versioning or does not carry a
   * version; that is not the same as this build knowing the version is
   * safe to read. Use [`AnyOperation::support`] for the verdict rather
   * than comparing this value directly.
   */
  schemaVersion?: number
}

/**
 * Generated factory for {@link RawOperationKind} record objects.
 */
export const RawOperationKind = (() => {
  const defaults = () => ({})
  const create = (() => {
    return uniffiCreateRecord<RawOperationKind, ReturnType<typeof defaults>>(
      defaults,
    )
  })()
  return Object.freeze({
    create,
    new: create,
    defaults: () => Object.freeze(defaults()) as Partial<RawOperationKind>,
  })
})()

const FfiConverterTypeRawOperationKind = (() => {
  type TypeName = RawOperationKind
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      return {
        kind: FfiConverterString.readFromCursor(c),
        module: FfiConverterOptionalString.readFromCursor(c),
        schemaVersion: FfiConverterOptionalUInt32.readFromCursor(c),
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      FfiConverterString.writeIntoCursor(value.kind, c)
      FfiConverterOptionalString.writeIntoCursor(value.module, c)
      FfiConverterOptionalUInt32.writeIntoCursor(value.schemaVersion, c)
    }
    allocationSize(value: TypeName): number {
      return (
        FfiConverterString.allocationSize(value.kind) +
        FfiConverterOptionalString.allocationSize(value.module) +
        FfiConverterOptionalUInt32.allocationSize(value.schemaVersion)
      )
    }
  }
  return new FFIConverter()
})()

/**
 * One independent subscription to a federation's balance.
 *
 * Obtained from [`Federation::balance_updates`]. Not `Clone`, for the same
 * reason [`OperationUpdates`](crate::OperationUpdates) is not: it is a
 * single cursor, and a second consumer should have a second subscription.
 * Dropping it stops only this subscription.
 */
export interface BalanceUpdatesLike {
  /**
   * See [`BalanceUpdates::next`].
   */
  next(asyncOpts_?: { signal: AbortSignal }) /*throws*/ : Promise<Amount>
}
/**
 * @deprecated Use `BalanceUpdatesLike` instead.
 */
export type BalanceUpdatesInterface = BalanceUpdatesLike

/**
 * One independent subscription to a federation's balance.
 *
 * Obtained from [`Federation::balance_updates`]. Not `Clone`, for the same
 * reason [`OperationUpdates`](crate::OperationUpdates) is not: it is a
 * single cursor, and a second consumer should have a second subscription.
 * Dropping it stops only this subscription.
 */
export class BalanceUpdates
  extends UniffiAbstractObject
  implements BalanceUpdatesLike
{
  readonly [uniffiTypeNameSymbol] = 'BalanceUpdates'
  readonly [destructorGuardSymbol]: UniffiGcObject
  readonly [pointerLiteralSymbol]: UniffiHandle
  // No primary constructor declared for this class.
  private constructor(pointer: UniffiHandle) {
    super()
    this[pointerLiteralSymbol] = pointer
    this[destructorGuardSymbol] =
      uniffiTypeBalanceUpdatesObjectFactory.bless(pointer)
  }

  /**
   * See [`BalanceUpdates::next`].
   */
  async next(asyncOpts_?: { signal: AbortSignal }): Promise<Amount> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_balanceupdates_next(
          uniffiTypeBalanceUpdatesObjectFactory.clonePointer(this),
        )
      },
      /*pollFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_poll_u64,
      /*cancelFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_cancel_u64,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_u64,
      /*freeFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_free_u64,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      /*liftFunc:*/ FfiConverterTypeAmount.lift.bind(FfiConverterTypeAmount),
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  uniffiDestroy(): void {
    const ptr = (this as any)[destructorGuardSymbol]
    if (ptr !== undefined) {
      const pointer = uniffiTypeBalanceUpdatesObjectFactory.pointer(this)
      uniffiTypeBalanceUpdatesObjectFactory.freePointer(pointer)
      uniffiTypeBalanceUpdatesObjectFactory.unbless(ptr)
      delete (this as any)[destructorGuardSymbol]
    }
  }

  static instanceOf(obj_: any): obj_ is BalanceUpdates {
    return uniffiTypeBalanceUpdatesObjectFactory.isConcreteType(obj_)
  }
}

const uniffiTypeBalanceUpdatesObjectFactory: UniffiObjectFactory<BalanceUpdatesLike> =
  (() => {
    /// <reference lib="es2021" />
    const registry =
      typeof FinalizationRegistry !== 'undefined'
        ? new FinalizationRegistry<UniffiHandle>((heldValue: UniffiHandle) => {
            uniffiTypeBalanceUpdatesObjectFactory.freePointer(heldValue)
          })
        : null

    return {
      create(pointer: UniffiHandle): BalanceUpdatesLike {
        const instance = Object.create(BalanceUpdates.prototype)
        instance[pointerLiteralSymbol] = pointer
        instance[destructorGuardSymbol] = this.bless(pointer)
        instance[uniffiTypeNameSymbol] = 'BalanceUpdates'
        return instance
      },

      bless(p: UniffiHandle): UniffiGcObject {
        const ptr = {
          p, // make sure this object doesn't get optimized away.
          markDestroyed: () => undefined,
        }
        if (registry) {
          registry.register(ptr, p, ptr)
        }
        return ptr
      },

      unbless(ptr_: UniffiGcObject) {
        if (registry) {
          registry.unregister(ptr_)
        }
      },

      pointer(obj_: BalanceUpdatesLike): UniffiHandle {
        if ((obj_ as any)[destructorGuardSymbol] === undefined) {
          throw new UniffiInternalError.UnexpectedNullPointer()
        }
        return (obj_ as any)[pointerLiteralSymbol]
      },

      clonePointer(obj_: BalanceUpdatesLike): UniffiHandle {
        const pointer = this.pointer(obj_)
        return uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_clone_balanceupdates(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      freePointer(pointer: UniffiHandle): void {
        uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_free_balanceupdates(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      isConcreteType(obj_: any): obj_ is BalanceUpdatesLike {
        return (
          obj_[destructorGuardSymbol] &&
          obj_[uniffiTypeNameSymbol] === 'BalanceUpdates'
        )
      },
    }
  })()
const FfiConverterTypeBalanceUpdates = new FfiConverterObject(
  uniffiTypeBalanceUpdatesObjectFactory,
)

/**
 * A frozen, executable plan for one out-of-band ecash send.
 *
 * Produced by [`Ecash::quote`] and consumed by [`Ecash::send`]. As with
 * [`LnQuote`](crate::LnQuote) and [`OnchainQuote`](crate::OnchainQuote),
 * the accessors expose exactly what a user must approve: display these
 * numbers, then give the quote back.
 *
 * The requested amount and the actual note value can differ, and this is
 * the ordinary case rather than an edge case: a mint issues notes in fixed
 * denominations (mintv2 rounds up to a multiple of 512 msat), so a request
 * is satisfied with notes worth at least as much, never less. Show
 * [`total`](EcashQuote::total) before the user agrees, because that is the
 * number their balance moves by.
 *
 * The resolved note value is quoted once here and appears nowhere in the
 * send's progress stream, so this executed quote is what
 * [`EcashSendDetails`] copies its terms from, for the whole life of the
 * operation and after a restart.
 */
export interface EcashQuoteLike {
  /**
   * When this quote stops being executable.
   *
   * Past this point [`Ecash::send`] fails with
   * [`QuoteExpired`](crate::ErrorCode::QuoteExpired). A quote can also
   * stop being executable before this point, if notes it planned to
   * spend are spent by another operation in the meantime; that is
   * reported as [`QuoteChanged`](crate::ErrorCode::QuoteChanged). The
   * remedy for both is the same: quote again and re-confirm.
   */
  expiresAt(): Timestamp
  /**
   * The federation this quote was created for.
   */
  federationId(): FederationId
  /**
   * What issuing and selecting those notes will cost, on top of
   * [`EcashQuote::notes_value`].
   *
   * Always zero today: [`Ecash::quote`] refuses with
   * [`NotSupported`](crate::ErrorCode::NotSupported) rather than freeze a
   * quote that would need the wallet to reissue itself change to
   * assemble the value, since [`Ecash::send`] has no way to perform that
   * reissue yet. The field stays, rather than being removed, because a
   * future build that can perform that reissue will report its real cost
   * here without changing this type's shape.
   */
  fee(): Amount
  /**
   * The value the notes will actually carry, what the receiver can
   * redeem.
   *
   * At or above [`EcashQuote::requested_amount`], never below it. This is
   * the figure activity history reports as an ecash send's
   * [`amount`](crate::ActivityItem::amount).
   */
  notesValue(): Amount
  /**
   * The amount [`Ecash::quote`] was asked for.
   *
   * Kept so that a confirmation screen or a receipt can show what was
   * requested next to what will actually be issued. It is a floor, and it
   * is not the figure the balance moves by; see [`EcashQuote::total`].
   */
  requestedAmount(): Amount
  /**
   * The total amount that will be debited from the balance:
   * [`EcashQuote::notes_value`] plus [`EcashQuote::fee`].
   *
   * This is the number to show as "you will pay".
   */
  total(): Amount
}
/**
 * @deprecated Use `EcashQuoteLike` instead.
 */
export type EcashQuoteInterface = EcashQuoteLike

/**
 * A frozen, executable plan for one out-of-band ecash send.
 *
 * Produced by [`Ecash::quote`] and consumed by [`Ecash::send`]. As with
 * [`LnQuote`](crate::LnQuote) and [`OnchainQuote`](crate::OnchainQuote),
 * the accessors expose exactly what a user must approve: display these
 * numbers, then give the quote back.
 *
 * The requested amount and the actual note value can differ, and this is
 * the ordinary case rather than an edge case: a mint issues notes in fixed
 * denominations (mintv2 rounds up to a multiple of 512 msat), so a request
 * is satisfied with notes worth at least as much, never less. Show
 * [`total`](EcashQuote::total) before the user agrees, because that is the
 * number their balance moves by.
 *
 * The resolved note value is quoted once here and appears nowhere in the
 * send's progress stream, so this executed quote is what
 * [`EcashSendDetails`] copies its terms from, for the whole life of the
 * operation and after a restart.
 */
export class EcashQuote extends UniffiAbstractObject implements EcashQuoteLike {
  readonly [uniffiTypeNameSymbol] = 'EcashQuote'
  readonly [destructorGuardSymbol]: UniffiGcObject
  readonly [pointerLiteralSymbol]: UniffiHandle
  // No primary constructor declared for this class.
  private constructor(pointer: UniffiHandle) {
    super()
    this[pointerLiteralSymbol] = pointer
    this[destructorGuardSymbol] =
      uniffiTypeEcashQuoteObjectFactory.bless(pointer)
  }

  /**
   * When this quote stops being executable.
   *
   * Past this point [`Ecash::send`] fails with
   * [`QuoteExpired`](crate::ErrorCode::QuoteExpired). A quote can also
   * stop being executable before this point, if notes it planned to
   * spend are spent by another operation in the meantime; that is
   * reported as [`QuoteChanged`](crate::ErrorCode::QuoteChanged). The
   * remedy for both is the same: quote again and re-confirm.
   */
  expiresAt(): Timestamp {
    return FfiConverterTypeTimestamp.lift(
      uniffiCaller.rustCall(
        /*caller:*/ (callStatus) => {
          return nativeModule().uniffi_fedimint_sdk_fn_method_ecashquote_expires_at(
            uniffiTypeEcashQuoteObjectFactory.clonePointer(this),
            callStatus,
          )
        },
        /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      ),
    )
  }

  /**
   * The federation this quote was created for.
   */
  federationId(): FederationId {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_ecashquote_federation_id(
          uniffiTypeEcashQuoteObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterTypeFederationId.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * What issuing and selecting those notes will cost, on top of
   * [`EcashQuote::notes_value`].
   *
   * Always zero today: [`Ecash::quote`] refuses with
   * [`NotSupported`](crate::ErrorCode::NotSupported) rather than freeze a
   * quote that would need the wallet to reissue itself change to
   * assemble the value, since [`Ecash::send`] has no way to perform that
   * reissue yet. The field stays, rather than being removed, because a
   * future build that can perform that reissue will report its real cost
   * here without changing this type's shape.
   */
  fee(): Amount {
    return FfiConverterTypeAmount.lift(
      uniffiCaller.rustCall(
        /*caller:*/ (callStatus) => {
          return nativeModule().uniffi_fedimint_sdk_fn_method_ecashquote_fee(
            uniffiTypeEcashQuoteObjectFactory.clonePointer(this),
            callStatus,
          )
        },
        /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      ),
    )
  }

  /**
   * The value the notes will actually carry, what the receiver can
   * redeem.
   *
   * At or above [`EcashQuote::requested_amount`], never below it. This is
   * the figure activity history reports as an ecash send's
   * [`amount`](crate::ActivityItem::amount).
   */
  notesValue(): Amount {
    return FfiConverterTypeAmount.lift(
      uniffiCaller.rustCall(
        /*caller:*/ (callStatus) => {
          return nativeModule().uniffi_fedimint_sdk_fn_method_ecashquote_notes_value(
            uniffiTypeEcashQuoteObjectFactory.clonePointer(this),
            callStatus,
          )
        },
        /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      ),
    )
  }

  /**
   * The amount [`Ecash::quote`] was asked for.
   *
   * Kept so that a confirmation screen or a receipt can show what was
   * requested next to what will actually be issued. It is a floor, and it
   * is not the figure the balance moves by; see [`EcashQuote::total`].
   */
  requestedAmount(): Amount {
    return FfiConverterTypeAmount.lift(
      uniffiCaller.rustCall(
        /*caller:*/ (callStatus) => {
          return nativeModule().uniffi_fedimint_sdk_fn_method_ecashquote_requested_amount(
            uniffiTypeEcashQuoteObjectFactory.clonePointer(this),
            callStatus,
          )
        },
        /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      ),
    )
  }

  /**
   * The total amount that will be debited from the balance:
   * [`EcashQuote::notes_value`] plus [`EcashQuote::fee`].
   *
   * This is the number to show as "you will pay".
   */
  total(): Amount {
    return FfiConverterTypeAmount.lift(
      uniffiCaller.rustCall(
        /*caller:*/ (callStatus) => {
          return nativeModule().uniffi_fedimint_sdk_fn_method_ecashquote_total(
            uniffiTypeEcashQuoteObjectFactory.clonePointer(this),
            callStatus,
          )
        },
        /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      ),
    )
  }

  uniffiDestroy(): void {
    const ptr = (this as any)[destructorGuardSymbol]
    if (ptr !== undefined) {
      const pointer = uniffiTypeEcashQuoteObjectFactory.pointer(this)
      uniffiTypeEcashQuoteObjectFactory.freePointer(pointer)
      uniffiTypeEcashQuoteObjectFactory.unbless(ptr)
      delete (this as any)[destructorGuardSymbol]
    }
  }

  static instanceOf(obj_: any): obj_ is EcashQuote {
    return uniffiTypeEcashQuoteObjectFactory.isConcreteType(obj_)
  }
}

const uniffiTypeEcashQuoteObjectFactory: UniffiObjectFactory<EcashQuoteLike> =
  (() => {
    /// <reference lib="es2021" />
    const registry =
      typeof FinalizationRegistry !== 'undefined'
        ? new FinalizationRegistry<UniffiHandle>((heldValue: UniffiHandle) => {
            uniffiTypeEcashQuoteObjectFactory.freePointer(heldValue)
          })
        : null

    return {
      create(pointer: UniffiHandle): EcashQuoteLike {
        const instance = Object.create(EcashQuote.prototype)
        instance[pointerLiteralSymbol] = pointer
        instance[destructorGuardSymbol] = this.bless(pointer)
        instance[uniffiTypeNameSymbol] = 'EcashQuote'
        return instance
      },

      bless(p: UniffiHandle): UniffiGcObject {
        const ptr = {
          p, // make sure this object doesn't get optimized away.
          markDestroyed: () => undefined,
        }
        if (registry) {
          registry.register(ptr, p, ptr)
        }
        return ptr
      },

      unbless(ptr_: UniffiGcObject) {
        if (registry) {
          registry.unregister(ptr_)
        }
      },

      pointer(obj_: EcashQuoteLike): UniffiHandle {
        if ((obj_ as any)[destructorGuardSymbol] === undefined) {
          throw new UniffiInternalError.UnexpectedNullPointer()
        }
        return (obj_ as any)[pointerLiteralSymbol]
      },

      clonePointer(obj_: EcashQuoteLike): UniffiHandle {
        const pointer = this.pointer(obj_)
        return uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_clone_ecashquote(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      freePointer(pointer: UniffiHandle): void {
        uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_free_ecashquote(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      isConcreteType(obj_: any): obj_ is EcashQuoteLike {
        return (
          obj_[destructorGuardSymbol] &&
          obj_[uniffiTypeNameSymbol] === 'EcashQuote'
        )
      },
    }
  })()
const FfiConverterTypeEcashQuote = new FfiConverterObject(
  uniffiTypeEcashQuoteObjectFactory,
)

// Enum: EcashReceiveState
export enum EcashReceiveState_Tags {
  Created = 'Created',
  Issuing = 'Issuing',
  Done = 'Done',
  Failed = 'Failed',
}
/**
 * The lifecycle of redeeming out-of-band ecash notes.
 *
 * Maps one-to-one onto upstream `fedimint-mint-client`'s
 * `ReissueExternalNotesState` (`Created`, `Issuing`, `Done`,
 * `Failed(String)`); the only change is carrying the failure reason as a
 * named field rather than a positional tuple.
 */

type EcashReceiveState_Created_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'EcashReceiveState'
  tag: EcashReceiveState_Tags.Created
}

type EcashReceiveState_Issuing_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'EcashReceiveState'
  tag: EcashReceiveState_Tags.Issuing
}

type EcashReceiveState_Done_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'EcashReceiveState'
  tag: EcashReceiveState_Tags.Done
}

type EcashReceiveState_Failed_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'EcashReceiveState'
  tag: EcashReceiveState_Tags.Failed
  inner: Readonly<{ reason: string }>
}
export type EcashReceiveState =
  | EcashReceiveState_Created_interface
  | EcashReceiveState_Issuing_interface
  | EcashReceiveState_Done_interface
  | EcashReceiveState_Failed_interface

export const EcashReceiveState = (() => {
  /**
   * The redemption has been accepted locally and is about to be
   * submitted to the federation.
   */
  class Created_
    extends UniffiEnum
    implements EcashReceiveState_Created_interface
  {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'EcashReceiveState'
    readonly tag = EcashReceiveState_Tags.Created
    constructor() {
      super('EcashReceiveState', 'Created')
    }

    static new(): Created_ {
      return new Created_()
    }

    static instanceOf(obj: any): obj is Created_ {
      return obj.tag === EcashReceiveState_Tags.Created
    }
  }
  /**
   * The federation is reissuing the notes to this client.
   */
  class Issuing_
    extends UniffiEnum
    implements EcashReceiveState_Issuing_interface
  {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'EcashReceiveState'
    readonly tag = EcashReceiveState_Tags.Issuing
    constructor() {
      super('EcashReceiveState', 'Issuing')
    }

    static new(): Issuing_ {
      return new Issuing_()
    }

    static instanceOf(obj: any): obj is Issuing_ {
      return obj.tag === EcashReceiveState_Tags.Issuing
    }
  }
  /**
   * Final: the notes were reissued and their value is spendable.
   */
  class Done_ extends UniffiEnum implements EcashReceiveState_Done_interface {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'EcashReceiveState'
    readonly tag = EcashReceiveState_Tags.Done
    constructor() {
      super('EcashReceiveState', 'Done')
    }

    static new(): Done_ {
      return new Done_()
    }

    static instanceOf(obj: any): obj is Done_ {
      return obj.tag === EcashReceiveState_Tags.Done
    }
  }
  /**
   * Final: the notes could not be redeemed, most often because they
   * were already spent or had been reclaimed by the sender.
   */
  class Failed_
    extends UniffiEnum
    implements EcashReceiveState_Failed_interface
  {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'EcashReceiveState'
    readonly tag = EcashReceiveState_Tags.Failed
    readonly inner: Readonly<{ reason: string }>
    constructor(inner: { reason: string }) {
      super('EcashReceiveState', 'Failed')

      this.inner = Object.freeze(inner)
    }
    static new(inner: { reason: string }): Failed_ {
      return new Failed_(inner)
    }

    static instanceOf(obj: any): obj is Failed_ {
      return obj.tag === EcashReceiveState_Tags.Failed
    }
  }

  function instanceOf(obj: any): obj is EcashReceiveState {
    return obj[uniffiTypeNameSymbol] === 'EcashReceiveState'
  }

  return Object.freeze({
    instanceOf,
    Created: Created_,
    Issuing: Issuing_,
    Done: Done_,
    Failed: Failed_,
  })
})()

// FfiConverter for enum EcashReceiveState
const FfiConverterTypeEcashReceiveState = (() => {
  type TypeName = EcashReceiveState
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      switch (c.readI32()) {
        case 1:
          return new EcashReceiveState.Created()
        case 2:
          return new EcashReceiveState.Issuing()
        case 3:
          return new EcashReceiveState.Done()
        case 4:
          return new EcashReceiveState.Failed({
            reason: FfiConverterString.readFromCursor(c),
          })
        default:
          throw new UniffiInternalError.UnexpectedEnumCase()
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      switch (value.tag) {
        case EcashReceiveState_Tags.Created: {
          c.writeI32(1)
          return
        }
        case EcashReceiveState_Tags.Issuing: {
          c.writeI32(2)
          return
        }
        case EcashReceiveState_Tags.Done: {
          c.writeI32(3)
          return
        }
        case EcashReceiveState_Tags.Failed: {
          c.writeI32(4)
          const inner = value.inner
          FfiConverterString.writeIntoCursor(inner.reason, c)
          return
        }
        default:
          // Throwing from here means that EcashReceiveState_Tags hasn't matched an ordinal.
          throw new UniffiInternalError.UnexpectedEnumCase()
      }
    }
    allocationSize(value: TypeName): number {
      switch (value.tag) {
        case EcashReceiveState_Tags.Created: {
          return 4
        }
        case EcashReceiveState_Tags.Issuing: {
          return 4
        }
        case EcashReceiveState_Tags.Done: {
          return 4
        }
        case EcashReceiveState_Tags.Failed: {
          const inner = value.inner
          let size = 4
          size += FfiConverterString.allocationSize(inner.reason)
          return size
        }
        default:
          throw new UniffiInternalError.UnexpectedEnumCase()
      }
    }
  }
  return new FFIConverter()
})()

/**
 * The UniFFI view of one `OperationUpdates<S>` instantiation, behind a lock so `next`'s
 * `&mut self` can cross as `&self`.
 */
export interface EcashReceiveOperationUpdatesLike {
  /**
   * See [`OperationUpdates::next`](crate::OperationUpdates::next).
   */
  next(asyncOpts_?: {
    signal: AbortSignal
  }) /*throws*/ : Promise<EcashReceiveState | undefined>
}
/**
 * @deprecated Use `EcashReceiveOperationUpdatesLike` instead.
 */
export type EcashReceiveOperationUpdatesInterface =
  EcashReceiveOperationUpdatesLike

/**
 * The UniFFI view of one `OperationUpdates<S>` instantiation, behind a lock so `next`'s
 * `&mut self` can cross as `&self`.
 */
export class EcashReceiveOperationUpdates
  extends UniffiAbstractObject
  implements EcashReceiveOperationUpdatesLike
{
  readonly [uniffiTypeNameSymbol] = 'EcashReceiveOperationUpdates'
  readonly [destructorGuardSymbol]: UniffiGcObject
  readonly [pointerLiteralSymbol]: UniffiHandle
  // No primary constructor declared for this class.
  private constructor(pointer: UniffiHandle) {
    super()
    this[pointerLiteralSymbol] = pointer
    this[destructorGuardSymbol] =
      uniffiTypeEcashReceiveOperationUpdatesObjectFactory.bless(pointer)
  }

  /**
   * See [`OperationUpdates::next`](crate::OperationUpdates::next).
   */
  async next(asyncOpts_?: {
    signal: AbortSignal
  }): Promise<EcashReceiveState | undefined> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_ecashreceiveoperationupdates_next(
          uniffiTypeEcashReceiveOperationUpdatesObjectFactory.clonePointer(
            this,
          ),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterOptionalTypeEcashReceiveState.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  uniffiDestroy(): void {
    const ptr = (this as any)[destructorGuardSymbol]
    if (ptr !== undefined) {
      const pointer =
        uniffiTypeEcashReceiveOperationUpdatesObjectFactory.pointer(this)
      uniffiTypeEcashReceiveOperationUpdatesObjectFactory.freePointer(pointer)
      uniffiTypeEcashReceiveOperationUpdatesObjectFactory.unbless(ptr)
      delete (this as any)[destructorGuardSymbol]
    }
  }

  static instanceOf(obj_: any): obj_ is EcashReceiveOperationUpdates {
    return uniffiTypeEcashReceiveOperationUpdatesObjectFactory.isConcreteType(
      obj_,
    )
  }
}

const uniffiTypeEcashReceiveOperationUpdatesObjectFactory: UniffiObjectFactory<EcashReceiveOperationUpdatesLike> =
  (() => {
    /// <reference lib="es2021" />
    const registry =
      typeof FinalizationRegistry !== 'undefined'
        ? new FinalizationRegistry<UniffiHandle>((heldValue: UniffiHandle) => {
            uniffiTypeEcashReceiveOperationUpdatesObjectFactory.freePointer(
              heldValue,
            )
          })
        : null

    return {
      create(pointer: UniffiHandle): EcashReceiveOperationUpdatesLike {
        const instance = Object.create(EcashReceiveOperationUpdates.prototype)
        instance[pointerLiteralSymbol] = pointer
        instance[destructorGuardSymbol] = this.bless(pointer)
        instance[uniffiTypeNameSymbol] = 'EcashReceiveOperationUpdates'
        return instance
      },

      bless(p: UniffiHandle): UniffiGcObject {
        const ptr = {
          p, // make sure this object doesn't get optimized away.
          markDestroyed: () => undefined,
        }
        if (registry) {
          registry.register(ptr, p, ptr)
        }
        return ptr
      },

      unbless(ptr_: UniffiGcObject) {
        if (registry) {
          registry.unregister(ptr_)
        }
      },

      pointer(obj_: EcashReceiveOperationUpdatesLike): UniffiHandle {
        if ((obj_ as any)[destructorGuardSymbol] === undefined) {
          throw new UniffiInternalError.UnexpectedNullPointer()
        }
        return (obj_ as any)[pointerLiteralSymbol]
      },

      clonePointer(obj_: EcashReceiveOperationUpdatesLike): UniffiHandle {
        const pointer = this.pointer(obj_)
        return uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_clone_ecashreceiveoperationupdates(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      freePointer(pointer: UniffiHandle): void {
        uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_free_ecashreceiveoperationupdates(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      isConcreteType(obj_: any): obj_ is EcashReceiveOperationUpdatesLike {
        return (
          obj_[destructorGuardSymbol] &&
          obj_[uniffiTypeNameSymbol] === 'EcashReceiveOperationUpdates'
        )
      },
    }
  })()
const FfiConverterTypeEcashReceiveOperationUpdates = new FfiConverterObject(
  uniffiTypeEcashReceiveOperationUpdatesObjectFactory,
)

/**
 * The UniFFI view of one `Operation<S>` instantiation: an opaque object wrapping the real
 * handle, forwarding every method.
 */
export interface EcashReceiveOperationLike {
  /**
   * See [`Operation::await_final`](crate::Operation::await_final).
   */
  awaitFinal(asyncOpts_?: {
    signal: AbortSignal
  }) /*throws*/ : Promise<EcashReceiveState>
  /**
   * See [`Operation::details`](crate::Operation::details).
   */
  details(asyncOpts_?: {
    signal: AbortSignal
  }) /*throws*/ : Promise<EcashReceiveDetails>
  /**
   * See [`Operation::id`](crate::Operation::id).
   */
  id(): OperationId
  /**
   * See [`Operation::state`](crate::Operation::state).
   */
  state(asyncOpts_?: {
    signal: AbortSignal
  }) /*throws*/ : Promise<EcashReceiveState>
  /**
   * See [`Operation::updates`](crate::Operation::updates).
   */
  updates(): EcashReceiveOperationUpdatesLike
}
/**
 * @deprecated Use `EcashReceiveOperationLike` instead.
 */
export type EcashReceiveOperationInterface = EcashReceiveOperationLike

/**
 * The UniFFI view of one `Operation<S>` instantiation: an opaque object wrapping the real
 * handle, forwarding every method.
 */
export class EcashReceiveOperation
  extends UniffiAbstractObject
  implements EcashReceiveOperationLike
{
  readonly [uniffiTypeNameSymbol] = 'EcashReceiveOperation'
  readonly [destructorGuardSymbol]: UniffiGcObject
  readonly [pointerLiteralSymbol]: UniffiHandle
  // No primary constructor declared for this class.
  private constructor(pointer: UniffiHandle) {
    super()
    this[pointerLiteralSymbol] = pointer
    this[destructorGuardSymbol] =
      uniffiTypeEcashReceiveOperationObjectFactory.bless(pointer)
  }

  /**
   * See [`Operation::await_final`](crate::Operation::await_final).
   */
  async awaitFinal(asyncOpts_?: {
    signal: AbortSignal
  }): Promise<EcashReceiveState> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_ecashreceiveoperation_await_final(
          uniffiTypeEcashReceiveOperationObjectFactory.clonePointer(this),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterTypeEcashReceiveState.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * See [`Operation::details`](crate::Operation::details).
   */
  async details(asyncOpts_?: {
    signal: AbortSignal
  }): Promise<EcashReceiveDetails> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_ecashreceiveoperation_details(
          uniffiTypeEcashReceiveOperationObjectFactory.clonePointer(this),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterTypeEcashReceiveDetails.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * See [`Operation::id`](crate::Operation::id).
   */
  id(): OperationId {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_ecashreceiveoperation_id(
          uniffiTypeEcashReceiveOperationObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterTypeOperationId.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * See [`Operation::state`](crate::Operation::state).
   */
  async state(asyncOpts_?: {
    signal: AbortSignal
  }): Promise<EcashReceiveState> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_ecashreceiveoperation_state(
          uniffiTypeEcashReceiveOperationObjectFactory.clonePointer(this),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterTypeEcashReceiveState.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * See [`Operation::updates`](crate::Operation::updates).
   */
  updates(): EcashReceiveOperationUpdatesLike {
    return FfiConverterTypeEcashReceiveOperationUpdates.lift(
      uniffiCaller.rustCall(
        /*caller:*/ (callStatus) => {
          return nativeModule().uniffi_fedimint_sdk_fn_method_ecashreceiveoperation_updates(
            uniffiTypeEcashReceiveOperationObjectFactory.clonePointer(this),
            callStatus,
          )
        },
        /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      ),
    )
  }

  uniffiDestroy(): void {
    const ptr = (this as any)[destructorGuardSymbol]
    if (ptr !== undefined) {
      const pointer = uniffiTypeEcashReceiveOperationObjectFactory.pointer(this)
      uniffiTypeEcashReceiveOperationObjectFactory.freePointer(pointer)
      uniffiTypeEcashReceiveOperationObjectFactory.unbless(ptr)
      delete (this as any)[destructorGuardSymbol]
    }
  }

  static instanceOf(obj_: any): obj_ is EcashReceiveOperation {
    return uniffiTypeEcashReceiveOperationObjectFactory.isConcreteType(obj_)
  }
}

const uniffiTypeEcashReceiveOperationObjectFactory: UniffiObjectFactory<EcashReceiveOperationLike> =
  (() => {
    /// <reference lib="es2021" />
    const registry =
      typeof FinalizationRegistry !== 'undefined'
        ? new FinalizationRegistry<UniffiHandle>((heldValue: UniffiHandle) => {
            uniffiTypeEcashReceiveOperationObjectFactory.freePointer(heldValue)
          })
        : null

    return {
      create(pointer: UniffiHandle): EcashReceiveOperationLike {
        const instance = Object.create(EcashReceiveOperation.prototype)
        instance[pointerLiteralSymbol] = pointer
        instance[destructorGuardSymbol] = this.bless(pointer)
        instance[uniffiTypeNameSymbol] = 'EcashReceiveOperation'
        return instance
      },

      bless(p: UniffiHandle): UniffiGcObject {
        const ptr = {
          p, // make sure this object doesn't get optimized away.
          markDestroyed: () => undefined,
        }
        if (registry) {
          registry.register(ptr, p, ptr)
        }
        return ptr
      },

      unbless(ptr_: UniffiGcObject) {
        if (registry) {
          registry.unregister(ptr_)
        }
      },

      pointer(obj_: EcashReceiveOperationLike): UniffiHandle {
        if ((obj_ as any)[destructorGuardSymbol] === undefined) {
          throw new UniffiInternalError.UnexpectedNullPointer()
        }
        return (obj_ as any)[pointerLiteralSymbol]
      },

      clonePointer(obj_: EcashReceiveOperationLike): UniffiHandle {
        const pointer = this.pointer(obj_)
        return uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_clone_ecashreceiveoperation(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      freePointer(pointer: UniffiHandle): void {
        uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_free_ecashreceiveoperation(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      isConcreteType(obj_: any): obj_ is EcashReceiveOperationLike {
        return (
          obj_[destructorGuardSymbol] &&
          obj_[uniffiTypeNameSymbol] === 'EcashReceiveOperation'
        )
      },
    }
  })()
const FfiConverterTypeEcashReceiveOperation = new FfiConverterObject(
  uniffiTypeEcashReceiveOperationObjectFactory,
)

/**
 * The ecash facade for one federation.
 *
 * Obtained from [`Federation::ecash`](crate::Federation::ecash), which
 * returns `None` when the federation has no mint module.
 *
 * Ecash here means *out-of-band* ecash: notes the sender takes out of
 * their balance and hands to a receiver over some channel the federation
 * knows nothing about, a chat message, a QR code, a file. The receiver
 * redeems them against the same federation. Ordinary in-federation
 * spending is not a separate concept; it is what lightning and on-chain
 * operations do with the balance.
 *
 * [`Ecash::quote`] plans a send and [`Ecash::send`] executes that plan,
 * exactly as [`Lightning::quote`](crate::Lightning::quote) and
 * [`Onchain::quote`](crate::Onchain::quote) do for their kinds of value.
 * Receiving is not quoted, because it presents the caller with no
 * decision: see [`Ecash::receive`].
 *
 * Every call on this facade, sending and receiving alike, is refused with
 * [`Recovering`](crate::ErrorCode::Recovering) while a recovery for the
 * federation is incomplete. A wallet whose note set was never fully
 * discovered is not safe to spend from, since a note the rescan never
 * reached can be double-spent.
 */
export interface EcashLike {
  /**
   * Plans an out-of-band send and returns an executable quote for it.
   *
   * The value that leaves the balance is generally *more* than `amount`:
   * a mint issues notes in fixed denominations, so the receiver ends up
   * with the smallest value the mint can represent at or above `amount`,
   * and assembling that value can itself cost a fee. The returned
   * [`EcashQuote`] is that plan, frozen: it binds the requested amount,
   * the note value that will actually be produced, the fee and the total
   * debit. Show it, then hand it back to [`Ecash::send`], which executes
   * exactly what was shown.
   *
   * `amount` is a floor rather than a promise, the least the receiver
   * must be able to redeem. [`EcashQuote::notes_value`] is what they will
   * actually be able to redeem, and it is the number to put in front of a
   * user beside [`EcashQuote::fee`] and [`EcashQuote::total`].
   *
   * Quoting neither debits the balance nor records anything: it plans.
   * Quotes expire; see [`EcashQuote::expires_at`].
   *
   * # Errors
   *
   * [`InvalidInput`](crate::ErrorCode::InvalidInput) for a zero amount,
   * which no note can carry,
   * [`InsufficientBalance`](crate::ErrorCode::InsufficientBalance) when
   * the balance cannot cover the rounded-up note value plus the fee,
   * which can happen for an `amount` the balance would have covered
   * exactly, and is itself a reason for this call to exist,
   * [`NotSupported`](crate::ErrorCode::NotSupported) if assembling
   * `amount` would require the wallet to reissue itself change: this
   * build only ever hands out notes it already holds in the exact
   * denominations requested, never mints new ones to make change, so a
   * wallet that cannot represent `amount` from its current notes cannot
   * send it at all yet (see [`Ecash::send`]'s errors for why), or if the
   * mint module disappeared from the federation's configuration after
   * this facade was obtained,
   * [`Recovering`](crate::ErrorCode::Recovering) while a recovery for
   * this federation is incomplete,
   * [`FederationUnreachable`](crate::ErrorCode::FederationUnreachable),
   * [`Timeout`](crate::ErrorCode::Timeout), and
   * [`FederationClosed`](crate::ErrorCode::FederationClosed).
   */
  quote(
    amount: Amount,
    asyncOpts_?: { signal: AbortSignal },
  ) /*throws*/ : Promise<EcashQuoteLike>
  /**
   * See [`Ecash::receive`].
   */
  receive(
    notes: NotesLike,
    asyncOpts_?: { signal: AbortSignal },
  ) /*throws*/ : Promise<EcashReceiveOperationLike>
  /**
   * See [`Ecash::send`]. Fails with
   * [`ErrorCode::QuoteExpired`](crate::ErrorCode::QuoteExpired) if `quote` was already sent.
   */
  send(
    quote: EcashQuoteLike,
    asyncOpts_?: { signal: AbortSignal },
  ) /*throws*/ : Promise<EcashSendHandle>
}
/**
 * @deprecated Use `EcashLike` instead.
 */
export type EcashInterface = EcashLike

/**
 * The ecash facade for one federation.
 *
 * Obtained from [`Federation::ecash`](crate::Federation::ecash), which
 * returns `None` when the federation has no mint module.
 *
 * Ecash here means *out-of-band* ecash: notes the sender takes out of
 * their balance and hands to a receiver over some channel the federation
 * knows nothing about, a chat message, a QR code, a file. The receiver
 * redeems them against the same federation. Ordinary in-federation
 * spending is not a separate concept; it is what lightning and on-chain
 * operations do with the balance.
 *
 * [`Ecash::quote`] plans a send and [`Ecash::send`] executes that plan,
 * exactly as [`Lightning::quote`](crate::Lightning::quote) and
 * [`Onchain::quote`](crate::Onchain::quote) do for their kinds of value.
 * Receiving is not quoted, because it presents the caller with no
 * decision: see [`Ecash::receive`].
 *
 * Every call on this facade, sending and receiving alike, is refused with
 * [`Recovering`](crate::ErrorCode::Recovering) while a recovery for the
 * federation is incomplete. A wallet whose note set was never fully
 * discovered is not safe to spend from, since a note the rescan never
 * reached can be double-spent.
 */
export class Ecash extends UniffiAbstractObject implements EcashLike {
  readonly [uniffiTypeNameSymbol] = 'Ecash'
  readonly [destructorGuardSymbol]: UniffiGcObject
  readonly [pointerLiteralSymbol]: UniffiHandle
  // No primary constructor declared for this class.
  private constructor(pointer: UniffiHandle) {
    super()
    this[pointerLiteralSymbol] = pointer
    this[destructorGuardSymbol] = uniffiTypeEcashObjectFactory.bless(pointer)
  }

  /**
   * Plans an out-of-band send and returns an executable quote for it.
   *
   * The value that leaves the balance is generally *more* than `amount`:
   * a mint issues notes in fixed denominations, so the receiver ends up
   * with the smallest value the mint can represent at or above `amount`,
   * and assembling that value can itself cost a fee. The returned
   * [`EcashQuote`] is that plan, frozen: it binds the requested amount,
   * the note value that will actually be produced, the fee and the total
   * debit. Show it, then hand it back to [`Ecash::send`], which executes
   * exactly what was shown.
   *
   * `amount` is a floor rather than a promise, the least the receiver
   * must be able to redeem. [`EcashQuote::notes_value`] is what they will
   * actually be able to redeem, and it is the number to put in front of a
   * user beside [`EcashQuote::fee`] and [`EcashQuote::total`].
   *
   * Quoting neither debits the balance nor records anything: it plans.
   * Quotes expire; see [`EcashQuote::expires_at`].
   *
   * # Errors
   *
   * [`InvalidInput`](crate::ErrorCode::InvalidInput) for a zero amount,
   * which no note can carry,
   * [`InsufficientBalance`](crate::ErrorCode::InsufficientBalance) when
   * the balance cannot cover the rounded-up note value plus the fee,
   * which can happen for an `amount` the balance would have covered
   * exactly, and is itself a reason for this call to exist,
   * [`NotSupported`](crate::ErrorCode::NotSupported) if assembling
   * `amount` would require the wallet to reissue itself change: this
   * build only ever hands out notes it already holds in the exact
   * denominations requested, never mints new ones to make change, so a
   * wallet that cannot represent `amount` from its current notes cannot
   * send it at all yet (see [`Ecash::send`]'s errors for why), or if the
   * mint module disappeared from the federation's configuration after
   * this facade was obtained,
   * [`Recovering`](crate::ErrorCode::Recovering) while a recovery for
   * this federation is incomplete,
   * [`FederationUnreachable`](crate::ErrorCode::FederationUnreachable),
   * [`Timeout`](crate::ErrorCode::Timeout), and
   * [`FederationClosed`](crate::ErrorCode::FederationClosed).
   */
  async quote(
    amount: Amount,
    asyncOpts_?: { signal: AbortSignal },
  ): Promise<EcashQuoteLike> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_ecash_quote(
          uniffiTypeEcashObjectFactory.clonePointer(this),
          FfiConverterTypeAmount.lower(amount, nativeModule().rustbuffer_alloc),
        )
      },
      /*pollFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_poll_u64,
      /*cancelFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_cancel_u64,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_u64,
      /*freeFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_free_u64,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      /*liftFunc:*/ FfiConverterTypeEcashQuote.lift.bind(
        FfiConverterTypeEcashQuote,
      ),
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * See [`Ecash::receive`].
   */
  async receive(
    notes: NotesLike,
    asyncOpts_?: { signal: AbortSignal },
  ): Promise<EcashReceiveOperationLike> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_ecash_receive(
          uniffiTypeEcashObjectFactory.clonePointer(this),
          FfiConverterTypeNotes.lower(notes, nativeModule().rustbuffer_alloc),
        )
      },
      /*pollFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_poll_u64,
      /*cancelFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_cancel_u64,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_u64,
      /*freeFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_free_u64,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      /*liftFunc:*/ FfiConverterTypeEcashReceiveOperation.lift.bind(
        FfiConverterTypeEcashReceiveOperation,
      ),
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * See [`Ecash::send`]. Fails with
   * [`ErrorCode::QuoteExpired`](crate::ErrorCode::QuoteExpired) if `quote` was already sent.
   */
  async send(
    quote: EcashQuoteLike,
    asyncOpts_?: { signal: AbortSignal },
  ): Promise<EcashSendHandle> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_ecash_send(
          uniffiTypeEcashObjectFactory.clonePointer(this),
          FfiConverterTypeEcashQuote.lower(
            quote,
            nativeModule().rustbuffer_alloc,
          ),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterTypeEcashSendHandle.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  uniffiDestroy(): void {
    const ptr = (this as any)[destructorGuardSymbol]
    if (ptr !== undefined) {
      const pointer = uniffiTypeEcashObjectFactory.pointer(this)
      uniffiTypeEcashObjectFactory.freePointer(pointer)
      uniffiTypeEcashObjectFactory.unbless(ptr)
      delete (this as any)[destructorGuardSymbol]
    }
  }

  static instanceOf(obj_: any): obj_ is Ecash {
    return uniffiTypeEcashObjectFactory.isConcreteType(obj_)
  }
}

const uniffiTypeEcashObjectFactory: UniffiObjectFactory<EcashLike> = (() => {
  /// <reference lib="es2021" />
  const registry =
    typeof FinalizationRegistry !== 'undefined'
      ? new FinalizationRegistry<UniffiHandle>((heldValue: UniffiHandle) => {
          uniffiTypeEcashObjectFactory.freePointer(heldValue)
        })
      : null

  return {
    create(pointer: UniffiHandle): EcashLike {
      const instance = Object.create(Ecash.prototype)
      instance[pointerLiteralSymbol] = pointer
      instance[destructorGuardSymbol] = this.bless(pointer)
      instance[uniffiTypeNameSymbol] = 'Ecash'
      return instance
    },

    bless(p: UniffiHandle): UniffiGcObject {
      const ptr = {
        p, // make sure this object doesn't get optimized away.
        markDestroyed: () => undefined,
      }
      if (registry) {
        registry.register(ptr, p, ptr)
      }
      return ptr
    },

    unbless(ptr_: UniffiGcObject) {
      if (registry) {
        registry.unregister(ptr_)
      }
    },

    pointer(obj_: EcashLike): UniffiHandle {
      if ((obj_ as any)[destructorGuardSymbol] === undefined) {
        throw new UniffiInternalError.UnexpectedNullPointer()
      }
      return (obj_ as any)[pointerLiteralSymbol]
    },

    clonePointer(obj_: EcashLike): UniffiHandle {
      const pointer = this.pointer(obj_)
      return uniffiCaller.rustCall(
        /*caller:*/ (callStatus) =>
          nativeModule().uniffi_fedimint_sdk_fn_clone_ecash(
            pointer,
            callStatus,
          ),
        /*liftString:*/ FfiConverterString.lift,
      )
    },

    freePointer(pointer: UniffiHandle): void {
      uniffiCaller.rustCall(
        /*caller:*/ (callStatus) =>
          nativeModule().uniffi_fedimint_sdk_fn_free_ecash(pointer, callStatus),
        /*liftString:*/ FfiConverterString.lift,
      )
    },

    isConcreteType(obj_: any): obj_ is EcashLike {
      return (
        obj_[destructorGuardSymbol] && obj_[uniffiTypeNameSymbol] === 'Ecash'
      )
    },
  }
})()
const FfiConverterTypeEcash = new FfiConverterObject(
  uniffiTypeEcashObjectFactory,
)

/**
 * An invite code for a federation.
 *
 * An invite code carries everything needed to locate and connect to a
 * federation's guardians before anything has been persisted locally. It is
 * opaque: callers pass it to a preview or join call rather than picking it
 * apart, with one deliberate exception:
 * [`federation_id`](InviteCode::federation_id), the key that every
 * per-federation call takes, is readable from the code without a network
 * round trip. It round-trips through [`Display`](core::fmt::Display) and
 * [`FromStr`](core::str::FromStr) with a validating parse, so it can be
 * entered as text, scanned from a QR code, or shared as a link and
 * reconstructed on the other end without any federation-specific parsing
 * logic outside this crate.
 *
 * # `Display` prints the code, `Debug` never does
 *
 * An invite code is not always public: it can embed an `api_secret`, the
 * credential a private federation requires before its guardians will answer
 * at all, so the code is a bearer credential and printing one can hand a
 * reader access to a federation meant to be closed.
 * [`Display`](core::fmt::Display) prints the code and is the deliberate way
 * to render, scan or share it. [`Debug`] is redacted instead, because it is
 * what logging, crash reporters and `assert!` failures reach for, and any
 * struct holding an `InviteCode` would otherwise print it merely by being
 * logged.
 */
export interface InviteCodeLike {
  /**
   * The invite code in its canonical `fed1…` bech32m string, for showing the
   * user, putting in a QR code, or sharing so someone else can join.
   *
   * This is the deliberate way to render the value; see the type-level
   * documentation for why [`Debug`] is not.
   */
  display(): string
  /**
   * The id of the federation this code invites to.
   *
   * Read from the code itself, which encodes it: no network round trip,
   * no join, and the same value [`FederationPreview::id`] reports after
   * one. It is the key every per-federation call on [`Sdk`](crate::Sdk)
   * takes, including [`federation_status`](crate::Sdk::federation_status),
   * [`reopen_federation`](crate::Sdk::reopen_federation),
   * [`recovery_status`](crate::Sdk::recovery_status) and
   * [`resume_recovery`](crate::Sdk::resume_recovery). An application
   * holding only an invite code can therefore check where that federation
   * stands before joining, and find its way back to a federation that a
   * failed seed recovery left committed but not open, when nothing else
   * identifies which stored federation that failed call produced.
   *
   * Not a credential: a federation id is public, unlike the `api_secret`
   * the code as a whole may embed.
   */
  federationId(): FederationId
}
/**
 * @deprecated Use `InviteCodeLike` instead.
 */
export type InviteCodeInterface = InviteCodeLike

/**
 * An invite code for a federation.
 *
 * An invite code carries everything needed to locate and connect to a
 * federation's guardians before anything has been persisted locally. It is
 * opaque: callers pass it to a preview or join call rather than picking it
 * apart, with one deliberate exception:
 * [`federation_id`](InviteCode::federation_id), the key that every
 * per-federation call takes, is readable from the code without a network
 * round trip. It round-trips through [`Display`](core::fmt::Display) and
 * [`FromStr`](core::str::FromStr) with a validating parse, so it can be
 * entered as text, scanned from a QR code, or shared as a link and
 * reconstructed on the other end without any federation-specific parsing
 * logic outside this crate.
 *
 * # `Display` prints the code, `Debug` never does
 *
 * An invite code is not always public: it can embed an `api_secret`, the
 * credential a private federation requires before its guardians will answer
 * at all, so the code is a bearer credential and printing one can hand a
 * reader access to a federation meant to be closed.
 * [`Display`](core::fmt::Display) prints the code and is the deliberate way
 * to render, scan or share it. [`Debug`] is redacted instead, because it is
 * what logging, crash reporters and `assert!` failures reach for, and any
 * struct holding an `InviteCode` would otherwise print it merely by being
 * logged.
 */
export class InviteCode extends UniffiAbstractObject implements InviteCodeLike {
  readonly [uniffiTypeNameSymbol] = 'InviteCode'
  readonly [destructorGuardSymbol]: UniffiGcObject
  readonly [pointerLiteralSymbol]: UniffiHandle
  // No primary constructor declared for this class.
  private constructor(pointer: UniffiHandle) {
    super()
    this[pointerLiteralSymbol] = pointer
    this[destructorGuardSymbol] =
      uniffiTypeInviteCodeObjectFactory.bless(pointer)
  }

  /**
   * Parses and validates an invite code from its canonical string form.
   *
   * # Errors
   *
   * [`InvalidInput`](crate::ErrorCode::InvalidInput) for a malformed value.
   * The rejected string is never echoed back: it may carry an `api_secret`.
   */
  static parse(code: string): InviteCodeLike /*throws*/ {
    return FfiConverterTypeInviteCode.lift(
      uniffiCaller.rustCallWithError(
        /*liftError:*/ FfiConverterTypeError__as_error.lift.bind(
          FfiConverterTypeError__as_error,
        ),
        /*caller:*/ (callStatus) => {
          return nativeModule().uniffi_fedimint_sdk_fn_constructor_invitecode_parse(
            FfiConverterString.lower(code, nativeModule().rustbuffer_alloc),
            callStatus,
          )
        },
        /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      ),
    )
  }

  /**
   * The invite code in its canonical `fed1…` bech32m string, for showing the
   * user, putting in a QR code, or sharing so someone else can join.
   *
   * This is the deliberate way to render the value; see the type-level
   * documentation for why [`Debug`] is not.
   */
  display(): string {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_invitecode_display(
          uniffiTypeInviteCodeObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterString.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * The id of the federation this code invites to.
   *
   * Read from the code itself, which encodes it: no network round trip,
   * no join, and the same value [`FederationPreview::id`] reports after
   * one. It is the key every per-federation call on [`Sdk`](crate::Sdk)
   * takes, including [`federation_status`](crate::Sdk::federation_status),
   * [`reopen_federation`](crate::Sdk::reopen_federation),
   * [`recovery_status`](crate::Sdk::recovery_status) and
   * [`resume_recovery`](crate::Sdk::resume_recovery). An application
   * holding only an invite code can therefore check where that federation
   * stands before joining, and find its way back to a federation that a
   * failed seed recovery left committed but not open, when nothing else
   * identifies which stored federation that failed call produced.
   *
   * Not a credential: a federation id is public, unlike the `api_secret`
   * the code as a whole may embed.
   */
  federationId(): FederationId {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_invitecode_federation_id(
          uniffiTypeInviteCodeObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterTypeFederationId.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  uniffiDestroy(): void {
    const ptr = (this as any)[destructorGuardSymbol]
    if (ptr !== undefined) {
      const pointer = uniffiTypeInviteCodeObjectFactory.pointer(this)
      uniffiTypeInviteCodeObjectFactory.freePointer(pointer)
      uniffiTypeInviteCodeObjectFactory.unbless(ptr)
      delete (this as any)[destructorGuardSymbol]
    }
  }

  static instanceOf(obj_: any): obj_ is InviteCode {
    return uniffiTypeInviteCodeObjectFactory.isConcreteType(obj_)
  }
}

const uniffiTypeInviteCodeObjectFactory: UniffiObjectFactory<InviteCodeLike> =
  (() => {
    /// <reference lib="es2021" />
    const registry =
      typeof FinalizationRegistry !== 'undefined'
        ? new FinalizationRegistry<UniffiHandle>((heldValue: UniffiHandle) => {
            uniffiTypeInviteCodeObjectFactory.freePointer(heldValue)
          })
        : null

    return {
      create(pointer: UniffiHandle): InviteCodeLike {
        const instance = Object.create(InviteCode.prototype)
        instance[pointerLiteralSymbol] = pointer
        instance[destructorGuardSymbol] = this.bless(pointer)
        instance[uniffiTypeNameSymbol] = 'InviteCode'
        return instance
      },

      bless(p: UniffiHandle): UniffiGcObject {
        const ptr = {
          p, // make sure this object doesn't get optimized away.
          markDestroyed: () => undefined,
        }
        if (registry) {
          registry.register(ptr, p, ptr)
        }
        return ptr
      },

      unbless(ptr_: UniffiGcObject) {
        if (registry) {
          registry.unregister(ptr_)
        }
      },

      pointer(obj_: InviteCodeLike): UniffiHandle {
        if ((obj_ as any)[destructorGuardSymbol] === undefined) {
          throw new UniffiInternalError.UnexpectedNullPointer()
        }
        return (obj_ as any)[pointerLiteralSymbol]
      },

      clonePointer(obj_: InviteCodeLike): UniffiHandle {
        const pointer = this.pointer(obj_)
        return uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_clone_invitecode(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      freePointer(pointer: UniffiHandle): void {
        uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_free_invitecode(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      isConcreteType(obj_: any): obj_ is InviteCodeLike {
        return (
          obj_[destructorGuardSymbol] &&
          obj_[uniffiTypeNameSymbol] === 'InviteCode'
        )
      },
    }
  })()
const FfiConverterTypeInviteCode = new FfiConverterObject(
  uniffiTypeInviteCodeObjectFactory,
)

/**
 * A frozen, executable plan for one lightning payment.
 *
 * Produced by [`Lightning::quote`] and consumed by [`Lightning::send`], whose
 * docs say exactly what is guaranteed to hold at execution. Everything a
 * user needs to approve is readable through the accessors below.
 */
export interface LnQuoteLike {
  /**
   * When this quote stops being executable.
   *
   * Past this point [`Lightning::send`] fails with
   * [`QuoteExpired`](crate::ErrorCode::QuoteExpired).
   */
  expiresAt(): Timestamp
  /**
   * The aggregate fee this payment will cost, on top of
   * [`LnQuote::invoice_amount`].
   *
   * Every debit that funding the payment incurs is in this one number:
   * the gateway's charge, the federation's own transaction fees and any
   * value too small for a note denomination to represent. It is not zero
   * on an internal route: no gateway means no gateway fee, but the
   * federation transaction still has costs.
   *
   * [`LnQuote::fee_breakdown`] itemises this same number. This accessor
   * is authoritative and the breakdown sums to it exactly.
   */
  fee(): Amount
  /**
   * The parts [`LnQuote::fee`] is made of, for an approval screen that
   * itemises them.
   */
  feeBreakdown(): LnFeeBreakdown
  /**
   * The invoice's amount: what reaches the payee if the payment succeeds.
   */
  invoiceAmount(): Amount
  /**
   * How this payment will be routed.
   */
  route(): LightningRoute
  /**
   * The whole debit this payment will make against the balance:
   * [`LnQuote::invoice_amount`] plus [`LnQuote::fee`].
   *
   * This is the number to show as "you will pay", and it is exact: the
   * debit execution is authorised to make, not a ceiling or an estimate.
   * A payment that would cost anything else is refused with
   * [`QuoteChanged`](crate::ErrorCode::QuoteChanged), whose
   * [`ErrorDetails::QuoteTermsChanged`](crate::ErrorDetails::QuoteTermsChanged)
   * names this total and the one the payment would now cost, so the user
   * re-approves a new number rather than quietly paying a different one.
   * The same figure is what [`LnSendDetails::total`] records.
   *
   * "This much or nothing" is a statement about the authorised debit, not
   * about what the balance does moment to moment. Executing the quote
   * submits a transaction, and submitting one takes the notes that are to
   * pay for it out of the spendable set before the federation has
   * accepted anything, so a funding attempt the federation then rejects
   * can remove value and restore it afterwards.
   *
   * Nothing else is ever debited than this total. What becomes of it when
   * the payment does not succeed is the ending's to say rather than the
   * quote's: [`LnSendState::Refunded`] is the one that promises no
   * lasting debit, reported only once the value is spendable again, and
   * [`LnSendState::Failed`] is the one that cannot promise it.
   */
  total(): Amount
}
/**
 * @deprecated Use `LnQuoteLike` instead.
 */
export type LnQuoteInterface = LnQuoteLike

/**
 * A frozen, executable plan for one lightning payment.
 *
 * Produced by [`Lightning::quote`] and consumed by [`Lightning::send`], whose
 * docs say exactly what is guaranteed to hold at execution. Everything a
 * user needs to approve is readable through the accessors below.
 */
export class LnQuote extends UniffiAbstractObject implements LnQuoteLike {
  readonly [uniffiTypeNameSymbol] = 'LnQuote'
  readonly [destructorGuardSymbol]: UniffiGcObject
  readonly [pointerLiteralSymbol]: UniffiHandle
  // No primary constructor declared for this class.
  private constructor(pointer: UniffiHandle) {
    super()
    this[pointerLiteralSymbol] = pointer
    this[destructorGuardSymbol] = uniffiTypeLnQuoteObjectFactory.bless(pointer)
  }

  /**
   * When this quote stops being executable.
   *
   * Past this point [`Lightning::send`] fails with
   * [`QuoteExpired`](crate::ErrorCode::QuoteExpired).
   */
  expiresAt(): Timestamp {
    return FfiConverterTypeTimestamp.lift(
      uniffiCaller.rustCall(
        /*caller:*/ (callStatus) => {
          return nativeModule().uniffi_fedimint_sdk_fn_method_lnquote_expires_at(
            uniffiTypeLnQuoteObjectFactory.clonePointer(this),
            callStatus,
          )
        },
        /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      ),
    )
  }

  /**
   * The aggregate fee this payment will cost, on top of
   * [`LnQuote::invoice_amount`].
   *
   * Every debit that funding the payment incurs is in this one number:
   * the gateway's charge, the federation's own transaction fees and any
   * value too small for a note denomination to represent. It is not zero
   * on an internal route: no gateway means no gateway fee, but the
   * federation transaction still has costs.
   *
   * [`LnQuote::fee_breakdown`] itemises this same number. This accessor
   * is authoritative and the breakdown sums to it exactly.
   */
  fee(): Amount {
    return FfiConverterTypeAmount.lift(
      uniffiCaller.rustCall(
        /*caller:*/ (callStatus) => {
          return nativeModule().uniffi_fedimint_sdk_fn_method_lnquote_fee(
            uniffiTypeLnQuoteObjectFactory.clonePointer(this),
            callStatus,
          )
        },
        /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      ),
    )
  }

  /**
   * The parts [`LnQuote::fee`] is made of, for an approval screen that
   * itemises them.
   */
  feeBreakdown(): LnFeeBreakdown {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_lnquote_fee_breakdown(
          uniffiTypeLnQuoteObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterTypeLnFeeBreakdown.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * The invoice's amount: what reaches the payee if the payment succeeds.
   */
  invoiceAmount(): Amount {
    return FfiConverterTypeAmount.lift(
      uniffiCaller.rustCall(
        /*caller:*/ (callStatus) => {
          return nativeModule().uniffi_fedimint_sdk_fn_method_lnquote_invoice_amount(
            uniffiTypeLnQuoteObjectFactory.clonePointer(this),
            callStatus,
          )
        },
        /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      ),
    )
  }

  /**
   * How this payment will be routed.
   */
  route(): LightningRoute {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_lnquote_route(
          uniffiTypeLnQuoteObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterTypeLightningRoute.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * The whole debit this payment will make against the balance:
   * [`LnQuote::invoice_amount`] plus [`LnQuote::fee`].
   *
   * This is the number to show as "you will pay", and it is exact: the
   * debit execution is authorised to make, not a ceiling or an estimate.
   * A payment that would cost anything else is refused with
   * [`QuoteChanged`](crate::ErrorCode::QuoteChanged), whose
   * [`ErrorDetails::QuoteTermsChanged`](crate::ErrorDetails::QuoteTermsChanged)
   * names this total and the one the payment would now cost, so the user
   * re-approves a new number rather than quietly paying a different one.
   * The same figure is what [`LnSendDetails::total`] records.
   *
   * "This much or nothing" is a statement about the authorised debit, not
   * about what the balance does moment to moment. Executing the quote
   * submits a transaction, and submitting one takes the notes that are to
   * pay for it out of the spendable set before the federation has
   * accepted anything, so a funding attempt the federation then rejects
   * can remove value and restore it afterwards.
   *
   * Nothing else is ever debited than this total. What becomes of it when
   * the payment does not succeed is the ending's to say rather than the
   * quote's: [`LnSendState::Refunded`] is the one that promises no
   * lasting debit, reported only once the value is spendable again, and
   * [`LnSendState::Failed`] is the one that cannot promise it.
   */
  total(): Amount {
    return FfiConverterTypeAmount.lift(
      uniffiCaller.rustCall(
        /*caller:*/ (callStatus) => {
          return nativeModule().uniffi_fedimint_sdk_fn_method_lnquote_total(
            uniffiTypeLnQuoteObjectFactory.clonePointer(this),
            callStatus,
          )
        },
        /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      ),
    )
  }

  uniffiDestroy(): void {
    const ptr = (this as any)[destructorGuardSymbol]
    if (ptr !== undefined) {
      const pointer = uniffiTypeLnQuoteObjectFactory.pointer(this)
      uniffiTypeLnQuoteObjectFactory.freePointer(pointer)
      uniffiTypeLnQuoteObjectFactory.unbless(ptr)
      delete (this as any)[destructorGuardSymbol]
    }
  }

  static instanceOf(obj_: any): obj_ is LnQuote {
    return uniffiTypeLnQuoteObjectFactory.isConcreteType(obj_)
  }
}

const uniffiTypeLnQuoteObjectFactory: UniffiObjectFactory<LnQuoteLike> =
  (() => {
    /// <reference lib="es2021" />
    const registry =
      typeof FinalizationRegistry !== 'undefined'
        ? new FinalizationRegistry<UniffiHandle>((heldValue: UniffiHandle) => {
            uniffiTypeLnQuoteObjectFactory.freePointer(heldValue)
          })
        : null

    return {
      create(pointer: UniffiHandle): LnQuoteLike {
        const instance = Object.create(LnQuote.prototype)
        instance[pointerLiteralSymbol] = pointer
        instance[destructorGuardSymbol] = this.bless(pointer)
        instance[uniffiTypeNameSymbol] = 'LnQuote'
        return instance
      },

      bless(p: UniffiHandle): UniffiGcObject {
        const ptr = {
          p, // make sure this object doesn't get optimized away.
          markDestroyed: () => undefined,
        }
        if (registry) {
          registry.register(ptr, p, ptr)
        }
        return ptr
      },

      unbless(ptr_: UniffiGcObject) {
        if (registry) {
          registry.unregister(ptr_)
        }
      },

      pointer(obj_: LnQuoteLike): UniffiHandle {
        if ((obj_ as any)[destructorGuardSymbol] === undefined) {
          throw new UniffiInternalError.UnexpectedNullPointer()
        }
        return (obj_ as any)[pointerLiteralSymbol]
      },

      clonePointer(obj_: LnQuoteLike): UniffiHandle {
        const pointer = this.pointer(obj_)
        return uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_clone_lnquote(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      freePointer(pointer: UniffiHandle): void {
        uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_free_lnquote(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      isConcreteType(obj_: any): obj_ is LnQuoteLike {
        return (
          obj_[destructorGuardSymbol] &&
          obj_[uniffiTypeNameSymbol] === 'LnQuote'
        )
      },
    }
  })()
const FfiConverterTypeLnQuote = new FfiConverterObject(
  uniffiTypeLnQuoteObjectFactory,
)

/**
 * Typealias from the type name used in the UDL file to the builtin type.  This
 * is needed because the UDL type name is used in function/method signatures.
 */
export type Preimage = string
// FfiConverter for Preimage, a type alias for string.
const FfiConverterTypePreimage = FfiConverterString

// Enum: LnSendState
export enum LnSendState_Tags {
  Created = 'Created',
  Funded = 'Funded',
  Success = 'Success',
  Refunded = 'Refunded',
  Failed = 'Failed',
}
/**
 * The lifecycle of an outgoing lightning payment.
 *
 * One lifecycle covers both internally settled and gateway-routed
 * payments, so an application needs one payment screen.
 *
 * The final states are drawn by what happened to the money.
 * [`Success`](Self::Success) means the payee was paid;
 * [`Refunded`](Self::Refunded) means the payment left no lasting debit and
 * the value is spendable again; [`Failed`](Self::Failed) means the payment
 * did not resolve into either. A payment has no cancellation: once sent it
 * runs to one of those endings.
 */

type LnSendState_Created_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'LnSendState'
  tag: LnSendState_Tags.Created
}

type LnSendState_Funded_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'LnSendState'
  tag: LnSendState_Tags.Funded
}

type LnSendState_Success_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'LnSendState'
  tag: LnSendState_Tags.Success
  inner: Readonly<{ preimage: Preimage; fee: Amount; route: LightningRoute }>
}

type LnSendState_Refunded_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'LnSendState'
  tag: LnSendState_Tags.Refunded
}

type LnSendState_Failed_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'LnSendState'
  tag: LnSendState_Tags.Failed
  inner: Readonly<{ reason: string }>
}
export type LnSendState =
  | LnSendState_Created_interface
  | LnSendState_Funded_interface
  | LnSendState_Success_interface
  | LnSendState_Refunded_interface
  | LnSendState_Failed_interface

export const LnSendState = (() => {
  /**
   * The payment has been accepted and is being funded.
   */
  class Created_ extends UniffiEnum implements LnSendState_Created_interface {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'LnSendState'
    readonly tag = LnSendState_Tags.Created
    constructor() {
      super('LnSendState', 'Created')
    }

    static new(): Created_ {
      return new Created_()
    }

    static instanceOf(obj: any): obj is Created_ {
      return obj.tag === LnSendState_Tags.Created
    }
  }
  /**
   * The payment is funded and in flight: handed to the gateway, or
   * committed internally.
   */
  class Funded_ extends UniffiEnum implements LnSendState_Funded_interface {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'LnSendState'
    readonly tag = LnSendState_Tags.Funded
    constructor() {
      super('LnSendState', 'Funded')
    }

    static new(): Funded_ {
      return new Funded_()
    }

    static instanceOf(obj: any): obj is Funded_ {
      return obj.tag === LnSendState_Tags.Funded
    }
  }
  /**
   * Final: the payee was paid and the [`Preimage`] proves it.
   */
  class Success_ extends UniffiEnum implements LnSendState_Success_interface {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'LnSendState'
    readonly tag = LnSendState_Tags.Success
    readonly inner: Readonly<{
      preimage: Preimage
      fee: Amount
      route: LightningRoute
    }>
    constructor(inner: {
      preimage: Preimage
      fee: Amount
      route: LightningRoute
    }) {
      super('LnSendState', 'Success')

      this.inner = Object.freeze(inner)
    }
    static new(inner: {
      preimage: Preimage
      fee: Amount
      route: LightningRoute
    }): Success_ {
      return new Success_(inner)
    }

    static instanceOf(obj: any): obj is Success_ {
      return obj.tag === LnSendState_Tags.Success
    }
  }
  /**
   * Final: the payment did not go through and the value it was
   * authorised for is spendable again.
   *
   * This is the ordinary failure of a lightning payment: no route, the
   * payee went away, the gateway gave up, or the federation rejected the
   * transaction that would have funded it. What the endings have in
   * common is the promise this state makes, which is about the balance
   * now and not about the route taken to it: whatever the payment
   * removed along the way, no part of it is still standing against the
   * balance. The money is safe.
   *
   * Reaching that promise is not always instant, and this state waits
   * for it. Funding a payment selects the notes that are to pay for it
   * before consensus has accepted anything, so a rejected funding
   * transaction leaves value that is neither spent nor yet spendable,
   * and returning it is a later transaction of the mint's own. The
   * payment stays non-final for as long as that runs. Only once it has
   * settled, and settled in a way that establishes the value came back,
   * is this state reported; a recovery that settles without
   * establishing it ends the payment in [`Failed`](Self::Failed)
   * instead.
   */
  class Refunded_ extends UniffiEnum implements LnSendState_Refunded_interface {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'LnSendState'
    readonly tag = LnSendState_Tags.Refunded
    constructor() {
      super('LnSendState', 'Refunded')
    }

    static new(): Refunded_ {
      return new Refunded_()
    }

    static instanceOf(obj: any): obj is Refunded_ {
      return obj.tag === LnSendState_Tags.Refunded
    }
  }
  /**
   * Final: the payment failed in a way that did not resolve into a clean
   * refund.
   *
   * Either the payment got far enough to be at risk and no refund
   * followed, or its funding was rejected and the value that attempt
   * removed could not be established as spendable again. Both mean the
   * same thing to an application: this state cannot say where the money
   * is, so read the balance rather than inferring one.
   */
  class Failed_ extends UniffiEnum implements LnSendState_Failed_interface {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'LnSendState'
    readonly tag = LnSendState_Tags.Failed
    readonly inner: Readonly<{ reason: string }>
    constructor(inner: { reason: string }) {
      super('LnSendState', 'Failed')

      this.inner = Object.freeze(inner)
    }
    static new(inner: { reason: string }): Failed_ {
      return new Failed_(inner)
    }

    static instanceOf(obj: any): obj is Failed_ {
      return obj.tag === LnSendState_Tags.Failed
    }
  }

  function instanceOf(obj: any): obj is LnSendState {
    return obj[uniffiTypeNameSymbol] === 'LnSendState'
  }

  return Object.freeze({
    instanceOf,
    Created: Created_,
    Funded: Funded_,
    Success: Success_,
    Refunded: Refunded_,
    Failed: Failed_,
  })
})()

// FfiConverter for enum LnSendState
const FfiConverterTypeLnSendState = (() => {
  type TypeName = LnSendState
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      switch (c.readI32()) {
        case 1:
          return new LnSendState.Created()
        case 2:
          return new LnSendState.Funded()
        case 3:
          return new LnSendState.Success({
            preimage: FfiConverterTypePreimage.readFromCursor(c),
            fee: FfiConverterTypeAmount.readFromCursor(c),
            route: FfiConverterTypeLightningRoute.readFromCursor(c),
          })
        case 4:
          return new LnSendState.Refunded()
        case 5:
          return new LnSendState.Failed({
            reason: FfiConverterString.readFromCursor(c),
          })
        default:
          throw new UniffiInternalError.UnexpectedEnumCase()
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      switch (value.tag) {
        case LnSendState_Tags.Created: {
          c.writeI32(1)
          return
        }
        case LnSendState_Tags.Funded: {
          c.writeI32(2)
          return
        }
        case LnSendState_Tags.Success: {
          c.writeI32(3)
          const inner = value.inner
          FfiConverterTypePreimage.writeIntoCursor(inner.preimage, c)
          FfiConverterTypeAmount.writeIntoCursor(inner.fee, c)
          FfiConverterTypeLightningRoute.writeIntoCursor(inner.route, c)
          return
        }
        case LnSendState_Tags.Refunded: {
          c.writeI32(4)
          return
        }
        case LnSendState_Tags.Failed: {
          c.writeI32(5)
          const inner = value.inner
          FfiConverterString.writeIntoCursor(inner.reason, c)
          return
        }
        default:
          // Throwing from here means that LnSendState_Tags hasn't matched an ordinal.
          throw new UniffiInternalError.UnexpectedEnumCase()
      }
    }
    allocationSize(value: TypeName): number {
      switch (value.tag) {
        case LnSendState_Tags.Created: {
          return 4
        }
        case LnSendState_Tags.Funded: {
          return 4
        }
        case LnSendState_Tags.Success: {
          const inner = value.inner
          let size = 4
          size += FfiConverterTypePreimage.allocationSize(inner.preimage)
          size += FfiConverterTypeAmount.allocationSize(inner.fee)
          size += FfiConverterTypeLightningRoute.allocationSize(inner.route)
          return size
        }
        case LnSendState_Tags.Refunded: {
          return 4
        }
        case LnSendState_Tags.Failed: {
          const inner = value.inner
          let size = 4
          size += FfiConverterString.allocationSize(inner.reason)
          return size
        }
        default:
          throw new UniffiInternalError.UnexpectedEnumCase()
      }
    }
  }
  return new FFIConverter()
})()

/**
 * The UniFFI view of one `OperationUpdates<S>` instantiation, behind a lock so `next`'s
 * `&mut self` can cross as `&self`.
 */
export interface LnSendOperationUpdatesLike {
  /**
   * See [`OperationUpdates::next`](crate::OperationUpdates::next).
   */
  next(asyncOpts_?: {
    signal: AbortSignal
  }) /*throws*/ : Promise<LnSendState | undefined>
}
/**
 * @deprecated Use `LnSendOperationUpdatesLike` instead.
 */
export type LnSendOperationUpdatesInterface = LnSendOperationUpdatesLike

/**
 * The UniFFI view of one `OperationUpdates<S>` instantiation, behind a lock so `next`'s
 * `&mut self` can cross as `&self`.
 */
export class LnSendOperationUpdates
  extends UniffiAbstractObject
  implements LnSendOperationUpdatesLike
{
  readonly [uniffiTypeNameSymbol] = 'LnSendOperationUpdates'
  readonly [destructorGuardSymbol]: UniffiGcObject
  readonly [pointerLiteralSymbol]: UniffiHandle
  // No primary constructor declared for this class.
  private constructor(pointer: UniffiHandle) {
    super()
    this[pointerLiteralSymbol] = pointer
    this[destructorGuardSymbol] =
      uniffiTypeLnSendOperationUpdatesObjectFactory.bless(pointer)
  }

  /**
   * See [`OperationUpdates::next`](crate::OperationUpdates::next).
   */
  async next(asyncOpts_?: {
    signal: AbortSignal
  }): Promise<LnSendState | undefined> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_lnsendoperationupdates_next(
          uniffiTypeLnSendOperationUpdatesObjectFactory.clonePointer(this),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterOptionalTypeLnSendState.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  uniffiDestroy(): void {
    const ptr = (this as any)[destructorGuardSymbol]
    if (ptr !== undefined) {
      const pointer =
        uniffiTypeLnSendOperationUpdatesObjectFactory.pointer(this)
      uniffiTypeLnSendOperationUpdatesObjectFactory.freePointer(pointer)
      uniffiTypeLnSendOperationUpdatesObjectFactory.unbless(ptr)
      delete (this as any)[destructorGuardSymbol]
    }
  }

  static instanceOf(obj_: any): obj_ is LnSendOperationUpdates {
    return uniffiTypeLnSendOperationUpdatesObjectFactory.isConcreteType(obj_)
  }
}

const uniffiTypeLnSendOperationUpdatesObjectFactory: UniffiObjectFactory<LnSendOperationUpdatesLike> =
  (() => {
    /// <reference lib="es2021" />
    const registry =
      typeof FinalizationRegistry !== 'undefined'
        ? new FinalizationRegistry<UniffiHandle>((heldValue: UniffiHandle) => {
            uniffiTypeLnSendOperationUpdatesObjectFactory.freePointer(heldValue)
          })
        : null

    return {
      create(pointer: UniffiHandle): LnSendOperationUpdatesLike {
        const instance = Object.create(LnSendOperationUpdates.prototype)
        instance[pointerLiteralSymbol] = pointer
        instance[destructorGuardSymbol] = this.bless(pointer)
        instance[uniffiTypeNameSymbol] = 'LnSendOperationUpdates'
        return instance
      },

      bless(p: UniffiHandle): UniffiGcObject {
        const ptr = {
          p, // make sure this object doesn't get optimized away.
          markDestroyed: () => undefined,
        }
        if (registry) {
          registry.register(ptr, p, ptr)
        }
        return ptr
      },

      unbless(ptr_: UniffiGcObject) {
        if (registry) {
          registry.unregister(ptr_)
        }
      },

      pointer(obj_: LnSendOperationUpdatesLike): UniffiHandle {
        if ((obj_ as any)[destructorGuardSymbol] === undefined) {
          throw new UniffiInternalError.UnexpectedNullPointer()
        }
        return (obj_ as any)[pointerLiteralSymbol]
      },

      clonePointer(obj_: LnSendOperationUpdatesLike): UniffiHandle {
        const pointer = this.pointer(obj_)
        return uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_clone_lnsendoperationupdates(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      freePointer(pointer: UniffiHandle): void {
        uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_free_lnsendoperationupdates(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      isConcreteType(obj_: any): obj_ is LnSendOperationUpdatesLike {
        return (
          obj_[destructorGuardSymbol] &&
          obj_[uniffiTypeNameSymbol] === 'LnSendOperationUpdates'
        )
      },
    }
  })()
const FfiConverterTypeLnSendOperationUpdates = new FfiConverterObject(
  uniffiTypeLnSendOperationUpdatesObjectFactory,
)

/**
 * The UniFFI view of one `Operation<S>` instantiation: an opaque object wrapping the real
 * handle, forwarding every method.
 */
export interface LnSendOperationLike {
  /**
   * See [`Operation::await_final`](crate::Operation::await_final).
   */
  awaitFinal(asyncOpts_?: {
    signal: AbortSignal
  }) /*throws*/ : Promise<LnSendState>
  /**
   * See [`Operation::details`](crate::Operation::details).
   */
  details(asyncOpts_?: {
    signal: AbortSignal
  }) /*throws*/ : Promise<LnSendDetails>
  /**
   * See [`Operation::id`](crate::Operation::id).
   */
  id(): OperationId
  /**
   * See [`Operation::state`](crate::Operation::state).
   */
  state(asyncOpts_?: { signal: AbortSignal }) /*throws*/ : Promise<LnSendState>
  /**
   * See [`Operation::updates`](crate::Operation::updates).
   */
  updates(): LnSendOperationUpdatesLike
}
/**
 * @deprecated Use `LnSendOperationLike` instead.
 */
export type LnSendOperationInterface = LnSendOperationLike

/**
 * The UniFFI view of one `Operation<S>` instantiation: an opaque object wrapping the real
 * handle, forwarding every method.
 */
export class LnSendOperation
  extends UniffiAbstractObject
  implements LnSendOperationLike
{
  readonly [uniffiTypeNameSymbol] = 'LnSendOperation'
  readonly [destructorGuardSymbol]: UniffiGcObject
  readonly [pointerLiteralSymbol]: UniffiHandle
  // No primary constructor declared for this class.
  private constructor(pointer: UniffiHandle) {
    super()
    this[pointerLiteralSymbol] = pointer
    this[destructorGuardSymbol] =
      uniffiTypeLnSendOperationObjectFactory.bless(pointer)
  }

  /**
   * See [`Operation::await_final`](crate::Operation::await_final).
   */
  async awaitFinal(asyncOpts_?: {
    signal: AbortSignal
  }): Promise<LnSendState> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_lnsendoperation_await_final(
          uniffiTypeLnSendOperationObjectFactory.clonePointer(this),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterTypeLnSendState.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * See [`Operation::details`](crate::Operation::details).
   */
  async details(asyncOpts_?: {
    signal: AbortSignal
  }): Promise<LnSendDetails> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_lnsendoperation_details(
          uniffiTypeLnSendOperationObjectFactory.clonePointer(this),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterTypeLnSendDetails.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * See [`Operation::id`](crate::Operation::id).
   */
  id(): OperationId {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_lnsendoperation_id(
          uniffiTypeLnSendOperationObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterTypeOperationId.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * See [`Operation::state`](crate::Operation::state).
   */
  async state(asyncOpts_?: {
    signal: AbortSignal
  }): Promise<LnSendState> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_lnsendoperation_state(
          uniffiTypeLnSendOperationObjectFactory.clonePointer(this),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterTypeLnSendState.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * See [`Operation::updates`](crate::Operation::updates).
   */
  updates(): LnSendOperationUpdatesLike {
    return FfiConverterTypeLnSendOperationUpdates.lift(
      uniffiCaller.rustCall(
        /*caller:*/ (callStatus) => {
          return nativeModule().uniffi_fedimint_sdk_fn_method_lnsendoperation_updates(
            uniffiTypeLnSendOperationObjectFactory.clonePointer(this),
            callStatus,
          )
        },
        /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      ),
    )
  }

  uniffiDestroy(): void {
    const ptr = (this as any)[destructorGuardSymbol]
    if (ptr !== undefined) {
      const pointer = uniffiTypeLnSendOperationObjectFactory.pointer(this)
      uniffiTypeLnSendOperationObjectFactory.freePointer(pointer)
      uniffiTypeLnSendOperationObjectFactory.unbless(ptr)
      delete (this as any)[destructorGuardSymbol]
    }
  }

  static instanceOf(obj_: any): obj_ is LnSendOperation {
    return uniffiTypeLnSendOperationObjectFactory.isConcreteType(obj_)
  }
}

const uniffiTypeLnSendOperationObjectFactory: UniffiObjectFactory<LnSendOperationLike> =
  (() => {
    /// <reference lib="es2021" />
    const registry =
      typeof FinalizationRegistry !== 'undefined'
        ? new FinalizationRegistry<UniffiHandle>((heldValue: UniffiHandle) => {
            uniffiTypeLnSendOperationObjectFactory.freePointer(heldValue)
          })
        : null

    return {
      create(pointer: UniffiHandle): LnSendOperationLike {
        const instance = Object.create(LnSendOperation.prototype)
        instance[pointerLiteralSymbol] = pointer
        instance[destructorGuardSymbol] = this.bless(pointer)
        instance[uniffiTypeNameSymbol] = 'LnSendOperation'
        return instance
      },

      bless(p: UniffiHandle): UniffiGcObject {
        const ptr = {
          p, // make sure this object doesn't get optimized away.
          markDestroyed: () => undefined,
        }
        if (registry) {
          registry.register(ptr, p, ptr)
        }
        return ptr
      },

      unbless(ptr_: UniffiGcObject) {
        if (registry) {
          registry.unregister(ptr_)
        }
      },

      pointer(obj_: LnSendOperationLike): UniffiHandle {
        if ((obj_ as any)[destructorGuardSymbol] === undefined) {
          throw new UniffiInternalError.UnexpectedNullPointer()
        }
        return (obj_ as any)[pointerLiteralSymbol]
      },

      clonePointer(obj_: LnSendOperationLike): UniffiHandle {
        const pointer = this.pointer(obj_)
        return uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_clone_lnsendoperation(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      freePointer(pointer: UniffiHandle): void {
        uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_free_lnsendoperation(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      isConcreteType(obj_: any): obj_ is LnSendOperationLike {
        return (
          obj_[destructorGuardSymbol] &&
          obj_[uniffiTypeNameSymbol] === 'LnSendOperation'
        )
      },
    }
  })()
const FfiConverterTypeLnSendOperation = new FfiConverterObject(
  uniffiTypeLnSendOperationObjectFactory,
)

/**
 * The lightning facade for one federation.
 *
 * Obtained from [`Federation::lightning`](crate::Federation::lightning),
 * which returns `None` when the federation has no lightning module.
 *
 * Gateway selection, gateway verification and fee quoting happen inside the
 * facade, before an invoice is created or a payment is funded. A gateway
 * problem is therefore an error from the call that started the operation,
 * not a failure halfway through it.
 */
export interface LightningLike {
  /**
   * Plans a payment and returns an executable quote for it.
   *
   * The returned [`LnQuote`] is the frozen plan for paying `invoice`: the
   * amount the invoice names, the route, the aggregate fee and the total
   * debit. Show those numbers to the user, then pass the quote to
   * [`Lightning::send`], whose docs say exactly what executing it
   * guarantees.
   *
   * The amount is always the invoice's own. An invoice that names no
   * amount cannot be paid through fedimint and is refused here with
   * [`AmountlessInvoice`](crate::ErrorCode::AmountlessInvoice); check
   * [`Bolt11Invoice::amount`](crate::Bolt11Invoice::amount) first to show
   * the user a better message than a failed quote.
   *
   * The invoice's network is checked here against
   * [`Federation::network`](crate::Federation::network). The comparison is
   * by BOLT11 currency class, which is all an invoice can express: a `tb`
   * invoice is compatible with a federation on testnet3 or testnet4 alike.
   * A mismatch fails with
   * [`NetworkMismatch`](crate::ErrorCode::NetworkMismatch), whose
   * [`ErrorDetails::NetworkMismatch`](crate::ErrorDetails::NetworkMismatch)
   * names the federation's network, every network the invoice could have
   * been for and the currency prefix that was seen.
   *
   * Quotes expire; see [`LnQuote::expires_at`].
   *
   * # Errors
   *
   * [`AmountlessInvoice`](crate::ErrorCode::AmountlessInvoice) for an
   * invoice that names no amount,
   * [`NetworkMismatch`](crate::ErrorCode::NetworkMismatch) for an invoice
   * denominated for another network,
   * [`InvalidInput`](crate::ErrorCode::InvalidInput) for an invoice that
   * has already expired,
   * [`GatewayUnavailable`](crate::ErrorCode::GatewayUnavailable) when no
   * gateway can be selected and verified,
   * [`InsufficientBalance`](crate::ErrorCode::InsufficientBalance) when
   * the balance cannot cover [`LnQuote::total`],
   * [`Recovering`](crate::ErrorCode::Recovering) while the federation's
   * recovery is incomplete,
   * [`NotSupported`](crate::ErrorCode::NotSupported),
   * [`FederationUnreachable`](crate::ErrorCode::FederationUnreachable),
   * [`Timeout`](crate::ErrorCode::Timeout),
   * [`Internal`](crate::ErrorCode::Internal) for a failure this crate
   * does not expect, which indicates a bug, and
   * [`FederationClosed`](crate::ErrorCode::FederationClosed).
   */
  quote(
    invoice: Bolt11Invoice,
    asyncOpts_?: { signal: AbortSignal },
  ) /*throws*/ : Promise<LnQuoteLike>
  /**
   * See [`Lightning::receive`].
   */
  receive(
    amount: Amount,
    description: string,
    asyncOpts_?: { signal: AbortSignal },
  ) /*throws*/ : Promise<LnReceiveHandle>
  /**
   * See [`Lightning::send`]. Fails with
   * [`ErrorCode::QuoteExpired`](crate::ErrorCode::QuoteExpired) if `quote` was already sent.
   */
  send(
    quote: LnQuoteLike,
    asyncOpts_?: { signal: AbortSignal },
  ) /*throws*/ : Promise<LnSendOperationLike>
}
/**
 * @deprecated Use `LightningLike` instead.
 */
export type LightningInterface = LightningLike

/**
 * The lightning facade for one federation.
 *
 * Obtained from [`Federation::lightning`](crate::Federation::lightning),
 * which returns `None` when the federation has no lightning module.
 *
 * Gateway selection, gateway verification and fee quoting happen inside the
 * facade, before an invoice is created or a payment is funded. A gateway
 * problem is therefore an error from the call that started the operation,
 * not a failure halfway through it.
 */
export class Lightning extends UniffiAbstractObject implements LightningLike {
  readonly [uniffiTypeNameSymbol] = 'Lightning'
  readonly [destructorGuardSymbol]: UniffiGcObject
  readonly [pointerLiteralSymbol]: UniffiHandle
  // No primary constructor declared for this class.
  private constructor(pointer: UniffiHandle) {
    super()
    this[pointerLiteralSymbol] = pointer
    this[destructorGuardSymbol] =
      uniffiTypeLightningObjectFactory.bless(pointer)
  }

  /**
   * Plans a payment and returns an executable quote for it.
   *
   * The returned [`LnQuote`] is the frozen plan for paying `invoice`: the
   * amount the invoice names, the route, the aggregate fee and the total
   * debit. Show those numbers to the user, then pass the quote to
   * [`Lightning::send`], whose docs say exactly what executing it
   * guarantees.
   *
   * The amount is always the invoice's own. An invoice that names no
   * amount cannot be paid through fedimint and is refused here with
   * [`AmountlessInvoice`](crate::ErrorCode::AmountlessInvoice); check
   * [`Bolt11Invoice::amount`](crate::Bolt11Invoice::amount) first to show
   * the user a better message than a failed quote.
   *
   * The invoice's network is checked here against
   * [`Federation::network`](crate::Federation::network). The comparison is
   * by BOLT11 currency class, which is all an invoice can express: a `tb`
   * invoice is compatible with a federation on testnet3 or testnet4 alike.
   * A mismatch fails with
   * [`NetworkMismatch`](crate::ErrorCode::NetworkMismatch), whose
   * [`ErrorDetails::NetworkMismatch`](crate::ErrorDetails::NetworkMismatch)
   * names the federation's network, every network the invoice could have
   * been for and the currency prefix that was seen.
   *
   * Quotes expire; see [`LnQuote::expires_at`].
   *
   * # Errors
   *
   * [`AmountlessInvoice`](crate::ErrorCode::AmountlessInvoice) for an
   * invoice that names no amount,
   * [`NetworkMismatch`](crate::ErrorCode::NetworkMismatch) for an invoice
   * denominated for another network,
   * [`InvalidInput`](crate::ErrorCode::InvalidInput) for an invoice that
   * has already expired,
   * [`GatewayUnavailable`](crate::ErrorCode::GatewayUnavailable) when no
   * gateway can be selected and verified,
   * [`InsufficientBalance`](crate::ErrorCode::InsufficientBalance) when
   * the balance cannot cover [`LnQuote::total`],
   * [`Recovering`](crate::ErrorCode::Recovering) while the federation's
   * recovery is incomplete,
   * [`NotSupported`](crate::ErrorCode::NotSupported),
   * [`FederationUnreachable`](crate::ErrorCode::FederationUnreachable),
   * [`Timeout`](crate::ErrorCode::Timeout),
   * [`Internal`](crate::ErrorCode::Internal) for a failure this crate
   * does not expect, which indicates a bug, and
   * [`FederationClosed`](crate::ErrorCode::FederationClosed).
   */
  async quote(
    invoice: Bolt11Invoice,
    asyncOpts_?: { signal: AbortSignal },
  ): Promise<LnQuoteLike> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_lightning_quote(
          uniffiTypeLightningObjectFactory.clonePointer(this),
          FfiConverterTypeBolt11Invoice.lower(
            invoice,
            nativeModule().rustbuffer_alloc,
          ),
        )
      },
      /*pollFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_poll_u64,
      /*cancelFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_cancel_u64,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_u64,
      /*freeFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_free_u64,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      /*liftFunc:*/ FfiConverterTypeLnQuote.lift.bind(FfiConverterTypeLnQuote),
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * See [`Lightning::receive`].
   */
  async receive(
    amount: Amount,
    description: string,
    asyncOpts_?: { signal: AbortSignal },
  ): Promise<LnReceiveHandle> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_lightning_receive(
          uniffiTypeLightningObjectFactory.clonePointer(this),
          FfiConverterTypeAmount.lower(amount, nativeModule().rustbuffer_alloc),
          FfiConverterString.lower(
            description,
            nativeModule().rustbuffer_alloc,
          ),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterTypeLnReceiveHandle.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * See [`Lightning::send`]. Fails with
   * [`ErrorCode::QuoteExpired`](crate::ErrorCode::QuoteExpired) if `quote` was already sent.
   */
  async send(
    quote: LnQuoteLike,
    asyncOpts_?: { signal: AbortSignal },
  ): Promise<LnSendOperationLike> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_lightning_send(
          uniffiTypeLightningObjectFactory.clonePointer(this),
          FfiConverterTypeLnQuote.lower(quote, nativeModule().rustbuffer_alloc),
        )
      },
      /*pollFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_poll_u64,
      /*cancelFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_cancel_u64,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_u64,
      /*freeFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_free_u64,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      /*liftFunc:*/ FfiConverterTypeLnSendOperation.lift.bind(
        FfiConverterTypeLnSendOperation,
      ),
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  uniffiDestroy(): void {
    const ptr = (this as any)[destructorGuardSymbol]
    if (ptr !== undefined) {
      const pointer = uniffiTypeLightningObjectFactory.pointer(this)
      uniffiTypeLightningObjectFactory.freePointer(pointer)
      uniffiTypeLightningObjectFactory.unbless(ptr)
      delete (this as any)[destructorGuardSymbol]
    }
  }

  static instanceOf(obj_: any): obj_ is Lightning {
    return uniffiTypeLightningObjectFactory.isConcreteType(obj_)
  }
}

const uniffiTypeLightningObjectFactory: UniffiObjectFactory<LightningLike> =
  (() => {
    /// <reference lib="es2021" />
    const registry =
      typeof FinalizationRegistry !== 'undefined'
        ? new FinalizationRegistry<UniffiHandle>((heldValue: UniffiHandle) => {
            uniffiTypeLightningObjectFactory.freePointer(heldValue)
          })
        : null

    return {
      create(pointer: UniffiHandle): LightningLike {
        const instance = Object.create(Lightning.prototype)
        instance[pointerLiteralSymbol] = pointer
        instance[destructorGuardSymbol] = this.bless(pointer)
        instance[uniffiTypeNameSymbol] = 'Lightning'
        return instance
      },

      bless(p: UniffiHandle): UniffiGcObject {
        const ptr = {
          p, // make sure this object doesn't get optimized away.
          markDestroyed: () => undefined,
        }
        if (registry) {
          registry.register(ptr, p, ptr)
        }
        return ptr
      },

      unbless(ptr_: UniffiGcObject) {
        if (registry) {
          registry.unregister(ptr_)
        }
      },

      pointer(obj_: LightningLike): UniffiHandle {
        if ((obj_ as any)[destructorGuardSymbol] === undefined) {
          throw new UniffiInternalError.UnexpectedNullPointer()
        }
        return (obj_ as any)[pointerLiteralSymbol]
      },

      clonePointer(obj_: LightningLike): UniffiHandle {
        const pointer = this.pointer(obj_)
        return uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_clone_lightning(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      freePointer(pointer: UniffiHandle): void {
        uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_free_lightning(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      isConcreteType(obj_: any): obj_ is LightningLike {
        return (
          obj_[destructorGuardSymbol] &&
          obj_[uniffiTypeNameSymbol] === 'Lightning'
        )
      },
    }
  })()
const FfiConverterTypeLightning = new FfiConverterObject(
  uniffiTypeLightningObjectFactory,
)

/**
 * The metadata facade for one federation.
 *
 * Obtained from [`Federation::meta`](crate::Federation::meta). Unlike the
 * capability facades this is unconditional: every federation has
 * configuration metadata, so there is always something here to read.
 *
 * # Two sources, one merged view
 *
 * A federation can describe itself in two places, and they are genuinely
 * different things:
 *
 * - **Configuration metadata** is baked into the federation's consensus
 * configuration. It is fixed for the life of that configuration, is
 * available locally without asking anyone, and is what a
 * [`FederationPreview`](crate::FederationPreview) shows before joining.
 * - **Consensus metadata** lives in the federation's meta module, is
 * agreed by the guardians at runtime, and is *revisioned*: the
 * guardians can change it, and each change bumps a revision number.
 * Not every federation runs a meta module.
 *
 * Most applications want neither of those specifically; they want to know
 * "what is this federation's welcome message" and to get the current
 * answer. [`Meta::get`] and [`Meta::all`] provide that as a merged view,
 * with a single precedence rule: **consensus metadata overrides
 * configuration metadata, per key**. A key present in both takes its value
 * from consensus, because consensus metadata is the one the guardians can
 * update; keys present in only one source appear unchanged.
 *
 * The raw sources stay available separately:
 * [`Meta::config_metadata`] and [`Meta::consensus_metadata`], so an
 * application that needs to know *where* a value came from, or that needs
 * the consensus revision, is not forced to work backwards from the merged
 * result.
 *
 * # The merged view is a lossy projection, and these are its exact rules
 *
 * The meta module stores arbitrary bytes ([`ConsensusMetadata::value`]),
 * in practice a UTF-8 JSON document but with no guarantee of either.
 * Turning those bytes into the flat `BTreeMap<String, String>` that
 * [`Meta::get`] and [`Meta::all`] return is a defined, lossy projection.
 * Every binding must behave identically here, so the rules are written down
 * rather than left to each implementation to settle:
 *
 * 1. **Decode as UTF-8.** If the bytes are not valid UTF-8, the consensus
 * document contributes **nothing**: no keys at all, and the merged view
 * is the configuration metadata alone.
 * 2. **Parse as JSON.** If the decoded text does not parse as JSON, the
 * consensus document again contributes nothing.
 * 3. **Require a top-level object.** If the document parses to anything
 * other than a JSON object (an array, a bare string, a number, a
 * boolean, `null`), it has no top-level entries to project, so it
 * contributes nothing.
 * 4. **Project each top-level entry by its value's type.** A **string**
 * value contributes its contents, unquoted. A **number**, **boolean**,
 * or **`null`** contributes its JSON text (`42`, `true`, `null`) and
 * stops being distinguishable from a string that happens to read the
 * same. A **nested object or array** cannot be projected to a string and
 * is **skipped**: that key contributes nothing from consensus.
 * 5. **Skipped is "not defined", never "defined as empty".** A key skipped
 * by rule 4 is treated exactly as though consensus had not defined it:
 * the configuration value for that key stands if there is one, and if
 * there is none the key is absent from the merged view entirely. An
 * unprojectable value never surfaces as an empty string and never blanks
 * out a configuration value it would otherwise have overridden.
 *
 * Note what rules 1 to 3 mean in practice: a consensus document that is not
 * UTF-8 JSON with an object at its root is *invisible* to the merged view.
 * That is deliberate: a partial or guessed projection would be worse than
 * none, and it is also why the merged view is never evidence that the meta
 * module is empty.
 *
 * None of this destroys anything. Everything the projection skips or
 * flattens is still in [`ConsensusMetadata::value`] exactly as consensus
 * stores it, which is what to read for anything that depends on the
 * document's structure, its scalar types, or its precise bytes.
 */
export interface MetaLike {
  /**
   * The whole merged view.
   *
   * Every key from either source, with consensus values winning where both
   * define one, subject to the same projection rules as [`Meta::get`]. A
   * key is present here only if the projection could
   * represent it as a string: an undecodable, unparseable, or non-object
   * consensus document contributes no keys, and a key whose consensus
   * value is a nested object or array is skipped in favour of the
   * configuration value, or omitted if there is none. This map is
   * therefore a view for rendering, never an inventory of what the
   * federation's metadata contains; [`Meta::consensus_metadata`] is that.
   *
   * Ordered by key: the map is a
   * [`BTreeMap`](std::collections::BTreeMap) rather than a hash map so
   * that iteration order is deterministic, which matters both for
   * rendering a stable list and for tests. Bindings receive it as their
   * host language's ordinary map or dictionary type.
   *
   * # Errors
   *
   * The same as [`Meta::get`].
   */
  all(asyncOpts_?: { signal: AbortSignal }) /*throws*/ : Promise<MetaMap>
  /**
   * The raw configuration metadata, exactly as the federation's
   * configuration declares it.
   *
   * Synchronous and infallible: this comes from configuration the SDK
   * already holds locally, so there is nothing to fetch and nothing to
   * fail. No consensus values are merged in.
   */
  configMetadata(): MetaMap
  /**
   * The raw consensus metadata, or `None` if this federation has no meta
   * module.
   *
   * `None` is an ordinary answer, not a failure: a federation without a
   * meta module is perfectly well-formed, and this is why [`Meta`]
   * itself is unconditional while the capability facades are
   * `Option`-returning: the absence lives here, at the level of the one
   * thing that can actually be absent.
   *
   * The returned value is unprojected and carries its revision, so an
   * application can parse the document itself and can tell whether it
   * has changed since it last looked.
   *
   * # Errors
   *
   * The same as [`Meta::get`].
   */
  consensusMetadata(asyncOpts_?: {
    signal: AbortSignal
  }) /*throws*/ : Promise<ConsensusMetadata | undefined>
  /**
   * Looks up one key in the merged view.
   *
   * Returns the consensus value if the meta module defines this key with
   * a value the projection can represent, the configuration value if only
   * the configuration does, and `None` if neither does. Asynchronous and
   * fallible because reading consensus metadata may require contacting the
   * federation.
   *
   * **`None` is not proof the key is unset.** The projection is governed
   * by the rules on [`Meta`] and it declines rather than guesses: a
   * consensus document that is not valid UTF-8, does not parse as JSON, or
   * is not a JSON object contributes no keys at all, and a key whose value
   * is a nested object or array is skipped as though consensus had not
   * defined it (the configuration value, if any, then stands). A caller
   * that needs to distinguish "absent" from "present but not projectable"
   * must read [`Meta::consensus_metadata`] and interpret
   * [`ConsensusMetadata::value`] itself.
   *
   * # Errors
   *
   * [`FederationUnreachable`](crate::ErrorCode::FederationUnreachable),
   * [`Timeout`](crate::ErrorCode::Timeout), and
   * [`FederationClosed`](crate::ErrorCode::FederationClosed).
   */
  get(
    key: string,
    asyncOpts_?: { signal: AbortSignal },
  ) /*throws*/ : Promise<string | undefined>
}
/**
 * @deprecated Use `MetaLike` instead.
 */
export type MetaInterface = MetaLike

/**
 * The metadata facade for one federation.
 *
 * Obtained from [`Federation::meta`](crate::Federation::meta). Unlike the
 * capability facades this is unconditional: every federation has
 * configuration metadata, so there is always something here to read.
 *
 * # Two sources, one merged view
 *
 * A federation can describe itself in two places, and they are genuinely
 * different things:
 *
 * - **Configuration metadata** is baked into the federation's consensus
 * configuration. It is fixed for the life of that configuration, is
 * available locally without asking anyone, and is what a
 * [`FederationPreview`](crate::FederationPreview) shows before joining.
 * - **Consensus metadata** lives in the federation's meta module, is
 * agreed by the guardians at runtime, and is *revisioned*: the
 * guardians can change it, and each change bumps a revision number.
 * Not every federation runs a meta module.
 *
 * Most applications want neither of those specifically; they want to know
 * "what is this federation's welcome message" and to get the current
 * answer. [`Meta::get`] and [`Meta::all`] provide that as a merged view,
 * with a single precedence rule: **consensus metadata overrides
 * configuration metadata, per key**. A key present in both takes its value
 * from consensus, because consensus metadata is the one the guardians can
 * update; keys present in only one source appear unchanged.
 *
 * The raw sources stay available separately:
 * [`Meta::config_metadata`] and [`Meta::consensus_metadata`], so an
 * application that needs to know *where* a value came from, or that needs
 * the consensus revision, is not forced to work backwards from the merged
 * result.
 *
 * # The merged view is a lossy projection, and these are its exact rules
 *
 * The meta module stores arbitrary bytes ([`ConsensusMetadata::value`]),
 * in practice a UTF-8 JSON document but with no guarantee of either.
 * Turning those bytes into the flat `BTreeMap<String, String>` that
 * [`Meta::get`] and [`Meta::all`] return is a defined, lossy projection.
 * Every binding must behave identically here, so the rules are written down
 * rather than left to each implementation to settle:
 *
 * 1. **Decode as UTF-8.** If the bytes are not valid UTF-8, the consensus
 * document contributes **nothing**: no keys at all, and the merged view
 * is the configuration metadata alone.
 * 2. **Parse as JSON.** If the decoded text does not parse as JSON, the
 * consensus document again contributes nothing.
 * 3. **Require a top-level object.** If the document parses to anything
 * other than a JSON object (an array, a bare string, a number, a
 * boolean, `null`), it has no top-level entries to project, so it
 * contributes nothing.
 * 4. **Project each top-level entry by its value's type.** A **string**
 * value contributes its contents, unquoted. A **number**, **boolean**,
 * or **`null`** contributes its JSON text (`42`, `true`, `null`) and
 * stops being distinguishable from a string that happens to read the
 * same. A **nested object or array** cannot be projected to a string and
 * is **skipped**: that key contributes nothing from consensus.
 * 5. **Skipped is "not defined", never "defined as empty".** A key skipped
 * by rule 4 is treated exactly as though consensus had not defined it:
 * the configuration value for that key stands if there is one, and if
 * there is none the key is absent from the merged view entirely. An
 * unprojectable value never surfaces as an empty string and never blanks
 * out a configuration value it would otherwise have overridden.
 *
 * Note what rules 1 to 3 mean in practice: a consensus document that is not
 * UTF-8 JSON with an object at its root is *invisible* to the merged view.
 * That is deliberate: a partial or guessed projection would be worse than
 * none, and it is also why the merged view is never evidence that the meta
 * module is empty.
 *
 * None of this destroys anything. Everything the projection skips or
 * flattens is still in [`ConsensusMetadata::value`] exactly as consensus
 * stores it, which is what to read for anything that depends on the
 * document's structure, its scalar types, or its precise bytes.
 */
export class Meta extends UniffiAbstractObject implements MetaLike {
  readonly [uniffiTypeNameSymbol] = 'Meta'
  readonly [destructorGuardSymbol]: UniffiGcObject
  readonly [pointerLiteralSymbol]: UniffiHandle
  // No primary constructor declared for this class.
  private constructor(pointer: UniffiHandle) {
    super()
    this[pointerLiteralSymbol] = pointer
    this[destructorGuardSymbol] = uniffiTypeMetaObjectFactory.bless(pointer)
  }

  /**
   * The whole merged view.
   *
   * Every key from either source, with consensus values winning where both
   * define one, subject to the same projection rules as [`Meta::get`]. A
   * key is present here only if the projection could
   * represent it as a string: an undecodable, unparseable, or non-object
   * consensus document contributes no keys, and a key whose consensus
   * value is a nested object or array is skipped in favour of the
   * configuration value, or omitted if there is none. This map is
   * therefore a view for rendering, never an inventory of what the
   * federation's metadata contains; [`Meta::consensus_metadata`] is that.
   *
   * Ordered by key: the map is a
   * [`BTreeMap`](std::collections::BTreeMap) rather than a hash map so
   * that iteration order is deterministic, which matters both for
   * rendering a stable list and for tests. Bindings receive it as their
   * host language's ordinary map or dictionary type.
   *
   * # Errors
   *
   * The same as [`Meta::get`].
   */
  async all(asyncOpts_?: { signal: AbortSignal }): Promise<MetaMap> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_meta_all(
          uniffiTypeMetaObjectFactory.clonePointer(this),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterTypeMetaMap.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * The raw configuration metadata, exactly as the federation's
   * configuration declares it.
   *
   * Synchronous and infallible: this comes from configuration the SDK
   * already holds locally, so there is nothing to fetch and nothing to
   * fail. No consensus values are merged in.
   */
  configMetadata(): MetaMap {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_meta_config_metadata(
          uniffiTypeMetaObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterTypeMetaMap.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * The raw consensus metadata, or `None` if this federation has no meta
   * module.
   *
   * `None` is an ordinary answer, not a failure: a federation without a
   * meta module is perfectly well-formed, and this is why [`Meta`]
   * itself is unconditional while the capability facades are
   * `Option`-returning: the absence lives here, at the level of the one
   * thing that can actually be absent.
   *
   * The returned value is unprojected and carries its revision, so an
   * application can parse the document itself and can tell whether it
   * has changed since it last looked.
   *
   * # Errors
   *
   * The same as [`Meta::get`].
   */
  async consensusMetadata(asyncOpts_?: {
    signal: AbortSignal
  }): Promise<ConsensusMetadata | undefined> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_meta_consensus_metadata(
          uniffiTypeMetaObjectFactory.clonePointer(this),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterOptionalTypeConsensusMetadata.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * Looks up one key in the merged view.
   *
   * Returns the consensus value if the meta module defines this key with
   * a value the projection can represent, the configuration value if only
   * the configuration does, and `None` if neither does. Asynchronous and
   * fallible because reading consensus metadata may require contacting the
   * federation.
   *
   * **`None` is not proof the key is unset.** The projection is governed
   * by the rules on [`Meta`] and it declines rather than guesses: a
   * consensus document that is not valid UTF-8, does not parse as JSON, or
   * is not a JSON object contributes no keys at all, and a key whose value
   * is a nested object or array is skipped as though consensus had not
   * defined it (the configuration value, if any, then stands). A caller
   * that needs to distinguish "absent" from "present but not projectable"
   * must read [`Meta::consensus_metadata`] and interpret
   * [`ConsensusMetadata::value`] itself.
   *
   * # Errors
   *
   * [`FederationUnreachable`](crate::ErrorCode::FederationUnreachable),
   * [`Timeout`](crate::ErrorCode::Timeout), and
   * [`FederationClosed`](crate::ErrorCode::FederationClosed).
   */
  async get(
    key: string,
    asyncOpts_?: { signal: AbortSignal },
  ): Promise<string | undefined> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_meta_get(
          uniffiTypeMetaObjectFactory.clonePointer(this),
          FfiConverterString.lower(key, nativeModule().rustbuffer_alloc),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterOptionalString.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  uniffiDestroy(): void {
    const ptr = (this as any)[destructorGuardSymbol]
    if (ptr !== undefined) {
      const pointer = uniffiTypeMetaObjectFactory.pointer(this)
      uniffiTypeMetaObjectFactory.freePointer(pointer)
      uniffiTypeMetaObjectFactory.unbless(ptr)
      delete (this as any)[destructorGuardSymbol]
    }
  }

  static instanceOf(obj_: any): obj_ is Meta {
    return uniffiTypeMetaObjectFactory.isConcreteType(obj_)
  }
}

const uniffiTypeMetaObjectFactory: UniffiObjectFactory<MetaLike> = (() => {
  /// <reference lib="es2021" />
  const registry =
    typeof FinalizationRegistry !== 'undefined'
      ? new FinalizationRegistry<UniffiHandle>((heldValue: UniffiHandle) => {
          uniffiTypeMetaObjectFactory.freePointer(heldValue)
        })
      : null

  return {
    create(pointer: UniffiHandle): MetaLike {
      const instance = Object.create(Meta.prototype)
      instance[pointerLiteralSymbol] = pointer
      instance[destructorGuardSymbol] = this.bless(pointer)
      instance[uniffiTypeNameSymbol] = 'Meta'
      return instance
    },

    bless(p: UniffiHandle): UniffiGcObject {
      const ptr = {
        p, // make sure this object doesn't get optimized away.
        markDestroyed: () => undefined,
      }
      if (registry) {
        registry.register(ptr, p, ptr)
      }
      return ptr
    },

    unbless(ptr_: UniffiGcObject) {
      if (registry) {
        registry.unregister(ptr_)
      }
    },

    pointer(obj_: MetaLike): UniffiHandle {
      if ((obj_ as any)[destructorGuardSymbol] === undefined) {
        throw new UniffiInternalError.UnexpectedNullPointer()
      }
      return (obj_ as any)[pointerLiteralSymbol]
    },

    clonePointer(obj_: MetaLike): UniffiHandle {
      const pointer = this.pointer(obj_)
      return uniffiCaller.rustCall(
        /*caller:*/ (callStatus) =>
          nativeModule().uniffi_fedimint_sdk_fn_clone_meta(pointer, callStatus),
        /*liftString:*/ FfiConverterString.lift,
      )
    },

    freePointer(pointer: UniffiHandle): void {
      uniffiCaller.rustCall(
        /*caller:*/ (callStatus) =>
          nativeModule().uniffi_fedimint_sdk_fn_free_meta(pointer, callStatus),
        /*liftString:*/ FfiConverterString.lift,
      )
    },

    isConcreteType(obj_: any): obj_ is MetaLike {
      return (
        obj_[destructorGuardSymbol] && obj_[uniffiTypeNameSymbol] === 'Meta'
      )
    },
  }
})()
const FfiConverterTypeMeta = new FfiConverterObject(uniffiTypeMetaObjectFactory)

/**
 * A frozen, executable plan for one on-chain withdrawal.
 *
 * Produced by [`Onchain::quote`] and consumed by [`Onchain::send`]. The
 * accessors expose exactly what a user must approve and nothing else.
 *
 * The accessors do not all speak the same unit: the destination amount is
 * whole [`Sats`](crate::Sats), and the fee and total are millisatoshi
 * [`Amount`](crate::Amount)s. See the [unit note](Onchain) on this facade
 * and [`OnchainQuote::fee`] for why.
 */
export interface OnchainQuoteLike {
  /**
   * The amount that will arrive at the destination address.
   *
   * Whole [`Sats`](crate::Sats), because this is the figure that becomes
   * an output in the withdrawal transaction, and a Bitcoin output cannot
   * hold a fraction of a satoshi. It is the same number the caller passed
   * to [`Onchain::quote`].
   *
   * This is *not* what leaves the balance; see [`OnchainQuote::total`].
   */
  amount(): Sats
  /**
   * When this quote stops being executable.
   *
   * Past this point [`Onchain::send`] fails with
   * [`QuoteExpired`](crate::ErrorCode::QuoteExpired). On-chain quotes
   * tend to be shorter-lived than lightning ones, because the fee
   * estimate they carry tracks a moving mempool.
   */
  expiresAt(): Timestamp
  /**
   * The exact aggregate cost of this withdrawal, over and above
   * [`OnchainQuote::amount`].
   *
   * This is every debit the withdrawal incurs beyond the destination
   * output, summed with nothing rounded away: the chain fee for the
   * destination output, the cost of funding it from the balance, and the
   * change and dust that funding leaves behind.
   * [`OnchainQuote::fee_breakdown`] names those parts individually.
   *
   * It is an [`Amount`](crate::Amount) rather than [`Sats`](crate::Sats)
   * because that sum is genuinely not a whole number of satoshis; see the
   * [unit note](Onchain) on this facade.
   *
   * Display it as it stands, or round it up. Never round it down, and
   * never re-express it in satoshis with
   * [`sats_floor`](crate::Amount::sats_floor);
   * [`to_sats_exact`](crate::Amount::to_sats_exact) will normally return
   * `None` here.
   */
  fee(): Amount
  /**
   * [`OnchainQuote::fee`], split into the named parts it is made of.
   *
   * This exists so that "why is the fee 1,234,567 msat and not a round
   * number of sats" has an answer an application can put on screen,
   * behind a disclosure, next to the aggregate. It re-reports the same
   * money as [`OnchainQuote::fee`]; it is not an additional charge.
   *
   * The aggregate remains the figure to charge and to compare against a
   * balance; see [`OnchainSendFeeBreakdown`] for why a caller should not
   * re-derive it by summing.
   */
  feeBreakdown(): OnchainSendFeeBreakdown
  /**
   * The total that will be debited from the balance:
   * [`OnchainQuote::amount`] converted to millisatoshis, plus
   * [`OnchainQuote::fee`].
   *
   * This is the number to show as "you will pay", and it is exact.
   *
   * It is also the debit execution is authorised to make, exactly, not a
   * ceiling or a prediction: nothing else is ever debited than this. A
   * withdrawal that would cost anything else by the time it executes is
   * refused with [`QuoteChanged`](crate::ErrorCode::QuoteChanged), so the
   * user re-approves a new number instead of quietly paying a different
   * one. This is the figure [`OnchainSendDetails::total`] records.
   *
   * That exactness is about the authorised debit, not about what the
   * balance does moment to moment. Executing the quote submits a
   * transaction, and submitting one takes the notes that are to fund it
   * out of the spendable set before the federation has accepted anything,
   * so a funding attempt the federation then rejects can remove value and
   * restore it afterwards.
   *
   * Nothing else is ever debited than this total. What becomes of it when
   * the withdrawal does not happen is the ending's to say rather than the
   * quote's: [`OnchainSendState::Refunded`] is the one that promises no
   * lasting debit, reported only once the value is spendable again, and
   * [`OnchainSendState::Failed`] is the one that cannot promise it.
   */
  total(): Amount
}
/**
 * @deprecated Use `OnchainQuoteLike` instead.
 */
export type OnchainQuoteInterface = OnchainQuoteLike

/**
 * A frozen, executable plan for one on-chain withdrawal.
 *
 * Produced by [`Onchain::quote`] and consumed by [`Onchain::send`]. The
 * accessors expose exactly what a user must approve and nothing else.
 *
 * The accessors do not all speak the same unit: the destination amount is
 * whole [`Sats`](crate::Sats), and the fee and total are millisatoshi
 * [`Amount`](crate::Amount)s. See the [unit note](Onchain) on this facade
 * and [`OnchainQuote::fee`] for why.
 */
export class OnchainQuote
  extends UniffiAbstractObject
  implements OnchainQuoteLike
{
  readonly [uniffiTypeNameSymbol] = 'OnchainQuote'
  readonly [destructorGuardSymbol]: UniffiGcObject
  readonly [pointerLiteralSymbol]: UniffiHandle
  // No primary constructor declared for this class.
  private constructor(pointer: UniffiHandle) {
    super()
    this[pointerLiteralSymbol] = pointer
    this[destructorGuardSymbol] =
      uniffiTypeOnchainQuoteObjectFactory.bless(pointer)
  }

  /**
   * The amount that will arrive at the destination address.
   *
   * Whole [`Sats`](crate::Sats), because this is the figure that becomes
   * an output in the withdrawal transaction, and a Bitcoin output cannot
   * hold a fraction of a satoshi. It is the same number the caller passed
   * to [`Onchain::quote`].
   *
   * This is *not* what leaves the balance; see [`OnchainQuote::total`].
   */
  amount(): Sats {
    return FfiConverterTypeSats.lift(
      uniffiCaller.rustCall(
        /*caller:*/ (callStatus) => {
          return nativeModule().uniffi_fedimint_sdk_fn_method_onchainquote_amount(
            uniffiTypeOnchainQuoteObjectFactory.clonePointer(this),
            callStatus,
          )
        },
        /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      ),
    )
  }

  /**
   * When this quote stops being executable.
   *
   * Past this point [`Onchain::send`] fails with
   * [`QuoteExpired`](crate::ErrorCode::QuoteExpired). On-chain quotes
   * tend to be shorter-lived than lightning ones, because the fee
   * estimate they carry tracks a moving mempool.
   */
  expiresAt(): Timestamp {
    return FfiConverterTypeTimestamp.lift(
      uniffiCaller.rustCall(
        /*caller:*/ (callStatus) => {
          return nativeModule().uniffi_fedimint_sdk_fn_method_onchainquote_expires_at(
            uniffiTypeOnchainQuoteObjectFactory.clonePointer(this),
            callStatus,
          )
        },
        /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      ),
    )
  }

  /**
   * The exact aggregate cost of this withdrawal, over and above
   * [`OnchainQuote::amount`].
   *
   * This is every debit the withdrawal incurs beyond the destination
   * output, summed with nothing rounded away: the chain fee for the
   * destination output, the cost of funding it from the balance, and the
   * change and dust that funding leaves behind.
   * [`OnchainQuote::fee_breakdown`] names those parts individually.
   *
   * It is an [`Amount`](crate::Amount) rather than [`Sats`](crate::Sats)
   * because that sum is genuinely not a whole number of satoshis; see the
   * [unit note](Onchain) on this facade.
   *
   * Display it as it stands, or round it up. Never round it down, and
   * never re-express it in satoshis with
   * [`sats_floor`](crate::Amount::sats_floor);
   * [`to_sats_exact`](crate::Amount::to_sats_exact) will normally return
   * `None` here.
   */
  fee(): Amount {
    return FfiConverterTypeAmount.lift(
      uniffiCaller.rustCall(
        /*caller:*/ (callStatus) => {
          return nativeModule().uniffi_fedimint_sdk_fn_method_onchainquote_fee(
            uniffiTypeOnchainQuoteObjectFactory.clonePointer(this),
            callStatus,
          )
        },
        /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      ),
    )
  }

  /**
   * [`OnchainQuote::fee`], split into the named parts it is made of.
   *
   * This exists so that "why is the fee 1,234,567 msat and not a round
   * number of sats" has an answer an application can put on screen,
   * behind a disclosure, next to the aggregate. It re-reports the same
   * money as [`OnchainQuote::fee`]; it is not an additional charge.
   *
   * The aggregate remains the figure to charge and to compare against a
   * balance; see [`OnchainSendFeeBreakdown`] for why a caller should not
   * re-derive it by summing.
   */
  feeBreakdown(): OnchainSendFeeBreakdown {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_onchainquote_fee_breakdown(
          uniffiTypeOnchainQuoteObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterTypeOnchainSendFeeBreakdown.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * The total that will be debited from the balance:
   * [`OnchainQuote::amount`] converted to millisatoshis, plus
   * [`OnchainQuote::fee`].
   *
   * This is the number to show as "you will pay", and it is exact.
   *
   * It is also the debit execution is authorised to make, exactly, not a
   * ceiling or a prediction: nothing else is ever debited than this. A
   * withdrawal that would cost anything else by the time it executes is
   * refused with [`QuoteChanged`](crate::ErrorCode::QuoteChanged), so the
   * user re-approves a new number instead of quietly paying a different
   * one. This is the figure [`OnchainSendDetails::total`] records.
   *
   * That exactness is about the authorised debit, not about what the
   * balance does moment to moment. Executing the quote submits a
   * transaction, and submitting one takes the notes that are to fund it
   * out of the spendable set before the federation has accepted anything,
   * so a funding attempt the federation then rejects can remove value and
   * restore it afterwards.
   *
   * Nothing else is ever debited than this total. What becomes of it when
   * the withdrawal does not happen is the ending's to say rather than the
   * quote's: [`OnchainSendState::Refunded`] is the one that promises no
   * lasting debit, reported only once the value is spendable again, and
   * [`OnchainSendState::Failed`] is the one that cannot promise it.
   */
  total(): Amount {
    return FfiConverterTypeAmount.lift(
      uniffiCaller.rustCall(
        /*caller:*/ (callStatus) => {
          return nativeModule().uniffi_fedimint_sdk_fn_method_onchainquote_total(
            uniffiTypeOnchainQuoteObjectFactory.clonePointer(this),
            callStatus,
          )
        },
        /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      ),
    )
  }

  uniffiDestroy(): void {
    const ptr = (this as any)[destructorGuardSymbol]
    if (ptr !== undefined) {
      const pointer = uniffiTypeOnchainQuoteObjectFactory.pointer(this)
      uniffiTypeOnchainQuoteObjectFactory.freePointer(pointer)
      uniffiTypeOnchainQuoteObjectFactory.unbless(ptr)
      delete (this as any)[destructorGuardSymbol]
    }
  }

  static instanceOf(obj_: any): obj_ is OnchainQuote {
    return uniffiTypeOnchainQuoteObjectFactory.isConcreteType(obj_)
  }
}

const uniffiTypeOnchainQuoteObjectFactory: UniffiObjectFactory<OnchainQuoteLike> =
  (() => {
    /// <reference lib="es2021" />
    const registry =
      typeof FinalizationRegistry !== 'undefined'
        ? new FinalizationRegistry<UniffiHandle>((heldValue: UniffiHandle) => {
            uniffiTypeOnchainQuoteObjectFactory.freePointer(heldValue)
          })
        : null

    return {
      create(pointer: UniffiHandle): OnchainQuoteLike {
        const instance = Object.create(OnchainQuote.prototype)
        instance[pointerLiteralSymbol] = pointer
        instance[destructorGuardSymbol] = this.bless(pointer)
        instance[uniffiTypeNameSymbol] = 'OnchainQuote'
        return instance
      },

      bless(p: UniffiHandle): UniffiGcObject {
        const ptr = {
          p, // make sure this object doesn't get optimized away.
          markDestroyed: () => undefined,
        }
        if (registry) {
          registry.register(ptr, p, ptr)
        }
        return ptr
      },

      unbless(ptr_: UniffiGcObject) {
        if (registry) {
          registry.unregister(ptr_)
        }
      },

      pointer(obj_: OnchainQuoteLike): UniffiHandle {
        if ((obj_ as any)[destructorGuardSymbol] === undefined) {
          throw new UniffiInternalError.UnexpectedNullPointer()
        }
        return (obj_ as any)[pointerLiteralSymbol]
      },

      clonePointer(obj_: OnchainQuoteLike): UniffiHandle {
        const pointer = this.pointer(obj_)
        return uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_clone_onchainquote(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      freePointer(pointer: UniffiHandle): void {
        uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_free_onchainquote(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      isConcreteType(obj_: any): obj_ is OnchainQuoteLike {
        return (
          obj_[destructorGuardSymbol] &&
          obj_[uniffiTypeNameSymbol] === 'OnchainQuote'
        )
      },
    }
  })()
const FfiConverterTypeOnchainQuote = new FfiConverterObject(
  uniffiTypeOnchainQuoteObjectFactory,
)

// Enum: OnchainSendState
export enum OnchainSendState_Tags {
  Created = 'Created',
  Succeeded = 'Succeeded',
  Refunded = 'Refunded',
  Failed = 'Failed',
}
/**
 * The lifecycle of an on-chain withdrawal.
 *
 * The four variants are the application-level lifecycle: accepted,
 * broadcast, did not happen with the funds safe, or did not resolve. The
 * last two are kept apart for the same reason
 * [`LnSendState`](crate::LnSendState) keeps `Refunded` and `Failed` apart:
 * whether the money is known to be safe is exactly what an application has
 * to tell the user.
 *
 * The terms the withdrawal was executed on (destination, amount, fee,
 * total) are not here. They belong to what the operation is rather than to
 * where it has got to, they are the same in every state, and a receipt has
 * to be renderable for a withdrawal that failed as much as for one that
 * succeeded. They live on [`OnchainSendDetails`].
 */

type OnchainSendState_Created_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'OnchainSendState'
  tag: OnchainSendState_Tags.Created
}

type OnchainSendState_Succeeded_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'OnchainSendState'
  tag: OnchainSendState_Tags.Succeeded
  inner: Readonly<{ txid: Txid }>
}

type OnchainSendState_Refunded_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'OnchainSendState'
  tag: OnchainSendState_Tags.Refunded
  inner: Readonly<{ reason: string }>
}

type OnchainSendState_Failed_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'OnchainSendState'
  tag: OnchainSendState_Tags.Failed
  inner: Readonly<{ reason: string }>
}
export type OnchainSendState =
  | OnchainSendState_Created_interface
  | OnchainSendState_Succeeded_interface
  | OnchainSendState_Refunded_interface
  | OnchainSendState_Failed_interface

export const OnchainSendState = (() => {
  /**
   * The withdrawal has been accepted and the federation is assembling
   * and signing the transaction.
   */
  class Created_
    extends UniffiEnum
    implements OnchainSendState_Created_interface
  {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'OnchainSendState'
    readonly tag = OnchainSendState_Tags.Created
    constructor() {
      super('OnchainSendState', 'Created')
    }

    static new(): Created_ {
      return new Created_()
    }

    static instanceOf(obj: any): obj is Created_ {
      return obj.tag === OnchainSendState_Tags.Created
    }
  }
  /**
   * Final: the federation broadcast the transaction.
   *
   * The funds have left the federation. Confirmation on the Bitcoin
   * network happens afterwards and is not tracked here.
   */
  class Succeeded_
    extends UniffiEnum
    implements OnchainSendState_Succeeded_interface
  {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'OnchainSendState'
    readonly tag = OnchainSendState_Tags.Succeeded
    readonly inner: Readonly<{ txid: Txid }>
    constructor(inner: { txid: Txid }) {
      super('OnchainSendState', 'Succeeded')

      this.inner = Object.freeze(inner)
    }
    static new(inner: { txid: Txid }): Succeeded_ {
      return new Succeeded_(inner)
    }

    static instanceOf(obj: any): obj is Succeeded_ {
      return obj.tag === OnchainSendState_Tags.Succeeded
    }
  }
  /**
   * Final: the withdrawal did not happen and the value it was authorised
   * for is spendable again.
   *
   * The federation rejected the transaction that would have funded the
   * withdrawal, and the value that transaction removed has come back.
   * Like [`LnSendState::Refunded`](crate::LnSendState::Refunded) this is
   * a success from the SDK's point of view: no part of the authorised
   * total is still standing against the balance, and the user quotes
   * again.
   *
   * The two halves of that are separate events and this state waits for
   * the second. Funding selects the notes that are to pay for the
   * withdrawal before consensus has accepted anything, so the rejection
   * leaves value that is neither spent nor yet spendable, and returning
   * it is a later transaction of the mint's own. The withdrawal stays
   * non-final while that runs, and reports this state only once it has
   * settled in a way that establishes the value came back. A recovery
   * that settles without establishing it ends in
   * [`Failed`](Self::Failed) instead.
   */
  class Refunded_
    extends UniffiEnum
    implements OnchainSendState_Refunded_interface
  {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'OnchainSendState'
    readonly tag = OnchainSendState_Tags.Refunded
    readonly inner: Readonly<{ reason: string }>
    constructor(inner: { reason: string }) {
      super('OnchainSendState', 'Refunded')

      this.inner = Object.freeze(inner)
    }
    static new(inner: { reason: string }): Refunded_ {
      return new Refunded_(inner)
    }

    static instanceOf(obj: any): obj is Refunded_ {
      return obj.tag === OnchainSendState_Tags.Refunded
    }
  }
  /**
   * Final: the withdrawal failed in a way that did not resolve into a
   * clean return.
   *
   * Either the funding was accepted and no transaction came of it, or it
   * was rejected and the value it removed could not be established as
   * spendable again. Either way this state cannot say where the funds
   * are. Render it as an error the user should report, and read the
   * balance for the rest; it is not the ordinary "rejected, try again"
   * ending, which is [`Refunded`](Self::Refunded).
   */
  class Failed_
    extends UniffiEnum
    implements OnchainSendState_Failed_interface
  {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'OnchainSendState'
    readonly tag = OnchainSendState_Tags.Failed
    readonly inner: Readonly<{ reason: string }>
    constructor(inner: { reason: string }) {
      super('OnchainSendState', 'Failed')

      this.inner = Object.freeze(inner)
    }
    static new(inner: { reason: string }): Failed_ {
      return new Failed_(inner)
    }

    static instanceOf(obj: any): obj is Failed_ {
      return obj.tag === OnchainSendState_Tags.Failed
    }
  }

  function instanceOf(obj: any): obj is OnchainSendState {
    return obj[uniffiTypeNameSymbol] === 'OnchainSendState'
  }

  return Object.freeze({
    instanceOf,
    Created: Created_,
    Succeeded: Succeeded_,
    Refunded: Refunded_,
    Failed: Failed_,
  })
})()

// FfiConverter for enum OnchainSendState
const FfiConverterTypeOnchainSendState = (() => {
  type TypeName = OnchainSendState
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      switch (c.readI32()) {
        case 1:
          return new OnchainSendState.Created()
        case 2:
          return new OnchainSendState.Succeeded({
            txid: FfiConverterTypeTxid.readFromCursor(c),
          })
        case 3:
          return new OnchainSendState.Refunded({
            reason: FfiConverterString.readFromCursor(c),
          })
        case 4:
          return new OnchainSendState.Failed({
            reason: FfiConverterString.readFromCursor(c),
          })
        default:
          throw new UniffiInternalError.UnexpectedEnumCase()
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      switch (value.tag) {
        case OnchainSendState_Tags.Created: {
          c.writeI32(1)
          return
        }
        case OnchainSendState_Tags.Succeeded: {
          c.writeI32(2)
          const inner = value.inner
          FfiConverterTypeTxid.writeIntoCursor(inner.txid, c)
          return
        }
        case OnchainSendState_Tags.Refunded: {
          c.writeI32(3)
          const inner = value.inner
          FfiConverterString.writeIntoCursor(inner.reason, c)
          return
        }
        case OnchainSendState_Tags.Failed: {
          c.writeI32(4)
          const inner = value.inner
          FfiConverterString.writeIntoCursor(inner.reason, c)
          return
        }
        default:
          // Throwing from here means that OnchainSendState_Tags hasn't matched an ordinal.
          throw new UniffiInternalError.UnexpectedEnumCase()
      }
    }
    allocationSize(value: TypeName): number {
      switch (value.tag) {
        case OnchainSendState_Tags.Created: {
          return 4
        }
        case OnchainSendState_Tags.Succeeded: {
          const inner = value.inner
          let size = 4
          size += FfiConverterTypeTxid.allocationSize(inner.txid)
          return size
        }
        case OnchainSendState_Tags.Refunded: {
          const inner = value.inner
          let size = 4
          size += FfiConverterString.allocationSize(inner.reason)
          return size
        }
        case OnchainSendState_Tags.Failed: {
          const inner = value.inner
          let size = 4
          size += FfiConverterString.allocationSize(inner.reason)
          return size
        }
        default:
          throw new UniffiInternalError.UnexpectedEnumCase()
      }
    }
  }
  return new FFIConverter()
})()

/**
 * The UniFFI view of one `OperationUpdates<S>` instantiation, behind a lock so `next`'s
 * `&mut self` can cross as `&self`.
 */
export interface OnchainSendOperationUpdatesLike {
  /**
   * See [`OperationUpdates::next`](crate::OperationUpdates::next).
   */
  next(asyncOpts_?: {
    signal: AbortSignal
  }) /*throws*/ : Promise<OnchainSendState | undefined>
}
/**
 * @deprecated Use `OnchainSendOperationUpdatesLike` instead.
 */
export type OnchainSendOperationUpdatesInterface =
  OnchainSendOperationUpdatesLike

/**
 * The UniFFI view of one `OperationUpdates<S>` instantiation, behind a lock so `next`'s
 * `&mut self` can cross as `&self`.
 */
export class OnchainSendOperationUpdates
  extends UniffiAbstractObject
  implements OnchainSendOperationUpdatesLike
{
  readonly [uniffiTypeNameSymbol] = 'OnchainSendOperationUpdates'
  readonly [destructorGuardSymbol]: UniffiGcObject
  readonly [pointerLiteralSymbol]: UniffiHandle
  // No primary constructor declared for this class.
  private constructor(pointer: UniffiHandle) {
    super()
    this[pointerLiteralSymbol] = pointer
    this[destructorGuardSymbol] =
      uniffiTypeOnchainSendOperationUpdatesObjectFactory.bless(pointer)
  }

  /**
   * See [`OperationUpdates::next`](crate::OperationUpdates::next).
   */
  async next(asyncOpts_?: {
    signal: AbortSignal
  }): Promise<OnchainSendState | undefined> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_onchainsendoperationupdates_next(
          uniffiTypeOnchainSendOperationUpdatesObjectFactory.clonePointer(this),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterOptionalTypeOnchainSendState.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  uniffiDestroy(): void {
    const ptr = (this as any)[destructorGuardSymbol]
    if (ptr !== undefined) {
      const pointer =
        uniffiTypeOnchainSendOperationUpdatesObjectFactory.pointer(this)
      uniffiTypeOnchainSendOperationUpdatesObjectFactory.freePointer(pointer)
      uniffiTypeOnchainSendOperationUpdatesObjectFactory.unbless(ptr)
      delete (this as any)[destructorGuardSymbol]
    }
  }

  static instanceOf(obj_: any): obj_ is OnchainSendOperationUpdates {
    return uniffiTypeOnchainSendOperationUpdatesObjectFactory.isConcreteType(
      obj_,
    )
  }
}

const uniffiTypeOnchainSendOperationUpdatesObjectFactory: UniffiObjectFactory<OnchainSendOperationUpdatesLike> =
  (() => {
    /// <reference lib="es2021" />
    const registry =
      typeof FinalizationRegistry !== 'undefined'
        ? new FinalizationRegistry<UniffiHandle>((heldValue: UniffiHandle) => {
            uniffiTypeOnchainSendOperationUpdatesObjectFactory.freePointer(
              heldValue,
            )
          })
        : null

    return {
      create(pointer: UniffiHandle): OnchainSendOperationUpdatesLike {
        const instance = Object.create(OnchainSendOperationUpdates.prototype)
        instance[pointerLiteralSymbol] = pointer
        instance[destructorGuardSymbol] = this.bless(pointer)
        instance[uniffiTypeNameSymbol] = 'OnchainSendOperationUpdates'
        return instance
      },

      bless(p: UniffiHandle): UniffiGcObject {
        const ptr = {
          p, // make sure this object doesn't get optimized away.
          markDestroyed: () => undefined,
        }
        if (registry) {
          registry.register(ptr, p, ptr)
        }
        return ptr
      },

      unbless(ptr_: UniffiGcObject) {
        if (registry) {
          registry.unregister(ptr_)
        }
      },

      pointer(obj_: OnchainSendOperationUpdatesLike): UniffiHandle {
        if ((obj_ as any)[destructorGuardSymbol] === undefined) {
          throw new UniffiInternalError.UnexpectedNullPointer()
        }
        return (obj_ as any)[pointerLiteralSymbol]
      },

      clonePointer(obj_: OnchainSendOperationUpdatesLike): UniffiHandle {
        const pointer = this.pointer(obj_)
        return uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_clone_onchainsendoperationupdates(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      freePointer(pointer: UniffiHandle): void {
        uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_free_onchainsendoperationupdates(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      isConcreteType(obj_: any): obj_ is OnchainSendOperationUpdatesLike {
        return (
          obj_[destructorGuardSymbol] &&
          obj_[uniffiTypeNameSymbol] === 'OnchainSendOperationUpdates'
        )
      },
    }
  })()
const FfiConverterTypeOnchainSendOperationUpdates = new FfiConverterObject(
  uniffiTypeOnchainSendOperationUpdatesObjectFactory,
)

/**
 * The UniFFI view of one `Operation<S>` instantiation: an opaque object wrapping the real
 * handle, forwarding every method.
 */
export interface OnchainSendOperationLike {
  /**
   * See [`Operation::await_final`](crate::Operation::await_final).
   */
  awaitFinal(asyncOpts_?: {
    signal: AbortSignal
  }) /*throws*/ : Promise<OnchainSendState>
  /**
   * See [`Operation::details`](crate::Operation::details).
   */
  details(asyncOpts_?: {
    signal: AbortSignal
  }) /*throws*/ : Promise<OnchainSendDetails>
  /**
   * See [`Operation::id`](crate::Operation::id).
   */
  id(): OperationId
  /**
   * See [`Operation::state`](crate::Operation::state).
   */
  state(asyncOpts_?: {
    signal: AbortSignal
  }) /*throws*/ : Promise<OnchainSendState>
  /**
   * See [`Operation::updates`](crate::Operation::updates).
   */
  updates(): OnchainSendOperationUpdatesLike
}
/**
 * @deprecated Use `OnchainSendOperationLike` instead.
 */
export type OnchainSendOperationInterface = OnchainSendOperationLike

/**
 * The UniFFI view of one `Operation<S>` instantiation: an opaque object wrapping the real
 * handle, forwarding every method.
 */
export class OnchainSendOperation
  extends UniffiAbstractObject
  implements OnchainSendOperationLike
{
  readonly [uniffiTypeNameSymbol] = 'OnchainSendOperation'
  readonly [destructorGuardSymbol]: UniffiGcObject
  readonly [pointerLiteralSymbol]: UniffiHandle
  // No primary constructor declared for this class.
  private constructor(pointer: UniffiHandle) {
    super()
    this[pointerLiteralSymbol] = pointer
    this[destructorGuardSymbol] =
      uniffiTypeOnchainSendOperationObjectFactory.bless(pointer)
  }

  /**
   * See [`Operation::await_final`](crate::Operation::await_final).
   */
  async awaitFinal(asyncOpts_?: {
    signal: AbortSignal
  }): Promise<OnchainSendState> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_onchainsendoperation_await_final(
          uniffiTypeOnchainSendOperationObjectFactory.clonePointer(this),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterTypeOnchainSendState.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * See [`Operation::details`](crate::Operation::details).
   */
  async details(asyncOpts_?: {
    signal: AbortSignal
  }): Promise<OnchainSendDetails> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_onchainsendoperation_details(
          uniffiTypeOnchainSendOperationObjectFactory.clonePointer(this),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterTypeOnchainSendDetails.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * See [`Operation::id`](crate::Operation::id).
   */
  id(): OperationId {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_onchainsendoperation_id(
          uniffiTypeOnchainSendOperationObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterTypeOperationId.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * See [`Operation::state`](crate::Operation::state).
   */
  async state(asyncOpts_?: {
    signal: AbortSignal
  }): Promise<OnchainSendState> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_onchainsendoperation_state(
          uniffiTypeOnchainSendOperationObjectFactory.clonePointer(this),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterTypeOnchainSendState.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * See [`Operation::updates`](crate::Operation::updates).
   */
  updates(): OnchainSendOperationUpdatesLike {
    return FfiConverterTypeOnchainSendOperationUpdates.lift(
      uniffiCaller.rustCall(
        /*caller:*/ (callStatus) => {
          return nativeModule().uniffi_fedimint_sdk_fn_method_onchainsendoperation_updates(
            uniffiTypeOnchainSendOperationObjectFactory.clonePointer(this),
            callStatus,
          )
        },
        /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      ),
    )
  }

  uniffiDestroy(): void {
    const ptr = (this as any)[destructorGuardSymbol]
    if (ptr !== undefined) {
      const pointer = uniffiTypeOnchainSendOperationObjectFactory.pointer(this)
      uniffiTypeOnchainSendOperationObjectFactory.freePointer(pointer)
      uniffiTypeOnchainSendOperationObjectFactory.unbless(ptr)
      delete (this as any)[destructorGuardSymbol]
    }
  }

  static instanceOf(obj_: any): obj_ is OnchainSendOperation {
    return uniffiTypeOnchainSendOperationObjectFactory.isConcreteType(obj_)
  }
}

const uniffiTypeOnchainSendOperationObjectFactory: UniffiObjectFactory<OnchainSendOperationLike> =
  (() => {
    /// <reference lib="es2021" />
    const registry =
      typeof FinalizationRegistry !== 'undefined'
        ? new FinalizationRegistry<UniffiHandle>((heldValue: UniffiHandle) => {
            uniffiTypeOnchainSendOperationObjectFactory.freePointer(heldValue)
          })
        : null

    return {
      create(pointer: UniffiHandle): OnchainSendOperationLike {
        const instance = Object.create(OnchainSendOperation.prototype)
        instance[pointerLiteralSymbol] = pointer
        instance[destructorGuardSymbol] = this.bless(pointer)
        instance[uniffiTypeNameSymbol] = 'OnchainSendOperation'
        return instance
      },

      bless(p: UniffiHandle): UniffiGcObject {
        const ptr = {
          p, // make sure this object doesn't get optimized away.
          markDestroyed: () => undefined,
        }
        if (registry) {
          registry.register(ptr, p, ptr)
        }
        return ptr
      },

      unbless(ptr_: UniffiGcObject) {
        if (registry) {
          registry.unregister(ptr_)
        }
      },

      pointer(obj_: OnchainSendOperationLike): UniffiHandle {
        if ((obj_ as any)[destructorGuardSymbol] === undefined) {
          throw new UniffiInternalError.UnexpectedNullPointer()
        }
        return (obj_ as any)[pointerLiteralSymbol]
      },

      clonePointer(obj_: OnchainSendOperationLike): UniffiHandle {
        const pointer = this.pointer(obj_)
        return uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_clone_onchainsendoperation(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      freePointer(pointer: UniffiHandle): void {
        uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_free_onchainsendoperation(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      isConcreteType(obj_: any): obj_ is OnchainSendOperationLike {
        return (
          obj_[destructorGuardSymbol] &&
          obj_[uniffiTypeNameSymbol] === 'OnchainSendOperation'
        )
      },
    }
  })()
const FfiConverterTypeOnchainSendOperation = new FfiConverterObject(
  uniffiTypeOnchainSendOperationObjectFactory,
)

/**
 * The on-chain facade for one federation, backed by its wallet module.
 *
 * Obtained from [`Federation::onchain`](crate::Federation::onchain), which
 * returns `None` when the federation has no wallet module.
 *
 * # Units: [`Sats`] for what moves on chain, [`Amount`] for what it costs
 *
 * A value here is [`Sats`](crate::Sats) when it is a figure that exists on
 * the Bitcoin chain, and [`Amount`](crate::Amount) when it is a figure that
 * exists inside the federation.
 *
 * - **Whole satoshis.** The amount that arrives at a withdrawal's
 * destination ([`OnchainQuote::amount`], [`Onchain::quote`]'s `amount`
 * argument, [`OnchainSendDetails::amount`]) and the gross amount a
 * deposit transaction pays in ([`OnchainReceiveState::Claimed`],
 * [`OnchainReceiveDetails::gross_deposited`]). Bitcoin has no sub-satoshi
 * unit, so these genuinely are whole satoshis.
 * - **Exact millisatoshis.** Every fee, every total debit, and the net
 * amount a deposit credits to the balance ([`OnchainQuote::fee`],
 * [`OnchainQuote::total`], [`OnchainSendDetails::total`],
 * [`OnchainReceiveState::Claimed`],
 * [`OnchainReceiveDetails::net_credit`]). A withdrawal's cost is more
 * than the chain fee for the destination output, and a deposit's cost is
 * a federation fee taken out of what arrives, so these sums are
 * routinely not whole satoshis.
 *
 * No conversion happens behind a caller's back. Moving between the two
 * units is always explicit: [`Sats::to_amount`](crate::Sats::to_amount)
 * upward, which is exact (one satoshi is exactly 1000 msat), and
 * [`Amount::to_sats_exact`](crate::Amount::to_sats_exact) downward, which
 * refuses rather than truncates.
 *
 * # The recovery lock applies to both directions
 *
 * Every call on this facade, deposits as much as withdrawals, is refused
 * with [`Recovering`](crate::ErrorCode::Recovering) while this federation's
 * recovery is incomplete. An attempt that stopped short holds the lock
 * exactly as firmly as one still in progress, and only a recovery that
 * reaches completion releases it. There is no acknowledge, no override, and
 * no way to spend or receive on a partially restored wallet.
 */
export interface OnchainLike {
  /**
   * Plans a withdrawal and returns an executable quote for it.
   *
   * Like its lightning counterpart, this exists because the cost is only
   * knowable after the SDK has worked out how the federation will build
   * and broadcast the transaction. The returned [`OnchainQuote`] binds
   * the destination address, the amount, the aggregate fee, the total
   * debit, and the federation configuration those were computed against,
   * and [`Onchain::send`] executes exactly that or refuses.
   *
   * `amount` is in whole [`Sats`](crate::Sats) because it is the amount
   * that will appear in the withdrawal transaction's output. The fee and
   * total that come back are [`Amount`](crate::Amount)s, because they are
   * not whole satoshis; see the [unit note](Onchain) on this facade and
   * [`OnchainQuote::fee`].
   *
   * This is also where the address's network is checked against the
   * federation's. A well-formed address for the wrong chain is caught
   * here, with
   * [`NetworkMismatch`](crate::ErrorCode::NetworkMismatch), rather than
   * after the funds have moved, since parsing an
   * [`Address`](crate::Address) cannot do this check: at parse time there
   * is no federation to compare against.
   *
   * # Errors
   *
   * [`NetworkMismatch`](crate::ErrorCode::NetworkMismatch),
   * [`InvalidInput`](crate::ErrorCode::InvalidInput) for an amount the
   * federation cannot withdraw (zero, or below its dust threshold),
   * [`InsufficientBalance`](crate::ErrorCode::InsufficientBalance) when
   * the balance cannot cover [`OnchainQuote::total`],
   * [`Recovering`](crate::ErrorCode::Recovering) while this federation's
   * recovery is incomplete,
   * [`NotSupported`](crate::ErrorCode::NotSupported),
   * [`FederationUnreachable`](crate::ErrorCode::FederationUnreachable),
   * [`Timeout`](crate::ErrorCode::Timeout), and
   * [`FederationClosed`](crate::ErrorCode::FederationClosed).
   */
  quote(
    address: Address,
    amount: Sats,
    asyncOpts_?: { signal: AbortSignal },
  ) /*throws*/ : Promise<OnchainQuoteLike>
  /**
   * See [`Onchain::receive`].
   */
  receive(asyncOpts_?: {
    signal: AbortSignal
  }) /*throws*/ : Promise<OnchainReceiveHandle>
  /**
   * See [`Onchain::send`]. Fails with
   * [`ErrorCode::QuoteExpired`](crate::ErrorCode::QuoteExpired) if `quote` was already sent.
   */
  send(
    quote: OnchainQuoteLike,
    asyncOpts_?: { signal: AbortSignal },
  ) /*throws*/ : Promise<OnchainSendOperationLike>
}
/**
 * @deprecated Use `OnchainLike` instead.
 */
export type OnchainInterface = OnchainLike

/**
 * The on-chain facade for one federation, backed by its wallet module.
 *
 * Obtained from [`Federation::onchain`](crate::Federation::onchain), which
 * returns `None` when the federation has no wallet module.
 *
 * # Units: [`Sats`] for what moves on chain, [`Amount`] for what it costs
 *
 * A value here is [`Sats`](crate::Sats) when it is a figure that exists on
 * the Bitcoin chain, and [`Amount`](crate::Amount) when it is a figure that
 * exists inside the federation.
 *
 * - **Whole satoshis.** The amount that arrives at a withdrawal's
 * destination ([`OnchainQuote::amount`], [`Onchain::quote`]'s `amount`
 * argument, [`OnchainSendDetails::amount`]) and the gross amount a
 * deposit transaction pays in ([`OnchainReceiveState::Claimed`],
 * [`OnchainReceiveDetails::gross_deposited`]). Bitcoin has no sub-satoshi
 * unit, so these genuinely are whole satoshis.
 * - **Exact millisatoshis.** Every fee, every total debit, and the net
 * amount a deposit credits to the balance ([`OnchainQuote::fee`],
 * [`OnchainQuote::total`], [`OnchainSendDetails::total`],
 * [`OnchainReceiveState::Claimed`],
 * [`OnchainReceiveDetails::net_credit`]). A withdrawal's cost is more
 * than the chain fee for the destination output, and a deposit's cost is
 * a federation fee taken out of what arrives, so these sums are
 * routinely not whole satoshis.
 *
 * No conversion happens behind a caller's back. Moving between the two
 * units is always explicit: [`Sats::to_amount`](crate::Sats::to_amount)
 * upward, which is exact (one satoshi is exactly 1000 msat), and
 * [`Amount::to_sats_exact`](crate::Amount::to_sats_exact) downward, which
 * refuses rather than truncates.
 *
 * # The recovery lock applies to both directions
 *
 * Every call on this facade, deposits as much as withdrawals, is refused
 * with [`Recovering`](crate::ErrorCode::Recovering) while this federation's
 * recovery is incomplete. An attempt that stopped short holds the lock
 * exactly as firmly as one still in progress, and only a recovery that
 * reaches completion releases it. There is no acknowledge, no override, and
 * no way to spend or receive on a partially restored wallet.
 */
export class Onchain extends UniffiAbstractObject implements OnchainLike {
  readonly [uniffiTypeNameSymbol] = 'Onchain'
  readonly [destructorGuardSymbol]: UniffiGcObject
  readonly [pointerLiteralSymbol]: UniffiHandle
  // No primary constructor declared for this class.
  private constructor(pointer: UniffiHandle) {
    super()
    this[pointerLiteralSymbol] = pointer
    this[destructorGuardSymbol] = uniffiTypeOnchainObjectFactory.bless(pointer)
  }

  /**
   * Plans a withdrawal and returns an executable quote for it.
   *
   * Like its lightning counterpart, this exists because the cost is only
   * knowable after the SDK has worked out how the federation will build
   * and broadcast the transaction. The returned [`OnchainQuote`] binds
   * the destination address, the amount, the aggregate fee, the total
   * debit, and the federation configuration those were computed against,
   * and [`Onchain::send`] executes exactly that or refuses.
   *
   * `amount` is in whole [`Sats`](crate::Sats) because it is the amount
   * that will appear in the withdrawal transaction's output. The fee and
   * total that come back are [`Amount`](crate::Amount)s, because they are
   * not whole satoshis; see the [unit note](Onchain) on this facade and
   * [`OnchainQuote::fee`].
   *
   * This is also where the address's network is checked against the
   * federation's. A well-formed address for the wrong chain is caught
   * here, with
   * [`NetworkMismatch`](crate::ErrorCode::NetworkMismatch), rather than
   * after the funds have moved, since parsing an
   * [`Address`](crate::Address) cannot do this check: at parse time there
   * is no federation to compare against.
   *
   * # Errors
   *
   * [`NetworkMismatch`](crate::ErrorCode::NetworkMismatch),
   * [`InvalidInput`](crate::ErrorCode::InvalidInput) for an amount the
   * federation cannot withdraw (zero, or below its dust threshold),
   * [`InsufficientBalance`](crate::ErrorCode::InsufficientBalance) when
   * the balance cannot cover [`OnchainQuote::total`],
   * [`Recovering`](crate::ErrorCode::Recovering) while this federation's
   * recovery is incomplete,
   * [`NotSupported`](crate::ErrorCode::NotSupported),
   * [`FederationUnreachable`](crate::ErrorCode::FederationUnreachable),
   * [`Timeout`](crate::ErrorCode::Timeout), and
   * [`FederationClosed`](crate::ErrorCode::FederationClosed).
   */
  async quote(
    address: Address,
    amount: Sats,
    asyncOpts_?: { signal: AbortSignal },
  ): Promise<OnchainQuoteLike> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_onchain_quote(
          uniffiTypeOnchainObjectFactory.clonePointer(this),
          FfiConverterTypeAddress.lower(
            address,
            nativeModule().rustbuffer_alloc,
          ),
          FfiConverterTypeSats.lower(amount, nativeModule().rustbuffer_alloc),
        )
      },
      /*pollFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_poll_u64,
      /*cancelFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_cancel_u64,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_u64,
      /*freeFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_free_u64,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      /*liftFunc:*/ FfiConverterTypeOnchainQuote.lift.bind(
        FfiConverterTypeOnchainQuote,
      ),
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * See [`Onchain::receive`].
   */
  async receive(asyncOpts_?: {
    signal: AbortSignal
  }): Promise<OnchainReceiveHandle> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_onchain_receive(
          uniffiTypeOnchainObjectFactory.clonePointer(this),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterTypeOnchainReceiveHandle.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * See [`Onchain::send`]. Fails with
   * [`ErrorCode::QuoteExpired`](crate::ErrorCode::QuoteExpired) if `quote` was already sent.
   */
  async send(
    quote: OnchainQuoteLike,
    asyncOpts_?: { signal: AbortSignal },
  ): Promise<OnchainSendOperationLike> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_onchain_send(
          uniffiTypeOnchainObjectFactory.clonePointer(this),
          FfiConverterTypeOnchainQuote.lower(
            quote,
            nativeModule().rustbuffer_alloc,
          ),
        )
      },
      /*pollFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_poll_u64,
      /*cancelFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_cancel_u64,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_u64,
      /*freeFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_free_u64,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      /*liftFunc:*/ FfiConverterTypeOnchainSendOperation.lift.bind(
        FfiConverterTypeOnchainSendOperation,
      ),
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  uniffiDestroy(): void {
    const ptr = (this as any)[destructorGuardSymbol]
    if (ptr !== undefined) {
      const pointer = uniffiTypeOnchainObjectFactory.pointer(this)
      uniffiTypeOnchainObjectFactory.freePointer(pointer)
      uniffiTypeOnchainObjectFactory.unbless(ptr)
      delete (this as any)[destructorGuardSymbol]
    }
  }

  static instanceOf(obj_: any): obj_ is Onchain {
    return uniffiTypeOnchainObjectFactory.isConcreteType(obj_)
  }
}

const uniffiTypeOnchainObjectFactory: UniffiObjectFactory<OnchainLike> =
  (() => {
    /// <reference lib="es2021" />
    const registry =
      typeof FinalizationRegistry !== 'undefined'
        ? new FinalizationRegistry<UniffiHandle>((heldValue: UniffiHandle) => {
            uniffiTypeOnchainObjectFactory.freePointer(heldValue)
          })
        : null

    return {
      create(pointer: UniffiHandle): OnchainLike {
        const instance = Object.create(Onchain.prototype)
        instance[pointerLiteralSymbol] = pointer
        instance[destructorGuardSymbol] = this.bless(pointer)
        instance[uniffiTypeNameSymbol] = 'Onchain'
        return instance
      },

      bless(p: UniffiHandle): UniffiGcObject {
        const ptr = {
          p, // make sure this object doesn't get optimized away.
          markDestroyed: () => undefined,
        }
        if (registry) {
          registry.register(ptr, p, ptr)
        }
        return ptr
      },

      unbless(ptr_: UniffiGcObject) {
        if (registry) {
          registry.unregister(ptr_)
        }
      },

      pointer(obj_: OnchainLike): UniffiHandle {
        if ((obj_ as any)[destructorGuardSymbol] === undefined) {
          throw new UniffiInternalError.UnexpectedNullPointer()
        }
        return (obj_ as any)[pointerLiteralSymbol]
      },

      clonePointer(obj_: OnchainLike): UniffiHandle {
        const pointer = this.pointer(obj_)
        return uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_clone_onchain(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      freePointer(pointer: UniffiHandle): void {
        uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_free_onchain(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      isConcreteType(obj_: any): obj_ is OnchainLike {
        return (
          obj_[destructorGuardSymbol] &&
          obj_[uniffiTypeNameSymbol] === 'Onchain'
        )
      },
    }
  })()
const FfiConverterTypeOnchain = new FfiConverterObject(
  uniffiTypeOnchainObjectFactory,
)

// Enum: RecoveryState
export enum RecoveryState_Tags {
  Running = 'Running',
  Done = 'Done',
  Failed = 'Failed',
}
/**
 * How a recovery is going.
 *
 * Deliberately coarse: this reports only what can be said truthfully, without a made-up
 * completion percentage.
 *
 * # Two different questions
 *
 * This enum answers both, and they are not the same question:
 *
 * - *Is the attempt over?*
 * [`OperationState::is_final`](crate::OperationState::is_final), true
 * for [`Done`](Self::Done) and for [`Failed`](Self::Failed), because
 * neither transitions again. Retrying starts a *new* operation with a
 * new id rather than reviving this one.
 * - *Is the wallet restored, and is the federation spendable?*
 * [`is_complete`](Self::is_complete), true for [`Done`](Self::Done)
 * only.
 *
 * Reading the first as an answer to the second is the trap: it would let a
 * stopped recovery look like a finished one and an incomplete wallet look
 * spendable. The module documentation states the invariant.
 */

type RecoveryState_Running_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'RecoveryState'
  tag: RecoveryState_Tags.Running
}

type RecoveryState_Done_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'RecoveryState'
  tag: RecoveryState_Tags.Done
}

type RecoveryState_Failed_interface = {
  /**
   * @private
   * This field is private and should not be used, use `tag` instead.
   */
  readonly [uniffiTypeNameSymbol]: 'RecoveryState'
  tag: RecoveryState_Tags.Failed
  inner: Readonly<{ reason: string }>
}
export type RecoveryState =
  | RecoveryState_Running_interface
  | RecoveryState_Done_interface
  | RecoveryState_Failed_interface

export const RecoveryState = (() => {
  /**
   * The rescan is running. Spends and receives are refused with
   * [`Recovering`](crate::ErrorCode::Recovering); balance and activity
   * are incomplete.
   */
  class Running_ extends UniffiEnum implements RecoveryState_Running_interface {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'RecoveryState'
    readonly tag = RecoveryState_Tags.Running
    constructor() {
      super('RecoveryState', 'Running')
    }

    static new(): Running_ {
      return new Running_()
    }

    static instanceOf(obj: any): obj is Running_ {
      return obj.tag === RecoveryState_Tags.Running
    }
  }
  /**
   * Final, and the wallet is recovered: the federation behaves like any
   * other joined federation and the recovery lock is released.
   *
   * This is the only state that releases the lock, and the only one for
   * which [`is_complete`](Self::is_complete) is true. It says the wallet
   * is restored: everything the seed owned in this federation that a
   * rescan can find has been found.
   */
  class Done_ extends UniffiEnum implements RecoveryState_Done_interface {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'RecoveryState'
    readonly tag = RecoveryState_Tags.Done
    constructor() {
      super('RecoveryState', 'Done')
    }

    static new(): Done_ {
      return new Done_()
    }

    static instanceOf(obj: any): obj is Done_ {
      return obj.tag === RecoveryState_Tags.Done
    }
  }
  /**
   * Final for this attempt, and the wallet is **not** recovered.
   *
   * The rescan stopped before completing. The federation stays joined
   * and stays recovery-locked: spends and receives keep failing with
   * [`Recovering`](crate::ErrorCode::Recovering), because a wallet whose
   * note set was never fully discovered is not safe to spend from
   * whether the rescan is still running or has given up. This state
   * releases nothing, [`is_complete`](Self::is_complete) is false, even
   * though [`OperationState::is_final`](crate::OperationState::is_final)
   * is true for it, which is a statement about the operation and not
   * about the wallet.
   *
   * Two things can follow, and nothing else:
   *
   * - **Retry.** [`Sdk::resume_recovery`] starts a fresh attempt for
   * this federation. It needs only the [`FederationId`], because the
   * federation is already joined. A retry can also arrive without
   * being asked for: reopening the federation restarts a stopped
   * rescan by itself, minting the new attempt exactly as
   * `resume_recovery` would, see the module documentation.
   * - **Erase and start over.** If retrying keeps stopping, the
   * documented exit is to erase the federation's local state and
   * recover it again from the invite code, with the costs the module
   * documentation lists. There is no third option that makes this
   * wallet spendable as it is.
   */
  class Failed_ extends UniffiEnum implements RecoveryState_Failed_interface {
    /**
     * @private
     * This field is private and should not be used, use `tag` instead.
     */
    readonly [uniffiTypeNameSymbol] = 'RecoveryState'
    readonly tag = RecoveryState_Tags.Failed
    readonly inner: Readonly<{ reason: string }>
    constructor(inner: { reason: string }) {
      super('RecoveryState', 'Failed')

      this.inner = Object.freeze(inner)
    }
    static new(inner: { reason: string }): Failed_ {
      return new Failed_(inner)
    }

    static instanceOf(obj: any): obj is Failed_ {
      return obj.tag === RecoveryState_Tags.Failed
    }
  }

  function instanceOf(obj: any): obj is RecoveryState {
    return obj[uniffiTypeNameSymbol] === 'RecoveryState'
  }

  return Object.freeze({
    instanceOf,
    Running: Running_,
    Done: Done_,
    Failed: Failed_,
  })
})()

// FfiConverter for enum RecoveryState
const FfiConverterTypeRecoveryState = (() => {
  type TypeName = RecoveryState
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      switch (c.readI32()) {
        case 1:
          return new RecoveryState.Running()
        case 2:
          return new RecoveryState.Done()
        case 3:
          return new RecoveryState.Failed({
            reason: FfiConverterString.readFromCursor(c),
          })
        default:
          throw new UniffiInternalError.UnexpectedEnumCase()
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      switch (value.tag) {
        case RecoveryState_Tags.Running: {
          c.writeI32(1)
          return
        }
        case RecoveryState_Tags.Done: {
          c.writeI32(2)
          return
        }
        case RecoveryState_Tags.Failed: {
          c.writeI32(3)
          const inner = value.inner
          FfiConverterString.writeIntoCursor(inner.reason, c)
          return
        }
        default:
          // Throwing from here means that RecoveryState_Tags hasn't matched an ordinal.
          throw new UniffiInternalError.UnexpectedEnumCase()
      }
    }
    allocationSize(value: TypeName): number {
      switch (value.tag) {
        case RecoveryState_Tags.Running: {
          return 4
        }
        case RecoveryState_Tags.Done: {
          return 4
        }
        case RecoveryState_Tags.Failed: {
          const inner = value.inner
          let size = 4
          size += FfiConverterString.allocationSize(inner.reason)
          return size
        }
        default:
          throw new UniffiInternalError.UnexpectedEnumCase()
      }
    }
  }
  return new FFIConverter()
})()

/**
 * The UniFFI view of one `OperationUpdates<S>` instantiation, behind a lock so `next`'s
 * `&mut self` can cross as `&self`.
 */
export interface RecoveryOperationUpdatesLike {
  /**
   * See [`OperationUpdates::next`](crate::OperationUpdates::next).
   */
  next(asyncOpts_?: {
    signal: AbortSignal
  }) /*throws*/ : Promise<RecoveryState | undefined>
}
/**
 * @deprecated Use `RecoveryOperationUpdatesLike` instead.
 */
export type RecoveryOperationUpdatesInterface = RecoveryOperationUpdatesLike

/**
 * The UniFFI view of one `OperationUpdates<S>` instantiation, behind a lock so `next`'s
 * `&mut self` can cross as `&self`.
 */
export class RecoveryOperationUpdates
  extends UniffiAbstractObject
  implements RecoveryOperationUpdatesLike
{
  readonly [uniffiTypeNameSymbol] = 'RecoveryOperationUpdates'
  readonly [destructorGuardSymbol]: UniffiGcObject
  readonly [pointerLiteralSymbol]: UniffiHandle
  // No primary constructor declared for this class.
  private constructor(pointer: UniffiHandle) {
    super()
    this[pointerLiteralSymbol] = pointer
    this[destructorGuardSymbol] =
      uniffiTypeRecoveryOperationUpdatesObjectFactory.bless(pointer)
  }

  /**
   * See [`OperationUpdates::next`](crate::OperationUpdates::next).
   */
  async next(asyncOpts_?: {
    signal: AbortSignal
  }): Promise<RecoveryState | undefined> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_recoveryoperationupdates_next(
          uniffiTypeRecoveryOperationUpdatesObjectFactory.clonePointer(this),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterOptionalTypeRecoveryState.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  uniffiDestroy(): void {
    const ptr = (this as any)[destructorGuardSymbol]
    if (ptr !== undefined) {
      const pointer =
        uniffiTypeRecoveryOperationUpdatesObjectFactory.pointer(this)
      uniffiTypeRecoveryOperationUpdatesObjectFactory.freePointer(pointer)
      uniffiTypeRecoveryOperationUpdatesObjectFactory.unbless(ptr)
      delete (this as any)[destructorGuardSymbol]
    }
  }

  static instanceOf(obj_: any): obj_ is RecoveryOperationUpdates {
    return uniffiTypeRecoveryOperationUpdatesObjectFactory.isConcreteType(obj_)
  }
}

const uniffiTypeRecoveryOperationUpdatesObjectFactory: UniffiObjectFactory<RecoveryOperationUpdatesLike> =
  (() => {
    /// <reference lib="es2021" />
    const registry =
      typeof FinalizationRegistry !== 'undefined'
        ? new FinalizationRegistry<UniffiHandle>((heldValue: UniffiHandle) => {
            uniffiTypeRecoveryOperationUpdatesObjectFactory.freePointer(
              heldValue,
            )
          })
        : null

    return {
      create(pointer: UniffiHandle): RecoveryOperationUpdatesLike {
        const instance = Object.create(RecoveryOperationUpdates.prototype)
        instance[pointerLiteralSymbol] = pointer
        instance[destructorGuardSymbol] = this.bless(pointer)
        instance[uniffiTypeNameSymbol] = 'RecoveryOperationUpdates'
        return instance
      },

      bless(p: UniffiHandle): UniffiGcObject {
        const ptr = {
          p, // make sure this object doesn't get optimized away.
          markDestroyed: () => undefined,
        }
        if (registry) {
          registry.register(ptr, p, ptr)
        }
        return ptr
      },

      unbless(ptr_: UniffiGcObject) {
        if (registry) {
          registry.unregister(ptr_)
        }
      },

      pointer(obj_: RecoveryOperationUpdatesLike): UniffiHandle {
        if ((obj_ as any)[destructorGuardSymbol] === undefined) {
          throw new UniffiInternalError.UnexpectedNullPointer()
        }
        return (obj_ as any)[pointerLiteralSymbol]
      },

      clonePointer(obj_: RecoveryOperationUpdatesLike): UniffiHandle {
        const pointer = this.pointer(obj_)
        return uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_clone_recoveryoperationupdates(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      freePointer(pointer: UniffiHandle): void {
        uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_free_recoveryoperationupdates(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      isConcreteType(obj_: any): obj_ is RecoveryOperationUpdatesLike {
        return (
          obj_[destructorGuardSymbol] &&
          obj_[uniffiTypeNameSymbol] === 'RecoveryOperationUpdates'
        )
      },
    }
  })()
const FfiConverterTypeRecoveryOperationUpdates = new FfiConverterObject(
  uniffiTypeRecoveryOperationUpdatesObjectFactory,
)

/**
 * The UniFFI view of one `Operation<S>` instantiation: an opaque object wrapping the real
 * handle, forwarding every method.
 */
export interface RecoveryOperationLike {
  /**
   * See [`Operation::await_final`](crate::Operation::await_final).
   */
  awaitFinal(asyncOpts_?: {
    signal: AbortSignal
  }) /*throws*/ : Promise<RecoveryState>
  /**
   * See [`Operation::id`](crate::Operation::id).
   */
  id(): OperationId
  /**
   * See [`Operation::state`](crate::Operation::state).
   */
  state(asyncOpts_?: {
    signal: AbortSignal
  }) /*throws*/ : Promise<RecoveryState>
  /**
   * See [`Operation::updates`](crate::Operation::updates).
   */
  updates(): RecoveryOperationUpdatesLike
}
/**
 * @deprecated Use `RecoveryOperationLike` instead.
 */
export type RecoveryOperationInterface = RecoveryOperationLike

/**
 * The UniFFI view of one `Operation<S>` instantiation: an opaque object wrapping the real
 * handle, forwarding every method.
 */
export class RecoveryOperation
  extends UniffiAbstractObject
  implements RecoveryOperationLike
{
  readonly [uniffiTypeNameSymbol] = 'RecoveryOperation'
  readonly [destructorGuardSymbol]: UniffiGcObject
  readonly [pointerLiteralSymbol]: UniffiHandle
  // No primary constructor declared for this class.
  private constructor(pointer: UniffiHandle) {
    super()
    this[pointerLiteralSymbol] = pointer
    this[destructorGuardSymbol] =
      uniffiTypeRecoveryOperationObjectFactory.bless(pointer)
  }

  /**
   * See [`Operation::await_final`](crate::Operation::await_final).
   */
  async awaitFinal(asyncOpts_?: {
    signal: AbortSignal
  }): Promise<RecoveryState> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_recoveryoperation_await_final(
          uniffiTypeRecoveryOperationObjectFactory.clonePointer(this),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterTypeRecoveryState.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * See [`Operation::id`](crate::Operation::id).
   */
  id(): OperationId {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_recoveryoperation_id(
          uniffiTypeRecoveryOperationObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterTypeOperationId.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * See [`Operation::state`](crate::Operation::state).
   */
  async state(asyncOpts_?: {
    signal: AbortSignal
  }): Promise<RecoveryState> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_recoveryoperation_state(
          uniffiTypeRecoveryOperationObjectFactory.clonePointer(this),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterTypeRecoveryState.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * See [`Operation::updates`](crate::Operation::updates).
   */
  updates(): RecoveryOperationUpdatesLike {
    return FfiConverterTypeRecoveryOperationUpdates.lift(
      uniffiCaller.rustCall(
        /*caller:*/ (callStatus) => {
          return nativeModule().uniffi_fedimint_sdk_fn_method_recoveryoperation_updates(
            uniffiTypeRecoveryOperationObjectFactory.clonePointer(this),
            callStatus,
          )
        },
        /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      ),
    )
  }

  uniffiDestroy(): void {
    const ptr = (this as any)[destructorGuardSymbol]
    if (ptr !== undefined) {
      const pointer = uniffiTypeRecoveryOperationObjectFactory.pointer(this)
      uniffiTypeRecoveryOperationObjectFactory.freePointer(pointer)
      uniffiTypeRecoveryOperationObjectFactory.unbless(ptr)
      delete (this as any)[destructorGuardSymbol]
    }
  }

  static instanceOf(obj_: any): obj_ is RecoveryOperation {
    return uniffiTypeRecoveryOperationObjectFactory.isConcreteType(obj_)
  }
}

const uniffiTypeRecoveryOperationObjectFactory: UniffiObjectFactory<RecoveryOperationLike> =
  (() => {
    /// <reference lib="es2021" />
    const registry =
      typeof FinalizationRegistry !== 'undefined'
        ? new FinalizationRegistry<UniffiHandle>((heldValue: UniffiHandle) => {
            uniffiTypeRecoveryOperationObjectFactory.freePointer(heldValue)
          })
        : null

    return {
      create(pointer: UniffiHandle): RecoveryOperationLike {
        const instance = Object.create(RecoveryOperation.prototype)
        instance[pointerLiteralSymbol] = pointer
        instance[destructorGuardSymbol] = this.bless(pointer)
        instance[uniffiTypeNameSymbol] = 'RecoveryOperation'
        return instance
      },

      bless(p: UniffiHandle): UniffiGcObject {
        const ptr = {
          p, // make sure this object doesn't get optimized away.
          markDestroyed: () => undefined,
        }
        if (registry) {
          registry.register(ptr, p, ptr)
        }
        return ptr
      },

      unbless(ptr_: UniffiGcObject) {
        if (registry) {
          registry.unregister(ptr_)
        }
      },

      pointer(obj_: RecoveryOperationLike): UniffiHandle {
        if ((obj_ as any)[destructorGuardSymbol] === undefined) {
          throw new UniffiInternalError.UnexpectedNullPointer()
        }
        return (obj_ as any)[pointerLiteralSymbol]
      },

      clonePointer(obj_: RecoveryOperationLike): UniffiHandle {
        const pointer = this.pointer(obj_)
        return uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_clone_recoveryoperation(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      freePointer(pointer: UniffiHandle): void {
        uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_free_recoveryoperation(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      isConcreteType(obj_: any): obj_ is RecoveryOperationLike {
        return (
          obj_[destructorGuardSymbol] &&
          obj_[uniffiTypeNameSymbol] === 'RecoveryOperation'
        )
      },
    }
  })()
const FfiConverterTypeRecoveryOperation = new FfiConverterObject(
  uniffiTypeRecoveryOperationObjectFactory,
)

/**
 * How far this build can go with one persisted operation, decided before any
 * of its state is read.
 *
 * Returned by [`AnyOperation::support`].
 * [`Observable`](OperationSupport::Observable) is the one answer that means
 * supported; every other variant names the condition that failed and means
 * there is no typed handle to be had in this build. They are different
 * things to say and to do: "written by a newer version than this one" is an
 * application that needs updating, "this build does not recognise it at
 * all" is one to put in a bug report verbatim.
 * [`AnyOperation::supported_kind`] flattens both into
 * [`UnsupportedOperation`](crate::ErrorCode::UnsupportedOperation) for
 * control flow, but a log line, a support ticket, or a message shown to a
 * user should use this type instead.
 */
export enum OperationSupport {
  /**
   * This build can observe the operation's typed state: the kind is one it
   * knows, and the recorded state schema is one it reads.
   *
   * The matching `as_*` accessor on [`AnyOperation`] returns `Some`, and
   * [`AnyOperation::supported_kind`] returns `Ok`. This is everything
   * establishable before the state is read, which is not the same as a
   * promise that reading it will succeed.
   */
  Observable,
  /**
   * The persisted discriminator is not one this build maps onto a kind, so
   * there is nothing to interpret it as.
   *
   * [`AnyOperation::kind`] reports [`OperationKind::Unknown`] for the same
   * record, and [`AnyOperation::raw_kind`] says what was actually written.
   */
  UnknownKind,
  /**
   * The kind is known, but the record's state was written at a schema
   * version newer than this build reads.
   *
   * This describes a gap between the record and this build, not a defect
   * in the record: the operation is real, the state is intact, and a
   * build of the version that wrote it reads it fine.
   */
  StateSchemaTooNew,
}

const FfiConverterTypeOperationSupport = (() => {
  type TypeName = OperationSupport
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      switch (c.readI32()) {
        case 1:
          return OperationSupport.Observable
        case 2:
          return OperationSupport.UnknownKind
        case 3:
          return OperationSupport.StateSchemaTooNew
        default:
          throw new UniffiInternalError.UnexpectedEnumCase()
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      switch (value) {
        case OperationSupport.Observable:
          return c.writeI32(1)
        case OperationSupport.UnknownKind:
          return c.writeI32(2)
        case OperationSupport.StateSchemaTooNew:
          return c.writeI32(3)
      }
    }
    allocationSize(value: TypeName): number {
      return 4
    }
  }
  return new FFIConverter()
})()

/**
 * An operation whose kind is known only at runtime.
 *
 * Returned by [`Federation::operation`](crate::Federation::operation),
 * which looks an operation up by id and therefore cannot know statically
 * what kind it is: the id may have come from persisted state, from an
 * [`ActivityItem`](crate::ActivityItem), or from another process's
 * notification. Read [`AnyOperation::kind`] to find out, then use the
 * matching accessor to recover a typed [`Operation`].
 *
 * Like [`Operation`], this is an observation handle over a detached,
 * persisted operation, and it is a cheap clone.
 *
 * # What "supported" means here
 *
 * An operation is supported when this build can actually observe its typed
 * state, which takes two things: the persisted discriminator maps onto a
 * known [`OperationKind`] other than [`Unknown`](OperationKind::Unknown),
 * and the persisted state schema is one this build reads (a record written
 * by a newer SDK can name a kind this build knows while using a state schema
 * it has never seen).
 *
 * Four calls answer four different questions about a record:
 * [`kind`](AnyOperation::kind) says what it is and never
 * fails; [`support`](AnyOperation::support) says how far this build can go
 * with it and why no further, as a plain value for logs and bug reports;
 * [`supported_kind`](AnyOperation::supported_kind) is the gate to pass
 * before acting on the operation, returning
 * [`UnsupportedOperation`](crate::ErrorCode::UnsupportedOperation) when it
 * is not supported; and [`raw_kind`](AnyOperation::raw_kind) gives the
 * persisted discriminator verbatim, for logs and bug reports.
 *
 * The seven `as_*` accessors return `Some` only when the kind matches and
 * the operation is supported, and `None` otherwise, without distinguishing
 * a plain kind mismatch from an unsupported record of the matching kind:
 * use [`support`](AnyOperation::support) or
 * [`supported_kind`](AnyOperation::supported_kind) first when the answer
 * matters, for example before showing an error to the user. This keeps a
 * caller from being handed a typed [`Operation`] whose state later fails to
 * decode from [`Operation::state`] instead of from this gate.
 *
 * This determination is made before any state is read, so it is not a
 * promise that reading the state will succeed. A record whose schema is
 * unrecorded is treated as not ruled out. The residue, a record that passed
 * both checks and still cannot be decoded, surfaces as
 * [`Internal`](crate::ErrorCode::Internal) from [`Operation::state`], not as
 * [`UnsupportedOperation`](crate::ErrorCode::UnsupportedOperation).
 */
export interface AnyOperationLike {
  /**
   * See [`AnyOperation::as_ecash_receive`].
   */
  asEcashReceive(): EcashReceiveOperationLike | undefined
  /**
   * See [`AnyOperation::as_ecash_send`].
   */
  asEcashSend(): EcashSendOperationLike | undefined
  /**
   * See [`AnyOperation::as_ln_receive`].
   */
  asLnReceive(): LnReceiveOperationLike | undefined
  /**
   * See [`AnyOperation::as_ln_send`].
   */
  asLnSend(): LnSendOperationLike | undefined
  /**
   * See [`AnyOperation::as_onchain_receive`].
   */
  asOnchainReceive(): OnchainReceiveOperationLike | undefined
  /**
   * See [`AnyOperation::as_onchain_send`].
   */
  asOnchainSend(): OnchainSendOperationLike | undefined
  /**
   * See [`AnyOperation::as_recovery`].
   */
  asRecovery(): RecoveryOperationLike | undefined
  /**
   * This operation's id.
   */
  id(): OperationId
  /**
   * What kind of operation this is.
   *
   * Never fails: an operation recorded by a build that understood
   * something this one does not still has an id, still has a row, and
   * reports [`OperationKind::Unknown`] here, see that variant. Use
   * [`raw_kind`](AnyOperation::raw_kind) to find out what it was recorded
   * as, and [`supported_kind`](AnyOperation::supported_kind) instead of
   * this when the next thing you do is act on the operation rather than
   * label it.
   *
   * This is the reading of the discriminator and nothing more, so it
   * answers with a real kind even when nothing can be done with the
   * operation: a record whose state was written at a schema this build
   * cannot read still says it is a lightning send, reported as unsupported
   * by [`support`](AnyOperation::support).
   */
  kind(): OperationKind
  /**
   * The discriminator this operation was persisted under, verbatim.
   *
   * [`kind`](AnyOperation::kind) is this SDK's reading of the record; this
   * is what the record actually says. The difference matters when the
   * reading is [`OperationKind::Unknown`]: an application that reports the
   * module and tag it did not recognise gives a user something to show
   * and a maintainer something to fix. Available for every kind, not only
   * the unknown one.
   *
   * For humans, never for control flow: use [`kind`](AnyOperation::kind)
   * and [`supported_kind`](AnyOperation::supported_kind) to branch on.
   * Infallible, like [`kind`](AnyOperation::kind): the record was read
   * when this handle was created.
   */
  rawKind(): RawOperationKind
  /**
   * How far this build can go with this operation, and why no further.
   *
   * The reason behind [`supported_kind`](AnyOperation::supported_kind), as
   * an ordinary value instead of an error, for a log line, a bug report,
   * or a message to a user. [`Observable`](OperationSupport::Observable)
   * means supported: the matching `as_*` accessor will hand back a typed
   * handle. Every other variant names the reason it will not, pair it with
   * [`raw_kind`](AnyOperation::raw_kind) to say which record it was about.
   *
   * Infallible and cheap: it reads no storage, touches no network, and
   * does not read the operation's state, so it is not a promise that
   * reading the state will succeed.
   */
  support(): OperationSupport
  /**
   * This operation's kind if this build can observe its typed state, and
   * [`UnsupportedOperation`](crate::ErrorCode::UnsupportedOperation) if it
   * cannot.
   *
   * The fallible twin of [`kind`](AnyOperation::kind) and the gate to pass
   * before acting on an operation. `Ok(kind)` means the accessor for that
   * kind will hand back a typed handle: it is never
   * `Ok(OperationKind::Unknown)`, and never `Ok` for a record whose state
   * was written at a schema version newer than this build reads, even when
   * the kind itself is one this build knows.
   * [`support`](AnyOperation::support) answers the same question and says
   * which condition failed, without an error.
   *
   * # Errors
   *
   * [`UnsupportedOperation`](crate::ErrorCode::UnsupportedOperation), and
   * nothing else, for an unrecognised discriminator or a state schema
   * newer than this build reads. This reads no storage and touches no
   * network: the record was already read to produce this handle.
   */
  supportedKind() /*throws*/ : OperationKind
}
/**
 * @deprecated Use `AnyOperationLike` instead.
 */
export type AnyOperationInterface = AnyOperationLike

/**
 * An operation whose kind is known only at runtime.
 *
 * Returned by [`Federation::operation`](crate::Federation::operation),
 * which looks an operation up by id and therefore cannot know statically
 * what kind it is: the id may have come from persisted state, from an
 * [`ActivityItem`](crate::ActivityItem), or from another process's
 * notification. Read [`AnyOperation::kind`] to find out, then use the
 * matching accessor to recover a typed [`Operation`].
 *
 * Like [`Operation`], this is an observation handle over a detached,
 * persisted operation, and it is a cheap clone.
 *
 * # What "supported" means here
 *
 * An operation is supported when this build can actually observe its typed
 * state, which takes two things: the persisted discriminator maps onto a
 * known [`OperationKind`] other than [`Unknown`](OperationKind::Unknown),
 * and the persisted state schema is one this build reads (a record written
 * by a newer SDK can name a kind this build knows while using a state schema
 * it has never seen).
 *
 * Four calls answer four different questions about a record:
 * [`kind`](AnyOperation::kind) says what it is and never
 * fails; [`support`](AnyOperation::support) says how far this build can go
 * with it and why no further, as a plain value for logs and bug reports;
 * [`supported_kind`](AnyOperation::supported_kind) is the gate to pass
 * before acting on the operation, returning
 * [`UnsupportedOperation`](crate::ErrorCode::UnsupportedOperation) when it
 * is not supported; and [`raw_kind`](AnyOperation::raw_kind) gives the
 * persisted discriminator verbatim, for logs and bug reports.
 *
 * The seven `as_*` accessors return `Some` only when the kind matches and
 * the operation is supported, and `None` otherwise, without distinguishing
 * a plain kind mismatch from an unsupported record of the matching kind:
 * use [`support`](AnyOperation::support) or
 * [`supported_kind`](AnyOperation::supported_kind) first when the answer
 * matters, for example before showing an error to the user. This keeps a
 * caller from being handed a typed [`Operation`] whose state later fails to
 * decode from [`Operation::state`] instead of from this gate.
 *
 * This determination is made before any state is read, so it is not a
 * promise that reading the state will succeed. A record whose schema is
 * unrecorded is treated as not ruled out. The residue, a record that passed
 * both checks and still cannot be decoded, surfaces as
 * [`Internal`](crate::ErrorCode::Internal) from [`Operation::state`], not as
 * [`UnsupportedOperation`](crate::ErrorCode::UnsupportedOperation).
 */
export class AnyOperation
  extends UniffiAbstractObject
  implements AnyOperationLike
{
  readonly [uniffiTypeNameSymbol] = 'AnyOperation'
  readonly [destructorGuardSymbol]: UniffiGcObject
  readonly [pointerLiteralSymbol]: UniffiHandle
  // No primary constructor declared for this class.
  private constructor(pointer: UniffiHandle) {
    super()
    this[pointerLiteralSymbol] = pointer
    this[destructorGuardSymbol] =
      uniffiTypeAnyOperationObjectFactory.bless(pointer)
  }

  /**
   * See [`AnyOperation::as_ecash_receive`].
   */
  asEcashReceive(): EcashReceiveOperationLike | undefined {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_anyoperation_as_ecash_receive(
          uniffiTypeAnyOperationObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterOptionalTypeEcashReceiveOperation.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * See [`AnyOperation::as_ecash_send`].
   */
  asEcashSend(): EcashSendOperationLike | undefined {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_anyoperation_as_ecash_send(
          uniffiTypeAnyOperationObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterOptionalTypeEcashSendOperation.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * See [`AnyOperation::as_ln_receive`].
   */
  asLnReceive(): LnReceiveOperationLike | undefined {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_anyoperation_as_ln_receive(
          uniffiTypeAnyOperationObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterOptionalTypeLnReceiveOperation.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * See [`AnyOperation::as_ln_send`].
   */
  asLnSend(): LnSendOperationLike | undefined {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_anyoperation_as_ln_send(
          uniffiTypeAnyOperationObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterOptionalTypeLnSendOperation.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * See [`AnyOperation::as_onchain_receive`].
   */
  asOnchainReceive(): OnchainReceiveOperationLike | undefined {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_anyoperation_as_onchain_receive(
          uniffiTypeAnyOperationObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterOptionalTypeOnchainReceiveOperation.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * See [`AnyOperation::as_onchain_send`].
   */
  asOnchainSend(): OnchainSendOperationLike | undefined {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_anyoperation_as_onchain_send(
          uniffiTypeAnyOperationObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterOptionalTypeOnchainSendOperation.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * See [`AnyOperation::as_recovery`].
   */
  asRecovery(): RecoveryOperationLike | undefined {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_anyoperation_as_recovery(
          uniffiTypeAnyOperationObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterOptionalTypeRecoveryOperation.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * This operation's id.
   */
  id(): OperationId {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_anyoperation_id(
          uniffiTypeAnyOperationObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterTypeOperationId.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * What kind of operation this is.
   *
   * Never fails: an operation recorded by a build that understood
   * something this one does not still has an id, still has a row, and
   * reports [`OperationKind::Unknown`] here, see that variant. Use
   * [`raw_kind`](AnyOperation::raw_kind) to find out what it was recorded
   * as, and [`supported_kind`](AnyOperation::supported_kind) instead of
   * this when the next thing you do is act on the operation rather than
   * label it.
   *
   * This is the reading of the discriminator and nothing more, so it
   * answers with a real kind even when nothing can be done with the
   * operation: a record whose state was written at a schema this build
   * cannot read still says it is a lightning send, reported as unsupported
   * by [`support`](AnyOperation::support).
   */
  kind(): OperationKind {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_anyoperation_kind(
          uniffiTypeAnyOperationObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterTypeOperationKind.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * The discriminator this operation was persisted under, verbatim.
   *
   * [`kind`](AnyOperation::kind) is this SDK's reading of the record; this
   * is what the record actually says. The difference matters when the
   * reading is [`OperationKind::Unknown`]: an application that reports the
   * module and tag it did not recognise gives a user something to show
   * and a maintainer something to fix. Available for every kind, not only
   * the unknown one.
   *
   * For humans, never for control flow: use [`kind`](AnyOperation::kind)
   * and [`supported_kind`](AnyOperation::supported_kind) to branch on.
   * Infallible, like [`kind`](AnyOperation::kind): the record was read
   * when this handle was created.
   */
  rawKind(): RawOperationKind {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_anyoperation_raw_kind(
          uniffiTypeAnyOperationObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterTypeRawOperationKind.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * How far this build can go with this operation, and why no further.
   *
   * The reason behind [`supported_kind`](AnyOperation::supported_kind), as
   * an ordinary value instead of an error, for a log line, a bug report,
   * or a message to a user. [`Observable`](OperationSupport::Observable)
   * means supported: the matching `as_*` accessor will hand back a typed
   * handle. Every other variant names the reason it will not, pair it with
   * [`raw_kind`](AnyOperation::raw_kind) to say which record it was about.
   *
   * Infallible and cheap: it reads no storage, touches no network, and
   * does not read the operation's state, so it is not a promise that
   * reading the state will succeed.
   */
  support(): OperationSupport {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_anyoperation_support(
          uniffiTypeAnyOperationObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterTypeOperationSupport.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * This operation's kind if this build can observe its typed state, and
   * [`UnsupportedOperation`](crate::ErrorCode::UnsupportedOperation) if it
   * cannot.
   *
   * The fallible twin of [`kind`](AnyOperation::kind) and the gate to pass
   * before acting on an operation. `Ok(kind)` means the accessor for that
   * kind will hand back a typed handle: it is never
   * `Ok(OperationKind::Unknown)`, and never `Ok` for a record whose state
   * was written at a schema version newer than this build reads, even when
   * the kind itself is one this build knows.
   * [`support`](AnyOperation::support) answers the same question and says
   * which condition failed, without an error.
   *
   * # Errors
   *
   * [`UnsupportedOperation`](crate::ErrorCode::UnsupportedOperation), and
   * nothing else, for an unrecognised discriminator or a state schema
   * newer than this build reads. This reads no storage and touches no
   * network: the record was already read to produce this handle.
   */
  supportedKind(): OperationKind /*throws*/ {
    const __rb: Uint8Array = uniffiCaller.rustCallWithError(
      /*liftError:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_anyoperation_supported_kind(
          uniffiTypeAnyOperationObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterTypeOperationKind.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  uniffiDestroy(): void {
    const ptr = (this as any)[destructorGuardSymbol]
    if (ptr !== undefined) {
      const pointer = uniffiTypeAnyOperationObjectFactory.pointer(this)
      uniffiTypeAnyOperationObjectFactory.freePointer(pointer)
      uniffiTypeAnyOperationObjectFactory.unbless(ptr)
      delete (this as any)[destructorGuardSymbol]
    }
  }

  static instanceOf(obj_: any): obj_ is AnyOperation {
    return uniffiTypeAnyOperationObjectFactory.isConcreteType(obj_)
  }
}

const uniffiTypeAnyOperationObjectFactory: UniffiObjectFactory<AnyOperationLike> =
  (() => {
    /// <reference lib="es2021" />
    const registry =
      typeof FinalizationRegistry !== 'undefined'
        ? new FinalizationRegistry<UniffiHandle>((heldValue: UniffiHandle) => {
            uniffiTypeAnyOperationObjectFactory.freePointer(heldValue)
          })
        : null

    return {
      create(pointer: UniffiHandle): AnyOperationLike {
        const instance = Object.create(AnyOperation.prototype)
        instance[pointerLiteralSymbol] = pointer
        instance[destructorGuardSymbol] = this.bless(pointer)
        instance[uniffiTypeNameSymbol] = 'AnyOperation'
        return instance
      },

      bless(p: UniffiHandle): UniffiGcObject {
        const ptr = {
          p, // make sure this object doesn't get optimized away.
          markDestroyed: () => undefined,
        }
        if (registry) {
          registry.register(ptr, p, ptr)
        }
        return ptr
      },

      unbless(ptr_: UniffiGcObject) {
        if (registry) {
          registry.unregister(ptr_)
        }
      },

      pointer(obj_: AnyOperationLike): UniffiHandle {
        if ((obj_ as any)[destructorGuardSymbol] === undefined) {
          throw new UniffiInternalError.UnexpectedNullPointer()
        }
        return (obj_ as any)[pointerLiteralSymbol]
      },

      clonePointer(obj_: AnyOperationLike): UniffiHandle {
        const pointer = this.pointer(obj_)
        return uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_clone_anyoperation(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      freePointer(pointer: UniffiHandle): void {
        uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_free_anyoperation(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      isConcreteType(obj_: any): obj_ is AnyOperationLike {
        return (
          obj_[destructorGuardSymbol] &&
          obj_[uniffiTypeNameSymbol] === 'AnyOperation'
        )
      },
    }
  })()
const FfiConverterTypeAnyOperation = new FfiConverterObject(
  uniffiTypeAnyOperationObjectFactory,
)

/**
 * A handle to one federation this SDK instance has joined.
 *
 * Everything an application does with a federation goes through this type:
 * reading its identity and balance, obtaining the [ecash](Federation::ecash),
 * [lightning](Federation::lightning) and [on-chain](Federation::onchain)
 * facades, reading [metadata](Federation::meta), reattaching to a running
 * [operation](Federation::operation), and paging through local
 * [activity](Federation::activity).
 *
 * Like the other handles in this crate it is a cheap clone over shared
 * state: every clone talks to the same running federation client, and it
 * is `Send + Sync` on native targets, with the same types compiled for a
 * single-threaded host on wasm.
 *
 * A handle keeps working until the federation is closed with
 * [`Sdk::close_federation`](crate::Sdk::close_federation), erased with
 * [`Sdk::forget_federation`](crate::Sdk::forget_federation), or the whole
 * instance is shut down. An application holding a stale handle degrades
 * into a reportable error rather than a crash: nothing here panics after a
 * close.
 *
 * # What a closed handle does
 *
 * "Fails with
 * [`FederationClosed`](crate::ErrorCode::FederationClosed)" applies to the
 * **fallible** calls: [`balance`](Federation::balance),
 * [`operation`](Federation::operation),
 * [`activity`](Federation::activity), and every call made through a
 * facade. The rest of this type returns plain
 * values and has no way to report a failure, so each has a defined closed
 * behaviour instead:
 *
 * - **The descriptive accessors keep answering.**
 * [`id`](Federation::id), [`name`](Federation::name),
 * [`network`](Federation::network),
 * [`invite_code`](Federation::invite_code) and
 * [`capabilities`](Federation::capabilities) go on returning the
 * configuration last known for this federation. A history screen can
 * still label rows with a federation that has been closed underneath it.
 * - **The facade accessors keep returning `Some`.**
 * [`ecash`](Federation::ecash), [`lightning`](Federation::lightning) and
 * [`onchain`](Federation::onchain) return a facade whenever the
 * federation had that module, closed or not, and the failure surfaces
 * from the facade call as
 * [`FederationClosed`](crate::ErrorCode::FederationClosed). Returning
 * `None` instead would be a lie with a specific documented meaning:
 * "this federation has no mint module", and would make a closed
 * federation indistinguishable from one that never supported ecash at
 * all. [`meta`](Federation::meta), which is unconditional, behaves the
 * same way.
 * - **[`balance_updates`](Federation::balance_updates) still hands out a
 * subscriber**, whose very first
 * [`next`](BalanceUpdates::next) yields
 * [`FederationClosed`](crate::ErrorCode::FederationClosed). The error is
 * where a caller can act on it, rather than being swallowed by an
 * accessor that cannot return one.
 */
export interface FederationLike {
  /**
   * Reads a page of local activity history, newest first.
   *
   * Pass `None` as `cursor` for the first page and the
   * [`next`](crate::ActivityPage::next) cursor of the previous page for
   * each following one. At most `limit` items are returned; fewer is
   * normal, and an empty page with no cursor means the end.
   *
   * Rows still in flight are brought up to date before the page is
   * returned, so a page that has any takes a moment longer than one
   * that has none.
   *
   * The history is *local*, see [`ActivityItem`](crate::ActivityItem)
   * for exactly what that excludes.
   *
   * # Errors
   *
   * [`Storage`](crate::ErrorCode::Storage),
   * [`InvalidInput`](crate::ErrorCode::InvalidInput) for a cursor that
   * is not one this federation issued or for a `limit` of zero, and
   * [`FederationClosed`](crate::ErrorCode::FederationClosed).
   */
  activity(
    cursor: Cursor | undefined,
    limit: number,
    asyncOpts_?: { signal: AbortSignal },
  ) /*throws*/ : Promise<ActivityPage>
  /**
   * The ecash balance: the value this instance currently holds as its
   * own, uncommitted notes.
   *
   * Value that is committed to an in-flight operation, funding a
   * lightning payment, sitting in out-of-band notes that have not been
   * redeemed or reclaimed, waiting on an on-chain deposit to confirm, is
   * not counted here.
   *
   * Holding is not spending, and this method takes no position on the
   * latter: whether a spend would be *permitted* is governed by the
   * federation's status, not by this number. The case where the two
   * diverge is a recovery-locked federation: while the rescan proceeds
   * the balance reported here is partial, still moving, and worth
   * showing as progress, yet none of it is spendable, and every spend
   * or receive is refused with
   * [`Recovering`](crate::ErrorCode::Recovering) no matter what this
   * method returned. It settles when recovery finishes. On a
   * [`Running`](crate::FederationStatus::Running) federation the two
   * notions coincide, and this is exactly the amount a spend can draw
   * on.
   *
   * # Errors
   *
   * [`Internal`](crate::ErrorCode::Internal) for a client with no usable balance source
   * (API-version negotiation left every module out, most often), which is not the caller's
   * doing and not a storage fault, and
   * [`FederationClosed`](crate::ErrorCode::FederationClosed).
   */
  balance(asyncOpts_?: { signal: AbortSignal }) /*throws*/ : Promise<Amount>
  /**
   * Opens a new, independent subscription to the balance.
   *
   * Each call returns its own cursor, exactly like
   * [`Operation::updates`](crate::Operation::updates): two subscribers
   * both see every change and neither consumes the other's updates.
   *
   * This cannot fail, so it hands out a subscriber even for a closed
   * federation; that subscriber's first
   * [`next`](BalanceUpdates::next) yields
   * [`FederationClosed`](crate::ErrorCode::FederationClosed).
   */
  balanceUpdates(): BalanceUpdatesLike
  /**
   * What this federation can do.
   *
   * Reported as plain booleans so an application can decide what to
   * render before the user touches anything. It answers the same
   * question as the three facade accessors below and exists alongside
   * them for the case where a screen needs to know about several
   * capabilities at once without taking handles it will not use.
   */
  capabilities(): Capabilities
  /**
   * See [`Federation::ecash`].
   */
  ecash(): EcashLike | undefined
  /**
   * This federation's id.
   */
  id(): FederationId
  /**
   * An invite code for this federation, suitable for sharing so someone
   * else can join it.
   */
  inviteCode(): InviteCodeLike
  /**
   * See [`Federation::lightning`].
   */
  lightning(): LightningLike | undefined
  /**
   * The metadata facade.
   *
   * Unconditional, unlike the three capability facades above: every
   * federation has configuration metadata, so there is always something
   * to read. A federation without a meta module simply has no consensus
   * metadata, which [`Meta`] reports as an absent value rather than as a
   * missing facade.
   */
  meta(): MetaLike
  /**
   * The federation's human-readable name, when its configuration
   * declares one.
   *
   * This is configuration metadata, not a verified or unique identifier:
   * two federations may present the same name. Identity is
   * [`Federation::id`].
   */
  name(): string | undefined
  /**
   * The Bitcoin network this federation operates on.
   *
   * On-chain addresses are validated against this when an on-chain quote
   * is requested, failing with
   * [`NetworkMismatch`](crate::ErrorCode::NetworkMismatch) on
   * disagreement. There is no second check at send time, because
   * [`Onchain::send`](crate::Onchain::send) takes only a quote: the
   * address is bound into the quote when it is issued.
   */
  network(): Network
  /**
   * See [`Federation::onchain`].
   */
  onchain(): OnchainLike | undefined
  /**
   * See [`Federation::operation`].
   */
  operation(
    id: OperationId,
    asyncOpts_?: { signal: AbortSignal },
  ) /*throws*/ : Promise<AnyOperationLike | undefined>
}
/**
 * @deprecated Use `FederationLike` instead.
 */
export type FederationInterface = FederationLike

/**
 * A handle to one federation this SDK instance has joined.
 *
 * Everything an application does with a federation goes through this type:
 * reading its identity and balance, obtaining the [ecash](Federation::ecash),
 * [lightning](Federation::lightning) and [on-chain](Federation::onchain)
 * facades, reading [metadata](Federation::meta), reattaching to a running
 * [operation](Federation::operation), and paging through local
 * [activity](Federation::activity).
 *
 * Like the other handles in this crate it is a cheap clone over shared
 * state: every clone talks to the same running federation client, and it
 * is `Send + Sync` on native targets, with the same types compiled for a
 * single-threaded host on wasm.
 *
 * A handle keeps working until the federation is closed with
 * [`Sdk::close_federation`](crate::Sdk::close_federation), erased with
 * [`Sdk::forget_federation`](crate::Sdk::forget_federation), or the whole
 * instance is shut down. An application holding a stale handle degrades
 * into a reportable error rather than a crash: nothing here panics after a
 * close.
 *
 * # What a closed handle does
 *
 * "Fails with
 * [`FederationClosed`](crate::ErrorCode::FederationClosed)" applies to the
 * **fallible** calls: [`balance`](Federation::balance),
 * [`operation`](Federation::operation),
 * [`activity`](Federation::activity), and every call made through a
 * facade. The rest of this type returns plain
 * values and has no way to report a failure, so each has a defined closed
 * behaviour instead:
 *
 * - **The descriptive accessors keep answering.**
 * [`id`](Federation::id), [`name`](Federation::name),
 * [`network`](Federation::network),
 * [`invite_code`](Federation::invite_code) and
 * [`capabilities`](Federation::capabilities) go on returning the
 * configuration last known for this federation. A history screen can
 * still label rows with a federation that has been closed underneath it.
 * - **The facade accessors keep returning `Some`.**
 * [`ecash`](Federation::ecash), [`lightning`](Federation::lightning) and
 * [`onchain`](Federation::onchain) return a facade whenever the
 * federation had that module, closed or not, and the failure surfaces
 * from the facade call as
 * [`FederationClosed`](crate::ErrorCode::FederationClosed). Returning
 * `None` instead would be a lie with a specific documented meaning:
 * "this federation has no mint module", and would make a closed
 * federation indistinguishable from one that never supported ecash at
 * all. [`meta`](Federation::meta), which is unconditional, behaves the
 * same way.
 * - **[`balance_updates`](Federation::balance_updates) still hands out a
 * subscriber**, whose very first
 * [`next`](BalanceUpdates::next) yields
 * [`FederationClosed`](crate::ErrorCode::FederationClosed). The error is
 * where a caller can act on it, rather than being swallowed by an
 * accessor that cannot return one.
 */
export class Federation extends UniffiAbstractObject implements FederationLike {
  readonly [uniffiTypeNameSymbol] = 'Federation'
  readonly [destructorGuardSymbol]: UniffiGcObject
  readonly [pointerLiteralSymbol]: UniffiHandle
  // No primary constructor declared for this class.
  private constructor(pointer: UniffiHandle) {
    super()
    this[pointerLiteralSymbol] = pointer
    this[destructorGuardSymbol] =
      uniffiTypeFederationObjectFactory.bless(pointer)
  }

  /**
   * Reads a page of local activity history, newest first.
   *
   * Pass `None` as `cursor` for the first page and the
   * [`next`](crate::ActivityPage::next) cursor of the previous page for
   * each following one. At most `limit` items are returned; fewer is
   * normal, and an empty page with no cursor means the end.
   *
   * Rows still in flight are brought up to date before the page is
   * returned, so a page that has any takes a moment longer than one
   * that has none.
   *
   * The history is *local*, see [`ActivityItem`](crate::ActivityItem)
   * for exactly what that excludes.
   *
   * # Errors
   *
   * [`Storage`](crate::ErrorCode::Storage),
   * [`InvalidInput`](crate::ErrorCode::InvalidInput) for a cursor that
   * is not one this federation issued or for a `limit` of zero, and
   * [`FederationClosed`](crate::ErrorCode::FederationClosed).
   */
  async activity(
    cursor: Cursor | undefined,
    limit: number,
    asyncOpts_?: { signal: AbortSignal },
  ): Promise<ActivityPage> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_federation_activity(
          uniffiTypeFederationObjectFactory.clonePointer(this),
          FfiConverterOptionalTypeCursor.lower(
            cursor,
            nativeModule().rustbuffer_alloc,
          ),
          FfiConverterUInt16.lower(limit, nativeModule().rustbuffer_alloc),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterTypeActivityPage.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * The ecash balance: the value this instance currently holds as its
   * own, uncommitted notes.
   *
   * Value that is committed to an in-flight operation, funding a
   * lightning payment, sitting in out-of-band notes that have not been
   * redeemed or reclaimed, waiting on an on-chain deposit to confirm, is
   * not counted here.
   *
   * Holding is not spending, and this method takes no position on the
   * latter: whether a spend would be *permitted* is governed by the
   * federation's status, not by this number. The case where the two
   * diverge is a recovery-locked federation: while the rescan proceeds
   * the balance reported here is partial, still moving, and worth
   * showing as progress, yet none of it is spendable, and every spend
   * or receive is refused with
   * [`Recovering`](crate::ErrorCode::Recovering) no matter what this
   * method returned. It settles when recovery finishes. On a
   * [`Running`](crate::FederationStatus::Running) federation the two
   * notions coincide, and this is exactly the amount a spend can draw
   * on.
   *
   * # Errors
   *
   * [`Internal`](crate::ErrorCode::Internal) for a client with no usable balance source
   * (API-version negotiation left every module out, most often), which is not the caller's
   * doing and not a storage fault, and
   * [`FederationClosed`](crate::ErrorCode::FederationClosed).
   */
  async balance(asyncOpts_?: {
    signal: AbortSignal
  }): Promise<Amount> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_federation_balance(
          uniffiTypeFederationObjectFactory.clonePointer(this),
        )
      },
      /*pollFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_poll_u64,
      /*cancelFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_cancel_u64,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_u64,
      /*freeFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_free_u64,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      /*liftFunc:*/ FfiConverterTypeAmount.lift.bind(FfiConverterTypeAmount),
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * Opens a new, independent subscription to the balance.
   *
   * Each call returns its own cursor, exactly like
   * [`Operation::updates`](crate::Operation::updates): two subscribers
   * both see every change and neither consumes the other's updates.
   *
   * This cannot fail, so it hands out a subscriber even for a closed
   * federation; that subscriber's first
   * [`next`](BalanceUpdates::next) yields
   * [`FederationClosed`](crate::ErrorCode::FederationClosed).
   */
  balanceUpdates(): BalanceUpdatesLike {
    return FfiConverterTypeBalanceUpdates.lift(
      uniffiCaller.rustCall(
        /*caller:*/ (callStatus) => {
          return nativeModule().uniffi_fedimint_sdk_fn_method_federation_balance_updates(
            uniffiTypeFederationObjectFactory.clonePointer(this),
            callStatus,
          )
        },
        /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      ),
    )
  }

  /**
   * What this federation can do.
   *
   * Reported as plain booleans so an application can decide what to
   * render before the user touches anything. It answers the same
   * question as the three facade accessors below and exists alongside
   * them for the case where a screen needs to know about several
   * capabilities at once without taking handles it will not use.
   */
  capabilities(): Capabilities {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_federation_capabilities(
          uniffiTypeFederationObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterTypeCapabilities.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * See [`Federation::ecash`].
   */
  ecash(): EcashLike | undefined {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_federation_ecash(
          uniffiTypeFederationObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterOptionalTypeEcash.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * This federation's id.
   */
  id(): FederationId {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_federation_id(
          uniffiTypeFederationObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterTypeFederationId.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * An invite code for this federation, suitable for sharing so someone
   * else can join it.
   */
  inviteCode(): InviteCodeLike {
    return FfiConverterTypeInviteCode.lift(
      uniffiCaller.rustCall(
        /*caller:*/ (callStatus) => {
          return nativeModule().uniffi_fedimint_sdk_fn_method_federation_invite_code(
            uniffiTypeFederationObjectFactory.clonePointer(this),
            callStatus,
          )
        },
        /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      ),
    )
  }

  /**
   * See [`Federation::lightning`].
   */
  lightning(): LightningLike | undefined {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_federation_lightning(
          uniffiTypeFederationObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterOptionalTypeLightning.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * The metadata facade.
   *
   * Unconditional, unlike the three capability facades above: every
   * federation has configuration metadata, so there is always something
   * to read. A federation without a meta module simply has no consensus
   * metadata, which [`Meta`] reports as an absent value rather than as a
   * missing facade.
   */
  meta(): MetaLike {
    return FfiConverterTypeMeta.lift(
      uniffiCaller.rustCall(
        /*caller:*/ (callStatus) => {
          return nativeModule().uniffi_fedimint_sdk_fn_method_federation_meta(
            uniffiTypeFederationObjectFactory.clonePointer(this),
            callStatus,
          )
        },
        /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      ),
    )
  }

  /**
   * The federation's human-readable name, when its configuration
   * declares one.
   *
   * This is configuration metadata, not a verified or unique identifier:
   * two federations may present the same name. Identity is
   * [`Federation::id`].
   */
  name(): string | undefined {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_federation_name(
          uniffiTypeFederationObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterOptionalString.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * The Bitcoin network this federation operates on.
   *
   * On-chain addresses are validated against this when an on-chain quote
   * is requested, failing with
   * [`NetworkMismatch`](crate::ErrorCode::NetworkMismatch) on
   * disagreement. There is no second check at send time, because
   * [`Onchain::send`](crate::Onchain::send) takes only a quote: the
   * address is bound into the quote when it is issued.
   */
  network(): Network {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_federation_network(
          uniffiTypeFederationObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterTypeNetwork.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * See [`Federation::onchain`].
   */
  onchain(): OnchainLike | undefined {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_federation_onchain(
          uniffiTypeFederationObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterOptionalTypeOnchain.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * See [`Federation::operation`].
   */
  async operation(
    id: OperationId,
    asyncOpts_?: { signal: AbortSignal },
  ): Promise<AnyOperationLike | undefined> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_federation_operation(
          uniffiTypeFederationObjectFactory.clonePointer(this),
          FfiConverterTypeOperationId.lower(
            id,
            nativeModule().rustbuffer_alloc,
          ),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterOptionalTypeAnyOperation.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  uniffiDestroy(): void {
    const ptr = (this as any)[destructorGuardSymbol]
    if (ptr !== undefined) {
      const pointer = uniffiTypeFederationObjectFactory.pointer(this)
      uniffiTypeFederationObjectFactory.freePointer(pointer)
      uniffiTypeFederationObjectFactory.unbless(ptr)
      delete (this as any)[destructorGuardSymbol]
    }
  }

  static instanceOf(obj_: any): obj_ is Federation {
    return uniffiTypeFederationObjectFactory.isConcreteType(obj_)
  }
}

const uniffiTypeFederationObjectFactory: UniffiObjectFactory<FederationLike> =
  (() => {
    /// <reference lib="es2021" />
    const registry =
      typeof FinalizationRegistry !== 'undefined'
        ? new FinalizationRegistry<UniffiHandle>((heldValue: UniffiHandle) => {
            uniffiTypeFederationObjectFactory.freePointer(heldValue)
          })
        : null

    return {
      create(pointer: UniffiHandle): FederationLike {
        const instance = Object.create(Federation.prototype)
        instance[pointerLiteralSymbol] = pointer
        instance[destructorGuardSymbol] = this.bless(pointer)
        instance[uniffiTypeNameSymbol] = 'Federation'
        return instance
      },

      bless(p: UniffiHandle): UniffiGcObject {
        const ptr = {
          p, // make sure this object doesn't get optimized away.
          markDestroyed: () => undefined,
        }
        if (registry) {
          registry.register(ptr, p, ptr)
        }
        return ptr
      },

      unbless(ptr_: UniffiGcObject) {
        if (registry) {
          registry.unregister(ptr_)
        }
      },

      pointer(obj_: FederationLike): UniffiHandle {
        if ((obj_ as any)[destructorGuardSymbol] === undefined) {
          throw new UniffiInternalError.UnexpectedNullPointer()
        }
        return (obj_ as any)[pointerLiteralSymbol]
      },

      clonePointer(obj_: FederationLike): UniffiHandle {
        const pointer = this.pointer(obj_)
        return uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_clone_federation(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      freePointer(pointer: UniffiHandle): void {
        uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_free_federation(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      isConcreteType(obj_: any): obj_ is FederationLike {
        return (
          obj_[destructorGuardSymbol] &&
          obj_[uniffiTypeNameSymbol] === 'Federation'
        )
      },
    }
  })()
const FfiConverterTypeFederation = new FfiConverterObject(
  uniffiTypeFederationObjectFactory,
)

/**
 * The result of [`Sdk::recover`]/[`Sdk::resume_recovery`], with `progress` crossing as
 * [`RecoveryOperation`] and `federation` as an `Arc`, rather than the generic
 * `Operation<RecoveryState>` and bare [`Federation`] the real [`Recovery`] carries — a
 * `Federation` referenced from a record field crosses as `Arc<Federation>` like any other object
 * reference.
 */
export type RecoveryHandle = {
  /**
   * See [`Recovery::federation`].
   */
  federation: FederationLike
  /**
   * See [`Recovery::progress`].
   */
  progress: RecoveryOperationLike
}

/**
 * Generated factory for {@link RecoveryHandle} record objects.
 */
export const RecoveryHandle = (() => {
  const defaults = () => ({})
  const create = (() => {
    return uniffiCreateRecord<RecoveryHandle, ReturnType<typeof defaults>>(
      defaults,
    )
  })()
  return Object.freeze({
    create,
    new: create,
    defaults: () => Object.freeze(defaults()) as Partial<RecoveryHandle>,
  })
})()

const FfiConverterTypeRecoveryHandle = (() => {
  type TypeName = RecoveryHandle
  class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
    readFromCursor(c: Cursor): TypeName {
      return {
        federation: FfiConverterTypeFederation.readFromCursor(c),
        progress: FfiConverterTypeRecoveryOperation.readFromCursor(c),
      }
    }
    writeIntoCursor(value: TypeName, c: Cursor): void {
      FfiConverterTypeFederation.writeIntoCursor(value.federation, c)
      FfiConverterTypeRecoveryOperation.writeIntoCursor(value.progress, c)
    }
    allocationSize(value: TypeName): number {
      return (
        FfiConverterTypeFederation.allocationSize(value.federation) +
        FfiConverterTypeRecoveryOperation.allocationSize(value.progress)
      )
    }
  }
  return new FFIConverter()
})()

/**
 * One independent subscription to every federation's status.
 *
 * Obtained from [`Sdk::federation_status_updates`]. Not `Clone`, for the
 * same reason [`BalanceUpdates`](crate::BalanceUpdates) is not: it is a
 * single cursor, and a second consumer should have a second subscription.
 * Dropping it stops only this subscription and never any work.
 */
export interface FederationStatusUpdatesLike {
  /**
   * See [`FederationStatusUpdates::next`].
   */
  next(asyncOpts_?: {
    signal: AbortSignal
  }) /*throws*/ : Promise<FederationInfo>
}
/**
 * @deprecated Use `FederationStatusUpdatesLike` instead.
 */
export type FederationStatusUpdatesInterface = FederationStatusUpdatesLike

/**
 * One independent subscription to every federation's status.
 *
 * Obtained from [`Sdk::federation_status_updates`]. Not `Clone`, for the
 * same reason [`BalanceUpdates`](crate::BalanceUpdates) is not: it is a
 * single cursor, and a second consumer should have a second subscription.
 * Dropping it stops only this subscription and never any work.
 */
export class FederationStatusUpdates
  extends UniffiAbstractObject
  implements FederationStatusUpdatesLike
{
  readonly [uniffiTypeNameSymbol] = 'FederationStatusUpdates'
  readonly [destructorGuardSymbol]: UniffiGcObject
  readonly [pointerLiteralSymbol]: UniffiHandle
  // No primary constructor declared for this class.
  private constructor(pointer: UniffiHandle) {
    super()
    this[pointerLiteralSymbol] = pointer
    this[destructorGuardSymbol] =
      uniffiTypeFederationStatusUpdatesObjectFactory.bless(pointer)
  }

  /**
   * See [`FederationStatusUpdates::next`].
   */
  async next(asyncOpts_?: {
    signal: AbortSignal
  }): Promise<FederationInfo> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_federationstatusupdates_next(
          uniffiTypeFederationStatusUpdatesObjectFactory.clonePointer(this),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterTypeFederationInfo.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  uniffiDestroy(): void {
    const ptr = (this as any)[destructorGuardSymbol]
    if (ptr !== undefined) {
      const pointer =
        uniffiTypeFederationStatusUpdatesObjectFactory.pointer(this)
      uniffiTypeFederationStatusUpdatesObjectFactory.freePointer(pointer)
      uniffiTypeFederationStatusUpdatesObjectFactory.unbless(ptr)
      delete (this as any)[destructorGuardSymbol]
    }
  }

  static instanceOf(obj_: any): obj_ is FederationStatusUpdates {
    return uniffiTypeFederationStatusUpdatesObjectFactory.isConcreteType(obj_)
  }
}

const uniffiTypeFederationStatusUpdatesObjectFactory: UniffiObjectFactory<FederationStatusUpdatesLike> =
  (() => {
    /// <reference lib="es2021" />
    const registry =
      typeof FinalizationRegistry !== 'undefined'
        ? new FinalizationRegistry<UniffiHandle>((heldValue: UniffiHandle) => {
            uniffiTypeFederationStatusUpdatesObjectFactory.freePointer(
              heldValue,
            )
          })
        : null

    return {
      create(pointer: UniffiHandle): FederationStatusUpdatesLike {
        const instance = Object.create(FederationStatusUpdates.prototype)
        instance[pointerLiteralSymbol] = pointer
        instance[destructorGuardSymbol] = this.bless(pointer)
        instance[uniffiTypeNameSymbol] = 'FederationStatusUpdates'
        return instance
      },

      bless(p: UniffiHandle): UniffiGcObject {
        const ptr = {
          p, // make sure this object doesn't get optimized away.
          markDestroyed: () => undefined,
        }
        if (registry) {
          registry.register(ptr, p, ptr)
        }
        return ptr
      },

      unbless(ptr_: UniffiGcObject) {
        if (registry) {
          registry.unregister(ptr_)
        }
      },

      pointer(obj_: FederationStatusUpdatesLike): UniffiHandle {
        if ((obj_ as any)[destructorGuardSymbol] === undefined) {
          throw new UniffiInternalError.UnexpectedNullPointer()
        }
        return (obj_ as any)[pointerLiteralSymbol]
      },

      clonePointer(obj_: FederationStatusUpdatesLike): UniffiHandle {
        const pointer = this.pointer(obj_)
        return uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_clone_federationstatusupdates(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      freePointer(pointer: UniffiHandle): void {
        uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_free_federationstatusupdates(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      isConcreteType(obj_: any): obj_ is FederationStatusUpdatesLike {
        return (
          obj_[destructorGuardSymbol] &&
          obj_[uniffiTypeNameSymbol] === 'FederationStatusUpdates'
        )
      },
    }
  })()
const FfiConverterTypeFederationStatusUpdates = new FfiConverterObject(
  uniffiTypeFederationStatusUpdatesObjectFactory,
)

/**
 * A BIP-39 seed phrase: the root secret an SDK instance is built from.
 *
 * `Mnemonic` implements neither [`Debug`] nor
 * [`Display`](core::fmt::Display), unlike every other data type in this
 * crate. A seed phrase must never be formattable by accident: `Debug`
 * output routinely ends up in logs, crash reports and `assert!` failure
 * messages, and `Display` would let a `Mnemonic` leak through generic
 * string formatting. The words are obtainable only through the explicit
 * [`Mnemonic::words`] call, which is the deliberate point at which a caller
 * chooses to have them as a plain string.
 *
 * This type's backing memory is zeroized when the value is dropped, so it
 * does not linger in memory after use.
 *
 * Once [`Mnemonic::words`] hands the phrase across a language boundary as a
 * plain string, this type's guarantees end: keeping that copy from
 * lingering in memory, being logged, or being written somewhere insecure is
 * the responsibility of the application embedding the SDK. Protecting the
 * *at-rest* copy inside the SDK's persistent storage, for example by
 * encrypting it or integrating with a platform keychain, is not provided by
 * this crate yet.
 */
export interface MnemonicLike {
  /**
   * Returns the mnemonic's words, in order, as owned strings.
   *
   * Calling this is the deliberate act of exporting the seed out of the
   * SDK's control (for backup display, for example); see the type-level
   * documentation for what protections do and don't extend past this
   * point.
   */
  words(): Array<string>
}
/**
 * @deprecated Use `MnemonicLike` instead.
 */
export type MnemonicInterface = MnemonicLike

/**
 * A BIP-39 seed phrase: the root secret an SDK instance is built from.
 *
 * `Mnemonic` implements neither [`Debug`] nor
 * [`Display`](core::fmt::Display), unlike every other data type in this
 * crate. A seed phrase must never be formattable by accident: `Debug`
 * output routinely ends up in logs, crash reports and `assert!` failure
 * messages, and `Display` would let a `Mnemonic` leak through generic
 * string formatting. The words are obtainable only through the explicit
 * [`Mnemonic::words`] call, which is the deliberate point at which a caller
 * chooses to have them as a plain string.
 *
 * This type's backing memory is zeroized when the value is dropped, so it
 * does not linger in memory after use.
 *
 * Once [`Mnemonic::words`] hands the phrase across a language boundary as a
 * plain string, this type's guarantees end: keeping that copy from
 * lingering in memory, being logged, or being written somewhere insecure is
 * the responsibility of the application embedding the SDK. Protecting the
 * *at-rest* copy inside the SDK's persistent storage, for example by
 * encrypting it or integrating with a platform keychain, is not provided by
 * this crate yet.
 */
export class Mnemonic extends UniffiAbstractObject implements MnemonicLike {
  readonly [uniffiTypeNameSymbol] = 'Mnemonic'
  readonly [destructorGuardSymbol]: UniffiGcObject
  readonly [pointerLiteralSymbol]: UniffiHandle
  // No primary constructor declared for this class.
  private constructor(pointer: UniffiHandle) {
    super()
    this[pointerLiteralSymbol] = pointer
    this[destructorGuardSymbol] = uniffiTypeMnemonicObjectFactory.bless(pointer)
  }

  /**
   * Rebuilds a mnemonic from a written-down phrase, given as its words in
   * order.
   *
   * The words are joined and validated exactly as [`FromStr`](core::str::FromStr)
   * does — case and stray whitespace are forgiven, the checksum is not.
   *
   * # Errors
   *
   * [`InvalidInput`](crate::ErrorCode::InvalidInput) for a malformed phrase,
   * wrong word count, or checksum failure.
   */
  static fromWords(words: Array<string>): MnemonicLike /*throws*/ {
    return FfiConverterTypeMnemonic.lift(
      uniffiCaller.rustCallWithError(
        /*liftError:*/ FfiConverterTypeError__as_error.lift.bind(
          FfiConverterTypeError__as_error,
        ),
        /*caller:*/ (callStatus) => {
          return nativeModule().uniffi_fedimint_sdk_fn_constructor_mnemonic_from_words(
            FfiConverterSequenceString.lower(
              words,
              nativeModule().rustbuffer_alloc,
            ),
            callStatus,
          )
        },
        /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      ),
    )
  }

  /**
   * Generates a fresh 12-word English BIP-39 mnemonic.
   *
   * Uses the platform's cryptographically secure random source, which can
   * genuinely be unavailable, so this reports a failure rather than
   * panicking or falling back to a weaker source.
   *
   * # Errors
   *
   * [`Entropy`](crate::ErrorCode::Entropy) if the platform's secure random
   * source was unavailable or failed. That is the only failure: nothing
   * here reads storage or contacts a federation.
   */
  static generate(): MnemonicLike /*throws*/ {
    return FfiConverterTypeMnemonic.lift(
      uniffiCaller.rustCallWithError(
        /*liftError:*/ FfiConverterTypeError__as_error.lift.bind(
          FfiConverterTypeError__as_error,
        ),
        /*caller:*/ (callStatus) => {
          return nativeModule().uniffi_fedimint_sdk_fn_constructor_mnemonic_generate(
            callStatus,
          )
        },
        /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      ),
    )
  }

  /**
   * Returns the mnemonic's words, in order, as owned strings.
   *
   * Calling this is the deliberate act of exporting the seed out of the
   * SDK's control (for backup display, for example); see the type-level
   * documentation for what protections do and don't extend past this
   * point.
   */
  words(): Array<string> {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_mnemonic_words(
          uniffiTypeMnemonicObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterSequenceString.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  uniffiDestroy(): void {
    const ptr = (this as any)[destructorGuardSymbol]
    if (ptr !== undefined) {
      const pointer = uniffiTypeMnemonicObjectFactory.pointer(this)
      uniffiTypeMnemonicObjectFactory.freePointer(pointer)
      uniffiTypeMnemonicObjectFactory.unbless(ptr)
      delete (this as any)[destructorGuardSymbol]
    }
  }

  static instanceOf(obj_: any): obj_ is Mnemonic {
    return uniffiTypeMnemonicObjectFactory.isConcreteType(obj_)
  }
}

const uniffiTypeMnemonicObjectFactory: UniffiObjectFactory<MnemonicLike> =
  (() => {
    /// <reference lib="es2021" />
    const registry =
      typeof FinalizationRegistry !== 'undefined'
        ? new FinalizationRegistry<UniffiHandle>((heldValue: UniffiHandle) => {
            uniffiTypeMnemonicObjectFactory.freePointer(heldValue)
          })
        : null

    return {
      create(pointer: UniffiHandle): MnemonicLike {
        const instance = Object.create(Mnemonic.prototype)
        instance[pointerLiteralSymbol] = pointer
        instance[destructorGuardSymbol] = this.bless(pointer)
        instance[uniffiTypeNameSymbol] = 'Mnemonic'
        return instance
      },

      bless(p: UniffiHandle): UniffiGcObject {
        const ptr = {
          p, // make sure this object doesn't get optimized away.
          markDestroyed: () => undefined,
        }
        if (registry) {
          registry.register(ptr, p, ptr)
        }
        return ptr
      },

      unbless(ptr_: UniffiGcObject) {
        if (registry) {
          registry.unregister(ptr_)
        }
      },

      pointer(obj_: MnemonicLike): UniffiHandle {
        if ((obj_ as any)[destructorGuardSymbol] === undefined) {
          throw new UniffiInternalError.UnexpectedNullPointer()
        }
        return (obj_ as any)[pointerLiteralSymbol]
      },

      clonePointer(obj_: MnemonicLike): UniffiHandle {
        const pointer = this.pointer(obj_)
        return uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_clone_mnemonic(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      freePointer(pointer: UniffiHandle): void {
        uniffiCaller.rustCall(
          /*caller:*/ (callStatus) =>
            nativeModule().uniffi_fedimint_sdk_fn_free_mnemonic(
              pointer,
              callStatus,
            ),
          /*liftString:*/ FfiConverterString.lift,
        )
      },

      isConcreteType(obj_: any): obj_ is MnemonicLike {
        return (
          obj_[destructorGuardSymbol] &&
          obj_[uniffiTypeNameSymbol] === 'Mnemonic'
        )
      },
    }
  })()
const FfiConverterTypeMnemonic = new FfiConverterObject(
  uniffiTypeMnemonicObjectFactory,
)

/**
 * A running SDK instance: one [`Storage`], one BIP-39 seed, and every
 * federation joined against them.
 *
 * `Sdk` is the root object an application builds once at startup (see
 * [`Sdk::builder`]) and keeps for the lifetime of the process. It is a
 * cheap handle over shared internal state: cloning it costs an atomic
 * refcount bump and every clone observes the same federations, the same
 * storage, and the same background work. It is `Send + Sync` on native
 * targets, so clones move between threads and tasks freely; wasm compiles the
 * same types for a single-threaded host, where those bounds are neither
 * available nor needed.
 *
 * # One seed, many federations
 *
 * An instance holds exactly one mnemonic. Each federation's client secret
 * is derived from it, domain-separated by federation id, using the scheme
 * documented on [`Mnemonic`], so joining a second federation never reuses
 * the first federation's secret, and the same seed restored elsewhere
 * reproduces the same per-federation secrets. Storage is likewise shared
 * and namespaced per federation internally; applications do not manage
 * per-federation locations.
 *
 * # Federation lifecycle
 *
 * Every federation this instance's storage remembers is in exactly one of
 * five states, and every lifecycle call on this type is a transition
 * between them. ([`Forgotten`](FederationStatus::Forgotten) is the one
 * [`FederationStatus`] variant that is not among them: it is a
 * notification that a federation is gone, not a state one is stored in.)
 *
 * - **[`Running`](FederationStatus::Running)**: open, everything the
 * federation offers is available.
 * - **[`Recovering`](FederationStatus::Recovering)**: open, with a live
 * handle, but the wallet is still being reconstructed from the seed, so
 * its balance and activity are incomplete and every spend and receive is
 * refused with [`Recovering`](crate::ErrorCode::Recovering) until the
 * reconstruction completes.
 *
 * These two are the **open** states: exactly what [`Sdk::federations`]
 * lists and what [`Sdk::federation`] hands back a live handle for.
 * Nothing in this SDK completes or cancels a reconstruction on demand,
 * so an unfinished one survives closing, reopening and restarting; the
 * destructive [`Sdk::forget_federation`] is the only way to be rid of
 * one without finishing it.
 * - **[`Quarantined`](FederationStatus::Quarantined)**: stored and intact,
 * but not running, because the SDK could not or would not operate on it
 * (a refused configuration, unreadable local state, or no guardian
 * answering). Nothing is deleted, and the state carries the
 * [`ErrorCode`](crate::ErrorCode) and message that explain why.
 * - **[`Closed`](FederationStatus::Closed)**: stored and intact, not
 * running, because the application asked for that with
 * [`Sdk::close_federation`].
 * - **[`Forgetting`](FederationStatus::Forgetting)**: an erase has been
 * committed and is being carried out, or is waiting to be finished by a
 * retry or a later [`SdkBuilder::build`]. The federation is never opened
 * again, never handed a handle, and its balance, history and local state
 * are already gone as far as this API is concerned. The only way out is
 * out of the storage entirely.
 *
 * **A stored federation is never silently absent.** [`Sdk::federations`]
 * lists what this instance has open; [`Sdk::stored_federations`] lists
 * everything the storage holds, in whatever state, and
 * [`Sdk::federation_status`] answers for a single id. Build a wallet list
 * from the second, so a closed, quarantined, or being-erased federation
 * appears as itself, with a reason, instead of disappearing between two
 * runs.
 *
 * **No single federation takes the instance down with it.**
 * [`SdkBuilder::build`] fails only when the root storage or the seed is
 * unsound. A federation that cannot be opened is quarantined and reported
 * instead, and a federation whose erase cannot be completed is left
 * [`Forgetting`](FederationStatus::Forgetting) and retried later, rather
 * than failing the whole instance.
 *
 * **Getting a stored federation open again takes one call and no invite
 * code.** [`Sdk::reopen_federation`] moves a federation out of
 * [`Closed`](FederationStatus::Closed) or
 * [`Quarantined`](FederationStatus::Quarantined) using the configuration
 * the SDK already holds.
 *
 * Status changes are observable as they happen, through
 * [`Sdk::federation_status_updates`].
 *
 * # Durability
 *
 * **Every transition an application can observe is durably committed
 * before it becomes observable.** A joined federation is persisted before
 * [`Sdk::join`] returns its handle, an operation before the call that
 * started it hands back an [`Operation`](crate::Operation), a state before
 * [`OperationUpdates::next`](crate::OperationUpdates::next) yields it, and
 * an erase before it begins. There is no window in which the SDK has told
 * the caller that value moved and could still forget it. Consequently
 * [`Sdk::shutdown`] is an optimisation, not a correctness requirement: an
 * abrupt kill loses nothing that was acknowledged. See [`Sdk::shutdown`]
 * for exactly what a caller may rely on after the process dies without
 * warning.
 *
 * # Closed handles
 *
 * Any [`Federation`] handle for a federation that is no longer running,
 * and every handle after [`Sdk::shutdown`], fails its **fallible** calls
 * with [`ErrorCode::FederationClosed`](crate::ErrorCode::FederationClosed)
 * rather than panicking or silently doing nothing. Its infallible
 * accessors keep answering; see [`Federation`] for what each of them
 * reports. The reason the federation stopped running is not encoded in
 * that error, one code covers all of them, and is read from
 * [`Sdk::federation_status`] instead.
 *
 * A [`Recovering`](FederationStatus::Recovering) federation is not one of
 * these: its handle is live, and the sends and receives it will not
 * perform yet fail with [`Recovering`](crate::ErrorCode::Recovering)
 * instead. `FederationClosed` means this handle is stale and no call on it
 * will ever work again; `Recovering` means the federation is working and
 * will accept this call once its wallet has been reconstructed. Retrying
 * the first with the same handle is pointless; retrying the second is the
 * whole plan.
 */
export interface SdkLike {
  /**
   * Stops running this federation while keeping all of its data.
   *
   * The federation's background workers stop, it is dropped from
   * [`Sdk::federations`], its status becomes
   * [`Closed`](FederationStatus::Closed), and it is no longer reopened
   * automatically by later builds. Nothing is deleted: it stays listed
   * by [`Sdk::stored_federations`], and [`Sdk::reopen_federation`]
   * restores access to all of it without an invite code. This is the
   * non-destructive half of leaving a federation;
   * [`Sdk::forget_federation`] is the destructive half.
   *
   * Any [`Federation`] handle an application still holds keeps existing
   * but stops doing work: its fallible calls fail with
   * [`FederationClosed`](crate::ErrorCode::FederationClosed), as do
   * pending [`BalanceUpdates::next`](crate::BalanceUpdates::next) and
   * [`OperationUpdates::next`](crate::OperationUpdates::next) calls
   * against it. Its infallible accessors keep answering.
   *
   * Closing is idempotent: an id that names no open federation is not an
   * error. That includes a quarantined federation, which closing turns
   * into a deliberate [`Closed`](FederationStatus::Closed) so later
   * builds stop retrying it.
   *
   * An id in [`Forgetting`](FederationStatus::Forgetting) is accepted
   * and changes nothing: it is already not running, so this returns
   * `Ok(())`, but its status stays `Forgetting` and the committed erase
   * still proceeds.
   *
   * A [`Recovering`](FederationStatus::Recovering) federation closes
   * like any other, and closing it is not a way out of the
   * reconstruction: its status becomes [`Closed`](FederationStatus::Closed),
   * the unfinished reconstruction is preserved, and
   * [`Sdk::reopen_federation`] brings it back
   * [`Recovering`](FederationStatus::Recovering) with that work resuming
   * where it stopped.
   *
   * # Errors
   *
   * [`FederationClosed`](crate::ErrorCode::FederationClosed) if the
   * whole instance has been shut down, which is the one error here that
   * means nothing happened.
   *
   * The federation is retired before anything else can fail, so the rest
   * describe how far the stop got and never whether it happened:
   * [`Storage`](crate::ErrorCode::Storage) if the choice could not be
   * persisted, which leaves the federation
   * [`Quarantined`](FederationStatus::Quarantined) for a later build or
   * [`Sdk::reopen_federation`] to retry, and
   * [`Internal`](crate::ErrorCode::Internal) if its client could not be
   * shut down cleanly because something else was still using it, which
   * leaves the federation [`Closed`](FederationStatus::Closed) with its
   * workers stopped but not waited for. Reopening it immediately after
   * that is the one thing worth avoiding.
   */
  closeFederation(
    id: FederationId,
    asyncOpts_?: { signal: AbortSignal },
  ) /*throws*/ : Promise<void>
  /**
   * Returns this instance's seed phrase, for the user to write down.
   *
   * The name says *export* on purpose: this is the one call that takes a
   * secret out of the SDK's custody, and it should be obvious at the
   * call site that this is what is happening.
   *
   * What the caller receives is a [`Mnemonic`], which neither prints nor
   * formats itself; extracting the words from it is a separate,
   * deliberate step ([`Mnemonic::words`]). Everything downstream of that
   * step is the application's responsibility, as documented on that
   * type.
   *
   * Infallible and synchronous: the seed is loaded once when the
   * instance is built and held in memory for its lifetime, so this does
   * not read storage and remains available after [`Sdk::shutdown`]. It
   * is also why [`SdkBuilder::build`] never fails over a federation: an
   * instance whose every federation is quarantined still exports its
   * seed, the user's route to their money by any other client.
   */
  exportMnemonic(): MnemonicLike
  /**
   * See [`Sdk::federation`].
   */
  federation(id: FederationId) /*throws*/ : FederationLike | undefined
  /**
   * See [`Sdk::federation_status`].
   */
  federationStatus(id: FederationId) /*throws*/ : FederationStatus | undefined
  /**
   * Opens a new, independent subscription to every federation's status.
   *
   * A status can change without the application having asked for
   * anything: guardians publish a configuration this SDK refuses and the
   * federation is quarantined, a recovery finishes, another clone of
   * this [`Sdk`] closes a federation, an erase completes.
   *
   * Each call returns its own cursor, exactly like
   * [`Federation::balance_updates`](crate::Federation::balance_updates):
   * two subscribers both see every change and neither consumes the
   * other's updates. This is instance-wide rather than per-federation,
   * so it yields a whole [`FederationInfo`], the same record
   * [`Sdk::stored_federations`] returns, and a list screen updates by
   * replacing the row whose [`id`](FederationInfo::id) matches.
   *
   * This cannot fail, so it hands out a subscriber even after
   * [`Sdk::shutdown`]; that subscriber's first
   * [`next`](FederationStatusUpdates::next) yields
   * [`FederationClosed`](crate::ErrorCode::FederationClosed).
   */
  federationStatusUpdates(): FederationStatusUpdatesLike
  /**
   * See [`Sdk::federations`].
   */
  federations(): Array<FederationLike>
  /**
   * Permanently deletes this federation's local state.
   *
   * This is destructive and unrecoverable from within the SDK: the
   * configuration, client state, operation log, and activity history for
   * the federation are erased. Only the seed survives, so re-joining the
   * federation later recovers whatever the federation itself can
   * reconstruct, not what was only ever recorded locally, and re-joining
   * needs an invite code again. [`Sdk::close_federation`] and
   * [`Sdk::reopen_federation`] exist so that merely wanting a federation
   * to stop running never requires this.
   *
   * The call runs in three phases whose order is part of the contract.
   *
   * # 1. Quiesce, before anything is checked
   *
   * The federation is retired first, atomically: it leaves
   * [`Sdk::federations`], every outstanding [`Federation`] handle,
   * facade, and subscriber for it is closed, and its background workers,
   * including a running seed rescan, are stopped after flushing their
   * state durably. Only then is eligibility evaluated, so nothing can
   * start new work on a federation that is already retired.
   *
   * # 2. Refuse unless it is safe, leaving the data intact
   *
   * The call then refuses unless all of the following hold:
   *
   * - **Zero spendable balance.** Any remaining spendable ecash fails
   * the call with
   * [`BalanceNotEmpty`](crate::ErrorCode::BalanceNotEmpty).
   * - **No non-final operations** and **no reclaimable outgoing value**
   * (out-of-band ecash a receiver has not redeemed and this instance
   * could still reclaim). Either fails the call with
   * [`PendingOperations`](crate::ErrorCode::PendingOperations).
   *
   * One class of non-final operation is exempt: an on-chain receive that
   * has not yet seen a transaction. It holds no value, so it never
   * blocks the erase; once a transaction has been seen it is an
   * ordinary pending operation and does block until claimed or failed.
   * See [`Onchain::receive`](crate::Onchain::receive).
   *
   * ## Recovery is never a reason to refuse
   *
   * A federation whose recovery has not completed is recovery-locked:
   * every spend and receive against it is refused with
   * [`Recovering`](crate::ErrorCode::Recovering), and no call in this
   * SDK stops or cancels a recovery. This erase is therefore the sole
   * exit from a recovery that cannot be finished, and **none of the
   * guards above may block it**:
   *
   * - A recovery-locked federation's balance does not count towards the
   * zero-balance guard: what it reports is provisional and none of it
   * is spendable.
   * - A rescan, running or stopped short of completing, does not block
   * as a pending operation; phase 1 aborts a running rescan as part of
   * the erase.
   * - Reclaimable outgoing value on a locked federation does not block
   * it either, and is forfeited by the erase.
   *
   * This call never returns [`Recovering`](crate::ErrorCode::Recovering).
   * What that exit costs should be put in front of the user before they
   * take it: the recovered-so-far state is thrown away, the local
   * activity history is gone for good, locally-recorded reclaimable
   * value is forfeited, and starting over needs the invite code again
   * and restarts the recovery, and the lock, from the beginning.
   *
   * A refusal in this phase deletes nothing, but leaves the federation
   * stopped, since it was quiesced in phase 1: its status afterwards is
   * [`Closed`](FederationStatus::Closed), and
   * [`Sdk::reopen_federation`] is how the application gets it running
   * again.
   *
   * # 3. Commit the erase before performing it
   *
   * A durable tombstone is written in a single step before any state is
   * removed, so the erase is atomic with respect to crashes and
   * failures: from the moment the tombstone lands, the federation is
   * gone as far as this API is concerned and the deletion is owed,
   * whether this call finishes it, a later [`Sdk::forget_federation`]
   * with the same id resumes it, or the next [`SdkBuilder::build`]
   * attempts it. A tombstoned federation is never opened, never handed a
   * handle, and never resurrected, no matter how many attempts fail:
   * [`Sdk::reopen_federation`] refuses it with
   * [`InvalidInput`](crate::ErrorCode::InvalidInput), and a fresh
   * [`Sdk::join`] of the same id finishes the erase first and then joins
   * from nothing.
   *
   * No deadline is promised: an erase whose completion keeps failing
   * stays committed and unfinished, and the federation sits in
   * [`Forgetting`](FederationStatus::Forgetting), visible in
   * [`Sdk::stored_federations`] and answering
   * [`Sdk::federation_status`], until an attempt succeeds. One
   * undeletable federation must not lock a user out of the others, of
   * their history, or of [`Sdk::export_mnemonic`].
   *
   * A federation is therefore always in exactly one of three states when
   * this call returns:
   *
   * - **Erased.** `Ok(())`, the state is gone,
   * [`Sdk::federation_status`] returns `None`, and the id no longer
   * appears in [`Sdk::stored_federations`].
   * - **Fully intact and closed.** The call refused in phase 2, or could
   * not even write the tombstone, and nothing was deleted.
   * - **Committed but unfinished.** The status is
   * [`Forgetting`](FederationStatus::Forgetting): listed by
   * [`Sdk::stored_federations`] and answered by
   * [`Sdk::federation_status`], absent from [`Sdk::federations`] and
   * from [`Sdk::federation`], refused by [`Sdk::reopen_federation`],
   * and safely retryable with the same id.
   *
   * Forgetting is idempotent: an id with no local state is not an error,
   * and an id that is already tombstoned finishes the erase and returns
   * `Ok(())` or, if it still cannot be finished, returns
   * [`Storage`](crate::ErrorCode::Storage) and leaves it
   * [`Forgetting`](FederationStatus::Forgetting) again.
   *
   * # Errors
   *
   * [`BalanceNotEmpty`](crate::ErrorCode::BalanceNotEmpty) and
   * [`PendingOperations`](crate::ErrorCode::PendingOperations) for a
   * phase-2 refusal, which deletes nothing;
   * [`Storage`](crate::ErrorCode::Storage) if the backend fails,
   * retryable either way; and
   * [`FederationClosed`](crate::ErrorCode::FederationClosed) after
   * shutdown. Never [`Recovering`](crate::ErrorCode::Recovering).
   */
  forgetFederation(
    id: FederationId,
    asyncOpts_?: { signal: AbortSignal },
  ) /*throws*/ : Promise<void>
  /**
   * Joins the federation named by `invite`, persists it, and returns a
   * handle to it.
   *
   * Joining derives this federation's client secret from the instance
   * seed, writes its configuration and client state to storage, and
   * starts its background workers, all before this call returns: a
   * process killed immediately afterwards comes back with the
   * federation joined. It is [`Running`](FederationStatus::Running) from
   * then on, and is reopened automatically by every subsequent
   * [`SdkBuilder::build`] against the same storage until it is closed or
   * forgotten.
   *
   * # The federation-wide module-generation rule
   *
   * Every module of a federation must be of the same generation. A mixed
   * federation is rejected with
   * [`UnsupportedFederation`](crate::ErrorCode::UnsupportedFederation),
   * carrying
   * [`ErrorDetails::MixedModuleGenerations`](crate::ErrorDetails::MixedModuleGenerations)
   * so the modules that conflict and the generations they declare are
   * readable as structured data. The rule is checked at [`Sdk::preview`],
   * here at join, when an existing federation is reopened, and again
   * whenever its configuration changes while the instance is running,
   * and it covers every module the federation runs, not only the ones
   * this SDK exposes as facades.
   *
   * ## When a running federation stops satisfying it
   *
   * A federation's configuration can change under a running instance. If
   * the new configuration becomes mixed, or otherwise one this SDK
   * refuses:
   *
   * 1. **The refused configuration is never adopted.** The last
   * configuration that validated stays in force, which is what
   * [`Federation::name`](crate::Federation::name),
   * [`Federation::network`](crate::Federation::network) and
   * [`Federation::capabilities`](crate::Federation::capabilities) keep
   * reporting.
   * 2. **The federation is quarantined, not closed and not erased.** Its
   * workers stop, it leaves [`Sdk::federations`], and its status
   * becomes [`Quarantined`](FederationStatus::Quarantined) carrying
   * [`UnsupportedFederation`](crate::ErrorCode::UnsupportedFederation)
   * and a message naming the conflict. Nothing local is deleted, so a
   * federation that becomes supported again comes back with
   * [`Sdk::reopen_federation`] and loses nothing.
   * 3. **Pending work terminates observably, and is not thrown away.**
   * In-flight operation state is flushed durably, and then every
   * outstanding
   * [`OperationUpdates::next`](crate::OperationUpdates::next) and
   * [`BalanceUpdates::next`](crate::BalanceUpdates::next) against the
   * federation resolves with
   * [`FederationClosed`](crate::ErrorCode::FederationClosed), as does
   * every subsequent fallible call on its handles. The operations
   * themselves are neither cancelled nor marked failed: their
   * persisted state is preserved and they resume where they left off
   * if the federation is reopened.
   *
   * The line between quarantine and ordinary trouble is drawn at
   * refusal, not at reachability: a running federation whose guardians
   * are unreachable is not quarantined, it is transient and the SDK
   * keeps retrying in the background.
   *
   * # Joining a federation whose erase is committed
   *
   * An id in [`Forgetting`](FederationStatus::Forgetting) is on its way
   * out of the storage, not "already joined", so joining it again is
   * allowed and produces a *new* federation rather than reviving the old
   * one. The committed erase is finished first, and the join then
   * proceeds as a first-time join: an invite code is required, the
   * configuration and client state are written fresh, and no balance,
   * operation log or activity history carries over from before.
   *
   * If that erase cannot be finished, the join fails with
   * [`Storage`](crate::ErrorCode::Storage) and writes nothing: the
   * federation stays [`Forgetting`](FederationStatus::Forgetting), to be
   * retried by a later [`SdkBuilder::build`] or
   * [`Sdk::forget_federation`].
   *
   * # A stale recovery intent does not survive a plain join
   *
   * A federation joined with this call can never be misclassified as
   * recovery-locked by a write an abandoned [`Sdk::recover`] attempt left
   * behind.
   *
   * # Errors
   *
   * [`AlreadyJoined`](crate::ErrorCode::AlreadyJoined) when this
   * instance already holds the federation, including closed or
   * quarantined, where [`Sdk::reopen_federation`] rather than a second
   * join is the call that wants making, and excluding an id in
   * [`Forgetting`](FederationStatus::Forgetting), which is handled as
   * above, plus every error [`Sdk::preview`] can produce, and
   * [`Storage`](crate::ErrorCode::Storage) if the join cannot be
   * persisted or a committed erase for the same id cannot be finished
   * first.
   */
  join(
    invite: InviteCodeLike,
    asyncOpts_?: { signal: AbortSignal },
  ) /*throws*/ : Promise<FederationLike>
  /**
   * Fetches a federation's configuration and renders it as a
   * [`FederationPreview`], without joining it or writing anything to
   * storage.
   *
   * This is the call behind a "join this federation?" screen: it
   * contacts the guardians named in the invite code, validates what they
   * return, and hands back the name, network, guardian count, module
   * list, and configuration metadata needed to show the user what they
   * are about to commit to.
   *
   * Validation here is the same validation [`Sdk::join`] performs,
   * including the module-generation rule described on that method: a
   * federation this SDK could not operate on is rejected at preview
   * rather than previewed and then refused at join.
   *
   * # Errors
   *
   * [`FederationUnreachable`](crate::ErrorCode::FederationUnreachable)
   * when no guardian answers,
   * [`Timeout`](crate::ErrorCode::Timeout) when they answer too slowly,
   * [`UnsupportedFederation`](crate::ErrorCode::UnsupportedFederation)
   * when the configuration mixes module generations or is otherwise one
   * this SDK refuses, and
   * [`FederationClosed`](crate::ErrorCode::FederationClosed) if the
   * instance has been shut down.
   */
  preview(
    invite: InviteCodeLike,
    asyncOpts_?: { signal: AbortSignal },
  ) /*throws*/ : Promise<FederationPreview>
  /**
   * See [`Sdk::recover`].
   */
  recover(
    invite: InviteCodeLike,
    asyncOpts_?: { signal: AbortSignal },
  ) /*throws*/ : Promise<RecoveryHandle>
  /**
   * See [`Sdk::recovery_status`].
   */
  recoveryStatus(
    id: FederationId,
    asyncOpts_?: { signal: AbortSignal },
  ) /*throws*/ : Promise<RecoveryState | undefined>
  /**
   * Starts a stored federation running again, without an invite code.
   *
   * This is the way back from [`Closed`](FederationStatus::Closed) and
   * from [`Quarantined`](FederationStatus::Quarantined). The SDK already
   * holds the federation's configuration and client state, so no invite
   * code is required or accepted.
   *
   * It runs the same open sequence [`SdkBuilder::build`] runs for a
   * remembered federation: revalidate the configuration against the
   * module-generation rule described on [`Sdk::join`], start the
   * background workers, and resume unfinished operations from where
   * they were persisted. On success the federation is open and appears
   * in [`Sdk::federations`] again, and it is reopened automatically by
   * later builds. Which open state it lands in is the federation's
   * choice, not this call's: [`Running`](FederationStatus::Running), or
   * [`Recovering`](FederationStatus::Recovering) if the reconstruction
   * of its wallet from the seed was still unfinished when it stopped.
   *
   * Handles obtained before the federation stopped running are *not*
   * revived: they stay closed, and their fallible calls keep failing
   * with [`FederationClosed`](crate::ErrorCode::FederationClosed); the
   * handle this call returns is the live one.
   *
   * Reopening a federation that is already open is not an error: it
   * returns the live handle. It does not, and cannot, hurry a
   * reconstruction along.
   *
   * A failed reopen leaves the federation
   * [`Quarantined`](FederationStatus::Quarantined) with the same
   * [`ErrorCode`](crate::ErrorCode) this call returns, so later builds
   * retry it too. [`Sdk::close_federation`] is how to give up on it
   * instead.
   *
   * # Errors
   *
   * [`InvalidInput`](crate::ErrorCode::InvalidInput) when this storage
   * holds no federation with that id, or holds one in
   * [`Forgetting`](FederationStatus::Forgetting): a committed erase is
   * never resurrected, however unfinished it is. Both cases mean the
   * same thing to a caller: the id names nothing openable. Then
   * [`UnsupportedFederation`](crate::ErrorCode::UnsupportedFederation)
   * for a configuration this SDK refuses,
   * [`FederationUnreachable`](crate::ErrorCode::FederationUnreachable)
   * and [`Timeout`](crate::ErrorCode::Timeout) when the guardians cannot
   * be reached in time, [`Storage`](crate::ErrorCode::Storage) if the
   * federation's local state cannot be read, and
   * [`FederationClosed`](crate::ErrorCode::FederationClosed) if the
   * whole instance has been shut down.
   */
  reopenFederation(
    id: FederationId,
    asyncOpts_?: { signal: AbortSignal },
  ) /*throws*/ : Promise<FederationLike>
  /**
   * See [`Sdk::resume_recovery`].
   */
  resumeRecovery(
    id: FederationId,
    asyncOpts_?: { signal: AbortSignal },
  ) /*throws*/ : Promise<RecoveryHandle>
  /**
   * Best-effort: flushes everything to storage, stops all background
   * work, and releases the storage lock.
   *
   * After this returns, every fallible call on every [`Sdk`] and
   * [`Federation`] handle, and every subscriber obtained from one, fails
   * with [`FederationClosed`](crate::ErrorCode::FederationClosed), with
   * [`Sdk::export_mnemonic`] the deliberate exception, alongside the
   * infallible status accessors ([`Sdk::stored_federations`],
   * [`Sdk::federation_status`]) and the infallible accessors on
   * [`Federation`]. Another instance may then open the same storage.
   * Shutdown is idempotent.
   *
   * # It is an optimisation, not a requirement
   *
   * **Correctness does not depend on this call**, because mobile
   * operating systems terminate backgrounded applications without
   * warning and a browser tab can vanish the same way. Everything a
   * caller can observe is already durable at the moment it becomes
   * observable, see the durability section on [`Sdk`], and what this
   * call adds is a flush of buffered non-critical state such as caches,
   * an orderly release of the storage lock, and a defined point after
   * which no background work is running.
   *
   * Call it from the platform's "entering background" or "about to
   * terminate" callback if there is one, and await it if you are allowed
   * to. Do not build anything on being able to.
   *
   * Skipping it is safe for correctness, but leaves one thing to mind in the same process: the
   * underlying store stays open until every [`Sdk`] and [`Federation`] handle over it, this
   * call included, has actually been dropped. A [`SdkBuilder::build`] against the same
   * location started before that point is left waiting on it.
   *
   * # What survives an abrupt kill
   *
   * If the process dies without this call, then on the next
   * [`SdkBuilder::build`] over the same storage:
   *
   * - **Everything acknowledged is there.** Any value a completed call
   * returned or a subscriber yielded is present.
   * - **In-flight operations resume by themselves**, from their last
   * persisted checkpoint, idempotently: neither dropped nor performed
   * twice.
   * - **A call that never returned may or may not have happened.** The
   * persisted state after reopening is authoritative, which is why
   * operations are addressed by a stable
   * [`OperationId`](crate::OperationId): retry by looking up the id,
   * not by repeating the request.
   * - **The seed is never at risk.** It is written before anything
   * derived from it exists.
   * - **A lock left behind is reclaimed, not fatal.**
   * [`StorageInUse`](crate::ErrorCode::StorageInUse) means genuinely
   * concurrent use, never a stale lock.
   * - **A committed erase stays committed.** The next build finishes the
   * deletion off, and if that attempt fails the federation stays
   * [`Forgetting`](FederationStatus::Forgetting) and is retried later;
   * it does not come back as a wallet and does not fail the build. See
   * [`Sdk::forget_federation`].
   *
   * Shutting down does not cancel operations in the sense of undoing
   * them: an operation that was running resumes when the storage is
   * opened again, exactly as it would after a crash.
   *
   * # Errors
   *
   * [`Storage`](crate::ErrorCode::Storage) if the final flush fails. The
   * instance is closed either way.
   */
  shutdown(asyncOpts_?: { signal: AbortSignal }) /*throws*/ : Promise<void>
  /**
   * Every federation this instance's storage holds, running or not, each
   * with its current [`FederationStatus`].
   *
   * This is the list a wallet screen should be built from.
   * [`Sdk::federations`] answers "what can I act on"; this answers "what
   * does this user have". A federation closed with
   * [`Sdk::close_federation`], one quarantined because it could not be
   * opened, and one whose erase is still finishing all appear here,
   * labelled, rather than being absent.
   *
   * It is a superset of [`Sdk::federations`]: every open federation
   * appears here too, carrying [`Running`](FederationStatus::Running) or
   * [`Recovering`](FederationStatus::Recovering). Only a federation that
   * has been fully forgotten appears in neither list.
   *
   * A [`Forgetting`](FederationStatus::Forgetting) row says "this id is
   * on its way out": the erase is committed, the federation will never
   * open again, [`Sdk::reopen_federation`] refuses it, and its balance
   * and history are gone as far as this API is concerned. Render it as
   * "removing…" and never as a wallet with a reconnect button. It stays
   * listed until the erase completes, announced once as
   * [`Forgotten`](FederationStatus::Forgotten) to
   * [`Sdk::federation_status_updates`] subscribers.
   *
   * This is also the way back to a federation an application cannot
   * reach by any other route: with [`Sdk::reopen_federation`], it closes
   * the gap that would otherwise require the original invite code again.
   *
   * Each entry is a small owned record rather than a handle; see
   * [`FederationInfo`]. The order is unspecified, as in
   * [`Sdk::federations`].
   *
   * Infallible and synchronous: the statuses are instance state, not a
   * storage read, and this keeps answering after [`Sdk::shutdown`],
   * reporting the last statuses the instance knew.
   */
  storedFederations(): Array<FederationInfo>
}
/**
 * @deprecated Use `SdkLike` instead.
 */
export type SdkInterface = SdkLike

/**
 * A running SDK instance: one [`Storage`], one BIP-39 seed, and every
 * federation joined against them.
 *
 * `Sdk` is the root object an application builds once at startup (see
 * [`Sdk::builder`]) and keeps for the lifetime of the process. It is a
 * cheap handle over shared internal state: cloning it costs an atomic
 * refcount bump and every clone observes the same federations, the same
 * storage, and the same background work. It is `Send + Sync` on native
 * targets, so clones move between threads and tasks freely; wasm compiles the
 * same types for a single-threaded host, where those bounds are neither
 * available nor needed.
 *
 * # One seed, many federations
 *
 * An instance holds exactly one mnemonic. Each federation's client secret
 * is derived from it, domain-separated by federation id, using the scheme
 * documented on [`Mnemonic`], so joining a second federation never reuses
 * the first federation's secret, and the same seed restored elsewhere
 * reproduces the same per-federation secrets. Storage is likewise shared
 * and namespaced per federation internally; applications do not manage
 * per-federation locations.
 *
 * # Federation lifecycle
 *
 * Every federation this instance's storage remembers is in exactly one of
 * five states, and every lifecycle call on this type is a transition
 * between them. ([`Forgotten`](FederationStatus::Forgotten) is the one
 * [`FederationStatus`] variant that is not among them: it is a
 * notification that a federation is gone, not a state one is stored in.)
 *
 * - **[`Running`](FederationStatus::Running)**: open, everything the
 * federation offers is available.
 * - **[`Recovering`](FederationStatus::Recovering)**: open, with a live
 * handle, but the wallet is still being reconstructed from the seed, so
 * its balance and activity are incomplete and every spend and receive is
 * refused with [`Recovering`](crate::ErrorCode::Recovering) until the
 * reconstruction completes.
 *
 * These two are the **open** states: exactly what [`Sdk::federations`]
 * lists and what [`Sdk::federation`] hands back a live handle for.
 * Nothing in this SDK completes or cancels a reconstruction on demand,
 * so an unfinished one survives closing, reopening and restarting; the
 * destructive [`Sdk::forget_federation`] is the only way to be rid of
 * one without finishing it.
 * - **[`Quarantined`](FederationStatus::Quarantined)**: stored and intact,
 * but not running, because the SDK could not or would not operate on it
 * (a refused configuration, unreadable local state, or no guardian
 * answering). Nothing is deleted, and the state carries the
 * [`ErrorCode`](crate::ErrorCode) and message that explain why.
 * - **[`Closed`](FederationStatus::Closed)**: stored and intact, not
 * running, because the application asked for that with
 * [`Sdk::close_federation`].
 * - **[`Forgetting`](FederationStatus::Forgetting)**: an erase has been
 * committed and is being carried out, or is waiting to be finished by a
 * retry or a later [`SdkBuilder::build`]. The federation is never opened
 * again, never handed a handle, and its balance, history and local state
 * are already gone as far as this API is concerned. The only way out is
 * out of the storage entirely.
 *
 * **A stored federation is never silently absent.** [`Sdk::federations`]
 * lists what this instance has open; [`Sdk::stored_federations`] lists
 * everything the storage holds, in whatever state, and
 * [`Sdk::federation_status`] answers for a single id. Build a wallet list
 * from the second, so a closed, quarantined, or being-erased federation
 * appears as itself, with a reason, instead of disappearing between two
 * runs.
 *
 * **No single federation takes the instance down with it.**
 * [`SdkBuilder::build`] fails only when the root storage or the seed is
 * unsound. A federation that cannot be opened is quarantined and reported
 * instead, and a federation whose erase cannot be completed is left
 * [`Forgetting`](FederationStatus::Forgetting) and retried later, rather
 * than failing the whole instance.
 *
 * **Getting a stored federation open again takes one call and no invite
 * code.** [`Sdk::reopen_federation`] moves a federation out of
 * [`Closed`](FederationStatus::Closed) or
 * [`Quarantined`](FederationStatus::Quarantined) using the configuration
 * the SDK already holds.
 *
 * Status changes are observable as they happen, through
 * [`Sdk::federation_status_updates`].
 *
 * # Durability
 *
 * **Every transition an application can observe is durably committed
 * before it becomes observable.** A joined federation is persisted before
 * [`Sdk::join`] returns its handle, an operation before the call that
 * started it hands back an [`Operation`](crate::Operation), a state before
 * [`OperationUpdates::next`](crate::OperationUpdates::next) yields it, and
 * an erase before it begins. There is no window in which the SDK has told
 * the caller that value moved and could still forget it. Consequently
 * [`Sdk::shutdown`] is an optimisation, not a correctness requirement: an
 * abrupt kill loses nothing that was acknowledged. See [`Sdk::shutdown`]
 * for exactly what a caller may rely on after the process dies without
 * warning.
 *
 * # Closed handles
 *
 * Any [`Federation`] handle for a federation that is no longer running,
 * and every handle after [`Sdk::shutdown`], fails its **fallible** calls
 * with [`ErrorCode::FederationClosed`](crate::ErrorCode::FederationClosed)
 * rather than panicking or silently doing nothing. Its infallible
 * accessors keep answering; see [`Federation`] for what each of them
 * reports. The reason the federation stopped running is not encoded in
 * that error, one code covers all of them, and is read from
 * [`Sdk::federation_status`] instead.
 *
 * A [`Recovering`](FederationStatus::Recovering) federation is not one of
 * these: its handle is live, and the sends and receives it will not
 * perform yet fail with [`Recovering`](crate::ErrorCode::Recovering)
 * instead. `FederationClosed` means this handle is stale and no call on it
 * will ever work again; `Recovering` means the federation is working and
 * will accept this call once its wallet has been reconstructed. Retrying
 * the first with the same handle is pointless; retrying the second is the
 * whole plan.
 */
export class Sdk extends UniffiAbstractObject implements SdkLike {
  readonly [uniffiTypeNameSymbol] = 'Sdk'
  readonly [destructorGuardSymbol]: UniffiGcObject
  readonly [pointerLiteralSymbol]: UniffiHandle
  // No primary constructor declared for this class.
  private constructor(pointer: UniffiHandle) {
    super()
    this[pointerLiteralSymbol] = pointer
    this[destructorGuardSymbol] = uniffiTypeSdkObjectFactory.bless(pointer)
  }

  /**
   * Stops running this federation while keeping all of its data.
   *
   * The federation's background workers stop, it is dropped from
   * [`Sdk::federations`], its status becomes
   * [`Closed`](FederationStatus::Closed), and it is no longer reopened
   * automatically by later builds. Nothing is deleted: it stays listed
   * by [`Sdk::stored_federations`], and [`Sdk::reopen_federation`]
   * restores access to all of it without an invite code. This is the
   * non-destructive half of leaving a federation;
   * [`Sdk::forget_federation`] is the destructive half.
   *
   * Any [`Federation`] handle an application still holds keeps existing
   * but stops doing work: its fallible calls fail with
   * [`FederationClosed`](crate::ErrorCode::FederationClosed), as do
   * pending [`BalanceUpdates::next`](crate::BalanceUpdates::next) and
   * [`OperationUpdates::next`](crate::OperationUpdates::next) calls
   * against it. Its infallible accessors keep answering.
   *
   * Closing is idempotent: an id that names no open federation is not an
   * error. That includes a quarantined federation, which closing turns
   * into a deliberate [`Closed`](FederationStatus::Closed) so later
   * builds stop retrying it.
   *
   * An id in [`Forgetting`](FederationStatus::Forgetting) is accepted
   * and changes nothing: it is already not running, so this returns
   * `Ok(())`, but its status stays `Forgetting` and the committed erase
   * still proceeds.
   *
   * A [`Recovering`](FederationStatus::Recovering) federation closes
   * like any other, and closing it is not a way out of the
   * reconstruction: its status becomes [`Closed`](FederationStatus::Closed),
   * the unfinished reconstruction is preserved, and
   * [`Sdk::reopen_federation`] brings it back
   * [`Recovering`](FederationStatus::Recovering) with that work resuming
   * where it stopped.
   *
   * # Errors
   *
   * [`FederationClosed`](crate::ErrorCode::FederationClosed) if the
   * whole instance has been shut down, which is the one error here that
   * means nothing happened.
   *
   * The federation is retired before anything else can fail, so the rest
   * describe how far the stop got and never whether it happened:
   * [`Storage`](crate::ErrorCode::Storage) if the choice could not be
   * persisted, which leaves the federation
   * [`Quarantined`](FederationStatus::Quarantined) for a later build or
   * [`Sdk::reopen_federation`] to retry, and
   * [`Internal`](crate::ErrorCode::Internal) if its client could not be
   * shut down cleanly because something else was still using it, which
   * leaves the federation [`Closed`](FederationStatus::Closed) with its
   * workers stopped but not waited for. Reopening it immediately after
   * that is the one thing worth avoiding.
   */
  async closeFederation(
    id: FederationId,
    asyncOpts_?: { signal: AbortSignal },
  ): Promise<void> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_sdk_close_federation(
          uniffiTypeSdkObjectFactory.clonePointer(this),
          FfiConverterTypeFederationId.lower(
            id,
            nativeModule().rustbuffer_alloc,
          ),
        )
      },
      /*pollFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_poll_void,
      /*cancelFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_cancel_void,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_void,
      /*freeFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_free_void,
      /*liftFunc:*/ (_v) => {},
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * Returns this instance's seed phrase, for the user to write down.
   *
   * The name says *export* on purpose: this is the one call that takes a
   * secret out of the SDK's custody, and it should be obvious at the
   * call site that this is what is happening.
   *
   * What the caller receives is a [`Mnemonic`], which neither prints nor
   * formats itself; extracting the words from it is a separate,
   * deliberate step ([`Mnemonic::words`]). Everything downstream of that
   * step is the application's responsibility, as documented on that
   * type.
   *
   * Infallible and synchronous: the seed is loaded once when the
   * instance is built and held in memory for its lifetime, so this does
   * not read storage and remains available after [`Sdk::shutdown`]. It
   * is also why [`SdkBuilder::build`] never fails over a federation: an
   * instance whose every federation is quarantined still exports its
   * seed, the user's route to their money by any other client.
   */
  exportMnemonic(): MnemonicLike {
    return FfiConverterTypeMnemonic.lift(
      uniffiCaller.rustCall(
        /*caller:*/ (callStatus) => {
          return nativeModule().uniffi_fedimint_sdk_fn_method_sdk_export_mnemonic(
            uniffiTypeSdkObjectFactory.clonePointer(this),
            callStatus,
          )
        },
        /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      ),
    )
  }

  /**
   * See [`Sdk::federation`].
   */
  federation(id: FederationId): FederationLike | undefined /*throws*/ {
    const __rb: Uint8Array = uniffiCaller.rustCallWithError(
      /*liftError:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_sdk_federation(
          uniffiTypeSdkObjectFactory.clonePointer(this),
          FfiConverterTypeFederationId.lower(
            id,
            nativeModule().rustbuffer_alloc,
          ),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterOptionalTypeFederation.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * See [`Sdk::federation_status`].
   */
  federationStatus(id: FederationId): FederationStatus | undefined /*throws*/ {
    const __rb: Uint8Array = uniffiCaller.rustCallWithError(
      /*liftError:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_sdk_federation_status(
          uniffiTypeSdkObjectFactory.clonePointer(this),
          FfiConverterTypeFederationId.lower(
            id,
            nativeModule().rustbuffer_alloc,
          ),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterOptionalTypeFederationStatus.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * Opens a new, independent subscription to every federation's status.
   *
   * A status can change without the application having asked for
   * anything: guardians publish a configuration this SDK refuses and the
   * federation is quarantined, a recovery finishes, another clone of
   * this [`Sdk`] closes a federation, an erase completes.
   *
   * Each call returns its own cursor, exactly like
   * [`Federation::balance_updates`](crate::Federation::balance_updates):
   * two subscribers both see every change and neither consumes the
   * other's updates. This is instance-wide rather than per-federation,
   * so it yields a whole [`FederationInfo`], the same record
   * [`Sdk::stored_federations`] returns, and a list screen updates by
   * replacing the row whose [`id`](FederationInfo::id) matches.
   *
   * This cannot fail, so it hands out a subscriber even after
   * [`Sdk::shutdown`]; that subscriber's first
   * [`next`](FederationStatusUpdates::next) yields
   * [`FederationClosed`](crate::ErrorCode::FederationClosed).
   */
  federationStatusUpdates(): FederationStatusUpdatesLike {
    return FfiConverterTypeFederationStatusUpdates.lift(
      uniffiCaller.rustCall(
        /*caller:*/ (callStatus) => {
          return nativeModule().uniffi_fedimint_sdk_fn_method_sdk_federation_status_updates(
            uniffiTypeSdkObjectFactory.clonePointer(this),
            callStatus,
          )
        },
        /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      ),
    )
  }

  /**
   * See [`Sdk::federations`].
   */
  federations(): Array<FederationLike> {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_sdk_federations(
          uniffiTypeSdkObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterSequenceTypeFederation.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  /**
   * Permanently deletes this federation's local state.
   *
   * This is destructive and unrecoverable from within the SDK: the
   * configuration, client state, operation log, and activity history for
   * the federation are erased. Only the seed survives, so re-joining the
   * federation later recovers whatever the federation itself can
   * reconstruct, not what was only ever recorded locally, and re-joining
   * needs an invite code again. [`Sdk::close_federation`] and
   * [`Sdk::reopen_federation`] exist so that merely wanting a federation
   * to stop running never requires this.
   *
   * The call runs in three phases whose order is part of the contract.
   *
   * # 1. Quiesce, before anything is checked
   *
   * The federation is retired first, atomically: it leaves
   * [`Sdk::federations`], every outstanding [`Federation`] handle,
   * facade, and subscriber for it is closed, and its background workers,
   * including a running seed rescan, are stopped after flushing their
   * state durably. Only then is eligibility evaluated, so nothing can
   * start new work on a federation that is already retired.
   *
   * # 2. Refuse unless it is safe, leaving the data intact
   *
   * The call then refuses unless all of the following hold:
   *
   * - **Zero spendable balance.** Any remaining spendable ecash fails
   * the call with
   * [`BalanceNotEmpty`](crate::ErrorCode::BalanceNotEmpty).
   * - **No non-final operations** and **no reclaimable outgoing value**
   * (out-of-band ecash a receiver has not redeemed and this instance
   * could still reclaim). Either fails the call with
   * [`PendingOperations`](crate::ErrorCode::PendingOperations).
   *
   * One class of non-final operation is exempt: an on-chain receive that
   * has not yet seen a transaction. It holds no value, so it never
   * blocks the erase; once a transaction has been seen it is an
   * ordinary pending operation and does block until claimed or failed.
   * See [`Onchain::receive`](crate::Onchain::receive).
   *
   * ## Recovery is never a reason to refuse
   *
   * A federation whose recovery has not completed is recovery-locked:
   * every spend and receive against it is refused with
   * [`Recovering`](crate::ErrorCode::Recovering), and no call in this
   * SDK stops or cancels a recovery. This erase is therefore the sole
   * exit from a recovery that cannot be finished, and **none of the
   * guards above may block it**:
   *
   * - A recovery-locked federation's balance does not count towards the
   * zero-balance guard: what it reports is provisional and none of it
   * is spendable.
   * - A rescan, running or stopped short of completing, does not block
   * as a pending operation; phase 1 aborts a running rescan as part of
   * the erase.
   * - Reclaimable outgoing value on a locked federation does not block
   * it either, and is forfeited by the erase.
   *
   * This call never returns [`Recovering`](crate::ErrorCode::Recovering).
   * What that exit costs should be put in front of the user before they
   * take it: the recovered-so-far state is thrown away, the local
   * activity history is gone for good, locally-recorded reclaimable
   * value is forfeited, and starting over needs the invite code again
   * and restarts the recovery, and the lock, from the beginning.
   *
   * A refusal in this phase deletes nothing, but leaves the federation
   * stopped, since it was quiesced in phase 1: its status afterwards is
   * [`Closed`](FederationStatus::Closed), and
   * [`Sdk::reopen_federation`] is how the application gets it running
   * again.
   *
   * # 3. Commit the erase before performing it
   *
   * A durable tombstone is written in a single step before any state is
   * removed, so the erase is atomic with respect to crashes and
   * failures: from the moment the tombstone lands, the federation is
   * gone as far as this API is concerned and the deletion is owed,
   * whether this call finishes it, a later [`Sdk::forget_federation`]
   * with the same id resumes it, or the next [`SdkBuilder::build`]
   * attempts it. A tombstoned federation is never opened, never handed a
   * handle, and never resurrected, no matter how many attempts fail:
   * [`Sdk::reopen_federation`] refuses it with
   * [`InvalidInput`](crate::ErrorCode::InvalidInput), and a fresh
   * [`Sdk::join`] of the same id finishes the erase first and then joins
   * from nothing.
   *
   * No deadline is promised: an erase whose completion keeps failing
   * stays committed and unfinished, and the federation sits in
   * [`Forgetting`](FederationStatus::Forgetting), visible in
   * [`Sdk::stored_federations`] and answering
   * [`Sdk::federation_status`], until an attempt succeeds. One
   * undeletable federation must not lock a user out of the others, of
   * their history, or of [`Sdk::export_mnemonic`].
   *
   * A federation is therefore always in exactly one of three states when
   * this call returns:
   *
   * - **Erased.** `Ok(())`, the state is gone,
   * [`Sdk::federation_status`] returns `None`, and the id no longer
   * appears in [`Sdk::stored_federations`].
   * - **Fully intact and closed.** The call refused in phase 2, or could
   * not even write the tombstone, and nothing was deleted.
   * - **Committed but unfinished.** The status is
   * [`Forgetting`](FederationStatus::Forgetting): listed by
   * [`Sdk::stored_federations`] and answered by
   * [`Sdk::federation_status`], absent from [`Sdk::federations`] and
   * from [`Sdk::federation`], refused by [`Sdk::reopen_federation`],
   * and safely retryable with the same id.
   *
   * Forgetting is idempotent: an id with no local state is not an error,
   * and an id that is already tombstoned finishes the erase and returns
   * `Ok(())` or, if it still cannot be finished, returns
   * [`Storage`](crate::ErrorCode::Storage) and leaves it
   * [`Forgetting`](FederationStatus::Forgetting) again.
   *
   * # Errors
   *
   * [`BalanceNotEmpty`](crate::ErrorCode::BalanceNotEmpty) and
   * [`PendingOperations`](crate::ErrorCode::PendingOperations) for a
   * phase-2 refusal, which deletes nothing;
   * [`Storage`](crate::ErrorCode::Storage) if the backend fails,
   * retryable either way; and
   * [`FederationClosed`](crate::ErrorCode::FederationClosed) after
   * shutdown. Never [`Recovering`](crate::ErrorCode::Recovering).
   */
  async forgetFederation(
    id: FederationId,
    asyncOpts_?: { signal: AbortSignal },
  ): Promise<void> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_sdk_forget_federation(
          uniffiTypeSdkObjectFactory.clonePointer(this),
          FfiConverterTypeFederationId.lower(
            id,
            nativeModule().rustbuffer_alloc,
          ),
        )
      },
      /*pollFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_poll_void,
      /*cancelFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_cancel_void,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_void,
      /*freeFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_free_void,
      /*liftFunc:*/ (_v) => {},
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * Joins the federation named by `invite`, persists it, and returns a
   * handle to it.
   *
   * Joining derives this federation's client secret from the instance
   * seed, writes its configuration and client state to storage, and
   * starts its background workers, all before this call returns: a
   * process killed immediately afterwards comes back with the
   * federation joined. It is [`Running`](FederationStatus::Running) from
   * then on, and is reopened automatically by every subsequent
   * [`SdkBuilder::build`] against the same storage until it is closed or
   * forgotten.
   *
   * # The federation-wide module-generation rule
   *
   * Every module of a federation must be of the same generation. A mixed
   * federation is rejected with
   * [`UnsupportedFederation`](crate::ErrorCode::UnsupportedFederation),
   * carrying
   * [`ErrorDetails::MixedModuleGenerations`](crate::ErrorDetails::MixedModuleGenerations)
   * so the modules that conflict and the generations they declare are
   * readable as structured data. The rule is checked at [`Sdk::preview`],
   * here at join, when an existing federation is reopened, and again
   * whenever its configuration changes while the instance is running,
   * and it covers every module the federation runs, not only the ones
   * this SDK exposes as facades.
   *
   * ## When a running federation stops satisfying it
   *
   * A federation's configuration can change under a running instance. If
   * the new configuration becomes mixed, or otherwise one this SDK
   * refuses:
   *
   * 1. **The refused configuration is never adopted.** The last
   * configuration that validated stays in force, which is what
   * [`Federation::name`](crate::Federation::name),
   * [`Federation::network`](crate::Federation::network) and
   * [`Federation::capabilities`](crate::Federation::capabilities) keep
   * reporting.
   * 2. **The federation is quarantined, not closed and not erased.** Its
   * workers stop, it leaves [`Sdk::federations`], and its status
   * becomes [`Quarantined`](FederationStatus::Quarantined) carrying
   * [`UnsupportedFederation`](crate::ErrorCode::UnsupportedFederation)
   * and a message naming the conflict. Nothing local is deleted, so a
   * federation that becomes supported again comes back with
   * [`Sdk::reopen_federation`] and loses nothing.
   * 3. **Pending work terminates observably, and is not thrown away.**
   * In-flight operation state is flushed durably, and then every
   * outstanding
   * [`OperationUpdates::next`](crate::OperationUpdates::next) and
   * [`BalanceUpdates::next`](crate::BalanceUpdates::next) against the
   * federation resolves with
   * [`FederationClosed`](crate::ErrorCode::FederationClosed), as does
   * every subsequent fallible call on its handles. The operations
   * themselves are neither cancelled nor marked failed: their
   * persisted state is preserved and they resume where they left off
   * if the federation is reopened.
   *
   * The line between quarantine and ordinary trouble is drawn at
   * refusal, not at reachability: a running federation whose guardians
   * are unreachable is not quarantined, it is transient and the SDK
   * keeps retrying in the background.
   *
   * # Joining a federation whose erase is committed
   *
   * An id in [`Forgetting`](FederationStatus::Forgetting) is on its way
   * out of the storage, not "already joined", so joining it again is
   * allowed and produces a *new* federation rather than reviving the old
   * one. The committed erase is finished first, and the join then
   * proceeds as a first-time join: an invite code is required, the
   * configuration and client state are written fresh, and no balance,
   * operation log or activity history carries over from before.
   *
   * If that erase cannot be finished, the join fails with
   * [`Storage`](crate::ErrorCode::Storage) and writes nothing: the
   * federation stays [`Forgetting`](FederationStatus::Forgetting), to be
   * retried by a later [`SdkBuilder::build`] or
   * [`Sdk::forget_federation`].
   *
   * # A stale recovery intent does not survive a plain join
   *
   * A federation joined with this call can never be misclassified as
   * recovery-locked by a write an abandoned [`Sdk::recover`] attempt left
   * behind.
   *
   * # Errors
   *
   * [`AlreadyJoined`](crate::ErrorCode::AlreadyJoined) when this
   * instance already holds the federation, including closed or
   * quarantined, where [`Sdk::reopen_federation`] rather than a second
   * join is the call that wants making, and excluding an id in
   * [`Forgetting`](FederationStatus::Forgetting), which is handled as
   * above, plus every error [`Sdk::preview`] can produce, and
   * [`Storage`](crate::ErrorCode::Storage) if the join cannot be
   * persisted or a committed erase for the same id cannot be finished
   * first.
   */
  async join(
    invite: InviteCodeLike,
    asyncOpts_?: { signal: AbortSignal },
  ): Promise<FederationLike> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_sdk_join(
          uniffiTypeSdkObjectFactory.clonePointer(this),
          FfiConverterTypeInviteCode.lower(
            invite,
            nativeModule().rustbuffer_alloc,
          ),
        )
      },
      /*pollFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_poll_u64,
      /*cancelFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_cancel_u64,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_u64,
      /*freeFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_free_u64,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      /*liftFunc:*/ FfiConverterTypeFederation.lift.bind(
        FfiConverterTypeFederation,
      ),
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * Fetches a federation's configuration and renders it as a
   * [`FederationPreview`], without joining it or writing anything to
   * storage.
   *
   * This is the call behind a "join this federation?" screen: it
   * contacts the guardians named in the invite code, validates what they
   * return, and hands back the name, network, guardian count, module
   * list, and configuration metadata needed to show the user what they
   * are about to commit to.
   *
   * Validation here is the same validation [`Sdk::join`] performs,
   * including the module-generation rule described on that method: a
   * federation this SDK could not operate on is rejected at preview
   * rather than previewed and then refused at join.
   *
   * # Errors
   *
   * [`FederationUnreachable`](crate::ErrorCode::FederationUnreachable)
   * when no guardian answers,
   * [`Timeout`](crate::ErrorCode::Timeout) when they answer too slowly,
   * [`UnsupportedFederation`](crate::ErrorCode::UnsupportedFederation)
   * when the configuration mixes module generations or is otherwise one
   * this SDK refuses, and
   * [`FederationClosed`](crate::ErrorCode::FederationClosed) if the
   * instance has been shut down.
   */
  async preview(
    invite: InviteCodeLike,
    asyncOpts_?: { signal: AbortSignal },
  ): Promise<FederationPreview> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_sdk_preview(
          uniffiTypeSdkObjectFactory.clonePointer(this),
          FfiConverterTypeInviteCode.lower(
            invite,
            nativeModule().rustbuffer_alloc,
          ),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterTypeFederationPreview.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * See [`Sdk::recover`].
   */
  async recover(
    invite: InviteCodeLike,
    asyncOpts_?: { signal: AbortSignal },
  ): Promise<RecoveryHandle> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_sdk_recover(
          uniffiTypeSdkObjectFactory.clonePointer(this),
          FfiConverterTypeInviteCode.lower(
            invite,
            nativeModule().rustbuffer_alloc,
          ),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterTypeRecoveryHandle.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * See [`Sdk::recovery_status`].
   */
  async recoveryStatus(
    id: FederationId,
    asyncOpts_?: { signal: AbortSignal },
  ): Promise<RecoveryState | undefined> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_sdk_recovery_status(
          uniffiTypeSdkObjectFactory.clonePointer(this),
          FfiConverterTypeFederationId.lower(
            id,
            nativeModule().rustbuffer_alloc,
          ),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterOptionalTypeRecoveryState.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * Starts a stored federation running again, without an invite code.
   *
   * This is the way back from [`Closed`](FederationStatus::Closed) and
   * from [`Quarantined`](FederationStatus::Quarantined). The SDK already
   * holds the federation's configuration and client state, so no invite
   * code is required or accepted.
   *
   * It runs the same open sequence [`SdkBuilder::build`] runs for a
   * remembered federation: revalidate the configuration against the
   * module-generation rule described on [`Sdk::join`], start the
   * background workers, and resume unfinished operations from where
   * they were persisted. On success the federation is open and appears
   * in [`Sdk::federations`] again, and it is reopened automatically by
   * later builds. Which open state it lands in is the federation's
   * choice, not this call's: [`Running`](FederationStatus::Running), or
   * [`Recovering`](FederationStatus::Recovering) if the reconstruction
   * of its wallet from the seed was still unfinished when it stopped.
   *
   * Handles obtained before the federation stopped running are *not*
   * revived: they stay closed, and their fallible calls keep failing
   * with [`FederationClosed`](crate::ErrorCode::FederationClosed); the
   * handle this call returns is the live one.
   *
   * Reopening a federation that is already open is not an error: it
   * returns the live handle. It does not, and cannot, hurry a
   * reconstruction along.
   *
   * A failed reopen leaves the federation
   * [`Quarantined`](FederationStatus::Quarantined) with the same
   * [`ErrorCode`](crate::ErrorCode) this call returns, so later builds
   * retry it too. [`Sdk::close_federation`] is how to give up on it
   * instead.
   *
   * # Errors
   *
   * [`InvalidInput`](crate::ErrorCode::InvalidInput) when this storage
   * holds no federation with that id, or holds one in
   * [`Forgetting`](FederationStatus::Forgetting): a committed erase is
   * never resurrected, however unfinished it is. Both cases mean the
   * same thing to a caller: the id names nothing openable. Then
   * [`UnsupportedFederation`](crate::ErrorCode::UnsupportedFederation)
   * for a configuration this SDK refuses,
   * [`FederationUnreachable`](crate::ErrorCode::FederationUnreachable)
   * and [`Timeout`](crate::ErrorCode::Timeout) when the guardians cannot
   * be reached in time, [`Storage`](crate::ErrorCode::Storage) if the
   * federation's local state cannot be read, and
   * [`FederationClosed`](crate::ErrorCode::FederationClosed) if the
   * whole instance has been shut down.
   */
  async reopenFederation(
    id: FederationId,
    asyncOpts_?: { signal: AbortSignal },
  ): Promise<FederationLike> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_sdk_reopen_federation(
          uniffiTypeSdkObjectFactory.clonePointer(this),
          FfiConverterTypeFederationId.lower(
            id,
            nativeModule().rustbuffer_alloc,
          ),
        )
      },
      /*pollFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_poll_u64,
      /*cancelFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_cancel_u64,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_u64,
      /*freeFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_free_u64,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      /*liftFunc:*/ FfiConverterTypeFederation.lift.bind(
        FfiConverterTypeFederation,
      ),
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * See [`Sdk::resume_recovery`].
   */
  async resumeRecovery(
    id: FederationId,
    asyncOpts_?: { signal: AbortSignal },
  ): Promise<RecoveryHandle> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_sdk_resume_recovery(
          uniffiTypeSdkObjectFactory.clonePointer(this),
          FfiConverterTypeFederationId.lower(
            id,
            nativeModule().rustbuffer_alloc,
          ),
        )
      },
      /*pollFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_poll_rust_buffer,
      /*cancelFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_cancel_rust_buffer,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_rust_buffer,
      /*freeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_free_rust_buffer,
      // Async returns always go through the JS-side converter: the
      // FFI symbol returns the future handle (u64), and the user-level
      // RustBuffer comes back via the shared `rust_future_complete_*`
      // export. The bytes the runtime hands back must be deserialized
      // here using the per-callable return-type converter.
      // Borrowed view over foreign memory: the call site owns the free,
      // as on the sync paths. Unconditional — a no-op where buffers are
      // already JS-owned.
      /*liftFunc:*/ (__rb) => {
        try {
          return FfiConverterTypeRecoveryHandle.lift(__rb)
        } finally {
          nativeModule().rustbuffer_free(__rb)
        }
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * Best-effort: flushes everything to storage, stops all background
   * work, and releases the storage lock.
   *
   * After this returns, every fallible call on every [`Sdk`] and
   * [`Federation`] handle, and every subscriber obtained from one, fails
   * with [`FederationClosed`](crate::ErrorCode::FederationClosed), with
   * [`Sdk::export_mnemonic`] the deliberate exception, alongside the
   * infallible status accessors ([`Sdk::stored_federations`],
   * [`Sdk::federation_status`]) and the infallible accessors on
   * [`Federation`]. Another instance may then open the same storage.
   * Shutdown is idempotent.
   *
   * # It is an optimisation, not a requirement
   *
   * **Correctness does not depend on this call**, because mobile
   * operating systems terminate backgrounded applications without
   * warning and a browser tab can vanish the same way. Everything a
   * caller can observe is already durable at the moment it becomes
   * observable, see the durability section on [`Sdk`], and what this
   * call adds is a flush of buffered non-critical state such as caches,
   * an orderly release of the storage lock, and a defined point after
   * which no background work is running.
   *
   * Call it from the platform's "entering background" or "about to
   * terminate" callback if there is one, and await it if you are allowed
   * to. Do not build anything on being able to.
   *
   * Skipping it is safe for correctness, but leaves one thing to mind in the same process: the
   * underlying store stays open until every [`Sdk`] and [`Federation`] handle over it, this
   * call included, has actually been dropped. A [`SdkBuilder::build`] against the same
   * location started before that point is left waiting on it.
   *
   * # What survives an abrupt kill
   *
   * If the process dies without this call, then on the next
   * [`SdkBuilder::build`] over the same storage:
   *
   * - **Everything acknowledged is there.** Any value a completed call
   * returned or a subscriber yielded is present.
   * - **In-flight operations resume by themselves**, from their last
   * persisted checkpoint, idempotently: neither dropped nor performed
   * twice.
   * - **A call that never returned may or may not have happened.** The
   * persisted state after reopening is authoritative, which is why
   * operations are addressed by a stable
   * [`OperationId`](crate::OperationId): retry by looking up the id,
   * not by repeating the request.
   * - **The seed is never at risk.** It is written before anything
   * derived from it exists.
   * - **A lock left behind is reclaimed, not fatal.**
   * [`StorageInUse`](crate::ErrorCode::StorageInUse) means genuinely
   * concurrent use, never a stale lock.
   * - **A committed erase stays committed.** The next build finishes the
   * deletion off, and if that attempt fails the federation stays
   * [`Forgetting`](FederationStatus::Forgetting) and is retried later;
   * it does not come back as a wallet and does not fail the build. See
   * [`Sdk::forget_federation`].
   *
   * Shutting down does not cancel operations in the sense of undoing
   * them: an operation that was running resumes when the storage is
   * opened again, exactly as it would after a crash.
   *
   * # Errors
   *
   * [`Storage`](crate::ErrorCode::Storage) if the final flush fails. The
   * instance is closed either way.
   */
  async shutdown(asyncOpts_?: {
    signal: AbortSignal
  }): Promise<void> /*throws*/ {
    return await uniffiRustCallAsync(
      /*rustCaller:*/ uniffiCaller,
      /*rustFutureFunc:*/ () => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_sdk_shutdown(
          uniffiTypeSdkObjectFactory.clonePointer(this),
        )
      },
      /*pollFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_poll_void,
      /*cancelFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_cancel_void,
      /*completeFunc:*/ nativeModule()
        .ffi_fedimint_sdk_rust_future_complete_void,
      /*freeFunc:*/ nativeModule().ffi_fedimint_sdk_rust_future_free_void,
      /*liftFunc:*/ (_v) => {},
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
      /*asyncOpts:*/ asyncOpts_,
      /*errorHandler:*/ FfiConverterTypeError__as_error.lift.bind(
        FfiConverterTypeError__as_error,
      ),
    )
  }

  /**
   * Every federation this instance's storage holds, running or not, each
   * with its current [`FederationStatus`].
   *
   * This is the list a wallet screen should be built from.
   * [`Sdk::federations`] answers "what can I act on"; this answers "what
   * does this user have". A federation closed with
   * [`Sdk::close_federation`], one quarantined because it could not be
   * opened, and one whose erase is still finishing all appear here,
   * labelled, rather than being absent.
   *
   * It is a superset of [`Sdk::federations`]: every open federation
   * appears here too, carrying [`Running`](FederationStatus::Running) or
   * [`Recovering`](FederationStatus::Recovering). Only a federation that
   * has been fully forgotten appears in neither list.
   *
   * A [`Forgetting`](FederationStatus::Forgetting) row says "this id is
   * on its way out": the erase is committed, the federation will never
   * open again, [`Sdk::reopen_federation`] refuses it, and its balance
   * and history are gone as far as this API is concerned. Render it as
   * "removing…" and never as a wallet with a reconnect button. It stays
   * listed until the erase completes, announced once as
   * [`Forgotten`](FederationStatus::Forgotten) to
   * [`Sdk::federation_status_updates`] subscribers.
   *
   * This is also the way back to a federation an application cannot
   * reach by any other route: with [`Sdk::reopen_federation`], it closes
   * the gap that would otherwise require the original invite code again.
   *
   * Each entry is a small owned record rather than a handle; see
   * [`FederationInfo`]. The order is unspecified, as in
   * [`Sdk::federations`].
   *
   * Infallible and synchronous: the statuses are instance state, not a
   * storage read, and this keeps answering after [`Sdk::shutdown`],
   * reporting the last statuses the instance knew.
   */
  storedFederations(): Array<FederationInfo> {
    const __rb: Uint8Array = uniffiCaller.rustCall(
      /*caller:*/ (callStatus) => {
        return nativeModule().uniffi_fedimint_sdk_fn_method_sdk_stored_federations(
          uniffiTypeSdkObjectFactory.clonePointer(this),
          callStatus,
        )
      },
      /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    )
    try {
      return FfiConverterSequenceTypeFederationInfo.lift(__rb)
    } finally {
      nativeModule().rustbuffer_free(__rb)
    }
  }

  uniffiDestroy(): void {
    const ptr = (this as any)[destructorGuardSymbol]
    if (ptr !== undefined) {
      const pointer = uniffiTypeSdkObjectFactory.pointer(this)
      uniffiTypeSdkObjectFactory.freePointer(pointer)
      uniffiTypeSdkObjectFactory.unbless(ptr)
      delete (this as any)[destructorGuardSymbol]
    }
  }

  static instanceOf(obj_: any): obj_ is Sdk {
    return uniffiTypeSdkObjectFactory.isConcreteType(obj_)
  }
}

const uniffiTypeSdkObjectFactory: UniffiObjectFactory<SdkLike> = (() => {
  /// <reference lib="es2021" />
  const registry =
    typeof FinalizationRegistry !== 'undefined'
      ? new FinalizationRegistry<UniffiHandle>((heldValue: UniffiHandle) => {
          uniffiTypeSdkObjectFactory.freePointer(heldValue)
        })
      : null

  return {
    create(pointer: UniffiHandle): SdkLike {
      const instance = Object.create(Sdk.prototype)
      instance[pointerLiteralSymbol] = pointer
      instance[destructorGuardSymbol] = this.bless(pointer)
      instance[uniffiTypeNameSymbol] = 'Sdk'
      return instance
    },

    bless(p: UniffiHandle): UniffiGcObject {
      const ptr = {
        p, // make sure this object doesn't get optimized away.
        markDestroyed: () => undefined,
      }
      if (registry) {
        registry.register(ptr, p, ptr)
      }
      return ptr
    },

    unbless(ptr_: UniffiGcObject) {
      if (registry) {
        registry.unregister(ptr_)
      }
    },

    pointer(obj_: SdkLike): UniffiHandle {
      if ((obj_ as any)[destructorGuardSymbol] === undefined) {
        throw new UniffiInternalError.UnexpectedNullPointer()
      }
      return (obj_ as any)[pointerLiteralSymbol]
    },

    clonePointer(obj_: SdkLike): UniffiHandle {
      const pointer = this.pointer(obj_)
      return uniffiCaller.rustCall(
        /*caller:*/ (callStatus) =>
          nativeModule().uniffi_fedimint_sdk_fn_clone_sdk(pointer, callStatus),
        /*liftString:*/ FfiConverterString.lift,
      )
    },

    freePointer(pointer: UniffiHandle): void {
      uniffiCaller.rustCall(
        /*caller:*/ (callStatus) =>
          nativeModule().uniffi_fedimint_sdk_fn_free_sdk(pointer, callStatus),
        /*liftString:*/ FfiConverterString.lift,
      )
    },

    isConcreteType(obj_: any): obj_ is SdkLike {
      return obj_[destructorGuardSymbol] && obj_[uniffiTypeNameSymbol] === 'Sdk'
    },
  }
})()
const FfiConverterTypeSdk = new FfiConverterObject(uniffiTypeSdkObjectFactory)

// FfiConverter for Amount | undefined
const FfiConverterOptionalTypeAmount = new FfiConverterOptional(
  FfiConverterTypeAmount,
)

// FfiConverter for Direction | undefined
const FfiConverterOptionalTypeDirection = new FfiConverterOptional(
  FfiConverterTypeDirection,
)

// FfiConverter for Array<ActivityItem>
const FfiConverterSequenceTypeActivityItem = new FfiConverterArray(
  FfiConverterTypeActivityItem,
)

// FfiConverter for Cursor | undefined
const FfiConverterOptionalTypeCursor = new FfiConverterOptional(
  FfiConverterTypeCursor,
)

// FfiConverter for DetailEnvelope | undefined
const FfiConverterOptionalTypeDetailEnvelope = new FfiConverterOptional(
  FfiConverterTypeDetailEnvelope,
)

// FfiConverter for NotesLike | undefined
const FfiConverterOptionalTypeNotes = new FfiConverterOptional(
  FfiConverterTypeNotes,
)

// FfiConverter for RawErrorDetails | undefined
const FfiConverterOptionalTypeRawErrorDetails = new FfiConverterOptional(
  FfiConverterTypeRawErrorDetails,
)

// FfiConverter for EcashSendState | undefined
const FfiConverterOptionalTypeEcashSendState = new FfiConverterOptional(
  FfiConverterTypeEcashSendState,
)

// FfiConverter for string | undefined
const FfiConverterOptionalString = new FfiConverterOptional(FfiConverterString)

// FfiConverter for Array<string>
const FfiConverterSequenceString = new FfiConverterArray(FfiConverterString)

// FfiConverter for Map<string, string>
const FfiConverterMapStringString = new FfiConverterMap(
  FfiConverterString,
  FfiConverterString,
)

/**
 * Typealias from the type name used in the UDL file to the builtin type.  This
 * is needed because the UDL type name is used in function/method signatures.
 */
export type MetaMap = Map<string, string>
// FfiConverter for MetaMap, a type alias for Map<string, string>.
const FfiConverterTypeMetaMap = FfiConverterMapStringString

// FfiConverter for GatewayId | undefined
const FfiConverterOptionalTypeGatewayId = new FfiConverterOptional(
  FfiConverterTypeGatewayId,
)

// FfiConverter for LnReceiveState | undefined
const FfiConverterOptionalTypeLnReceiveState = new FfiConverterOptional(
  FfiConverterTypeLnReceiveState,
)

// FfiConverter for Txid | undefined
const FfiConverterOptionalTypeTxid = new FfiConverterOptional(
  FfiConverterTypeTxid,
)

// FfiConverter for Sats | undefined
const FfiConverterOptionalTypeSats = new FfiConverterOptional(
  FfiConverterTypeSats,
)

// FfiConverter for OnchainReceiveFeeBreakdown | undefined
const FfiConverterOptionalTypeOnchainReceiveFeeBreakdown =
  new FfiConverterOptional(FfiConverterTypeOnchainReceiveFeeBreakdown)

// FfiConverter for OnchainReceiveState | undefined
const FfiConverterOptionalTypeOnchainReceiveState = new FfiConverterOptional(
  FfiConverterTypeOnchainReceiveState,
)

// FfiConverter for number | undefined
const FfiConverterOptionalUInt32 = new FfiConverterOptional(FfiConverterUInt32)

// FfiConverter for EcashReceiveState | undefined
const FfiConverterOptionalTypeEcashReceiveState = new FfiConverterOptional(
  FfiConverterTypeEcashReceiveState,
)

// FfiConverter for EcashLike | undefined
const FfiConverterOptionalTypeEcash = new FfiConverterOptional(
  FfiConverterTypeEcash,
)

// FfiConverter for LnSendState | undefined
const FfiConverterOptionalTypeLnSendState = new FfiConverterOptional(
  FfiConverterTypeLnSendState,
)

// FfiConverter for LightningLike | undefined
const FfiConverterOptionalTypeLightning = new FfiConverterOptional(
  FfiConverterTypeLightning,
)

// FfiConverter for ConsensusMetadata | undefined
const FfiConverterOptionalTypeConsensusMetadata = new FfiConverterOptional(
  FfiConverterTypeConsensusMetadata,
)

// FfiConverter for OnchainSendState | undefined
const FfiConverterOptionalTypeOnchainSendState = new FfiConverterOptional(
  FfiConverterTypeOnchainSendState,
)

// FfiConverter for OnchainLike | undefined
const FfiConverterOptionalTypeOnchain = new FfiConverterOptional(
  FfiConverterTypeOnchain,
)

// FfiConverter for EcashReceiveOperationLike | undefined
const FfiConverterOptionalTypeEcashReceiveOperation = new FfiConverterOptional(
  FfiConverterTypeEcashReceiveOperation,
)

// FfiConverter for EcashSendOperationLike | undefined
const FfiConverterOptionalTypeEcashSendOperation = new FfiConverterOptional(
  FfiConverterTypeEcashSendOperation,
)

// FfiConverter for LnReceiveOperationLike | undefined
const FfiConverterOptionalTypeLnReceiveOperation = new FfiConverterOptional(
  FfiConverterTypeLnReceiveOperation,
)

// FfiConverter for LnSendOperationLike | undefined
const FfiConverterOptionalTypeLnSendOperation = new FfiConverterOptional(
  FfiConverterTypeLnSendOperation,
)

// FfiConverter for OnchainReceiveOperationLike | undefined
const FfiConverterOptionalTypeOnchainReceiveOperation =
  new FfiConverterOptional(FfiConverterTypeOnchainReceiveOperation)

// FfiConverter for OnchainSendOperationLike | undefined
const FfiConverterOptionalTypeOnchainSendOperation = new FfiConverterOptional(
  FfiConverterTypeOnchainSendOperation,
)

// FfiConverter for RecoveryState | undefined
const FfiConverterOptionalTypeRecoveryState = new FfiConverterOptional(
  FfiConverterTypeRecoveryState,
)

// FfiConverter for RecoveryOperationLike | undefined
const FfiConverterOptionalTypeRecoveryOperation = new FfiConverterOptional(
  FfiConverterTypeRecoveryOperation,
)

// FfiConverter for AnyOperationLike | undefined
const FfiConverterOptionalTypeAnyOperation = new FfiConverterOptional(
  FfiConverterTypeAnyOperation,
)

// FfiConverter for FederationLike | undefined
const FfiConverterOptionalTypeFederation = new FfiConverterOptional(
  FfiConverterTypeFederation,
)

// FfiConverter for FederationStatus | undefined
const FfiConverterOptionalTypeFederationStatus = new FfiConverterOptional(
  FfiConverterTypeFederationStatus,
)

// FfiConverter for Array<FederationLike>
const FfiConverterSequenceTypeFederation = new FfiConverterArray(
  FfiConverterTypeFederation,
)

// FfiConverter for Array<FederationInfo>
const FfiConverterSequenceTypeFederationInfo = new FfiConverterArray(
  FfiConverterTypeFederationInfo,
)

// FfiConverter for MnemonicLike | undefined
const FfiConverterOptionalTypeMnemonic = new FfiConverterOptional(
  FfiConverterTypeMnemonic,
)

/**
 * This should be called before anything else.
 *
 * It is likely that this is being done for you by the library's `index.ts`.
 *
 * It checks versions of uniffi between when the Rust scaffolding was generated
 * and when the bindings were generated.
 *
 * It also initializes the machinery to enable Rust to talk back to Javascript.
 */
function uniffiEnsureInitialized() {
  // Get the bindings contract version from our ComponentInterface
  const bindingsContractVersion = 30
  // Get the scaffolding contract version by calling the into the dylib
  const scaffoldingContractVersion =
    nativeModule().ffi_fedimint_sdk_uniffi_contract_version()
  if (bindingsContractVersion !== scaffoldingContractVersion) {
    throw new UniffiInternalError.ContractVersionMismatch(
      scaffoldingContractVersion,
      bindingsContractVersion,
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_func_create_fedimint_sdk() !==
    48291
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_func_create_fedimint_sdk',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_anyoperation_as_ecash_receive() !==
    63118
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_anyoperation_as_ecash_receive',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_anyoperation_as_ecash_send() !==
    20206
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_anyoperation_as_ecash_send',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_anyoperation_as_ln_receive() !==
    2777
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_anyoperation_as_ln_receive',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_anyoperation_as_ln_send() !==
    50853
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_anyoperation_as_ln_send',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_anyoperation_as_onchain_receive() !==
    56021
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_anyoperation_as_onchain_receive',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_anyoperation_as_onchain_send() !==
    39355
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_anyoperation_as_onchain_send',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_anyoperation_as_recovery() !==
    2821
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_anyoperation_as_recovery',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_anyoperation_id() !==
    3708
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_anyoperation_id',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_anyoperation_kind() !==
    987
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_anyoperation_kind',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_anyoperation_raw_kind() !==
    14791
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_anyoperation_raw_kind',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_anyoperation_support() !==
    27121
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_anyoperation_support',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_anyoperation_supported_kind() !==
    62005
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_anyoperation_supported_kind',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_balanceupdates_next() !==
    16663
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_balanceupdates_next',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_ecash_quote() !== 1080
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_ecash_quote',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_ecash_receive() !== 5761
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_ecash_receive',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_ecash_send() !== 47208
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_ecash_send',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_ecashquote_expires_at() !==
    285
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_ecashquote_expires_at',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_ecashquote_federation_id() !==
    35837
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_ecashquote_federation_id',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_ecashquote_fee() !== 3696
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_ecashquote_fee',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_ecashquote_notes_value() !==
    42939
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_ecashquote_notes_value',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_ecashquote_requested_amount() !==
    12836
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_ecashquote_requested_amount',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_ecashquote_total() !==
    64215
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_ecashquote_total',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_ecashreceiveoperation_await_final() !==
    30012
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_ecashreceiveoperation_await_final',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_ecashreceiveoperation_details() !==
    20833
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_ecashreceiveoperation_details',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_ecashreceiveoperation_id() !==
    38101
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_ecashreceiveoperation_id',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_ecashreceiveoperation_state() !==
    59796
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_ecashreceiveoperation_state',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_ecashreceiveoperation_updates() !==
    52874
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_ecashreceiveoperation_updates',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_ecashreceiveoperationupdates_next() !==
    52597
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_ecashreceiveoperationupdates_next',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_ecashsendoperation_await_final() !==
    56401
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_ecashsendoperation_await_final',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_ecashsendoperation_details() !==
    35826
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_ecashsendoperation_details',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_ecashsendoperation_id() !==
    6186
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_ecashsendoperation_id',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_ecashsendoperation_request_cancel() !==
    53008
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_ecashsendoperation_request_cancel',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_ecashsendoperation_state() !==
    23776
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_ecashsendoperation_state',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_ecashsendoperation_updates() !==
    25219
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_ecashsendoperation_updates',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_ecashsendoperationupdates_next() !==
    11022
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_ecashsendoperationupdates_next',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_error_code() !== 35955
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_error_code',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_error_details() !== 27244
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_error_details',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_error_reason() !== 18279
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_error_reason',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_federation_activity() !==
    26156
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_federation_activity',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_federation_balance() !==
    45089
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_federation_balance',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_federation_balance_updates() !==
    60113
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_federation_balance_updates',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_federation_capabilities() !==
    44324
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_federation_capabilities',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_federation_ecash() !==
    1190
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_federation_ecash',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_federation_id() !== 587
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_federation_id',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_federation_invite_code() !==
    44926
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_federation_invite_code',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_federation_lightning() !==
    34074
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_federation_lightning',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_federation_meta() !==
    57000
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_federation_meta',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_federation_name() !==
    24493
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_federation_name',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_federation_network() !==
    50016
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_federation_network',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_federation_onchain() !==
    14727
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_federation_onchain',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_federation_operation() !==
    10937
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_federation_operation',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_federationstatusupdates_next() !==
    65
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_federationstatusupdates_next',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_invitecode_display() !==
    50582
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_invitecode_display',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_invitecode_federation_id() !==
    56294
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_invitecode_federation_id',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_constructor_invitecode_parse() !==
    19713
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_constructor_invitecode_parse',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_lightning_quote() !==
    30010
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_lightning_quote',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_lightning_receive() !==
    32079
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_lightning_receive',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_lightning_send() !==
    63506
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_lightning_send',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_lnquote_expires_at() !==
    37933
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_lnquote_expires_at',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_lnquote_fee() !== 20356
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_lnquote_fee',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_lnquote_fee_breakdown() !==
    49717
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_lnquote_fee_breakdown',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_lnquote_invoice_amount() !==
    54616
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_lnquote_invoice_amount',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_lnquote_route() !== 34644
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_lnquote_route',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_lnquote_total() !== 36887
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_lnquote_total',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_lnreceiveoperation_await_final() !==
    25940
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_lnreceiveoperation_await_final',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_lnreceiveoperation_details() !==
    46781
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_lnreceiveoperation_details',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_lnreceiveoperation_id() !==
    36883
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_lnreceiveoperation_id',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_lnreceiveoperation_state() !==
    7091
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_lnreceiveoperation_state',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_lnreceiveoperation_updates() !==
    60837
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_lnreceiveoperation_updates',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_lnreceiveoperationupdates_next() !==
    65339
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_lnreceiveoperationupdates_next',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_lnsendoperation_await_final() !==
    53572
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_lnsendoperation_await_final',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_lnsendoperation_details() !==
    41206
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_lnsendoperation_details',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_lnsendoperation_id() !==
    14870
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_lnsendoperation_id',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_lnsendoperation_state() !==
    58873
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_lnsendoperation_state',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_lnsendoperation_updates() !==
    41276
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_lnsendoperation_updates',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_lnsendoperationupdates_next() !==
    32690
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_lnsendoperationupdates_next',
    )
  }
  if (nativeModule().uniffi_fedimint_sdk_checksum_method_meta_all() !== 17691) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_meta_all',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_meta_config_metadata() !==
    5102
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_meta_config_metadata',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_meta_consensus_metadata() !==
    39839
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_meta_consensus_metadata',
    )
  }
  if (nativeModule().uniffi_fedimint_sdk_checksum_method_meta_get() !== 53450) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_meta_get',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_mnemonic_words() !==
    50659
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_mnemonic_words',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_constructor_mnemonic_from_words() !==
    53726
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_constructor_mnemonic_from_words',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_constructor_mnemonic_generate() !==
    23973
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_constructor_mnemonic_generate',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_notes_display() !== 13241
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_notes_display',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_notes_value() !== 14874
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_notes_value',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_constructor_notes_parse() !==
    9865
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_constructor_notes_parse',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_onchain_quote() !== 17134
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_onchain_quote',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_onchain_receive() !==
    8075
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_onchain_receive',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_onchain_send() !== 21779
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_onchain_send',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_onchainquote_amount() !==
    12693
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_onchainquote_amount',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_onchainquote_expires_at() !==
    36127
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_onchainquote_expires_at',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_onchainquote_fee() !==
    11433
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_onchainquote_fee',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_onchainquote_fee_breakdown() !==
    56579
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_onchainquote_fee_breakdown',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_onchainquote_total() !==
    62547
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_onchainquote_total',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_onchainreceiveoperation_await_final() !==
    41015
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_onchainreceiveoperation_await_final',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_onchainreceiveoperation_details() !==
    48715
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_onchainreceiveoperation_details',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_onchainreceiveoperation_id() !==
    53181
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_onchainreceiveoperation_id',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_onchainreceiveoperation_state() !==
    62996
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_onchainreceiveoperation_state',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_onchainreceiveoperation_updates() !==
    12430
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_onchainreceiveoperation_updates',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_onchainreceiveoperationupdates_next() !==
    1084
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_onchainreceiveoperationupdates_next',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_onchainsendoperation_await_final() !==
    55948
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_onchainsendoperation_await_final',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_onchainsendoperation_details() !==
    18550
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_onchainsendoperation_details',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_onchainsendoperation_id() !==
    13176
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_onchainsendoperation_id',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_onchainsendoperation_state() !==
    10226
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_onchainsendoperation_state',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_onchainsendoperation_updates() !==
    57975
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_onchainsendoperation_updates',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_onchainsendoperationupdates_next() !==
    47805
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_onchainsendoperationupdates_next',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_recoveryoperation_await_final() !==
    28673
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_recoveryoperation_await_final',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_recoveryoperation_id() !==
    63406
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_recoveryoperation_id',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_recoveryoperation_state() !==
    45846
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_recoveryoperation_state',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_recoveryoperation_updates() !==
    32065
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_recoveryoperation_updates',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_recoveryoperationupdates_next() !==
    56183
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_recoveryoperationupdates_next',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_sdk_close_federation() !==
    13652
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_sdk_close_federation',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_sdk_export_mnemonic() !==
    7225
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_sdk_export_mnemonic',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_sdk_federation() !==
    49289
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_sdk_federation',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_sdk_federation_status() !==
    13192
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_sdk_federation_status',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_sdk_federation_status_updates() !==
    2108
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_sdk_federation_status_updates',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_sdk_federations() !==
    34293
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_sdk_federations',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_sdk_forget_federation() !==
    8383
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_sdk_forget_federation',
    )
  }
  if (nativeModule().uniffi_fedimint_sdk_checksum_method_sdk_join() !== 46680) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_sdk_join',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_sdk_preview() !== 19136
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_sdk_preview',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_sdk_recover() !== 46258
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_sdk_recover',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_sdk_recovery_status() !==
    51047
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_sdk_recovery_status',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_sdk_reopen_federation() !==
    6521
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_sdk_reopen_federation',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_sdk_resume_recovery() !==
    8045
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_sdk_resume_recovery',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_sdk_shutdown() !== 26130
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_sdk_shutdown',
    )
  }
  if (
    nativeModule().uniffi_fedimint_sdk_checksum_method_sdk_stored_federations() !==
    52355
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      'uniffi_fedimint_sdk_checksum_method_sdk_stored_federations',
    )
  }
}

export default Object.freeze({
  initialize: uniffiEnsureInitialized,
  converters: {
    FfiConverterTypeActivityItem,
    FfiConverterTypeActivityPage,
    FfiConverterTypeActivityStatus,
    FfiConverterTypeAddress,
    FfiConverterTypeAmount,
    FfiConverterTypeAnyOperation,
    FfiConverterTypeBalanceUpdates,
    FfiConverterTypeBolt11Invoice,
    FfiConverterTypeCapabilities,
    FfiConverterTypeConsensusMetadata,
    FfiConverterTypeCursor,
    FfiConverterTypeDetailEnvelope,
    FfiConverterTypeDiagnostic,
    FfiConverterTypeDirection,
    FfiConverterTypeEcash,
    FfiConverterTypeEcashQuote,
    FfiConverterTypeEcashReceiveDetails,
    FfiConverterTypeEcashReceiveOperation,
    FfiConverterTypeEcashReceiveOperationUpdates,
    FfiConverterTypeEcashReceiveState,
    FfiConverterTypeEcashSendDetails,
    FfiConverterTypeEcashSendHandle,
    FfiConverterTypeEcashSendOperation,
    FfiConverterTypeEcashSendOperationUpdates,
    FfiConverterTypeEcashSendState,
    FfiConverterTypeError,
    FfiConverterTypeErrorCode,
    FfiConverterTypeError__as_error,
    FfiConverterTypeFederation,
    FfiConverterTypeFederationId,
    FfiConverterTypeFederationInfo,
    FfiConverterTypeFederationPreview,
    FfiConverterTypeFederationStatus,
    FfiConverterTypeFederationStatusUpdates,
    FfiConverterTypeGatewayId,
    FfiConverterTypeInviteCode,
    FfiConverterTypeLightning,
    FfiConverterTypeLightningRoute,
    FfiConverterTypeLnFeeBreakdown,
    FfiConverterTypeLnQuote,
    FfiConverterTypeLnReceiveDetails,
    FfiConverterTypeLnReceiveHandle,
    FfiConverterTypeLnReceiveOperation,
    FfiConverterTypeLnReceiveOperationUpdates,
    FfiConverterTypeLnReceiveState,
    FfiConverterTypeLnSendDetails,
    FfiConverterTypeLnSendOperation,
    FfiConverterTypeLnSendOperationUpdates,
    FfiConverterTypeLnSendState,
    FfiConverterTypeMeta,
    FfiConverterTypeMetaMap,
    FfiConverterTypeMnemonic,
    FfiConverterTypeNetwork,
    FfiConverterTypeNotes,
    FfiConverterTypeOnchain,
    FfiConverterTypeOnchainQuote,
    FfiConverterTypeOnchainReceiveDetails,
    FfiConverterTypeOnchainReceiveFeeBreakdown,
    FfiConverterTypeOnchainReceiveHandle,
    FfiConverterTypeOnchainReceiveOperation,
    FfiConverterTypeOnchainReceiveOperationUpdates,
    FfiConverterTypeOnchainReceiveState,
    FfiConverterTypeOnchainSendDetails,
    FfiConverterTypeOnchainSendFeeBreakdown,
    FfiConverterTypeOnchainSendOperation,
    FfiConverterTypeOnchainSendOperationUpdates,
    FfiConverterTypeOnchainSendState,
    FfiConverterTypeOperationId,
    FfiConverterTypeOperationKind,
    FfiConverterTypeOperationSupport,
    FfiConverterTypePreimage,
    FfiConverterTypeRawErrorDetails,
    FfiConverterTypeRawOperationKind,
    FfiConverterTypeRecoveryHandle,
    FfiConverterTypeRecoveryOperation,
    FfiConverterTypeRecoveryOperationUpdates,
    FfiConverterTypeRecoveryState,
    FfiConverterTypeSats,
    FfiConverterTypeSdk,
    FfiConverterTypeTimestamp,
    FfiConverterTypeTxid,
  },
})
